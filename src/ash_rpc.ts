// Generated by AshTypescript - RPC Actions
// Do not edit this file manually

import { Channel } from "phoenix";

import type { AppFilterInput, AppLibraryFilterInput, AppLibraryResourceSchema, AppLibrarySortField, AppResourceSchema, AppSortField, AppTemplateFilterInput, AppTemplateResourceSchema, AppTemplateSortField, AppVersionFilterInput, AppVersionResourceSchema, AppVersionSortField, AshDate, AshRpcError, AssessmentGraphResourceSchema, AssessmentResponseFilterInput, AssessmentResponseResourceSchema, AssessmentResponseSortField, ChatBindingFilterInput, ChatBindingResourceSchema, ChatBindingSortField, ChatConfigFilterInput, ChatConfigResourceSchema, ChatConfigSortField, ChatMessageResourceSchema, ChatReplyResourceSchema, ChatSuggestionsResourceSchema, ConditionalPaginatedResult, ConditionalPaginatedResultMixed, CourseActivityCompletionResourceSchema, CourseEnrollmentFilterInput, CourseEnrollmentResourceSchema, CourseEnrollmentSortField, Decimal, DriveConnectionFilterInput, DriveConnectionResourceSchema, DriveConnectionSortField, EnrollmentCompletionsResourceSchema, FeatureFlagFilterInput, FeatureFlagResourceSchema, FeatureFlagSortField, FeedbackFilterInput, FeedbackResourceSchema, FeedbackSortField, GithubConnectionFilterInput, GithubConnectionResourceSchema, GithubConnectionSortField, HomepageCardFilterInput, HomepageCardResourceSchema, HomepageCardSortField, InferResult, InterviewGeneratedContentFilterInput, InterviewGeneratedContentResourceSchema, InterviewGeneratedContentSortField, InterviewSessionFilterInput, InterviewSessionResourceSchema, InterviewSessionSortField, InterviewTurnResourceSchema, LibraryAccessGrantFilterInput, LibraryAccessGrantResourceSchema, LibraryAccessGrantSortField, LibraryFileFilterInput, LibraryFileResourceSchema, LibraryFileSortField, LibraryFilterInput, LibraryResourceSchema, LibrarySortField, OriginAssetFilterInput, OriginAssetResourceSchema, OriginAssetSortField, OriginEntityFilterInput, OriginEntityMembershipFilterInput, OriginEntityMembershipResourceSchema, OriginEntityMembershipSortField, OriginEntityResourceSchema, OriginEntitySortField, OriginsAppsEdgeInputSchema, OriginsAppsNodeInputSchema, OriginsIdentityAppearanceConfigInputSchema, OriginsIdentityBrandConversationConfigInputSchema, OriginsIdentityPromptContextGuardrailConfigInputSchema, OriginsIdentityPublicProfileConfigInputSchema, OriginsIdentitySocialLinkInputSchema, OriginsIdentitySoulConfigCommunicationStyleInputSchema, OriginsIdentitySoulConfigExperienceSettingsInputSchema, OriginsIdentitySoulConfigResponseFormatPreferencesInputSchema, PodcastConfigFilterInput, PodcastConfigResourceSchema, PodcastConfigSortField, PodcastEpisodeSettingsFilterInput, PodcastEpisodeSettingsResourceSchema, PodcastEpisodeSettingsSortField, PromptContextFilterInput, PromptContextResourceSchema, PromptContextSortField, PromptToolFilterInput, PromptToolResourceSchema, PromptToolSortField, PublicConversationFilterInput, PublicConversationResourceSchema, PublicConversationSortField, ResolvedPlaybackResourceSchema, ScrapedWebsiteContentResourceSchema, SetupProgressFilterInput, SetupProgressResourceSchema, SetupProgressSortField, SocialSignInResponseResourceSchema, SortString, SoulConfigFilterInput, SoulConfigResourceSchema, SoulConfigSortField, StaffTenantGrantFilterInput, StaffTenantGrantResourceSchema, StaffTenantGrantSortField, TenantFilterInput, TenantResourceSchema, TenantSortField, UUID, UnifiedFieldSelection, UserFilterInput, UserProfileFilterInput, UserProfileResourceSchema, UserProfileSortField, UserResourceSchema, UserSortField, UtcDateTime, UtcDateTimeUsec, ValidationResult, WaitlistEntryFilterInput, WaitlistEntryResourceSchema, WaitlistEntrySortField, WebhookDeliveryFilterInput, WebhookDeliveryResourceSchema, WebhookDeliverySortField, WebhookSubscriptionFilterInput, WebhookSubscriptionResourceSchema, WebhookSubscriptionSortField, YoutubeEpisodeFilterInput, YoutubeEpisodeResourceSchema, YoutubeEpisodeSortField } from "./ash_types";
export type * from "./ash_types";

// Helper Functions

/**
 * Configuration options for action RPC requests
 */
export interface ActionConfig {
  // Request data
  input?: Record<string, any>;
  identity?: any;
  fields?: Array<string | Record<string, any>>; // Field selection
  filter?: Record<string, any>; // Filter options (for reads)
  sort?: string | string[]; // Sort options
  page?:
    | {
        // Offset-based pagination
        limit?: number;
        offset?: number;
        count?: boolean;
      }
    | {
        // Keyset pagination
        limit?: number;
        after?: string;
        before?: string;
      };

  // Metadata
  metadata_fields?: ReadonlyArray<string>;

  // HTTP customization
  headers?: Record<string, string>; // Custom headers
  fetch_options?: RequestInit; // Fetch options (signal, cache, etc.)
  custom_fetch?: (
    input: RequestInfo | URL,
    init?: RequestInit,
  ) => Promise<Response>;

  // Multitenancy
  tenant?: string; // Tenant parameter

  // Hook context
  hook_ctx?: Record<string, any>;
}

/**
 * Configuration options for validation RPC requests
 */
export interface ValidationConfig {
  // Request data
  input?: Record<string, any>;

  // HTTP customization
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (
    input: RequestInfo | URL,
    init?: RequestInit,
  ) => Promise<Response>;

  // Hook context
  hook_ctx?: Record<string, any>;
}


/**
 * Configuration options for action channel RPC requests
 */
export interface ActionChannelConfig {
  // Request data
  input?: Record<string, any>;
  identity?: any;
  fields?: ReadonlyArray<string | Record<string, any>>;
  filter?: Record<string, any>;
  sort?: string | string[];
  page?:
    | {
        limit?: number;
        offset?: number;
        count?: boolean;
      }
    | {
        limit?: number;
        after?: string;
        before?: string;
      };

  // Metadata
  metadata_fields?: ReadonlyArray<string>;

  // Channel-specific
  channel: any; // Phoenix Channel
  result_handler: (result: any) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;

  // Multitenancy
  tenant?: string;

  // Hook context
  hook_ctx?: Record<string, any>;
}


/**
 * Configuration options for validation channel RPC requests
 */
export interface ValidationChannelConfig {
  // Request data
  input?: Record<string, any>;
  identity?: any;

  // Channel-specific
  channel: any; // Phoenix Channel
  result_handler: (result: any) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;

  // Multitenancy
  tenant?: string;

  // Hook context
  hook_ctx?: Record<string, any>;
}


/**
 * Gets the CSRF token from the page's meta tag
 * Returns null if no CSRF token is found
 */
export function getPhoenixCSRFToken(): string | null {
  return document
    ?.querySelector("meta[name='csrf-token']")
    ?.getAttribute("content") || null;
}

/**
 * Builds headers object with CSRF token for Phoenix applications
 * Returns headers object with X-CSRF-Token (if available)
 */
export function buildCSRFHeaders(headers: Record<string, string> = {}): Record<string, string> {
  const csrfToken = getPhoenixCSRFToken();
  if (csrfToken) {
    headers["X-CSRF-Token"] = csrfToken;
  }

  return headers;
}

/**
 * Internal helper function for making action RPC requests
 * Handles hooks, request configuration, fetch execution, and error handling
 * @param config Configuration matching ActionConfig
 */
export async function executeActionRpcRequest<T>(
  payload: Record<string, any>,
  config: ActionConfig
): Promise<T> {
    const processedConfig = config;

  const headers: Record<string, string> = {
    "Content-Type": "application/json",
    ...processedConfig.headers,
    ...config.headers,
  };

  const fetchFunction = config.custom_fetch || processedConfig.custom_fetch || fetch;
  const fetchOptions: RequestInit = {
    ...processedConfig.fetch_options,
    ...config.fetch_options,
    method: "POST",
    headers,
    body: JSON.stringify(payload),
  };

  const response = await fetchFunction("/rpc/run", fetchOptions);
  const result = response.ok ? await response.json() : null;


  if (!response.ok) {
    return {
      success: false,
      errors: [
        {
          type: "network_error",
          message: `Network request failed: ${response.statusText}`,
          short_message: "Network error",
          vars: { statusCode: response.status, statusText: response.statusText },
          fields: [],
          path: [],
          details: { statusCode: response.status }
        }
      ],
    } as T;
  }

  return result as T;
}


/**
 * Internal helper function for making validation RPC requests
 * Handles hooks, request configuration, fetch execution, and error handling
 * @param config Configuration matching ValidationConfig
 */
export async function executeValidationRpcRequest<T>(
  payload: Record<string, any>,
  config: ValidationConfig
): Promise<T> {
    const processedConfig = config;

  const headers: Record<string, string> = {
    "Content-Type": "application/json",
    ...processedConfig.headers,
    ...config.headers,
  };

  const fetchFunction = config.custom_fetch || processedConfig.custom_fetch || fetch;
  const fetchOptions: RequestInit = {
    ...processedConfig.fetch_options,
    ...config.fetch_options,
    method: "POST",
    headers,
    body: JSON.stringify(payload),
  };

  const response = await fetchFunction("/rpc/validate", fetchOptions);
  const result = response.ok ? await response.json() : null;


  if (!response.ok) {
    return {
      success: false,
      errors: [
        {
          type: "network_error",
          message: `Network request failed: ${response.statusText}`,
          short_message: "Network error",
          vars: { statusCode: response.status, statusText: response.statusText },
          fields: [],
          path: [],
          details: { statusCode: response.status }
        }
      ],
    } as T;
  }

  return result as T;
}


/**
 * Internal helper function for making action channel push requests
 * Handles hooks and channel push with receive handlers
 * @param config Configuration matching ActionChannelConfig
 */
export async function executeActionChannelPush<T>(
  channel: any,
  payload: Record<string, any>,
  timeout: number | undefined,
  config: ActionChannelConfig
) {
    const processedConfig = config;

  const effectiveTimeout = timeout;

  channel
    .push("run", payload, effectiveTimeout)
    .receive("ok", async (result: T) => {

      config.result_handler(result);
    })
    .receive("error", async (error: any) => {

      (config.error_handler
        ? config.error_handler
        : (error: any) => {
            console.error(
              `An error occurred while running action ${payload.action}:`,
              error
            );
          })(error);
    })
    .receive("timeout", async () => {

      (config.timeout_handler
        ? config.timeout_handler
        : () => {
            console.error(`Timeout occurred while running action ${payload.action}`);
          })();
    });
}


/**
 * Internal helper function for making validation channel push requests
 * Handles hooks and channel push with receive handlers
 * @param config Configuration matching ValidationChannelConfig
 */
export async function executeValidationChannelPush<T>(
  channel: any,
  payload: Record<string, any>,
  timeout: number | undefined,
  config: ValidationChannelConfig
) {
    const processedConfig = config;

  const effectiveTimeout = timeout;

  channel
    .push("validate", payload, effectiveTimeout)
    .receive("ok", async (result: T) => {

      config.result_handler(result);
    })
    .receive("error", async (error: any) => {

      (config.error_handler
        ? config.error_handler
        : (error: any) => {
            console.error(
              `An error occurred while running action ${payload.action}:`,
              error
            );
          })(error);
    })
    .receive("timeout", async () => {

      (config.timeout_handler
        ? config.timeout_handler
        : () => {
            console.error(`Timeout occurred while running action ${payload.action}`);
          })();
    });
}



export type ListMyTenantGrantsFields = UnifiedFieldSelection<StaffTenantGrantResourceSchema>[];
export type InferListMyTenantGrantsResult<
  Fields extends ListMyTenantGrantsFields,
> = Array<InferResult<StaffTenantGrantResourceSchema, Fields>>;

export type ListMyTenantGrantsResult<Fields extends ListMyTenantGrantsFields> = | { success: true; data: InferListMyTenantGrantsResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read StaffTenantGrant records
 *
 * @ashActionType :read
 */
export async function list_my_tenant_grants<Fields extends ListMyTenantGrantsFields>(
  config: {
  tenant?: string;
  fields: Fields;
  filter?: StaffTenantGrantFilterInput;
  sort?: SortString<StaffTenantGrantSortField> | SortString<StaffTenantGrantSortField>[];
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ListMyTenantGrantsResult<Fields>> {
  const payload = {
    action: "list_my_tenant_grants",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort })
  };

  return executeActionRpcRequest<ListMyTenantGrantsResult<Fields>>(
    payload,
    config
  );
}


/**
 * Validate: Read StaffTenantGrant records
 *
 * @ashActionType :read
 * @validation true
 */
export async function validate_list_my_tenant_grants(
  config: {
  tenant?: string;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "list_my_tenant_grants",
    ...(config.tenant !== undefined && { tenant: config.tenant })
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Read StaffTenantGrant records
 *
 * @ashActionType :read
 * @validation true
 */
export async function validate_list_my_tenant_grants_channel(config: {
  channel: Channel;
  tenant?: string;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "list_my_tenant_grants",
    ...(config.tenant !== undefined && { tenant: config.tenant })
  },
    config.timeout,
    config
  );
}


/**
 * Read StaffTenantGrant records
 *
 * @ashActionType :read
 */
export async function list_my_tenant_grants_channel<Fields extends ListMyTenantGrantsFields>(config: {
  channel: Channel;
  tenant?: string;
  fields: Fields;
  filter?: StaffTenantGrantFilterInput;
  sort?: SortString<StaffTenantGrantSortField> | SortString<StaffTenantGrantSortField>[];
  result_handler: (result: ListMyTenantGrantsResult<Fields>) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<ListMyTenantGrantsResult<Fields>>(
    config.channel,
    {
    action: "list_my_tenant_grants",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort })
  },
    config.timeout,
    config
  );
}


export type CreateTenantForUserInput = {
  slug: string;
  display_name: string;
  picture_url?: string | null;
  profile?: Record<string, any> | null;
};

export type InferCreateTenantForUserResult = Record<string, any>;

export type CreateTenantForUserResult = | { success: true; data: InferCreateTenantForUserResult; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Execute generic action on Tenant
 *
 * @ashActionType :action
 */
export async function create_tenant_for_user(
  config: {
  tenant?: string;
  input: CreateTenantForUserInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<CreateTenantForUserResult> {
  const payload = {
    action: "create_tenant_for_user",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeActionRpcRequest<CreateTenantForUserResult>(
    payload,
    config
  );
}


/**
 * Validate: Execute generic action on Tenant
 *
 * @ashActionType :action
 * @validation true
 */
export async function validate_create_tenant_for_user(
  config: {
  tenant?: string;
  input: CreateTenantForUserInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "create_tenant_for_user",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Execute generic action on Tenant
 *
 * @ashActionType :action
 * @validation true
 */
export async function validate_create_tenant_for_user_channel(config: {
  channel: Channel;
  tenant?: string;
  input: CreateTenantForUserInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "create_tenant_for_user",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Execute generic action on Tenant
 *
 * @ashActionType :action
 */
export async function create_tenant_for_user_channel(config: {
  channel: Channel;
  tenant?: string;
  input: CreateTenantForUserInput;
  result_handler: (result: CreateTenantForUserResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<CreateTenantForUserResult>(
    config.channel,
    {
    action: "create_tenant_for_user",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


export type GetTenantInput = {
  id: UUID;
};

export type GetTenantFields = UnifiedFieldSelection<TenantResourceSchema>[];
export type InferGetTenantResult<
  Fields extends GetTenantFields,
> = InferResult<TenantResourceSchema, Fields>;

export type GetTenantResult<Fields extends GetTenantFields> = | { success: true; data: InferGetTenantResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read Tenant records
 *
 * @ashActionType :read
 */
export async function get_tenant<Fields extends GetTenantFields>(
  config: {
  tenant?: string;
  input: GetTenantInput;
  fields: Fields;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<GetTenantResult<Fields>> {
  const payload = {
    action: "get_tenant",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<GetTenantResult<Fields>>(
    payload,
    config
  );
}


/**
 * Validate: Read Tenant records
 *
 * @ashActionType :read
 * @validation true
 */
export async function validate_get_tenant(
  config: {
  tenant?: string;
  input: GetTenantInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "get_tenant",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Read Tenant records
 *
 * @ashActionType :read
 * @validation true
 */
export async function validate_get_tenant_channel(config: {
  channel: Channel;
  tenant?: string;
  input: GetTenantInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "get_tenant",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Read Tenant records
 *
 * @ashActionType :read
 */
export async function get_tenant_channel<Fields extends GetTenantFields>(config: {
  channel: Channel;
  tenant?: string;
  input: GetTenantInput;
  fields: Fields;
  result_handler: (result: GetTenantResult<Fields>) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<GetTenantResult<Fields>>(
    config.channel,
    {
    action: "get_tenant",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  },
    config.timeout,
    config
  );
}


export type ListAllTenantsFields = UnifiedFieldSelection<TenantResourceSchema>[];
export type InferListAllTenantsResult<
  Fields extends ListAllTenantsFields,
> = Array<InferResult<TenantResourceSchema, Fields>>;

export type ListAllTenantsResult<Fields extends ListAllTenantsFields> = | { success: true; data: InferListAllTenantsResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read Tenant records
 *
 * @ashActionType :read
 */
export async function list_all_tenants<Fields extends ListAllTenantsFields>(
  config: {
  tenant?: string;
  fields: Fields;
  filter?: TenantFilterInput;
  sort?: SortString<TenantSortField> | SortString<TenantSortField>[];
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ListAllTenantsResult<Fields>> {
  const payload = {
    action: "list_all_tenants",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort })
  };

  return executeActionRpcRequest<ListAllTenantsResult<Fields>>(
    payload,
    config
  );
}


/**
 * Validate: Read Tenant records
 *
 * @ashActionType :read
 * @validation true
 */
export async function validate_list_all_tenants(
  config: {
  tenant?: string;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "list_all_tenants",
    ...(config.tenant !== undefined && { tenant: config.tenant })
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Read Tenant records
 *
 * @ashActionType :read
 * @validation true
 */
export async function validate_list_all_tenants_channel(config: {
  channel: Channel;
  tenant?: string;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "list_all_tenants",
    ...(config.tenant !== undefined && { tenant: config.tenant })
  },
    config.timeout,
    config
  );
}


/**
 * Read Tenant records
 *
 * @ashActionType :read
 */
export async function list_all_tenants_channel<Fields extends ListAllTenantsFields>(config: {
  channel: Channel;
  tenant?: string;
  fields: Fields;
  filter?: TenantFilterInput;
  sort?: SortString<TenantSortField> | SortString<TenantSortField>[];
  result_handler: (result: ListAllTenantsResult<Fields>) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<ListAllTenantsResult<Fields>>(
    config.channel,
    {
    action: "list_all_tenants",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort })
  },
    config.timeout,
    config
  );
}


export type ListMyTenantsFields = UnifiedFieldSelection<TenantResourceSchema>[];
export type InferListMyTenantsResult<
  Fields extends ListMyTenantsFields,
> = Array<InferResult<TenantResourceSchema, Fields>>;

export type ListMyTenantsResult<Fields extends ListMyTenantsFields> = | { success: true; data: InferListMyTenantsResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read Tenant records
 *
 * @ashActionType :read
 */
export async function list_my_tenants<Fields extends ListMyTenantsFields>(
  config: {
  tenant?: string;
  fields: Fields;
  filter?: TenantFilterInput;
  sort?: SortString<TenantSortField> | SortString<TenantSortField>[];
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ListMyTenantsResult<Fields>> {
  const payload = {
    action: "list_my_tenants",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort })
  };

  return executeActionRpcRequest<ListMyTenantsResult<Fields>>(
    payload,
    config
  );
}


/**
 * Validate: Read Tenant records
 *
 * @ashActionType :read
 * @validation true
 */
export async function validate_list_my_tenants(
  config: {
  tenant?: string;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "list_my_tenants",
    ...(config.tenant !== undefined && { tenant: config.tenant })
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Read Tenant records
 *
 * @ashActionType :read
 * @validation true
 */
export async function validate_list_my_tenants_channel(config: {
  channel: Channel;
  tenant?: string;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "list_my_tenants",
    ...(config.tenant !== undefined && { tenant: config.tenant })
  },
    config.timeout,
    config
  );
}


/**
 * Read Tenant records
 *
 * @ashActionType :read
 */
export async function list_my_tenants_channel<Fields extends ListMyTenantsFields>(config: {
  channel: Channel;
  tenant?: string;
  fields: Fields;
  filter?: TenantFilterInput;
  sort?: SortString<TenantSortField> | SortString<TenantSortField>[];
  result_handler: (result: ListMyTenantsResult<Fields>) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<ListMyTenantsResult<Fields>>(
    config.channel,
    {
    action: "list_my_tenants",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort })
  },
    config.timeout,
    config
  );
}


export type GetCurrentUserFields = UnifiedFieldSelection<UserResourceSchema>[];
export type InferGetCurrentUserResult<
  Fields extends GetCurrentUserFields,
> = InferResult<UserResourceSchema, Fields>;

export type GetCurrentUserResult<Fields extends GetCurrentUserFields> = | { success: true; data: InferGetCurrentUserResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read User records
 *
 * @ashActionType :read
 */
export async function get_current_user<Fields extends GetCurrentUserFields>(
  config: {
  tenant?: string;
  fields: Fields;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<GetCurrentUserResult<Fields>> {
  const payload = {
    action: "get_current_user",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<GetCurrentUserResult<Fields>>(
    payload,
    config
  );
}


/**
 * Validate: Read User records
 *
 * @ashActionType :read
 * @validation true
 */
export async function validate_get_current_user(
  config: {
  tenant?: string;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "get_current_user",
    ...(config.tenant !== undefined && { tenant: config.tenant })
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Read User records
 *
 * @ashActionType :read
 * @validation true
 */
export async function validate_get_current_user_channel(config: {
  channel: Channel;
  tenant?: string;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "get_current_user",
    ...(config.tenant !== undefined && { tenant: config.tenant })
  },
    config.timeout,
    config
  );
}


/**
 * Read User records
 *
 * @ashActionType :read
 */
export async function get_current_user_channel<Fields extends GetCurrentUserFields>(config: {
  channel: Channel;
  tenant?: string;
  fields: Fields;
  result_handler: (result: GetCurrentUserResult<Fields>) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<GetCurrentUserResult<Fields>>(
    config.channel,
    {
    action: "get_current_user",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    ...(config.fields !== undefined && { fields: config.fields })
  },
    config.timeout,
    config
  );
}


export type ListAllUsersFields = UnifiedFieldSelection<UserResourceSchema>[];
export type InferListAllUsersResult<
  Fields extends ListAllUsersFields,
> = Array<InferResult<UserResourceSchema, Fields>>;

export type ListAllUsersResult<Fields extends ListAllUsersFields> = | { success: true; data: InferListAllUsersResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read User records
 *
 * @ashActionType :read
 */
export async function list_all_users<Fields extends ListAllUsersFields>(
  config: {
  tenant?: string;
  fields: Fields;
  filter?: UserFilterInput;
  sort?: SortString<UserSortField> | SortString<UserSortField>[];
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ListAllUsersResult<Fields>> {
  const payload = {
    action: "list_all_users",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort })
  };

  return executeActionRpcRequest<ListAllUsersResult<Fields>>(
    payload,
    config
  );
}


/**
 * Validate: Read User records
 *
 * @ashActionType :read
 * @validation true
 */
export async function validate_list_all_users(
  config: {
  tenant?: string;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "list_all_users",
    ...(config.tenant !== undefined && { tenant: config.tenant })
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Read User records
 *
 * @ashActionType :read
 * @validation true
 */
export async function validate_list_all_users_channel(config: {
  channel: Channel;
  tenant?: string;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "list_all_users",
    ...(config.tenant !== undefined && { tenant: config.tenant })
  },
    config.timeout,
    config
  );
}


/**
 * Read User records
 *
 * @ashActionType :read
 */
export async function list_all_users_channel<Fields extends ListAllUsersFields>(config: {
  channel: Channel;
  tenant?: string;
  fields: Fields;
  filter?: UserFilterInput;
  sort?: SortString<UserSortField> | SortString<UserSortField>[];
  result_handler: (result: ListAllUsersResult<Fields>) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<ListAllUsersResult<Fields>>(
    config.channel,
    {
    action: "list_all_users",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort })
  },
    config.timeout,
    config
  );
}


export type RegisterWithInvitationInput = {
  invitation_token: UUID;
  password: string;
  password_confirmation: string;
};

export type RegisterWithInvitationFields = UnifiedFieldSelection<UserResourceSchema>[];

export type RegisterWithInvitationMetadata = {
  token: string;
};

export type InferRegisterWithInvitationResult<
  Fields extends RegisterWithInvitationFields | undefined,
  MetadataFields extends ReadonlyArray<keyof RegisterWithInvitationMetadata> = []
> = InferResult<UserResourceSchema, Fields>;

export type RegisterWithInvitationResult<Fields extends RegisterWithInvitationFields | undefined = undefined, MetadataFields extends ReadonlyArray<keyof RegisterWithInvitationMetadata> = []> = | { success: true; data: InferRegisterWithInvitationResult<Fields>; metadata: Pick<RegisterWithInvitationMetadata, MetadataFields[number]>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Create a new User
 *
 * @ashActionType :create
 */
export async function register_with_invitation<Fields extends RegisterWithInvitationFields | undefined = undefined, MetadataFields extends ReadonlyArray<keyof RegisterWithInvitationMetadata> = []>(
  config: {
  tenant?: string;
  input: RegisterWithInvitationInput;
  fields?: Fields;
  metadata_fields?: MetadataFields;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<RegisterWithInvitationResult<Fields extends undefined ? [] : Fields, MetadataFields>> {
  const payload = {
    action: "register_with_invitation",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.metadata_fields && { metadata_fields: config.metadata_fields })
  };

  return executeActionRpcRequest<RegisterWithInvitationResult<Fields extends undefined ? [] : Fields, MetadataFields>>(
    payload,
    config
  );
}


/**
 * Validate: Create a new User
 *
 * @ashActionType :create
 * @validation true
 */
export async function validate_register_with_invitation(
  config: {
  tenant?: string;
  input: RegisterWithInvitationInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "register_with_invitation",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Create a new User
 *
 * @ashActionType :create
 * @validation true
 */
export async function validate_register_with_invitation_channel(config: {
  channel: Channel;
  tenant?: string;
  input: RegisterWithInvitationInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "register_with_invitation",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Create a new User
 *
 * @ashActionType :create
 */
export async function register_with_invitation_channel<Fields extends RegisterWithInvitationFields | undefined = undefined, MetadataFields extends ReadonlyArray<keyof RegisterWithInvitationMetadata> = []>(config: {
  channel: Channel;
  tenant?: string;
  input: RegisterWithInvitationInput;
  fields?: Fields;
  metadata_fields?: MetadataFields;
  result_handler: (result: RegisterWithInvitationResult<Fields, MetadataFields>) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<RegisterWithInvitationResult<Fields, MetadataFields>>(
    config.channel,
    {
    action: "register_with_invitation",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.metadata_fields && { metadata_fields: config.metadata_fields })
  },
    config.timeout,
    config
  );
}


export type RegisterWithPasswordInput = {
  email: string;
  password: string;
  password_confirmation: string;
  confirm_redirect_path?: string | null;
};

export type RegisterWithPasswordFields = UnifiedFieldSelection<UserResourceSchema>[];

export type RegisterWithPasswordMetadata = {
  token: string;
};

export type InferRegisterWithPasswordResult<
  Fields extends RegisterWithPasswordFields | undefined,
  MetadataFields extends ReadonlyArray<keyof RegisterWithPasswordMetadata> = []
> = InferResult<UserResourceSchema, Fields>;

export type RegisterWithPasswordResult<Fields extends RegisterWithPasswordFields | undefined = undefined, MetadataFields extends ReadonlyArray<keyof RegisterWithPasswordMetadata> = []> = | { success: true; data: InferRegisterWithPasswordResult<Fields>; metadata: Pick<RegisterWithPasswordMetadata, MetadataFields[number]>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Create a new User
 *
 * @ashActionType :create
 */
export async function register_with_password<Fields extends RegisterWithPasswordFields | undefined = undefined, MetadataFields extends ReadonlyArray<keyof RegisterWithPasswordMetadata> = []>(
  config: {
  tenant?: string;
  input: RegisterWithPasswordInput;
  fields?: Fields;
  metadata_fields?: MetadataFields;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<RegisterWithPasswordResult<Fields extends undefined ? [] : Fields, MetadataFields>> {
  const payload = {
    action: "register_with_password",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.metadata_fields && { metadata_fields: config.metadata_fields })
  };

  return executeActionRpcRequest<RegisterWithPasswordResult<Fields extends undefined ? [] : Fields, MetadataFields>>(
    payload,
    config
  );
}


/**
 * Validate: Create a new User
 *
 * @ashActionType :create
 * @validation true
 */
export async function validate_register_with_password(
  config: {
  tenant?: string;
  input: RegisterWithPasswordInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "register_with_password",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Create a new User
 *
 * @ashActionType :create
 * @validation true
 */
export async function validate_register_with_password_channel(config: {
  channel: Channel;
  tenant?: string;
  input: RegisterWithPasswordInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "register_with_password",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Create a new User
 *
 * @ashActionType :create
 */
export async function register_with_password_channel<Fields extends RegisterWithPasswordFields | undefined = undefined, MetadataFields extends ReadonlyArray<keyof RegisterWithPasswordMetadata> = []>(config: {
  channel: Channel;
  tenant?: string;
  input: RegisterWithPasswordInput;
  fields?: Fields;
  metadata_fields?: MetadataFields;
  result_handler: (result: RegisterWithPasswordResult<Fields, MetadataFields>) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<RegisterWithPasswordResult<Fields, MetadataFields>>(
    config.channel,
    {
    action: "register_with_password",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.metadata_fields && { metadata_fields: config.metadata_fields })
  },
    config.timeout,
    config
  );
}


export type SignInWithAppleTokenInput = {
  id_token: string;
};

export type InferSignInWithAppleTokenResult = SocialSignInResponseResourceSchema;

export type SignInWithAppleTokenResult = | { success: true; data: InferSignInWithAppleTokenResult; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Execute generic action on User
 *
 * @ashActionType :action
 */
export async function sign_in_with_apple_token(
  config: {
  tenant?: string;
  input: SignInWithAppleTokenInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<SignInWithAppleTokenResult> {
  const payload = {
    action: "sign_in_with_apple_token",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeActionRpcRequest<SignInWithAppleTokenResult>(
    payload,
    config
  );
}


/**
 * Validate: Execute generic action on User
 *
 * @ashActionType :action
 * @validation true
 */
export async function validate_sign_in_with_apple_token(
  config: {
  tenant?: string;
  input: SignInWithAppleTokenInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "sign_in_with_apple_token",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Execute generic action on User
 *
 * @ashActionType :action
 * @validation true
 */
export async function validate_sign_in_with_apple_token_channel(config: {
  channel: Channel;
  tenant?: string;
  input: SignInWithAppleTokenInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "sign_in_with_apple_token",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Execute generic action on User
 *
 * @ashActionType :action
 */
export async function sign_in_with_apple_token_channel(config: {
  channel: Channel;
  tenant?: string;
  input: SignInWithAppleTokenInput;
  result_handler: (result: SignInWithAppleTokenResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<SignInWithAppleTokenResult>(
    config.channel,
    {
    action: "sign_in_with_apple_token",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


export type SignInWithGoogleTokenInput = {
  id_token: string;
};

export type InferSignInWithGoogleTokenResult = SocialSignInResponseResourceSchema;

export type SignInWithGoogleTokenResult = | { success: true; data: InferSignInWithGoogleTokenResult; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Execute generic action on User
 *
 * @ashActionType :action
 */
export async function sign_in_with_google_token(
  config: {
  tenant?: string;
  input: SignInWithGoogleTokenInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<SignInWithGoogleTokenResult> {
  const payload = {
    action: "sign_in_with_google_token",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeActionRpcRequest<SignInWithGoogleTokenResult>(
    payload,
    config
  );
}


/**
 * Validate: Execute generic action on User
 *
 * @ashActionType :action
 * @validation true
 */
export async function validate_sign_in_with_google_token(
  config: {
  tenant?: string;
  input: SignInWithGoogleTokenInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "sign_in_with_google_token",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Execute generic action on User
 *
 * @ashActionType :action
 * @validation true
 */
export async function validate_sign_in_with_google_token_channel(config: {
  channel: Channel;
  tenant?: string;
  input: SignInWithGoogleTokenInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "sign_in_with_google_token",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Execute generic action on User
 *
 * @ashActionType :action
 */
export async function sign_in_with_google_token_channel(config: {
  channel: Channel;
  tenant?: string;
  input: SignInWithGoogleTokenInput;
  result_handler: (result: SignInWithGoogleTokenResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<SignInWithGoogleTokenResult>(
    config.channel,
    {
    action: "sign_in_with_google_token",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


export type SignInWithPasswordInput = {
  email: string;
  password: string;
};

export type SignInWithPasswordFields = UnifiedFieldSelection<UserResourceSchema>[];

export type SignInWithPasswordMetadata = {
  token: string;
};

export type InferSignInWithPasswordResult<
  Fields extends SignInWithPasswordFields,
  MetadataFields extends ReadonlyArray<keyof SignInWithPasswordMetadata> = []
> = (InferResult<UserResourceSchema, Fields> & Pick<SignInWithPasswordMetadata, MetadataFields[number]>);

export type SignInWithPasswordResult<Fields extends SignInWithPasswordFields, MetadataFields extends ReadonlyArray<keyof SignInWithPasswordMetadata> = []> = | { success: true; data: InferSignInWithPasswordResult<Fields, MetadataFields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read User records
 *
 * @ashActionType :read
 */
export async function sign_in_with_password<Fields extends SignInWithPasswordFields, MetadataFields extends ReadonlyArray<keyof SignInWithPasswordMetadata> = []>(
  config: {
  tenant?: string;
  input: SignInWithPasswordInput;
  fields: Fields;
  metadata_fields?: MetadataFields;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<SignInWithPasswordResult<Fields, MetadataFields>> {
  const payload = {
    action: "sign_in_with_password",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.metadata_fields && { metadata_fields: config.metadata_fields })
  };

  return executeActionRpcRequest<SignInWithPasswordResult<Fields, MetadataFields>>(
    payload,
    config
  );
}


/**
 * Validate: Read User records
 *
 * @ashActionType :read
 * @validation true
 */
export async function validate_sign_in_with_password(
  config: {
  tenant?: string;
  input: SignInWithPasswordInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "sign_in_with_password",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Read User records
 *
 * @ashActionType :read
 * @validation true
 */
export async function validate_sign_in_with_password_channel(config: {
  channel: Channel;
  tenant?: string;
  input: SignInWithPasswordInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "sign_in_with_password",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Read User records
 *
 * @ashActionType :read
 */
export async function sign_in_with_password_channel<Fields extends SignInWithPasswordFields, MetadataFields extends ReadonlyArray<keyof SignInWithPasswordMetadata> = []>(config: {
  channel: Channel;
  tenant?: string;
  input: SignInWithPasswordInput;
  fields: Fields;
  metadata_fields?: MetadataFields;
  result_handler: (result: SignInWithPasswordResult<Fields, MetadataFields>) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<SignInWithPasswordResult<Fields, MetadataFields>>(
    config.channel,
    {
    action: "sign_in_with_password",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.metadata_fields && { metadata_fields: config.metadata_fields })
  },
    config.timeout,
    config
  );
}


export type StartTenantSessionInput = {
  tenant_id: UUID;
};

export type InferStartTenantSessionResult = Record<string, any>;

export type StartTenantSessionResult = | { success: true; data: InferStartTenantSessionResult; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Execute generic action on User
 *
 * @ashActionType :action
 */
export async function start_tenant_session(
  config: {
  tenant?: string;
  input: StartTenantSessionInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<StartTenantSessionResult> {
  const payload = {
    action: "start_tenant_session",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeActionRpcRequest<StartTenantSessionResult>(
    payload,
    config
  );
}


/**
 * Validate: Execute generic action on User
 *
 * @ashActionType :action
 * @validation true
 */
export async function validate_start_tenant_session(
  config: {
  tenant?: string;
  input: StartTenantSessionInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "start_tenant_session",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Execute generic action on User
 *
 * @ashActionType :action
 * @validation true
 */
export async function validate_start_tenant_session_channel(config: {
  channel: Channel;
  tenant?: string;
  input: StartTenantSessionInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "start_tenant_session",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Execute generic action on User
 *
 * @ashActionType :action
 */
export async function start_tenant_session_channel(config: {
  channel: Channel;
  tenant?: string;
  input: StartTenantSessionInput;
  result_handler: (result: StartTenantSessionResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<StartTenantSessionResult>(
    config.channel,
    {
    action: "start_tenant_session",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


export type ApproveWaitlistEntryFields = UnifiedFieldSelection<WaitlistEntryResourceSchema>[];

export type InferApproveWaitlistEntryResult<
  Fields extends ApproveWaitlistEntryFields | undefined,
> = InferResult<WaitlistEntryResourceSchema, Fields>;

export type ApproveWaitlistEntryResult<Fields extends ApproveWaitlistEntryFields | undefined = undefined> = | { success: true; data: InferApproveWaitlistEntryResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing WaitlistEntry
 *
 * @ashActionType :update
 */
export async function approve_waitlist_entry<Fields extends ApproveWaitlistEntryFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUID;
  fields?: Fields;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ApproveWaitlistEntryResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "approve_waitlist_entry",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<ApproveWaitlistEntryResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


/**
 * Validate: Update an existing WaitlistEntry
 *
 * @ashActionType :update
 * @validation true
 */
export async function validate_approve_waitlist_entry(
  config: {
  tenant?: string;
  identity: UUID | string;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "approve_waitlist_entry",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Update an existing WaitlistEntry
 *
 * @ashActionType :update
 * @validation true
 */
export async function validate_approve_waitlist_entry_channel(config: {
  channel: Channel;
  tenant?: string;
  identity: UUID | string;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "approve_waitlist_entry",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity
  },
    config.timeout,
    config
  );
}


/**
 * Update an existing WaitlistEntry
 *
 * @ashActionType :update
 */
export async function approve_waitlist_entry_channel<Fields extends ApproveWaitlistEntryFields | undefined = undefined>(config: {
  channel: Channel;
  tenant?: string;
  identity: UUID;
  fields?: Fields;
  result_handler: (result: ApproveWaitlistEntryResult<Fields>) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<ApproveWaitlistEntryResult<Fields>>(
    config.channel,
    {
    action: "approve_waitlist_entry",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    ...(config.fields !== undefined && { fields: config.fields })
  },
    config.timeout,
    config
  );
}


export type DeleteWaitlistEntryResult = | { success: true; data: {}; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Delete a WaitlistEntry
 *
 * @ashActionType :destroy
 */
export async function delete_waitlist_entry(
  config: {
  tenant?: string;
  identity: UUID;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<DeleteWaitlistEntryResult> {
  const payload = {
    action: "delete_waitlist_entry",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity
  };

  return executeActionRpcRequest<DeleteWaitlistEntryResult>(
    payload,
    config
  );
}


/**
 * Validate: Delete a WaitlistEntry
 *
 * @ashActionType :destroy
 * @validation true
 */
export async function validate_delete_waitlist_entry(
  config: {
  tenant?: string;
  identity: UUID | string;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "delete_waitlist_entry",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Delete a WaitlistEntry
 *
 * @ashActionType :destroy
 * @validation true
 */
export async function validate_delete_waitlist_entry_channel(config: {
  channel: Channel;
  tenant?: string;
  identity: UUID | string;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "delete_waitlist_entry",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity
  },
    config.timeout,
    config
  );
}


/**
 * Delete a WaitlistEntry
 *
 * @ashActionType :destroy
 */
export async function delete_waitlist_entry_channel(config: {
  channel: Channel;
  tenant?: string;
  identity: UUID;
  result_handler: (result: DeleteWaitlistEntryResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<DeleteWaitlistEntryResult>(
    config.channel,
    {
    action: "delete_waitlist_entry",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity
  },
    config.timeout,
    config
  );
}


export type GetWaitlistEntryByTokenInput = {
  invitation_token: UUID;
};

export type GetWaitlistEntryByTokenFields = UnifiedFieldSelection<WaitlistEntryResourceSchema>[];
export type InferGetWaitlistEntryByTokenResult<
  Fields extends GetWaitlistEntryByTokenFields,
> = InferResult<WaitlistEntryResourceSchema, Fields>;

export type GetWaitlistEntryByTokenResult<Fields extends GetWaitlistEntryByTokenFields> = | { success: true; data: InferGetWaitlistEntryByTokenResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read WaitlistEntry records
 *
 * @ashActionType :read
 */
export async function get_waitlist_entry_by_token<Fields extends GetWaitlistEntryByTokenFields>(
  config: {
  tenant?: string;
  input: GetWaitlistEntryByTokenInput;
  fields: Fields;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<GetWaitlistEntryByTokenResult<Fields>> {
  const payload = {
    action: "get_waitlist_entry_by_token",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<GetWaitlistEntryByTokenResult<Fields>>(
    payload,
    config
  );
}


/**
 * Validate: Read WaitlistEntry records
 *
 * @ashActionType :read
 * @validation true
 */
export async function validate_get_waitlist_entry_by_token(
  config: {
  tenant?: string;
  input: GetWaitlistEntryByTokenInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "get_waitlist_entry_by_token",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Read WaitlistEntry records
 *
 * @ashActionType :read
 * @validation true
 */
export async function validate_get_waitlist_entry_by_token_channel(config: {
  channel: Channel;
  tenant?: string;
  input: GetWaitlistEntryByTokenInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "get_waitlist_entry_by_token",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Read WaitlistEntry records
 *
 * @ashActionType :read
 */
export async function get_waitlist_entry_by_token_channel<Fields extends GetWaitlistEntryByTokenFields>(config: {
  channel: Channel;
  tenant?: string;
  input: GetWaitlistEntryByTokenInput;
  fields: Fields;
  result_handler: (result: GetWaitlistEntryByTokenResult<Fields>) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<GetWaitlistEntryByTokenResult<Fields>>(
    config.channel,
    {
    action: "get_waitlist_entry_by_token",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  },
    config.timeout,
    config
  );
}


export type ListWaitlistEntriesFields = UnifiedFieldSelection<WaitlistEntryResourceSchema>[];


export type InferListWaitlistEntriesResult<
  Fields extends ListWaitlistEntriesFields | undefined,
  Page extends ListWaitlistEntriesConfig["page"] = undefined
> = ConditionalPaginatedResultMixed<Page, Array<InferResult<WaitlistEntryResourceSchema, Fields>>, {
  results: Array<InferResult<WaitlistEntryResourceSchema, Fields>>;
  has_more: boolean;
  limit: number;
  offset: number;
  count?: number | null;
  type: "offset";
}, {
  results: Array<InferResult<WaitlistEntryResourceSchema, Fields>>;
  has_more: boolean;
  limit: number;
  after: string | null;
  before: string | null;
  previous_page: string;
  next_page: string;
  count?: number | null;
  type: "keyset";
}>;

export type ListWaitlistEntriesConfig = {
  tenant?: string;
  fields: ListWaitlistEntriesFields;
  filter?: WaitlistEntryFilterInput;
  sort?: SortString<WaitlistEntrySortField> | SortString<WaitlistEntrySortField>[];
  page?: (
    {
      limit?: number;
      offset?: number;
      count?: boolean;
    } | {
      limit?: number;
      after?: string;
      before?: string;
    }
  );
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
};

export type ListWaitlistEntriesResult<Fields extends ListWaitlistEntriesFields, Page extends ListWaitlistEntriesConfig["page"] = undefined> = | { success: true; data: InferListWaitlistEntriesResult<Fields, Page>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read WaitlistEntry records
 *
 * @ashActionType :read
 */
export async function list_waitlist_entries<Fields extends ListWaitlistEntriesFields, Config extends ListWaitlistEntriesConfig = ListWaitlistEntriesConfig>(
  config: Config & { fields: Fields }
): Promise<ListWaitlistEntriesResult<Fields, Config["page"]>> {
  const payload = {
    action: "list_waitlist_entries",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort }),
    ...(config.page && { page: config.page })
  };

  return executeActionRpcRequest<ListWaitlistEntriesResult<Fields, Config["page"]>>(
    payload,
    config
  );
}


/**
 * Validate: Read WaitlistEntry records
 *
 * @ashActionType :read
 * @validation true
 */
export async function validate_list_waitlist_entries(
  config: {
  tenant?: string;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "list_waitlist_entries",
    ...(config.tenant !== undefined && { tenant: config.tenant })
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Read WaitlistEntry records
 *
 * @ashActionType :read
 * @validation true
 */
export async function validate_list_waitlist_entries_channel(config: {
  channel: Channel;
  tenant?: string;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "list_waitlist_entries",
    ...(config.tenant !== undefined && { tenant: config.tenant })
  },
    config.timeout,
    config
  );
}


/**
 * Read WaitlistEntry records
 *
 * @ashActionType :read
 */
export async function list_waitlist_entries_channel<Fields extends ListWaitlistEntriesFields>(config: {
  channel: Channel;
  tenant?: string;
  fields: Fields;
  filter?: WaitlistEntryFilterInput;
  sort?: SortString<WaitlistEntrySortField> | SortString<WaitlistEntrySortField>[];
  page?: (
    {
      limit?: number;
      offset?: number;
      count?: boolean;
    } | {
      limit?: number;
      after?: string;
      before?: string;
    }
  );
  result_handler: (result: ListWaitlistEntriesResult<Fields>) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<ListWaitlistEntriesResult<Fields>>(
    config.channel,
    {
    action: "list_waitlist_entries",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort }),
    ...(config.page && { page: config.page })
  },
    config.timeout,
    config
  );
}


export type RejectWaitlistEntryFields = UnifiedFieldSelection<WaitlistEntryResourceSchema>[];

export type InferRejectWaitlistEntryResult<
  Fields extends RejectWaitlistEntryFields | undefined,
> = InferResult<WaitlistEntryResourceSchema, Fields>;

export type RejectWaitlistEntryResult<Fields extends RejectWaitlistEntryFields | undefined = undefined> = | { success: true; data: InferRejectWaitlistEntryResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing WaitlistEntry
 *
 * @ashActionType :update
 */
export async function reject_waitlist_entry<Fields extends RejectWaitlistEntryFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUID;
  fields?: Fields;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<RejectWaitlistEntryResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "reject_waitlist_entry",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<RejectWaitlistEntryResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


/**
 * Validate: Update an existing WaitlistEntry
 *
 * @ashActionType :update
 * @validation true
 */
export async function validate_reject_waitlist_entry(
  config: {
  tenant?: string;
  identity: UUID | string;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "reject_waitlist_entry",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Update an existing WaitlistEntry
 *
 * @ashActionType :update
 * @validation true
 */
export async function validate_reject_waitlist_entry_channel(config: {
  channel: Channel;
  tenant?: string;
  identity: UUID | string;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "reject_waitlist_entry",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity
  },
    config.timeout,
    config
  );
}


/**
 * Update an existing WaitlistEntry
 *
 * @ashActionType :update
 */
export async function reject_waitlist_entry_channel<Fields extends RejectWaitlistEntryFields | undefined = undefined>(config: {
  channel: Channel;
  tenant?: string;
  identity: UUID;
  fields?: Fields;
  result_handler: (result: RejectWaitlistEntryResult<Fields>) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<RejectWaitlistEntryResult<Fields>>(
    config.channel,
    {
    action: "reject_waitlist_entry",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    ...(config.fields !== undefined && { fields: config.fields })
  },
    config.timeout,
    config
  );
}


export type SubmitWaitlistEntryInput = {
  email: string;
  full_name: string;
  company_name?: string | null;
  role?: string | null;
  interest_areas?: Array<string> | null;
  message?: string | null;
  source?: string | null;
};

export type SubmitWaitlistEntryFields = UnifiedFieldSelection<WaitlistEntryResourceSchema>[];

export type InferSubmitWaitlistEntryResult<
  Fields extends SubmitWaitlistEntryFields | undefined,
> = InferResult<WaitlistEntryResourceSchema, Fields>;

export type SubmitWaitlistEntryResult<Fields extends SubmitWaitlistEntryFields | undefined = undefined> = | { success: true; data: InferSubmitWaitlistEntryResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Create a new WaitlistEntry
 *
 * @ashActionType :create
 */
export async function submit_waitlist_entry<Fields extends SubmitWaitlistEntryFields | undefined = undefined>(
  config: {
  tenant?: string;
  input: SubmitWaitlistEntryInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<SubmitWaitlistEntryResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "submit_waitlist_entry",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<SubmitWaitlistEntryResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


/**
 * Validate: Create a new WaitlistEntry
 *
 * @ashActionType :create
 * @validation true
 */
export async function validate_submit_waitlist_entry(
  config: {
  tenant?: string;
  input: SubmitWaitlistEntryInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "submit_waitlist_entry",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Create a new WaitlistEntry
 *
 * @ashActionType :create
 * @validation true
 */
export async function validate_submit_waitlist_entry_channel(config: {
  channel: Channel;
  tenant?: string;
  input: SubmitWaitlistEntryInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "submit_waitlist_entry",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Create a new WaitlistEntry
 *
 * @ashActionType :create
 */
export async function submit_waitlist_entry_channel<Fields extends SubmitWaitlistEntryFields | undefined = undefined>(config: {
  channel: Channel;
  tenant?: string;
  input: SubmitWaitlistEntryInput;
  fields?: Fields;
  result_handler: (result: SubmitWaitlistEntryResult<Fields>) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<SubmitWaitlistEntryResult<Fields>>(
    config.channel,
    {
    action: "submit_waitlist_entry",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  },
    config.timeout,
    config
  );
}


export type AvatarPublicChatInput = {
  origin_entity_id: UUID;
  episode_id?: UUID | null;
  context_id?: UUID | null;
  message: string;
  conversation_id?: UUID | null;
  session_id?: string | null;
  ip_address?: string | null;
  conversation_history?: Array<Record<string, any>> | null;
};

export type InferAvatarPublicChatResult = ChatReplyResourceSchema;

export type AvatarPublicChatResult = | { success: true; data: InferAvatarPublicChatResult; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Execute generic action on Conversation
 *
 * @ashActionType :action
 */
export async function avatar_public_chat(
  config: {
  tenant?: string;
  input: AvatarPublicChatInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<AvatarPublicChatResult> {
  const payload = {
    action: "avatar_public_chat",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeActionRpcRequest<AvatarPublicChatResult>(
    payload,
    config
  );
}


/**
 * Validate: Execute generic action on Conversation
 *
 * @ashActionType :action
 * @validation true
 */
export async function validate_avatar_public_chat(
  config: {
  tenant?: string;
  input: AvatarPublicChatInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "avatar_public_chat",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Execute generic action on Conversation
 *
 * @ashActionType :action
 * @validation true
 */
export async function validate_avatar_public_chat_channel(config: {
  channel: Channel;
  tenant?: string;
  input: AvatarPublicChatInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "avatar_public_chat",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Execute generic action on Conversation
 *
 * @ashActionType :action
 */
export async function avatar_public_chat_channel(config: {
  channel: Channel;
  tenant?: string;
  input: AvatarPublicChatInput;
  result_handler: (result: AvatarPublicChatResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<AvatarPublicChatResult>(
    config.channel,
    {
    action: "avatar_public_chat",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


export type CreateConversationInput = {
  channel?: string | null;
  closed_at?: UtcDateTimeUsec | null;
  is_root?: boolean | null;
  metadata?: Record<string, any> | null;
  origin_entity_id: UUID;
  phone_number?: string | null;
  root_conversation_id?: UUID | null;
  session_id?: string | null;
  sms_opt_in?: boolean | null;
  status?: string | null;
  user_email?: string | null;
  user_id?: UUID | null;
  user_name?: string | null;
};

export type CreateConversationFields = UnifiedFieldSelection<PublicConversationResourceSchema>[];

export type InferCreateConversationResult<
  Fields extends CreateConversationFields | undefined,
> = InferResult<PublicConversationResourceSchema, Fields>;

export type CreateConversationResult<Fields extends CreateConversationFields | undefined = undefined> = | { success: true; data: InferCreateConversationResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Create a new Conversation
 *
 * @ashActionType :create
 */
export async function create_conversation<Fields extends CreateConversationFields | undefined = undefined>(
  config: {
  tenant?: string;
  input: CreateConversationInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<CreateConversationResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "create_conversation",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<CreateConversationResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


/**
 * Validate: Create a new Conversation
 *
 * @ashActionType :create
 * @validation true
 */
export async function validate_create_conversation(
  config: {
  tenant?: string;
  input: CreateConversationInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "create_conversation",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Create a new Conversation
 *
 * @ashActionType :create
 * @validation true
 */
export async function validate_create_conversation_channel(config: {
  channel: Channel;
  tenant?: string;
  input: CreateConversationInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "create_conversation",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Create a new Conversation
 *
 * @ashActionType :create
 */
export async function create_conversation_channel<Fields extends CreateConversationFields | undefined = undefined>(config: {
  channel: Channel;
  tenant?: string;
  input: CreateConversationInput;
  fields?: Fields;
  result_handler: (result: CreateConversationResult<Fields>) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<CreateConversationResult<Fields>>(
    config.channel,
    {
    action: "create_conversation",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  },
    config.timeout,
    config
  );
}


export type FindRootConversationInput = {
  origin_entity_id: UUID;
  user_id?: UUID | null;
  session_id?: string | null;
};

export type FindRootConversationFields = UnifiedFieldSelection<PublicConversationResourceSchema>[];
export type InferFindRootConversationResult<
  Fields extends FindRootConversationFields,
> = InferResult<PublicConversationResourceSchema, Fields>;

export type FindRootConversationResult<Fields extends FindRootConversationFields> = | { success: true; data: InferFindRootConversationResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read Conversation records
 *
 * @ashActionType :read
 */
export async function find_root_conversation<Fields extends FindRootConversationFields>(
  config: {
  tenant?: string;
  input: FindRootConversationInput;
  fields: Fields;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<FindRootConversationResult<Fields>> {
  const payload = {
    action: "find_root_conversation",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<FindRootConversationResult<Fields>>(
    payload,
    config
  );
}


/**
 * Validate: Read Conversation records
 *
 * @ashActionType :read
 * @validation true
 */
export async function validate_find_root_conversation(
  config: {
  tenant?: string;
  input: FindRootConversationInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "find_root_conversation",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Read Conversation records
 *
 * @ashActionType :read
 * @validation true
 */
export async function validate_find_root_conversation_channel(config: {
  channel: Channel;
  tenant?: string;
  input: FindRootConversationInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "find_root_conversation",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Read Conversation records
 *
 * @ashActionType :read
 */
export async function find_root_conversation_channel<Fields extends FindRootConversationFields>(config: {
  channel: Channel;
  tenant?: string;
  input: FindRootConversationInput;
  fields: Fields;
  result_handler: (result: FindRootConversationResult<Fields>) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<FindRootConversationResult<Fields>>(
    config.channel,
    {
    action: "find_root_conversation",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  },
    config.timeout,
    config
  );
}


export type GenerateChatSuggestionsInput = {
  messages: Array<Record<string, any>>;
  avatar_context: Record<string, any>;
};

export type InferGenerateChatSuggestionsResult = ChatSuggestionsResourceSchema;

export type GenerateChatSuggestionsResult = | { success: true; data: InferGenerateChatSuggestionsResult; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Execute generic action on Conversation
 *
 * @ashActionType :action
 */
export async function generate_chat_suggestions(
  config: {
  tenant?: string;
  input: GenerateChatSuggestionsInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<GenerateChatSuggestionsResult> {
  const payload = {
    action: "generate_chat_suggestions",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeActionRpcRequest<GenerateChatSuggestionsResult>(
    payload,
    config
  );
}


/**
 * Validate: Execute generic action on Conversation
 *
 * @ashActionType :action
 * @validation true
 */
export async function validate_generate_chat_suggestions(
  config: {
  tenant?: string;
  input: GenerateChatSuggestionsInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "generate_chat_suggestions",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Execute generic action on Conversation
 *
 * @ashActionType :action
 * @validation true
 */
export async function validate_generate_chat_suggestions_channel(config: {
  channel: Channel;
  tenant?: string;
  input: GenerateChatSuggestionsInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "generate_chat_suggestions",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Execute generic action on Conversation
 *
 * @ashActionType :action
 */
export async function generate_chat_suggestions_channel(config: {
  channel: Channel;
  tenant?: string;
  input: GenerateChatSuggestionsInput;
  result_handler: (result: GenerateChatSuggestionsResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<GenerateChatSuggestionsResult>(
    config.channel,
    {
    action: "generate_chat_suggestions",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


export type GetBrandUsersInput = {
  origin_entity_id: UUID;
};

export type InferGetBrandUsersResult = Array<Record<string, any>>;

export type GetBrandUsersResult = | { success: true; data: InferGetBrandUsersResult; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Execute generic action on Conversation
 *
 * @ashActionType :action
 */
export async function get_brand_users(
  config: {
  tenant?: string;
  input: GetBrandUsersInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<GetBrandUsersResult> {
  const payload = {
    action: "get_brand_users",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeActionRpcRequest<GetBrandUsersResult>(
    payload,
    config
  );
}


/**
 * Validate: Execute generic action on Conversation
 *
 * @ashActionType :action
 * @validation true
 */
export async function validate_get_brand_users(
  config: {
  tenant?: string;
  input: GetBrandUsersInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "get_brand_users",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Execute generic action on Conversation
 *
 * @ashActionType :action
 * @validation true
 */
export async function validate_get_brand_users_channel(config: {
  channel: Channel;
  tenant?: string;
  input: GetBrandUsersInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "get_brand_users",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Execute generic action on Conversation
 *
 * @ashActionType :action
 */
export async function get_brand_users_channel(config: {
  channel: Channel;
  tenant?: string;
  input: GetBrandUsersInput;
  result_handler: (result: GetBrandUsersResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<GetBrandUsersResult>(
    config.channel,
    {
    action: "get_brand_users",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


export type GetConversationHistoryInput = {
  conversation_id: UUID;
};

export type InferGetConversationHistoryResult = Array<ChatMessageResourceSchema>;

export type GetConversationHistoryResult = | { success: true; data: InferGetConversationHistoryResult; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Execute generic action on Conversation
 *
 * @ashActionType :action
 */
export async function get_conversation_history(
  config: {
  tenant?: string;
  input: GetConversationHistoryInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<GetConversationHistoryResult> {
  const payload = {
    action: "get_conversation_history",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeActionRpcRequest<GetConversationHistoryResult>(
    payload,
    config
  );
}


/**
 * Validate: Execute generic action on Conversation
 *
 * @ashActionType :action
 * @validation true
 */
export async function validate_get_conversation_history(
  config: {
  tenant?: string;
  input: GetConversationHistoryInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "get_conversation_history",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Execute generic action on Conversation
 *
 * @ashActionType :action
 * @validation true
 */
export async function validate_get_conversation_history_channel(config: {
  channel: Channel;
  tenant?: string;
  input: GetConversationHistoryInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "get_conversation_history",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Execute generic action on Conversation
 *
 * @ashActionType :action
 */
export async function get_conversation_history_channel(config: {
  channel: Channel;
  tenant?: string;
  input: GetConversationHistoryInput;
  result_handler: (result: GetConversationHistoryResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<GetConversationHistoryResult>(
    config.channel,
    {
    action: "get_conversation_history",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


export type ListConversationsFields = UnifiedFieldSelection<PublicConversationResourceSchema>[];


export type InferListConversationsResult<
  Fields extends ListConversationsFields | undefined,
  Page extends ListConversationsConfig["page"] = undefined
> = ConditionalPaginatedResultMixed<Page, Array<InferResult<PublicConversationResourceSchema, Fields>>, {
  results: Array<InferResult<PublicConversationResourceSchema, Fields>>;
  has_more: boolean;
  limit: number;
  offset: number;
  count?: number | null;
  type: "offset";
}, {
  results: Array<InferResult<PublicConversationResourceSchema, Fields>>;
  has_more: boolean;
  limit: number;
  after: string | null;
  before: string | null;
  previous_page: string;
  next_page: string;
  count?: number | null;
  type: "keyset";
}>;

export type ListConversationsConfig = {
  tenant?: string;
  fields: ListConversationsFields;
  filter?: PublicConversationFilterInput;
  sort?: SortString<PublicConversationSortField> | SortString<PublicConversationSortField>[];
  page?: (
    {
      limit?: number;
      offset?: number;
      count?: boolean;
    } | {
      limit?: number;
      after?: string;
      before?: string;
    }
  );
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
};

export type ListConversationsResult<Fields extends ListConversationsFields, Page extends ListConversationsConfig["page"] = undefined> = | { success: true; data: InferListConversationsResult<Fields, Page>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read Conversation records
 *
 * @ashActionType :read
 */
export async function list_conversations<Fields extends ListConversationsFields, Config extends ListConversationsConfig = ListConversationsConfig>(
  config: Config & { fields: Fields }
): Promise<ListConversationsResult<Fields, Config["page"]>> {
  const payload = {
    action: "list_conversations",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort }),
    ...(config.page && { page: config.page })
  };

  return executeActionRpcRequest<ListConversationsResult<Fields, Config["page"]>>(
    payload,
    config
  );
}


/**
 * Validate: Read Conversation records
 *
 * @ashActionType :read
 * @validation true
 */
export async function validate_list_conversations(
  config: {
  tenant?: string;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "list_conversations",
    ...(config.tenant !== undefined && { tenant: config.tenant })
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Read Conversation records
 *
 * @ashActionType :read
 * @validation true
 */
export async function validate_list_conversations_channel(config: {
  channel: Channel;
  tenant?: string;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "list_conversations",
    ...(config.tenant !== undefined && { tenant: config.tenant })
  },
    config.timeout,
    config
  );
}


/**
 * Read Conversation records
 *
 * @ashActionType :read
 */
export async function list_conversations_channel<Fields extends ListConversationsFields>(config: {
  channel: Channel;
  tenant?: string;
  fields: Fields;
  filter?: PublicConversationFilterInput;
  sort?: SortString<PublicConversationSortField> | SortString<PublicConversationSortField>[];
  page?: (
    {
      limit?: number;
      offset?: number;
      count?: boolean;
    } | {
      limit?: number;
      after?: string;
      before?: string;
    }
  );
  result_handler: (result: ListConversationsResult<Fields>) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<ListConversationsResult<Fields>>(
    config.channel,
    {
    action: "list_conversations",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort }),
    ...(config.page && { page: config.page })
  },
    config.timeout,
    config
  );
}


export type ListMyConversationsInput = {
  user_id: UUID;
};

export type ListMyConversationsFields = UnifiedFieldSelection<PublicConversationResourceSchema>[];
export type InferListMyConversationsResult<
  Fields extends ListMyConversationsFields,
> = Array<InferResult<PublicConversationResourceSchema, Fields>>;

export type ListMyConversationsResult<Fields extends ListMyConversationsFields> = | { success: true; data: InferListMyConversationsResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read Conversation records
 *
 * @ashActionType :read
 */
export async function list_my_conversations<Fields extends ListMyConversationsFields>(
  config: {
  tenant?: string;
  input: ListMyConversationsInput;
  fields: Fields;
  filter?: PublicConversationFilterInput;
  sort?: SortString<PublicConversationSortField> | SortString<PublicConversationSortField>[];
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ListMyConversationsResult<Fields>> {
  const payload = {
    action: "list_my_conversations",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort })
  };

  return executeActionRpcRequest<ListMyConversationsResult<Fields>>(
    payload,
    config
  );
}


/**
 * Validate: Read Conversation records
 *
 * @ashActionType :read
 * @validation true
 */
export async function validate_list_my_conversations(
  config: {
  tenant?: string;
  input: ListMyConversationsInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "list_my_conversations",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Read Conversation records
 *
 * @ashActionType :read
 * @validation true
 */
export async function validate_list_my_conversations_channel(config: {
  channel: Channel;
  tenant?: string;
  input: ListMyConversationsInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "list_my_conversations",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Read Conversation records
 *
 * @ashActionType :read
 */
export async function list_my_conversations_channel<Fields extends ListMyConversationsFields>(config: {
  channel: Channel;
  tenant?: string;
  input: ListMyConversationsInput;
  fields: Fields;
  filter?: PublicConversationFilterInput;
  sort?: SortString<PublicConversationSortField> | SortString<PublicConversationSortField>[];
  result_handler: (result: ListMyConversationsResult<Fields>) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<ListMyConversationsResult<Fields>>(
    config.channel,
    {
    action: "list_my_conversations",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort })
  },
    config.timeout,
    config
  );
}


export type PreviewChatInput = {
  origin_entity_id: UUID;
  message: string;
  conversation_history?: Array<Record<string, any>> | null;
  context_id?: UUID | null;
  conversation_id?: UUID | null;
  session_id?: string | null;
};

export type InferPreviewChatResult = ChatReplyResourceSchema;

export type PreviewChatResult = | { success: true; data: InferPreviewChatResult; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Execute generic action on Conversation
 *
 * @ashActionType :action
 */
export async function preview_chat(
  config: {
  tenant?: string;
  input: PreviewChatInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<PreviewChatResult> {
  const payload = {
    action: "preview_chat",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeActionRpcRequest<PreviewChatResult>(
    payload,
    config
  );
}


/**
 * Validate: Execute generic action on Conversation
 *
 * @ashActionType :action
 * @validation true
 */
export async function validate_preview_chat(
  config: {
  tenant?: string;
  input: PreviewChatInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "preview_chat",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Execute generic action on Conversation
 *
 * @ashActionType :action
 * @validation true
 */
export async function validate_preview_chat_channel(config: {
  channel: Channel;
  tenant?: string;
  input: PreviewChatInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "preview_chat",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Execute generic action on Conversation
 *
 * @ashActionType :action
 */
export async function preview_chat_channel(config: {
  channel: Channel;
  tenant?: string;
  input: PreviewChatInput;
  result_handler: (result: PreviewChatResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<PreviewChatResult>(
    config.channel,
    {
    action: "preview_chat",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


export type UpdateConversationInput = {
  channel?: string | null;
  closed_at?: UtcDateTimeUsec | null;
  is_root?: boolean | null;
  metadata?: Record<string, any> | null;
  origin_entity_id?: UUID;
  phone_number?: string | null;
  root_conversation_id?: UUID | null;
  session_id?: string | null;
  sms_opt_in?: boolean | null;
  status?: string | null;
  user_email?: string | null;
  user_id?: UUID | null;
  user_name?: string | null;
};

export type UpdateConversationFields = UnifiedFieldSelection<PublicConversationResourceSchema>[];

export type InferUpdateConversationResult<
  Fields extends UpdateConversationFields | undefined,
> = InferResult<PublicConversationResourceSchema, Fields>;

export type UpdateConversationResult<Fields extends UpdateConversationFields | undefined = undefined> = | { success: true; data: InferUpdateConversationResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing Conversation
 *
 * @ashActionType :update
 */
export async function update_conversation<Fields extends UpdateConversationFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUID;
  input: UpdateConversationInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<UpdateConversationResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "update_conversation",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<UpdateConversationResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


/**
 * Validate: Update an existing Conversation
 *
 * @ashActionType :update
 * @validation true
 */
export async function validate_update_conversation(
  config: {
  tenant?: string;
  identity: UUID | string;
  input: UpdateConversationInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "update_conversation",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Update an existing Conversation
 *
 * @ashActionType :update
 * @validation true
 */
export async function validate_update_conversation_channel(config: {
  channel: Channel;
  tenant?: string;
  identity: UUID | string;
  input: UpdateConversationInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "update_conversation",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Update an existing Conversation
 *
 * @ashActionType :update
 */
export async function update_conversation_channel<Fields extends UpdateConversationFields | undefined = undefined>(config: {
  channel: Channel;
  tenant?: string;
  identity: UUID;
  input: UpdateConversationInput;
  fields?: Fields;
  result_handler: (result: UpdateConversationResult<Fields>) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<UpdateConversationResult<Fields>>(
    config.channel,
    {
    action: "update_conversation",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  },
    config.timeout,
    config
  );
}


export type PresignStorageUrlInput = {
  storage_path: string;
};

export type InferPresignStorageUrlResult = string;

export type PresignStorageUrlResult = | { success: true; data: InferPresignStorageUrlResult; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Execute generic action on MessageAttachment
 *
 * @ashActionType :action
 */
export async function presign_storage_url(
  config: {
  tenant?: string;
  input: PresignStorageUrlInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<PresignStorageUrlResult> {
  const payload = {
    action: "presign_storage_url",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeActionRpcRequest<PresignStorageUrlResult>(
    payload,
    config
  );
}


/**
 * Validate: Execute generic action on MessageAttachment
 *
 * @ashActionType :action
 * @validation true
 */
export async function validate_presign_storage_url(
  config: {
  tenant?: string;
  input: PresignStorageUrlInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "presign_storage_url",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Execute generic action on MessageAttachment
 *
 * @ashActionType :action
 * @validation true
 */
export async function validate_presign_storage_url_channel(config: {
  channel: Channel;
  tenant?: string;
  input: PresignStorageUrlInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "presign_storage_url",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Execute generic action on MessageAttachment
 *
 * @ashActionType :action
 */
export async function presign_storage_url_channel(config: {
  channel: Channel;
  tenant?: string;
  input: PresignStorageUrlInput;
  result_handler: (result: PresignStorageUrlResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<PresignStorageUrlResult>(
    config.channel,
    {
    action: "presign_storage_url",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


export type UploadChatAttachmentInput = {
  file_data: string;
  file_name: string;
  file_type: string;
  file_size: number;
  pending_message_id: UUID;
};

export type InferUploadChatAttachmentResult = Record<string, any>;

export type UploadChatAttachmentResult = | { success: true; data: InferUploadChatAttachmentResult; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Execute generic action on MessageAttachment
 *
 * @ashActionType :action
 */
export async function upload_chat_attachment(
  config: {
  tenant?: string;
  input: UploadChatAttachmentInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<UploadChatAttachmentResult> {
  const payload = {
    action: "upload_chat_attachment",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeActionRpcRequest<UploadChatAttachmentResult>(
    payload,
    config
  );
}


/**
 * Validate: Execute generic action on MessageAttachment
 *
 * @ashActionType :action
 * @validation true
 */
export async function validate_upload_chat_attachment(
  config: {
  tenant?: string;
  input: UploadChatAttachmentInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "upload_chat_attachment",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Execute generic action on MessageAttachment
 *
 * @ashActionType :action
 * @validation true
 */
export async function validate_upload_chat_attachment_channel(config: {
  channel: Channel;
  tenant?: string;
  input: UploadChatAttachmentInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "upload_chat_attachment",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Execute generic action on MessageAttachment
 *
 * @ashActionType :action
 */
export async function upload_chat_attachment_channel(config: {
  channel: Channel;
  tenant?: string;
  input: UploadChatAttachmentInput;
  result_handler: (result: UploadChatAttachmentResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<UploadChatAttachmentResult>(
    config.channel,
    {
    action: "upload_chat_attachment",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


export type CreateAppInput = {
  app_type: string;
  title?: string | null;
  slug: string;
  source?: string;
  markdoc_content?: string;
  generation_status?: string;
  generation_error?: string | null;
  generation_format?: string | null;
  generation_prompt?: string | null;
  is_published?: boolean | null;
  meta?: Record<string, any> | null;
  origin_entity_id: UUID;
};

export type CreateAppFields = UnifiedFieldSelection<AppResourceSchema>[];

export type InferCreateAppResult<
  Fields extends CreateAppFields | undefined,
> = InferResult<AppResourceSchema, Fields>;

export type CreateAppResult<Fields extends CreateAppFields | undefined = undefined> = | { success: true; data: InferCreateAppResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Create a new App
 *
 * @ashActionType :create
 */
export async function create_app<Fields extends CreateAppFields | undefined = undefined>(
  config: {
  tenant?: string;
  input: CreateAppInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<CreateAppResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "create_app",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<CreateAppResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


/**
 * Validate: Create a new App
 *
 * @ashActionType :create
 * @validation true
 */
export async function validate_create_app(
  config: {
  tenant?: string;
  input: CreateAppInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "create_app",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Create a new App
 *
 * @ashActionType :create
 * @validation true
 */
export async function validate_create_app_channel(config: {
  channel: Channel;
  tenant?: string;
  input: CreateAppInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "create_app",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Create a new App
 *
 * @ashActionType :create
 */
export async function create_app_channel<Fields extends CreateAppFields | undefined = undefined>(config: {
  channel: Channel;
  tenant?: string;
  input: CreateAppInput;
  fields?: Fields;
  result_handler: (result: CreateAppResult<Fields>) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<CreateAppResult<Fields>>(
    config.channel,
    {
    action: "create_app",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  },
    config.timeout,
    config
  );
}


export type CreateAppFromTemplateInput = {
  origin_entity_id: UUID;
  template_id: UUID;
};

export type CreateAppFromTemplateFields = UnifiedFieldSelection<AppResourceSchema>[];

export type InferCreateAppFromTemplateResult<
  Fields extends CreateAppFromTemplateFields | undefined,
> = InferResult<AppResourceSchema, Fields>;

export type CreateAppFromTemplateResult<Fields extends CreateAppFromTemplateFields | undefined = undefined> = | { success: true; data: InferCreateAppFromTemplateResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Create a new App
 *
 * @ashActionType :create
 */
export async function create_app_from_template<Fields extends CreateAppFromTemplateFields | undefined = undefined>(
  config: {
  tenant?: string;
  input: CreateAppFromTemplateInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<CreateAppFromTemplateResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "create_app_from_template",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<CreateAppFromTemplateResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


/**
 * Validate: Create a new App
 *
 * @ashActionType :create
 * @validation true
 */
export async function validate_create_app_from_template(
  config: {
  tenant?: string;
  input: CreateAppFromTemplateInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "create_app_from_template",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Create a new App
 *
 * @ashActionType :create
 * @validation true
 */
export async function validate_create_app_from_template_channel(config: {
  channel: Channel;
  tenant?: string;
  input: CreateAppFromTemplateInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "create_app_from_template",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Create a new App
 *
 * @ashActionType :create
 */
export async function create_app_from_template_channel<Fields extends CreateAppFromTemplateFields | undefined = undefined>(config: {
  channel: Channel;
  tenant?: string;
  input: CreateAppFromTemplateInput;
  fields?: Fields;
  result_handler: (result: CreateAppFromTemplateResult<Fields>) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<CreateAppFromTemplateResult<Fields>>(
    config.channel,
    {
    action: "create_app_from_template",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  },
    config.timeout,
    config
  );
}


export type DestroyAppResult = | { success: true; data: {}; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Delete a App
 *
 * @ashActionType :destroy
 */
export async function destroy_app(
  config: {
  tenant?: string;
  identity: UUID;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<DestroyAppResult> {
  const payload = {
    action: "destroy_app",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity
  };

  return executeActionRpcRequest<DestroyAppResult>(
    payload,
    config
  );
}


/**
 * Validate: Delete a App
 *
 * @ashActionType :destroy
 * @validation true
 */
export async function validate_destroy_app(
  config: {
  tenant?: string;
  identity: UUID | string;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "destroy_app",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Delete a App
 *
 * @ashActionType :destroy
 * @validation true
 */
export async function validate_destroy_app_channel(config: {
  channel: Channel;
  tenant?: string;
  identity: UUID | string;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "destroy_app",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity
  },
    config.timeout,
    config
  );
}


/**
 * Delete a App
 *
 * @ashActionType :destroy
 */
export async function destroy_app_channel(config: {
  channel: Channel;
  tenant?: string;
  identity: UUID;
  result_handler: (result: DestroyAppResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<DestroyAppResult>(
    config.channel,
    {
    action: "destroy_app",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity
  },
    config.timeout,
    config
  );
}


export type DuplicateAppInput = {
  source_id: UUID;
};

export type DuplicateAppFields = UnifiedFieldSelection<AppResourceSchema>[];

export type InferDuplicateAppResult<
  Fields extends DuplicateAppFields | undefined,
> = InferResult<AppResourceSchema, Fields>;

export type DuplicateAppResult<Fields extends DuplicateAppFields | undefined = undefined> = | { success: true; data: InferDuplicateAppResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Create a new App
 *
 * @ashActionType :create
 */
export async function duplicate_app<Fields extends DuplicateAppFields | undefined = undefined>(
  config: {
  tenant?: string;
  input: DuplicateAppInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<DuplicateAppResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "duplicate_app",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<DuplicateAppResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


/**
 * Validate: Create a new App
 *
 * @ashActionType :create
 * @validation true
 */
export async function validate_duplicate_app(
  config: {
  tenant?: string;
  input: DuplicateAppInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "duplicate_app",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Create a new App
 *
 * @ashActionType :create
 * @validation true
 */
export async function validate_duplicate_app_channel(config: {
  channel: Channel;
  tenant?: string;
  input: DuplicateAppInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "duplicate_app",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Create a new App
 *
 * @ashActionType :create
 */
export async function duplicate_app_channel<Fields extends DuplicateAppFields | undefined = undefined>(config: {
  channel: Channel;
  tenant?: string;
  input: DuplicateAppInput;
  fields?: Fields;
  result_handler: (result: DuplicateAppResult<Fields>) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<DuplicateAppResult<Fields>>(
    config.channel,
    {
    action: "duplicate_app",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  },
    config.timeout,
    config
  );
}


export type ForUserPersonalizedInput = {
  app_id: UUID;
  user_id: UUID;
};

export type InferForUserPersonalizedResult = Record<string, any>;

export type ForUserPersonalizedResult = | { success: true; data: InferForUserPersonalizedResult; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Execute generic action on App
 *
 * @ashActionType :action
 */
export async function for_user_personalized(
  config: {
  tenant?: string;
  input: ForUserPersonalizedInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ForUserPersonalizedResult> {
  const payload = {
    action: "for_user_personalized",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeActionRpcRequest<ForUserPersonalizedResult>(
    payload,
    config
  );
}


/**
 * Validate: Execute generic action on App
 *
 * @ashActionType :action
 * @validation true
 */
export async function validate_for_user_personalized(
  config: {
  tenant?: string;
  input: ForUserPersonalizedInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "for_user_personalized",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Execute generic action on App
 *
 * @ashActionType :action
 * @validation true
 */
export async function validate_for_user_personalized_channel(config: {
  channel: Channel;
  tenant?: string;
  input: ForUserPersonalizedInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "for_user_personalized",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Execute generic action on App
 *
 * @ashActionType :action
 */
export async function for_user_personalized_channel(config: {
  channel: Channel;
  tenant?: string;
  input: ForUserPersonalizedInput;
  result_handler: (result: ForUserPersonalizedResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<ForUserPersonalizedResult>(
    config.channel,
    {
    action: "for_user_personalized",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


export type GetAppInput = {
  id: UUID;
};

export type GetAppFields = UnifiedFieldSelection<AppResourceSchema>[];
export type InferGetAppResult<
  Fields extends GetAppFields,
> = InferResult<AppResourceSchema, Fields>;

export type GetAppResult<Fields extends GetAppFields> = | { success: true; data: InferGetAppResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read App records
 *
 * @ashActionType :read
 */
export async function get_app<Fields extends GetAppFields>(
  config: {
  tenant?: string;
  input: GetAppInput;
  fields: Fields;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<GetAppResult<Fields>> {
  const payload = {
    action: "get_app",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<GetAppResult<Fields>>(
    payload,
    config
  );
}


/**
 * Validate: Read App records
 *
 * @ashActionType :read
 * @validation true
 */
export async function validate_get_app(
  config: {
  tenant?: string;
  input: GetAppInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "get_app",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Read App records
 *
 * @ashActionType :read
 * @validation true
 */
export async function validate_get_app_channel(config: {
  channel: Channel;
  tenant?: string;
  input: GetAppInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "get_app",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Read App records
 *
 * @ashActionType :read
 */
export async function get_app_channel<Fields extends GetAppFields>(config: {
  channel: Channel;
  tenant?: string;
  input: GetAppInput;
  fields: Fields;
  result_handler: (result: GetAppResult<Fields>) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<GetAppResult<Fields>>(
    config.channel,
    {
    action: "get_app",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  },
    config.timeout,
    config
  );
}


export type GetAppForOriginTypeAndSlugInput = {
  origin_entity_id: UUID;
  app_type: string;
  slug: string;
};

export type GetAppForOriginTypeAndSlugFields = UnifiedFieldSelection<AppResourceSchema>[];
export type InferGetAppForOriginTypeAndSlugResult<
  Fields extends GetAppForOriginTypeAndSlugFields,
> = InferResult<AppResourceSchema, Fields>;

export type GetAppForOriginTypeAndSlugResult<Fields extends GetAppForOriginTypeAndSlugFields> = | { success: true; data: InferGetAppForOriginTypeAndSlugResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read App records
 *
 * @ashActionType :read
 */
export async function get_app_for_origin_type_and_slug<Fields extends GetAppForOriginTypeAndSlugFields>(
  config: {
  tenant?: string;
  input: GetAppForOriginTypeAndSlugInput;
  fields: Fields;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<GetAppForOriginTypeAndSlugResult<Fields>> {
  const payload = {
    action: "get_app_for_origin_type_and_slug",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<GetAppForOriginTypeAndSlugResult<Fields>>(
    payload,
    config
  );
}


/**
 * Validate: Read App records
 *
 * @ashActionType :read
 * @validation true
 */
export async function validate_get_app_for_origin_type_and_slug(
  config: {
  tenant?: string;
  input: GetAppForOriginTypeAndSlugInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "get_app_for_origin_type_and_slug",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Read App records
 *
 * @ashActionType :read
 * @validation true
 */
export async function validate_get_app_for_origin_type_and_slug_channel(config: {
  channel: Channel;
  tenant?: string;
  input: GetAppForOriginTypeAndSlugInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "get_app_for_origin_type_and_slug",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Read App records
 *
 * @ashActionType :read
 */
export async function get_app_for_origin_type_and_slug_channel<Fields extends GetAppForOriginTypeAndSlugFields>(config: {
  channel: Channel;
  tenant?: string;
  input: GetAppForOriginTypeAndSlugInput;
  fields: Fields;
  result_handler: (result: GetAppForOriginTypeAndSlugResult<Fields>) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<GetAppForOriginTypeAndSlugResult<Fields>>(
    config.channel,
    {
    action: "get_app_for_origin_type_and_slug",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  },
    config.timeout,
    config
  );
}


export type GetAssessmentGraphInput = {
  app_id: UUID;
};

export type InferGetAssessmentGraphResult = AssessmentGraphResourceSchema;

export type GetAssessmentGraphResult = | { success: true; data: InferGetAssessmentGraphResult; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Execute generic action on App
 *
 * @ashActionType :action
 */
export async function get_assessment_graph(
  config: {
  tenant?: string;
  input: GetAssessmentGraphInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<GetAssessmentGraphResult> {
  const payload = {
    action: "get_assessment_graph",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeActionRpcRequest<GetAssessmentGraphResult>(
    payload,
    config
  );
}


/**
 * Validate: Execute generic action on App
 *
 * @ashActionType :action
 * @validation true
 */
export async function validate_get_assessment_graph(
  config: {
  tenant?: string;
  input: GetAssessmentGraphInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "get_assessment_graph",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Execute generic action on App
 *
 * @ashActionType :action
 * @validation true
 */
export async function validate_get_assessment_graph_channel(config: {
  channel: Channel;
  tenant?: string;
  input: GetAssessmentGraphInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "get_assessment_graph",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Execute generic action on App
 *
 * @ashActionType :action
 */
export async function get_assessment_graph_channel(config: {
  channel: Channel;
  tenant?: string;
  input: GetAssessmentGraphInput;
  result_handler: (result: GetAssessmentGraphResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<GetAssessmentGraphResult>(
    config.channel,
    {
    action: "get_assessment_graph",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


export type ListAppsForOriginInput = {
  origin_entity_id: UUID;
};

export type ListAppsForOriginFields = UnifiedFieldSelection<AppResourceSchema>[];
export type InferListAppsForOriginResult<
  Fields extends ListAppsForOriginFields,
> = Array<InferResult<AppResourceSchema, Fields>>;

export type ListAppsForOriginResult<Fields extends ListAppsForOriginFields> = | { success: true; data: InferListAppsForOriginResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read App records
 *
 * @ashActionType :read
 */
export async function list_apps_for_origin<Fields extends ListAppsForOriginFields>(
  config: {
  tenant?: string;
  input: ListAppsForOriginInput;
  fields: Fields;
  filter?: AppFilterInput;
  sort?: SortString<AppSortField> | SortString<AppSortField>[];
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ListAppsForOriginResult<Fields>> {
  const payload = {
    action: "list_apps_for_origin",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort })
  };

  return executeActionRpcRequest<ListAppsForOriginResult<Fields>>(
    payload,
    config
  );
}


/**
 * Validate: Read App records
 *
 * @ashActionType :read
 * @validation true
 */
export async function validate_list_apps_for_origin(
  config: {
  tenant?: string;
  input: ListAppsForOriginInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "list_apps_for_origin",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Read App records
 *
 * @ashActionType :read
 * @validation true
 */
export async function validate_list_apps_for_origin_channel(config: {
  channel: Channel;
  tenant?: string;
  input: ListAppsForOriginInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "list_apps_for_origin",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Read App records
 *
 * @ashActionType :read
 */
export async function list_apps_for_origin_channel<Fields extends ListAppsForOriginFields>(config: {
  channel: Channel;
  tenant?: string;
  input: ListAppsForOriginInput;
  fields: Fields;
  filter?: AppFilterInput;
  sort?: SortString<AppSortField> | SortString<AppSortField>[];
  result_handler: (result: ListAppsForOriginResult<Fields>) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<ListAppsForOriginResult<Fields>>(
    config.channel,
    {
    action: "list_apps_for_origin",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort })
  },
    config.timeout,
    config
  );
}


export type ListAppsForOriginAndTypeInput = {
  origin_entity_id: UUID;
  app_type: string;
};

export type ListAppsForOriginAndTypeFields = UnifiedFieldSelection<AppResourceSchema>[];
export type InferListAppsForOriginAndTypeResult<
  Fields extends ListAppsForOriginAndTypeFields,
> = Array<InferResult<AppResourceSchema, Fields>>;

export type ListAppsForOriginAndTypeResult<Fields extends ListAppsForOriginAndTypeFields> = | { success: true; data: InferListAppsForOriginAndTypeResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read App records
 *
 * @ashActionType :read
 */
export async function list_apps_for_origin_and_type<Fields extends ListAppsForOriginAndTypeFields>(
  config: {
  tenant?: string;
  input: ListAppsForOriginAndTypeInput;
  fields: Fields;
  filter?: AppFilterInput;
  sort?: SortString<AppSortField> | SortString<AppSortField>[];
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ListAppsForOriginAndTypeResult<Fields>> {
  const payload = {
    action: "list_apps_for_origin_and_type",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort })
  };

  return executeActionRpcRequest<ListAppsForOriginAndTypeResult<Fields>>(
    payload,
    config
  );
}


/**
 * Validate: Read App records
 *
 * @ashActionType :read
 * @validation true
 */
export async function validate_list_apps_for_origin_and_type(
  config: {
  tenant?: string;
  input: ListAppsForOriginAndTypeInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "list_apps_for_origin_and_type",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Read App records
 *
 * @ashActionType :read
 * @validation true
 */
export async function validate_list_apps_for_origin_and_type_channel(config: {
  channel: Channel;
  tenant?: string;
  input: ListAppsForOriginAndTypeInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "list_apps_for_origin_and_type",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Read App records
 *
 * @ashActionType :read
 */
export async function list_apps_for_origin_and_type_channel<Fields extends ListAppsForOriginAndTypeFields>(config: {
  channel: Channel;
  tenant?: string;
  input: ListAppsForOriginAndTypeInput;
  fields: Fields;
  filter?: AppFilterInput;
  sort?: SortString<AppSortField> | SortString<AppSortField>[];
  result_handler: (result: ListAppsForOriginAndTypeResult<Fields>) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<ListAppsForOriginAndTypeResult<Fields>>(
    config.channel,
    {
    action: "list_apps_for_origin_and_type",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort })
  },
    config.timeout,
    config
  );
}


export type ParseAssessmentBlocksInput = {
  content: string;
};

export type InferParseAssessmentBlocksResult = Record<string, any>;

export type ParseAssessmentBlocksResult = | { success: true; data: InferParseAssessmentBlocksResult; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Execute generic action on App
 *
 * @ashActionType :action
 */
export async function parse_assessment_blocks(
  config: {
  tenant?: string;
  input: ParseAssessmentBlocksInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ParseAssessmentBlocksResult> {
  const payload = {
    action: "parse_assessment_blocks",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeActionRpcRequest<ParseAssessmentBlocksResult>(
    payload,
    config
  );
}


/**
 * Validate: Execute generic action on App
 *
 * @ashActionType :action
 * @validation true
 */
export async function validate_parse_assessment_blocks(
  config: {
  tenant?: string;
  input: ParseAssessmentBlocksInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "parse_assessment_blocks",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Execute generic action on App
 *
 * @ashActionType :action
 * @validation true
 */
export async function validate_parse_assessment_blocks_channel(config: {
  channel: Channel;
  tenant?: string;
  input: ParseAssessmentBlocksInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "parse_assessment_blocks",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Execute generic action on App
 *
 * @ashActionType :action
 */
export async function parse_assessment_blocks_channel(config: {
  channel: Channel;
  tenant?: string;
  input: ParseAssessmentBlocksInput;
  result_handler: (result: ParseAssessmentBlocksResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<ParseAssessmentBlocksResult>(
    config.channel,
    {
    action: "parse_assessment_blocks",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


export type ParseCourseBlocksInput = {
  content: string;
};

export type InferParseCourseBlocksResult = Record<string, any>;

export type ParseCourseBlocksResult = | { success: true; data: InferParseCourseBlocksResult; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Execute generic action on App
 *
 * @ashActionType :action
 */
export async function parse_course_blocks(
  config: {
  tenant?: string;
  input: ParseCourseBlocksInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ParseCourseBlocksResult> {
  const payload = {
    action: "parse_course_blocks",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeActionRpcRequest<ParseCourseBlocksResult>(
    payload,
    config
  );
}


/**
 * Validate: Execute generic action on App
 *
 * @ashActionType :action
 * @validation true
 */
export async function validate_parse_course_blocks(
  config: {
  tenant?: string;
  input: ParseCourseBlocksInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "parse_course_blocks",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Execute generic action on App
 *
 * @ashActionType :action
 * @validation true
 */
export async function validate_parse_course_blocks_channel(config: {
  channel: Channel;
  tenant?: string;
  input: ParseCourseBlocksInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "parse_course_blocks",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Execute generic action on App
 *
 * @ashActionType :action
 */
export async function parse_course_blocks_channel(config: {
  channel: Channel;
  tenant?: string;
  input: ParseCourseBlocksInput;
  result_handler: (result: ParseCourseBlocksResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<ParseCourseBlocksResult>(
    config.channel,
    {
    action: "parse_course_blocks",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


export type ParseLandingBlocksInput = {
  content: string;
};

export type InferParseLandingBlocksResult = Record<string, any>;

export type ParseLandingBlocksResult = | { success: true; data: InferParseLandingBlocksResult; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Execute generic action on App
 *
 * @ashActionType :action
 */
export async function parse_landing_blocks(
  config: {
  tenant?: string;
  input: ParseLandingBlocksInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ParseLandingBlocksResult> {
  const payload = {
    action: "parse_landing_blocks",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeActionRpcRequest<ParseLandingBlocksResult>(
    payload,
    config
  );
}


/**
 * Validate: Execute generic action on App
 *
 * @ashActionType :action
 * @validation true
 */
export async function validate_parse_landing_blocks(
  config: {
  tenant?: string;
  input: ParseLandingBlocksInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "parse_landing_blocks",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Execute generic action on App
 *
 * @ashActionType :action
 * @validation true
 */
export async function validate_parse_landing_blocks_channel(config: {
  channel: Channel;
  tenant?: string;
  input: ParseLandingBlocksInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "parse_landing_blocks",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Execute generic action on App
 *
 * @ashActionType :action
 */
export async function parse_landing_blocks_channel(config: {
  channel: Channel;
  tenant?: string;
  input: ParseLandingBlocksInput;
  result_handler: (result: ParseLandingBlocksResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<ParseLandingBlocksResult>(
    config.channel,
    {
    action: "parse_landing_blocks",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


export type RestoreAppVersionInput = {
  version_id: UUID;
};

export type RestoreAppVersionFields = UnifiedFieldSelection<AppResourceSchema>[];

export type InferRestoreAppVersionResult<
  Fields extends RestoreAppVersionFields | undefined,
> = InferResult<AppResourceSchema, Fields>;

export type RestoreAppVersionResult<Fields extends RestoreAppVersionFields | undefined = undefined> = | { success: true; data: InferRestoreAppVersionResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing App
 *
 * @ashActionType :update
 */
export async function restore_app_version<Fields extends RestoreAppVersionFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUID;
  input: RestoreAppVersionInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<RestoreAppVersionResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "restore_app_version",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<RestoreAppVersionResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


/**
 * Validate: Update an existing App
 *
 * @ashActionType :update
 * @validation true
 */
export async function validate_restore_app_version(
  config: {
  tenant?: string;
  identity: UUID | string;
  input: RestoreAppVersionInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "restore_app_version",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Update an existing App
 *
 * @ashActionType :update
 * @validation true
 */
export async function validate_restore_app_version_channel(config: {
  channel: Channel;
  tenant?: string;
  identity: UUID | string;
  input: RestoreAppVersionInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "restore_app_version",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Update an existing App
 *
 * @ashActionType :update
 */
export async function restore_app_version_channel<Fields extends RestoreAppVersionFields | undefined = undefined>(config: {
  channel: Channel;
  tenant?: string;
  identity: UUID;
  input: RestoreAppVersionInput;
  fields?: Fields;
  result_handler: (result: RestoreAppVersionResult<Fields>) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<RestoreAppVersionResult<Fields>>(
    config.channel,
    {
    action: "restore_app_version",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  },
    config.timeout,
    config
  );
}


export type SendAppChatMessageInput = {
  app_id: UUID;
  message: string;
};

export type InferSendAppChatMessageResult = Record<string, any>;

export type SendAppChatMessageResult = | { success: true; data: InferSendAppChatMessageResult; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Execute generic action on App
 *
 * @ashActionType :action
 */
export async function send_app_chat_message(
  config: {
  tenant?: string;
  input: SendAppChatMessageInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<SendAppChatMessageResult> {
  const payload = {
    action: "send_app_chat_message",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeActionRpcRequest<SendAppChatMessageResult>(
    payload,
    config
  );
}


/**
 * Validate: Execute generic action on App
 *
 * @ashActionType :action
 * @validation true
 */
export async function validate_send_app_chat_message(
  config: {
  tenant?: string;
  input: SendAppChatMessageInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "send_app_chat_message",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Execute generic action on App
 *
 * @ashActionType :action
 * @validation true
 */
export async function validate_send_app_chat_message_channel(config: {
  channel: Channel;
  tenant?: string;
  input: SendAppChatMessageInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "send_app_chat_message",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Execute generic action on App
 *
 * @ashActionType :action
 */
export async function send_app_chat_message_channel(config: {
  channel: Channel;
  tenant?: string;
  input: SendAppChatMessageInput;
  result_handler: (result: SendAppChatMessageResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<SendAppChatMessageResult>(
    config.channel,
    {
    action: "send_app_chat_message",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


export type UpdateAppInput = {
  app_type?: string;
  title?: string | null;
  slug?: string;
  source?: string;
  markdoc_content?: string;
  generation_status?: string;
  generation_error?: string | null;
  generation_format?: string | null;
  generation_prompt?: string | null;
  is_published?: boolean | null;
  meta?: Record<string, any> | null;
  origin_entity_id?: UUID;
};

export type UpdateAppFields = UnifiedFieldSelection<AppResourceSchema>[];

export type InferUpdateAppResult<
  Fields extends UpdateAppFields | undefined,
> = InferResult<AppResourceSchema, Fields>;

export type UpdateAppResult<Fields extends UpdateAppFields | undefined = undefined> = | { success: true; data: InferUpdateAppResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing App
 *
 * @ashActionType :update
 */
export async function update_app<Fields extends UpdateAppFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUID;
  input: UpdateAppInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<UpdateAppResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "update_app",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<UpdateAppResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


/**
 * Validate: Update an existing App
 *
 * @ashActionType :update
 * @validation true
 */
export async function validate_update_app(
  config: {
  tenant?: string;
  identity: UUID | string;
  input: UpdateAppInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "update_app",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Update an existing App
 *
 * @ashActionType :update
 * @validation true
 */
export async function validate_update_app_channel(config: {
  channel: Channel;
  tenant?: string;
  identity: UUID | string;
  input: UpdateAppInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "update_app",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Update an existing App
 *
 * @ashActionType :update
 */
export async function update_app_channel<Fields extends UpdateAppFields | undefined = undefined>(config: {
  channel: Channel;
  tenant?: string;
  identity: UUID;
  input: UpdateAppInput;
  fields?: Fields;
  result_handler: (result: UpdateAppResult<Fields>) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<UpdateAppResult<Fields>>(
    config.channel,
    {
    action: "update_app",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  },
    config.timeout,
    config
  );
}


export type ListAppVersionsFields = UnifiedFieldSelection<AppVersionResourceSchema>[];


export type InferListAppVersionsResult<
  Fields extends ListAppVersionsFields | undefined,
  Page extends ListAppVersionsConfig["page"] = undefined
> = ConditionalPaginatedResultMixed<Page, Array<InferResult<AppVersionResourceSchema, Fields>>, {
  results: Array<InferResult<AppVersionResourceSchema, Fields>>;
  has_more: boolean;
  limit: number;
  offset: number;
  count?: number | null;
  type: "offset";
}, {
  results: Array<InferResult<AppVersionResourceSchema, Fields>>;
  has_more: boolean;
  limit: number;
  after: string | null;
  before: string | null;
  previous_page: string;
  next_page: string;
  count?: number | null;
  type: "keyset";
}>;

export type ListAppVersionsConfig = {
  tenant?: string;
  fields: ListAppVersionsFields;
  filter?: AppVersionFilterInput;
  sort?: SortString<AppVersionSortField> | SortString<AppVersionSortField>[];
  page?: (
    {
      limit?: number;
      offset?: number;
      count?: boolean;
    } | {
      limit?: number;
      after?: string;
      before?: string;
    }
  );
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
};

export type ListAppVersionsResult<Fields extends ListAppVersionsFields, Page extends ListAppVersionsConfig["page"] = undefined> = | { success: true; data: InferListAppVersionsResult<Fields, Page>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read Version records
 *
 * @ashActionType :read
 */
export async function list_app_versions<Fields extends ListAppVersionsFields, Config extends ListAppVersionsConfig = ListAppVersionsConfig>(
  config: Config & { fields: Fields }
): Promise<ListAppVersionsResult<Fields, Config["page"]>> {
  const payload = {
    action: "list_app_versions",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort }),
    ...(config.page && { page: config.page })
  };

  return executeActionRpcRequest<ListAppVersionsResult<Fields, Config["page"]>>(
    payload,
    config
  );
}


/**
 * Validate: Read Version records
 *
 * @ashActionType :read
 * @validation true
 */
export async function validate_list_app_versions(
  config: {
  tenant?: string;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "list_app_versions",
    ...(config.tenant !== undefined && { tenant: config.tenant })
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Read Version records
 *
 * @ashActionType :read
 * @validation true
 */
export async function validate_list_app_versions_channel(config: {
  channel: Channel;
  tenant?: string;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "list_app_versions",
    ...(config.tenant !== undefined && { tenant: config.tenant })
  },
    config.timeout,
    config
  );
}


/**
 * Read Version records
 *
 * @ashActionType :read
 */
export async function list_app_versions_channel<Fields extends ListAppVersionsFields>(config: {
  channel: Channel;
  tenant?: string;
  fields: Fields;
  filter?: AppVersionFilterInput;
  sort?: SortString<AppVersionSortField> | SortString<AppVersionSortField>[];
  page?: (
    {
      limit?: number;
      offset?: number;
      count?: boolean;
    } | {
      limit?: number;
      after?: string;
      before?: string;
    }
  );
  result_handler: (result: ListAppVersionsResult<Fields>) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<ListAppVersionsResult<Fields>>(
    config.channel,
    {
    action: "list_app_versions",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort }),
    ...(config.page && { page: config.page })
  },
    config.timeout,
    config
  );
}


export type AttachLibraryToAppInput = {
  app_id: UUID;
  library_id: UUID;
};

export type AttachLibraryToAppFields = UnifiedFieldSelection<AppLibraryResourceSchema>[];

export type InferAttachLibraryToAppResult<
  Fields extends AttachLibraryToAppFields | undefined,
> = InferResult<AppLibraryResourceSchema, Fields>;

export type AttachLibraryToAppResult<Fields extends AttachLibraryToAppFields | undefined = undefined> = | { success: true; data: InferAttachLibraryToAppResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Create a new AppLibrary
 *
 * @ashActionType :create
 */
export async function attach_library_to_app<Fields extends AttachLibraryToAppFields | undefined = undefined>(
  config: {
  tenant?: string;
  input: AttachLibraryToAppInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<AttachLibraryToAppResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "attach_library_to_app",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<AttachLibraryToAppResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


/**
 * Validate: Create a new AppLibrary
 *
 * @ashActionType :create
 * @validation true
 */
export async function validate_attach_library_to_app(
  config: {
  tenant?: string;
  input: AttachLibraryToAppInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "attach_library_to_app",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Create a new AppLibrary
 *
 * @ashActionType :create
 * @validation true
 */
export async function validate_attach_library_to_app_channel(config: {
  channel: Channel;
  tenant?: string;
  input: AttachLibraryToAppInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "attach_library_to_app",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Create a new AppLibrary
 *
 * @ashActionType :create
 */
export async function attach_library_to_app_channel<Fields extends AttachLibraryToAppFields | undefined = undefined>(config: {
  channel: Channel;
  tenant?: string;
  input: AttachLibraryToAppInput;
  fields?: Fields;
  result_handler: (result: AttachLibraryToAppResult<Fields>) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<AttachLibraryToAppResult<Fields>>(
    config.channel,
    {
    action: "attach_library_to_app",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  },
    config.timeout,
    config
  );
}


export type DetachLibraryFromAppResult = | { success: true; data: {}; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Delete a AppLibrary
 *
 * @ashActionType :destroy
 */
export async function detach_library_from_app(
  config: {
  tenant?: string;
  identity: UUID;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<DetachLibraryFromAppResult> {
  const payload = {
    action: "detach_library_from_app",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity
  };

  return executeActionRpcRequest<DetachLibraryFromAppResult>(
    payload,
    config
  );
}


/**
 * Validate: Delete a AppLibrary
 *
 * @ashActionType :destroy
 * @validation true
 */
export async function validate_detach_library_from_app(
  config: {
  tenant?: string;
  identity: UUID | string;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "detach_library_from_app",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Delete a AppLibrary
 *
 * @ashActionType :destroy
 * @validation true
 */
export async function validate_detach_library_from_app_channel(config: {
  channel: Channel;
  tenant?: string;
  identity: UUID | string;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "detach_library_from_app",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity
  },
    config.timeout,
    config
  );
}


/**
 * Delete a AppLibrary
 *
 * @ashActionType :destroy
 */
export async function detach_library_from_app_channel(config: {
  channel: Channel;
  tenant?: string;
  identity: UUID;
  result_handler: (result: DetachLibraryFromAppResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<DetachLibraryFromAppResult>(
    config.channel,
    {
    action: "detach_library_from_app",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity
  },
    config.timeout,
    config
  );
}


export type ListAppLibrariesFields = UnifiedFieldSelection<AppLibraryResourceSchema>[];


export type InferListAppLibrariesResult<
  Fields extends ListAppLibrariesFields | undefined,
  Page extends ListAppLibrariesConfig["page"] = undefined
> = ConditionalPaginatedResultMixed<Page, Array<InferResult<AppLibraryResourceSchema, Fields>>, {
  results: Array<InferResult<AppLibraryResourceSchema, Fields>>;
  has_more: boolean;
  limit: number;
  offset: number;
  count?: number | null;
  type: "offset";
}, {
  results: Array<InferResult<AppLibraryResourceSchema, Fields>>;
  has_more: boolean;
  limit: number;
  after: string | null;
  before: string | null;
  previous_page: string;
  next_page: string;
  count?: number | null;
  type: "keyset";
}>;

export type ListAppLibrariesConfig = {
  tenant?: string;
  fields: ListAppLibrariesFields;
  filter?: AppLibraryFilterInput;
  sort?: SortString<AppLibrarySortField> | SortString<AppLibrarySortField>[];
  page?: (
    {
      limit?: number;
      offset?: number;
      count?: boolean;
    } | {
      limit?: number;
      after?: string;
      before?: string;
    }
  );
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
};

export type ListAppLibrariesResult<Fields extends ListAppLibrariesFields, Page extends ListAppLibrariesConfig["page"] = undefined> = | { success: true; data: InferListAppLibrariesResult<Fields, Page>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read AppLibrary records
 *
 * @ashActionType :read
 */
export async function list_app_libraries<Fields extends ListAppLibrariesFields, Config extends ListAppLibrariesConfig = ListAppLibrariesConfig>(
  config: Config & { fields: Fields }
): Promise<ListAppLibrariesResult<Fields, Config["page"]>> {
  const payload = {
    action: "list_app_libraries",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort }),
    ...(config.page && { page: config.page })
  };

  return executeActionRpcRequest<ListAppLibrariesResult<Fields, Config["page"]>>(
    payload,
    config
  );
}


/**
 * Validate: Read AppLibrary records
 *
 * @ashActionType :read
 * @validation true
 */
export async function validate_list_app_libraries(
  config: {
  tenant?: string;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "list_app_libraries",
    ...(config.tenant !== undefined && { tenant: config.tenant })
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Read AppLibrary records
 *
 * @ashActionType :read
 * @validation true
 */
export async function validate_list_app_libraries_channel(config: {
  channel: Channel;
  tenant?: string;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "list_app_libraries",
    ...(config.tenant !== undefined && { tenant: config.tenant })
  },
    config.timeout,
    config
  );
}


/**
 * Read AppLibrary records
 *
 * @ashActionType :read
 */
export async function list_app_libraries_channel<Fields extends ListAppLibrariesFields>(config: {
  channel: Channel;
  tenant?: string;
  fields: Fields;
  filter?: AppLibraryFilterInput;
  sort?: SortString<AppLibrarySortField> | SortString<AppLibrarySortField>[];
  page?: (
    {
      limit?: number;
      offset?: number;
      count?: boolean;
    } | {
      limit?: number;
      after?: string;
      before?: string;
    }
  );
  result_handler: (result: ListAppLibrariesResult<Fields>) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<ListAppLibrariesResult<Fields>>(
    config.channel,
    {
    action: "list_app_libraries",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort }),
    ...(config.page && { page: config.page })
  },
    config.timeout,
    config
  );
}


export type ListLibrariesForAppInput = {
  app_id: UUID;
};

export type ListLibrariesForAppFields = UnifiedFieldSelection<AppLibraryResourceSchema>[];
export type InferListLibrariesForAppResult<
  Fields extends ListLibrariesForAppFields,
> = Array<InferResult<AppLibraryResourceSchema, Fields>>;

export type ListLibrariesForAppResult<Fields extends ListLibrariesForAppFields> = | { success: true; data: InferListLibrariesForAppResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read AppLibrary records
 *
 * @ashActionType :read
 */
export async function list_libraries_for_app<Fields extends ListLibrariesForAppFields>(
  config: {
  tenant?: string;
  input: ListLibrariesForAppInput;
  fields: Fields;
  filter?: AppLibraryFilterInput;
  sort?: SortString<AppLibrarySortField> | SortString<AppLibrarySortField>[];
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ListLibrariesForAppResult<Fields>> {
  const payload = {
    action: "list_libraries_for_app",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort })
  };

  return executeActionRpcRequest<ListLibrariesForAppResult<Fields>>(
    payload,
    config
  );
}


/**
 * Validate: Read AppLibrary records
 *
 * @ashActionType :read
 * @validation true
 */
export async function validate_list_libraries_for_app(
  config: {
  tenant?: string;
  input: ListLibrariesForAppInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "list_libraries_for_app",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Read AppLibrary records
 *
 * @ashActionType :read
 * @validation true
 */
export async function validate_list_libraries_for_app_channel(config: {
  channel: Channel;
  tenant?: string;
  input: ListLibrariesForAppInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "list_libraries_for_app",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Read AppLibrary records
 *
 * @ashActionType :read
 */
export async function list_libraries_for_app_channel<Fields extends ListLibrariesForAppFields>(config: {
  channel: Channel;
  tenant?: string;
  input: ListLibrariesForAppInput;
  fields: Fields;
  filter?: AppLibraryFilterInput;
  sort?: SortString<AppLibrarySortField> | SortString<AppLibrarySortField>[];
  result_handler: (result: ListLibrariesForAppResult<Fields>) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<ListLibrariesForAppResult<Fields>>(
    config.channel,
    {
    action: "list_libraries_for_app",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort })
  },
    config.timeout,
    config
  );
}


export type CompleteAssessmentInput = {
  completed_at?: UtcDateTime | null;
  email?: string | null;
};

export type CompleteAssessmentFields = UnifiedFieldSelection<AssessmentResponseResourceSchema>[];

export type InferCompleteAssessmentResult<
  Fields extends CompleteAssessmentFields | undefined,
> = InferResult<AssessmentResponseResourceSchema, Fields>;

export type CompleteAssessmentResult<Fields extends CompleteAssessmentFields | undefined = undefined> = | { success: true; data: InferCompleteAssessmentResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing AssessmentResponse
 *
 * @ashActionType :update
 */
export async function complete_assessment<Fields extends CompleteAssessmentFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUID;
  input?: CompleteAssessmentInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<CompleteAssessmentResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "complete_assessment",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<CompleteAssessmentResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


/**
 * Validate: Update an existing AssessmentResponse
 *
 * @ashActionType :update
 * @validation true
 */
export async function validate_complete_assessment(
  config: {
  tenant?: string;
  identity: UUID | string;
  input?: CompleteAssessmentInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "complete_assessment",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Update an existing AssessmentResponse
 *
 * @ashActionType :update
 * @validation true
 */
export async function validate_complete_assessment_channel(config: {
  channel: Channel;
  tenant?: string;
  identity: UUID | string;
  input?: CompleteAssessmentInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "complete_assessment",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Update an existing AssessmentResponse
 *
 * @ashActionType :update
 */
export async function complete_assessment_channel<Fields extends CompleteAssessmentFields | undefined = undefined>(config: {
  channel: Channel;
  tenant?: string;
  identity: UUID;
  input?: CompleteAssessmentInput;
  fields?: Fields;
  result_handler: (result: CompleteAssessmentResult<Fields>) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<CompleteAssessmentResult<Fields>>(
    config.channel,
    {
    action: "complete_assessment",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  },
    config.timeout,
    config
  );
}


export type CreateAssessmentResponseInput = {
  app_id: UUID;
  user_id?: UUID | null;
  email?: string | null;
  graph_snapshot_nodes?: Array<OriginsAppsNodeInputSchema> | null;
  graph_snapshot_edges?: Array<OriginsAppsEdgeInputSchema> | null;
  result?: Record<string, any> | null;
  attribution?: Record<string, any> | null;
  full_name?: string | null;
  phone?: string | null;
  completed_at?: UtcDateTime | null;
  answers?: Record<string, any> | null;
  path_history?: Array<string> | null;
  current_node?: string | null;
};

export type CreateAssessmentResponseFields = UnifiedFieldSelection<AssessmentResponseResourceSchema>[];

export type InferCreateAssessmentResponseResult<
  Fields extends CreateAssessmentResponseFields | undefined,
> = InferResult<AssessmentResponseResourceSchema, Fields>;

export type CreateAssessmentResponseResult<Fields extends CreateAssessmentResponseFields | undefined = undefined> = | { success: true; data: InferCreateAssessmentResponseResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Create a new AssessmentResponse
 *
 * @ashActionType :create
 */
export async function create_assessment_response<Fields extends CreateAssessmentResponseFields | undefined = undefined>(
  config: {
  tenant?: string;
  input: CreateAssessmentResponseInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<CreateAssessmentResponseResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "create_assessment_response",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<CreateAssessmentResponseResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


/**
 * Validate: Create a new AssessmentResponse
 *
 * @ashActionType :create
 * @validation true
 */
export async function validate_create_assessment_response(
  config: {
  tenant?: string;
  input: CreateAssessmentResponseInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "create_assessment_response",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Create a new AssessmentResponse
 *
 * @ashActionType :create
 * @validation true
 */
export async function validate_create_assessment_response_channel(config: {
  channel: Channel;
  tenant?: string;
  input: CreateAssessmentResponseInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "create_assessment_response",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Create a new AssessmentResponse
 *
 * @ashActionType :create
 */
export async function create_assessment_response_channel<Fields extends CreateAssessmentResponseFields | undefined = undefined>(config: {
  channel: Channel;
  tenant?: string;
  input: CreateAssessmentResponseInput;
  fields?: Fields;
  result_handler: (result: CreateAssessmentResponseResult<Fields>) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<CreateAssessmentResponseResult<Fields>>(
    config.channel,
    {
    action: "create_assessment_response",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  },
    config.timeout,
    config
  );
}


export type GetMyAssessmentResponseInput = {
  app_id: UUID;
  user_id: UUID;
};

export type GetMyAssessmentResponseFields = UnifiedFieldSelection<AssessmentResponseResourceSchema>[];
export type InferGetMyAssessmentResponseResult<
  Fields extends GetMyAssessmentResponseFields,
> = Array<InferResult<AssessmentResponseResourceSchema, Fields>>;

export type GetMyAssessmentResponseResult<Fields extends GetMyAssessmentResponseFields> = | { success: true; data: InferGetMyAssessmentResponseResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read AssessmentResponse records
 *
 * @ashActionType :read
 */
export async function get_my_assessment_response<Fields extends GetMyAssessmentResponseFields>(
  config: {
  tenant?: string;
  input: GetMyAssessmentResponseInput;
  fields: Fields;
  filter?: AssessmentResponseFilterInput;
  sort?: SortString<AssessmentResponseSortField> | SortString<AssessmentResponseSortField>[];
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<GetMyAssessmentResponseResult<Fields>> {
  const payload = {
    action: "get_my_assessment_response",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort })
  };

  return executeActionRpcRequest<GetMyAssessmentResponseResult<Fields>>(
    payload,
    config
  );
}


/**
 * Validate: Read AssessmentResponse records
 *
 * @ashActionType :read
 * @validation true
 */
export async function validate_get_my_assessment_response(
  config: {
  tenant?: string;
  input: GetMyAssessmentResponseInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "get_my_assessment_response",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Read AssessmentResponse records
 *
 * @ashActionType :read
 * @validation true
 */
export async function validate_get_my_assessment_response_channel(config: {
  channel: Channel;
  tenant?: string;
  input: GetMyAssessmentResponseInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "get_my_assessment_response",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Read AssessmentResponse records
 *
 * @ashActionType :read
 */
export async function get_my_assessment_response_channel<Fields extends GetMyAssessmentResponseFields>(config: {
  channel: Channel;
  tenant?: string;
  input: GetMyAssessmentResponseInput;
  fields: Fields;
  filter?: AssessmentResponseFilterInput;
  sort?: SortString<AssessmentResponseSortField> | SortString<AssessmentResponseSortField>[];
  result_handler: (result: GetMyAssessmentResponseResult<Fields>) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<GetMyAssessmentResponseResult<Fields>>(
    config.channel,
    {
    action: "get_my_assessment_response",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort })
  },
    config.timeout,
    config
  );
}


export type SyncAssessmentProgressInput = {
  answers?: Record<string, any> | null;
  path_history?: Array<string> | null;
  current_node?: string | null;
};

export type SyncAssessmentProgressFields = UnifiedFieldSelection<AssessmentResponseResourceSchema>[];

export type InferSyncAssessmentProgressResult<
  Fields extends SyncAssessmentProgressFields | undefined,
> = InferResult<AssessmentResponseResourceSchema, Fields>;

export type SyncAssessmentProgressResult<Fields extends SyncAssessmentProgressFields | undefined = undefined> = | { success: true; data: InferSyncAssessmentProgressResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing AssessmentResponse
 *
 * @ashActionType :update
 */
export async function sync_assessment_progress<Fields extends SyncAssessmentProgressFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUID;
  input?: SyncAssessmentProgressInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<SyncAssessmentProgressResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "sync_assessment_progress",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<SyncAssessmentProgressResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


/**
 * Validate: Update an existing AssessmentResponse
 *
 * @ashActionType :update
 * @validation true
 */
export async function validate_sync_assessment_progress(
  config: {
  tenant?: string;
  identity: UUID | string;
  input?: SyncAssessmentProgressInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "sync_assessment_progress",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Update an existing AssessmentResponse
 *
 * @ashActionType :update
 * @validation true
 */
export async function validate_sync_assessment_progress_channel(config: {
  channel: Channel;
  tenant?: string;
  identity: UUID | string;
  input?: SyncAssessmentProgressInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "sync_assessment_progress",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Update an existing AssessmentResponse
 *
 * @ashActionType :update
 */
export async function sync_assessment_progress_channel<Fields extends SyncAssessmentProgressFields | undefined = undefined>(config: {
  channel: Channel;
  tenant?: string;
  identity: UUID;
  input?: SyncAssessmentProgressInput;
  fields?: Fields;
  result_handler: (result: SyncAssessmentProgressResult<Fields>) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<SyncAssessmentProgressResult<Fields>>(
    config.channel,
    {
    action: "sync_assessment_progress",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  },
    config.timeout,
    config
  );
}


export type AnswerActivityPromptInput = {
  enrollment_id: UUID;
  activity_id: string;
  step_id?: string | null;
  completed_date: AshDate;
  prompt_id: string;
  value?: string | null;
};

export type AnswerActivityPromptFields = UnifiedFieldSelection<CourseActivityCompletionResourceSchema>[];

export type InferAnswerActivityPromptResult<
  Fields extends AnswerActivityPromptFields | undefined,
> = InferResult<CourseActivityCompletionResourceSchema, Fields>;

export type AnswerActivityPromptResult<Fields extends AnswerActivityPromptFields | undefined = undefined> = | { success: true; data: InferAnswerActivityPromptResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Create a new CourseActivityCompletion
 *
 * @ashActionType :create
 */
export async function answer_activity_prompt<Fields extends AnswerActivityPromptFields | undefined = undefined>(
  config: {
  tenant?: string;
  input: AnswerActivityPromptInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<AnswerActivityPromptResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "answer_activity_prompt",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<AnswerActivityPromptResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


/**
 * Validate: Create a new CourseActivityCompletion
 *
 * @ashActionType :create
 * @validation true
 */
export async function validate_answer_activity_prompt(
  config: {
  tenant?: string;
  input: AnswerActivityPromptInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "answer_activity_prompt",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Create a new CourseActivityCompletion
 *
 * @ashActionType :create
 * @validation true
 */
export async function validate_answer_activity_prompt_channel(config: {
  channel: Channel;
  tenant?: string;
  input: AnswerActivityPromptInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "answer_activity_prompt",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Create a new CourseActivityCompletion
 *
 * @ashActionType :create
 */
export async function answer_activity_prompt_channel<Fields extends AnswerActivityPromptFields | undefined = undefined>(config: {
  channel: Channel;
  tenant?: string;
  input: AnswerActivityPromptInput;
  fields?: Fields;
  result_handler: (result: AnswerActivityPromptResult<Fields>) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<AnswerActivityPromptResult<Fields>>(
    config.channel,
    {
    action: "answer_activity_prompt",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  },
    config.timeout,
    config
  );
}


export type CompleteActivityStepInput = {
  enrollment_id: UUID;
  activity_id: string;
  step_id?: string | null;
  completed_date: AshDate;
  notes?: string | null;
  duration_seconds?: number | null;
};

export type CompleteActivityStepFields = UnifiedFieldSelection<CourseActivityCompletionResourceSchema>[];

export type InferCompleteActivityStepResult<
  Fields extends CompleteActivityStepFields | undefined,
> = InferResult<CourseActivityCompletionResourceSchema, Fields>;

export type CompleteActivityStepResult<Fields extends CompleteActivityStepFields | undefined = undefined> = | { success: true; data: InferCompleteActivityStepResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Create a new CourseActivityCompletion
 *
 * @ashActionType :create
 */
export async function complete_activity_step<Fields extends CompleteActivityStepFields | undefined = undefined>(
  config: {
  tenant?: string;
  input: CompleteActivityStepInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<CompleteActivityStepResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "complete_activity_step",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<CompleteActivityStepResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


/**
 * Validate: Create a new CourseActivityCompletion
 *
 * @ashActionType :create
 * @validation true
 */
export async function validate_complete_activity_step(
  config: {
  tenant?: string;
  input: CompleteActivityStepInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "complete_activity_step",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Create a new CourseActivityCompletion
 *
 * @ashActionType :create
 * @validation true
 */
export async function validate_complete_activity_step_channel(config: {
  channel: Channel;
  tenant?: string;
  input: CompleteActivityStepInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "complete_activity_step",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Create a new CourseActivityCompletion
 *
 * @ashActionType :create
 */
export async function complete_activity_step_channel<Fields extends CompleteActivityStepFields | undefined = undefined>(config: {
  channel: Channel;
  tenant?: string;
  input: CompleteActivityStepInput;
  fields?: Fields;
  result_handler: (result: CompleteActivityStepResult<Fields>) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<CompleteActivityStepResult<Fields>>(
    config.channel,
    {
    action: "complete_activity_step",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  },
    config.timeout,
    config
  );
}


export type AdvanceEnrollmentSessionInput = {
  session_id?: string | null;
};

export type AdvanceEnrollmentSessionFields = UnifiedFieldSelection<CourseEnrollmentResourceSchema>[];

export type InferAdvanceEnrollmentSessionResult<
  Fields extends AdvanceEnrollmentSessionFields | undefined,
> = InferResult<CourseEnrollmentResourceSchema, Fields>;

export type AdvanceEnrollmentSessionResult<Fields extends AdvanceEnrollmentSessionFields | undefined = undefined> = | { success: true; data: InferAdvanceEnrollmentSessionResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing CourseEnrollment
 *
 * @ashActionType :update
 */
export async function advance_enrollment_session<Fields extends AdvanceEnrollmentSessionFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUID;
  input?: AdvanceEnrollmentSessionInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<AdvanceEnrollmentSessionResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "advance_enrollment_session",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<AdvanceEnrollmentSessionResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


/**
 * Validate: Update an existing CourseEnrollment
 *
 * @ashActionType :update
 * @validation true
 */
export async function validate_advance_enrollment_session(
  config: {
  tenant?: string;
  identity: UUID | string;
  input?: AdvanceEnrollmentSessionInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "advance_enrollment_session",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Update an existing CourseEnrollment
 *
 * @ashActionType :update
 * @validation true
 */
export async function validate_advance_enrollment_session_channel(config: {
  channel: Channel;
  tenant?: string;
  identity: UUID | string;
  input?: AdvanceEnrollmentSessionInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "advance_enrollment_session",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Update an existing CourseEnrollment
 *
 * @ashActionType :update
 */
export async function advance_enrollment_session_channel<Fields extends AdvanceEnrollmentSessionFields | undefined = undefined>(config: {
  channel: Channel;
  tenant?: string;
  identity: UUID;
  input?: AdvanceEnrollmentSessionInput;
  fields?: Fields;
  result_handler: (result: AdvanceEnrollmentSessionResult<Fields>) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<AdvanceEnrollmentSessionResult<Fields>>(
    config.channel,
    {
    action: "advance_enrollment_session",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  },
    config.timeout,
    config
  );
}


export type CompleteEnrollmentFields = UnifiedFieldSelection<CourseEnrollmentResourceSchema>[];

export type InferCompleteEnrollmentResult<
  Fields extends CompleteEnrollmentFields | undefined,
> = InferResult<CourseEnrollmentResourceSchema, Fields>;

export type CompleteEnrollmentResult<Fields extends CompleteEnrollmentFields | undefined = undefined> = | { success: true; data: InferCompleteEnrollmentResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing CourseEnrollment
 *
 * @ashActionType :update
 */
export async function complete_enrollment<Fields extends CompleteEnrollmentFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUID;
  fields?: Fields;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<CompleteEnrollmentResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "complete_enrollment",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<CompleteEnrollmentResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


/**
 * Validate: Update an existing CourseEnrollment
 *
 * @ashActionType :update
 * @validation true
 */
export async function validate_complete_enrollment(
  config: {
  tenant?: string;
  identity: UUID | string;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "complete_enrollment",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Update an existing CourseEnrollment
 *
 * @ashActionType :update
 * @validation true
 */
export async function validate_complete_enrollment_channel(config: {
  channel: Channel;
  tenant?: string;
  identity: UUID | string;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "complete_enrollment",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity
  },
    config.timeout,
    config
  );
}


/**
 * Update an existing CourseEnrollment
 *
 * @ashActionType :update
 */
export async function complete_enrollment_channel<Fields extends CompleteEnrollmentFields | undefined = undefined>(config: {
  channel: Channel;
  tenant?: string;
  identity: UUID;
  fields?: Fields;
  result_handler: (result: CompleteEnrollmentResult<Fields>) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<CompleteEnrollmentResult<Fields>>(
    config.channel,
    {
    action: "complete_enrollment",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    ...(config.fields !== undefined && { fields: config.fields })
  },
    config.timeout,
    config
  );
}


export type CreateEnrollmentInput = {
  current_phase_id?: string | null;
  current_session_id?: string | null;
  enrolled_at?: UtcDateTime | null;
  streak_count?: number | null;
  streak_last_date?: AshDate | null;
  settings?: Record<string, any> | null;
  user_id: UUID;
  character_id: UUID;
  app_id: UUID;
};

export type CreateEnrollmentFields = UnifiedFieldSelection<CourseEnrollmentResourceSchema>[];

export type InferCreateEnrollmentResult<
  Fields extends CreateEnrollmentFields | undefined,
> = InferResult<CourseEnrollmentResourceSchema, Fields>;

export type CreateEnrollmentResult<Fields extends CreateEnrollmentFields | undefined = undefined> = | { success: true; data: InferCreateEnrollmentResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Create a new CourseEnrollment
 *
 * @ashActionType :create
 */
export async function create_enrollment<Fields extends CreateEnrollmentFields | undefined = undefined>(
  config: {
  tenant?: string;
  input: CreateEnrollmentInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<CreateEnrollmentResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "create_enrollment",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<CreateEnrollmentResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


/**
 * Validate: Create a new CourseEnrollment
 *
 * @ashActionType :create
 * @validation true
 */
export async function validate_create_enrollment(
  config: {
  tenant?: string;
  input: CreateEnrollmentInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "create_enrollment",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Create a new CourseEnrollment
 *
 * @ashActionType :create
 * @validation true
 */
export async function validate_create_enrollment_channel(config: {
  channel: Channel;
  tenant?: string;
  input: CreateEnrollmentInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "create_enrollment",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Create a new CourseEnrollment
 *
 * @ashActionType :create
 */
export async function create_enrollment_channel<Fields extends CreateEnrollmentFields | undefined = undefined>(config: {
  channel: Channel;
  tenant?: string;
  input: CreateEnrollmentInput;
  fields?: Fields;
  result_handler: (result: CreateEnrollmentResult<Fields>) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<CreateEnrollmentResult<Fields>>(
    config.channel,
    {
    action: "create_enrollment",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  },
    config.timeout,
    config
  );
}


export type FindEnrollmentInput = {
  user_id: UUID;
  character_id: UUID;
};

export type FindEnrollmentFields = UnifiedFieldSelection<CourseEnrollmentResourceSchema>[];
export type InferFindEnrollmentResult<
  Fields extends FindEnrollmentFields,
> = Array<InferResult<CourseEnrollmentResourceSchema, Fields>>;

export type FindEnrollmentResult<Fields extends FindEnrollmentFields> = | { success: true; data: InferFindEnrollmentResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read CourseEnrollment records
 *
 * @ashActionType :read
 */
export async function find_enrollment<Fields extends FindEnrollmentFields>(
  config: {
  tenant?: string;
  input: FindEnrollmentInput;
  fields: Fields;
  filter?: CourseEnrollmentFilterInput;
  sort?: SortString<CourseEnrollmentSortField> | SortString<CourseEnrollmentSortField>[];
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<FindEnrollmentResult<Fields>> {
  const payload = {
    action: "find_enrollment",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort })
  };

  return executeActionRpcRequest<FindEnrollmentResult<Fields>>(
    payload,
    config
  );
}


/**
 * Validate: Read CourseEnrollment records
 *
 * @ashActionType :read
 * @validation true
 */
export async function validate_find_enrollment(
  config: {
  tenant?: string;
  input: FindEnrollmentInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "find_enrollment",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Read CourseEnrollment records
 *
 * @ashActionType :read
 * @validation true
 */
export async function validate_find_enrollment_channel(config: {
  channel: Channel;
  tenant?: string;
  input: FindEnrollmentInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "find_enrollment",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Read CourseEnrollment records
 *
 * @ashActionType :read
 */
export async function find_enrollment_channel<Fields extends FindEnrollmentFields>(config: {
  channel: Channel;
  tenant?: string;
  input: FindEnrollmentInput;
  fields: Fields;
  filter?: CourseEnrollmentFilterInput;
  sort?: SortString<CourseEnrollmentSortField> | SortString<CourseEnrollmentSortField>[];
  result_handler: (result: FindEnrollmentResult<Fields>) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<FindEnrollmentResult<Fields>>(
    config.channel,
    {
    action: "find_enrollment",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort })
  },
    config.timeout,
    config
  );
}


export type GetEnrollmentInput = {
  id: UUID;
};

export type GetEnrollmentFields = UnifiedFieldSelection<CourseEnrollmentResourceSchema>[];
export type InferGetEnrollmentResult<
  Fields extends GetEnrollmentFields,
> = InferResult<CourseEnrollmentResourceSchema, Fields>;

export type GetEnrollmentResult<Fields extends GetEnrollmentFields> = | { success: true; data: InferGetEnrollmentResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read CourseEnrollment records
 *
 * @ashActionType :read
 */
export async function get_enrollment<Fields extends GetEnrollmentFields>(
  config: {
  tenant?: string;
  input: GetEnrollmentInput;
  fields: Fields;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<GetEnrollmentResult<Fields>> {
  const payload = {
    action: "get_enrollment",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<GetEnrollmentResult<Fields>>(
    payload,
    config
  );
}


/**
 * Validate: Read CourseEnrollment records
 *
 * @ashActionType :read
 * @validation true
 */
export async function validate_get_enrollment(
  config: {
  tenant?: string;
  input: GetEnrollmentInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "get_enrollment",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Read CourseEnrollment records
 *
 * @ashActionType :read
 * @validation true
 */
export async function validate_get_enrollment_channel(config: {
  channel: Channel;
  tenant?: string;
  input: GetEnrollmentInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "get_enrollment",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Read CourseEnrollment records
 *
 * @ashActionType :read
 */
export async function get_enrollment_channel<Fields extends GetEnrollmentFields>(config: {
  channel: Channel;
  tenant?: string;
  input: GetEnrollmentInput;
  fields: Fields;
  result_handler: (result: GetEnrollmentResult<Fields>) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<GetEnrollmentResult<Fields>>(
    config.channel,
    {
    action: "get_enrollment",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  },
    config.timeout,
    config
  );
}


export type GetEnrollmentCompletionsInput = {
  id: UUID;
};

export type InferGetEnrollmentCompletionsResult = EnrollmentCompletionsResourceSchema;

export type GetEnrollmentCompletionsResult = | { success: true; data: InferGetEnrollmentCompletionsResult; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Execute generic action on CourseEnrollment
 *
 * @ashActionType :action
 */
export async function get_enrollment_completions(
  config: {
  tenant?: string;
  input: GetEnrollmentCompletionsInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<GetEnrollmentCompletionsResult> {
  const payload = {
    action: "get_enrollment_completions",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeActionRpcRequest<GetEnrollmentCompletionsResult>(
    payload,
    config
  );
}


/**
 * Validate: Execute generic action on CourseEnrollment
 *
 * @ashActionType :action
 * @validation true
 */
export async function validate_get_enrollment_completions(
  config: {
  tenant?: string;
  input: GetEnrollmentCompletionsInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "get_enrollment_completions",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Execute generic action on CourseEnrollment
 *
 * @ashActionType :action
 * @validation true
 */
export async function validate_get_enrollment_completions_channel(config: {
  channel: Channel;
  tenant?: string;
  input: GetEnrollmentCompletionsInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "get_enrollment_completions",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Execute generic action on CourseEnrollment
 *
 * @ashActionType :action
 */
export async function get_enrollment_completions_channel(config: {
  channel: Channel;
  tenant?: string;
  input: GetEnrollmentCompletionsInput;
  result_handler: (result: GetEnrollmentCompletionsResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<GetEnrollmentCompletionsResult>(
    config.channel,
    {
    action: "get_enrollment_completions",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


export type PauseEnrollmentFields = UnifiedFieldSelection<CourseEnrollmentResourceSchema>[];

export type InferPauseEnrollmentResult<
  Fields extends PauseEnrollmentFields | undefined,
> = InferResult<CourseEnrollmentResourceSchema, Fields>;

export type PauseEnrollmentResult<Fields extends PauseEnrollmentFields | undefined = undefined> = | { success: true; data: InferPauseEnrollmentResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing CourseEnrollment
 *
 * @ashActionType :update
 */
export async function pause_enrollment<Fields extends PauseEnrollmentFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUID;
  fields?: Fields;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<PauseEnrollmentResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "pause_enrollment",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<PauseEnrollmentResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


/**
 * Validate: Update an existing CourseEnrollment
 *
 * @ashActionType :update
 * @validation true
 */
export async function validate_pause_enrollment(
  config: {
  tenant?: string;
  identity: UUID | string;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "pause_enrollment",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Update an existing CourseEnrollment
 *
 * @ashActionType :update
 * @validation true
 */
export async function validate_pause_enrollment_channel(config: {
  channel: Channel;
  tenant?: string;
  identity: UUID | string;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "pause_enrollment",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity
  },
    config.timeout,
    config
  );
}


/**
 * Update an existing CourseEnrollment
 *
 * @ashActionType :update
 */
export async function pause_enrollment_channel<Fields extends PauseEnrollmentFields | undefined = undefined>(config: {
  channel: Channel;
  tenant?: string;
  identity: UUID;
  fields?: Fields;
  result_handler: (result: PauseEnrollmentResult<Fields>) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<PauseEnrollmentResult<Fields>>(
    config.channel,
    {
    action: "pause_enrollment",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    ...(config.fields !== undefined && { fields: config.fields })
  },
    config.timeout,
    config
  );
}


export type ReactivateEnrollmentFields = UnifiedFieldSelection<CourseEnrollmentResourceSchema>[];

export type InferReactivateEnrollmentResult<
  Fields extends ReactivateEnrollmentFields | undefined,
> = InferResult<CourseEnrollmentResourceSchema, Fields>;

export type ReactivateEnrollmentResult<Fields extends ReactivateEnrollmentFields | undefined = undefined> = | { success: true; data: InferReactivateEnrollmentResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing CourseEnrollment
 *
 * @ashActionType :update
 */
export async function reactivate_enrollment<Fields extends ReactivateEnrollmentFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUID;
  fields?: Fields;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ReactivateEnrollmentResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "reactivate_enrollment",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<ReactivateEnrollmentResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


/**
 * Validate: Update an existing CourseEnrollment
 *
 * @ashActionType :update
 * @validation true
 */
export async function validate_reactivate_enrollment(
  config: {
  tenant?: string;
  identity: UUID | string;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "reactivate_enrollment",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Update an existing CourseEnrollment
 *
 * @ashActionType :update
 * @validation true
 */
export async function validate_reactivate_enrollment_channel(config: {
  channel: Channel;
  tenant?: string;
  identity: UUID | string;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "reactivate_enrollment",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity
  },
    config.timeout,
    config
  );
}


/**
 * Update an existing CourseEnrollment
 *
 * @ashActionType :update
 */
export async function reactivate_enrollment_channel<Fields extends ReactivateEnrollmentFields | undefined = undefined>(config: {
  channel: Channel;
  tenant?: string;
  identity: UUID;
  fields?: Fields;
  result_handler: (result: ReactivateEnrollmentResult<Fields>) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<ReactivateEnrollmentResult<Fields>>(
    config.channel,
    {
    action: "reactivate_enrollment",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    ...(config.fields !== undefined && { fields: config.fields })
  },
    config.timeout,
    config
  );
}


export type ResumeEnrollmentFields = UnifiedFieldSelection<CourseEnrollmentResourceSchema>[];

export type InferResumeEnrollmentResult<
  Fields extends ResumeEnrollmentFields | undefined,
> = InferResult<CourseEnrollmentResourceSchema, Fields>;

export type ResumeEnrollmentResult<Fields extends ResumeEnrollmentFields | undefined = undefined> = | { success: true; data: InferResumeEnrollmentResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing CourseEnrollment
 *
 * @ashActionType :update
 */
export async function resume_enrollment<Fields extends ResumeEnrollmentFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUID;
  fields?: Fields;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ResumeEnrollmentResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "resume_enrollment",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<ResumeEnrollmentResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


/**
 * Validate: Update an existing CourseEnrollment
 *
 * @ashActionType :update
 * @validation true
 */
export async function validate_resume_enrollment(
  config: {
  tenant?: string;
  identity: UUID | string;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "resume_enrollment",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Update an existing CourseEnrollment
 *
 * @ashActionType :update
 * @validation true
 */
export async function validate_resume_enrollment_channel(config: {
  channel: Channel;
  tenant?: string;
  identity: UUID | string;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "resume_enrollment",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity
  },
    config.timeout,
    config
  );
}


/**
 * Update an existing CourseEnrollment
 *
 * @ashActionType :update
 */
export async function resume_enrollment_channel<Fields extends ResumeEnrollmentFields | undefined = undefined>(config: {
  channel: Channel;
  tenant?: string;
  identity: UUID;
  fields?: Fields;
  result_handler: (result: ResumeEnrollmentResult<Fields>) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<ResumeEnrollmentResult<Fields>>(
    config.channel,
    {
    action: "resume_enrollment",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    ...(config.fields !== undefined && { fields: config.fields })
  },
    config.timeout,
    config
  );
}


export type StartEnrollmentFields = UnifiedFieldSelection<CourseEnrollmentResourceSchema>[];

export type InferStartEnrollmentResult<
  Fields extends StartEnrollmentFields | undefined,
> = InferResult<CourseEnrollmentResourceSchema, Fields>;

export type StartEnrollmentResult<Fields extends StartEnrollmentFields | undefined = undefined> = | { success: true; data: InferStartEnrollmentResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing CourseEnrollment
 *
 * @ashActionType :update
 */
export async function start_enrollment<Fields extends StartEnrollmentFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUID;
  fields?: Fields;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<StartEnrollmentResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "start_enrollment",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<StartEnrollmentResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


/**
 * Validate: Update an existing CourseEnrollment
 *
 * @ashActionType :update
 * @validation true
 */
export async function validate_start_enrollment(
  config: {
  tenant?: string;
  identity: UUID | string;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "start_enrollment",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Update an existing CourseEnrollment
 *
 * @ashActionType :update
 * @validation true
 */
export async function validate_start_enrollment_channel(config: {
  channel: Channel;
  tenant?: string;
  identity: UUID | string;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "start_enrollment",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity
  },
    config.timeout,
    config
  );
}


/**
 * Update an existing CourseEnrollment
 *
 * @ashActionType :update
 */
export async function start_enrollment_channel<Fields extends StartEnrollmentFields | undefined = undefined>(config: {
  channel: Channel;
  tenant?: string;
  identity: UUID;
  fields?: Fields;
  result_handler: (result: StartEnrollmentResult<Fields>) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<StartEnrollmentResult<Fields>>(
    config.channel,
    {
    action: "start_enrollment",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    ...(config.fields !== undefined && { fields: config.fields })
  },
    config.timeout,
    config
  );
}


export type ListAppTemplatesFields = UnifiedFieldSelection<AppTemplateResourceSchema>[];
export type InferListAppTemplatesResult<
  Fields extends ListAppTemplatesFields,
> = Array<InferResult<AppTemplateResourceSchema, Fields>>;

export type ListAppTemplatesResult<Fields extends ListAppTemplatesFields> = | { success: true; data: InferListAppTemplatesResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read Template records
 *
 * @ashActionType :read
 */
export async function list_app_templates<Fields extends ListAppTemplatesFields>(
  config: {
  tenant?: string;
  fields: Fields;
  filter?: AppTemplateFilterInput;
  sort?: SortString<AppTemplateSortField> | SortString<AppTemplateSortField>[];
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ListAppTemplatesResult<Fields>> {
  const payload = {
    action: "list_app_templates",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort })
  };

  return executeActionRpcRequest<ListAppTemplatesResult<Fields>>(
    payload,
    config
  );
}


/**
 * Validate: Read Template records
 *
 * @ashActionType :read
 * @validation true
 */
export async function validate_list_app_templates(
  config: {
  tenant?: string;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "list_app_templates",
    ...(config.tenant !== undefined && { tenant: config.tenant })
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Read Template records
 *
 * @ashActionType :read
 * @validation true
 */
export async function validate_list_app_templates_channel(config: {
  channel: Channel;
  tenant?: string;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "list_app_templates",
    ...(config.tenant !== undefined && { tenant: config.tenant })
  },
    config.timeout,
    config
  );
}


/**
 * Read Template records
 *
 * @ashActionType :read
 */
export async function list_app_templates_channel<Fields extends ListAppTemplatesFields>(config: {
  channel: Channel;
  tenant?: string;
  fields: Fields;
  filter?: AppTemplateFilterInput;
  sort?: SortString<AppTemplateSortField> | SortString<AppTemplateSortField>[];
  result_handler: (result: ListAppTemplatesResult<Fields>) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<ListAppTemplatesResult<Fields>>(
    config.channel,
    {
    action: "list_app_templates",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort })
  },
    config.timeout,
    config
  );
}


export type ListAppTemplatesForTypeInput = {
  app_type: string;
};

export type ListAppTemplatesForTypeFields = UnifiedFieldSelection<AppTemplateResourceSchema>[];
export type InferListAppTemplatesForTypeResult<
  Fields extends ListAppTemplatesForTypeFields,
> = Array<InferResult<AppTemplateResourceSchema, Fields>>;

export type ListAppTemplatesForTypeResult<Fields extends ListAppTemplatesForTypeFields> = | { success: true; data: InferListAppTemplatesForTypeResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read Template records
 *
 * @ashActionType :read
 */
export async function list_app_templates_for_type<Fields extends ListAppTemplatesForTypeFields>(
  config: {
  tenant?: string;
  input: ListAppTemplatesForTypeInput;
  fields: Fields;
  filter?: AppTemplateFilterInput;
  sort?: SortString<AppTemplateSortField> | SortString<AppTemplateSortField>[];
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ListAppTemplatesForTypeResult<Fields>> {
  const payload = {
    action: "list_app_templates_for_type",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort })
  };

  return executeActionRpcRequest<ListAppTemplatesForTypeResult<Fields>>(
    payload,
    config
  );
}


/**
 * Validate: Read Template records
 *
 * @ashActionType :read
 * @validation true
 */
export async function validate_list_app_templates_for_type(
  config: {
  tenant?: string;
  input: ListAppTemplatesForTypeInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "list_app_templates_for_type",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Read Template records
 *
 * @ashActionType :read
 * @validation true
 */
export async function validate_list_app_templates_for_type_channel(config: {
  channel: Channel;
  tenant?: string;
  input: ListAppTemplatesForTypeInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "list_app_templates_for_type",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Read Template records
 *
 * @ashActionType :read
 */
export async function list_app_templates_for_type_channel<Fields extends ListAppTemplatesForTypeFields>(config: {
  channel: Channel;
  tenant?: string;
  input: ListAppTemplatesForTypeInput;
  fields: Fields;
  filter?: AppTemplateFilterInput;
  sort?: SortString<AppTemplateSortField> | SortString<AppTemplateSortField>[];
  result_handler: (result: ListAppTemplatesForTypeResult<Fields>) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<ListAppTemplatesForTypeResult<Fields>>(
    config.channel,
    {
    action: "list_app_templates_for_type",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort })
  },
    config.timeout,
    config
  );
}


export type ListWebhookDeliveriesInput = {
  origin_entity_id: UUID;
};

export type ListWebhookDeliveriesFields = UnifiedFieldSelection<WebhookDeliveryResourceSchema>[];
export type InferListWebhookDeliveriesResult<
  Fields extends ListWebhookDeliveriesFields,
> = Array<InferResult<WebhookDeliveryResourceSchema, Fields>>;

export type ListWebhookDeliveriesResult<Fields extends ListWebhookDeliveriesFields> = | { success: true; data: InferListWebhookDeliveriesResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read WebhookDelivery records
 *
 * @ashActionType :read
 */
export async function list_webhook_deliveries<Fields extends ListWebhookDeliveriesFields>(
  config: {
  tenant?: string;
  input: ListWebhookDeliveriesInput;
  fields: Fields;
  filter?: WebhookDeliveryFilterInput;
  sort?: SortString<WebhookDeliverySortField> | SortString<WebhookDeliverySortField>[];
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ListWebhookDeliveriesResult<Fields>> {
  const payload = {
    action: "list_webhook_deliveries",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort })
  };

  return executeActionRpcRequest<ListWebhookDeliveriesResult<Fields>>(
    payload,
    config
  );
}


/**
 * Validate: Read WebhookDelivery records
 *
 * @ashActionType :read
 * @validation true
 */
export async function validate_list_webhook_deliveries(
  config: {
  tenant?: string;
  input: ListWebhookDeliveriesInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "list_webhook_deliveries",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Read WebhookDelivery records
 *
 * @ashActionType :read
 * @validation true
 */
export async function validate_list_webhook_deliveries_channel(config: {
  channel: Channel;
  tenant?: string;
  input: ListWebhookDeliveriesInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "list_webhook_deliveries",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Read WebhookDelivery records
 *
 * @ashActionType :read
 */
export async function list_webhook_deliveries_channel<Fields extends ListWebhookDeliveriesFields>(config: {
  channel: Channel;
  tenant?: string;
  input: ListWebhookDeliveriesInput;
  fields: Fields;
  filter?: WebhookDeliveryFilterInput;
  sort?: SortString<WebhookDeliverySortField> | SortString<WebhookDeliverySortField>[];
  result_handler: (result: ListWebhookDeliveriesResult<Fields>) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<ListWebhookDeliveriesResult<Fields>>(
    config.channel,
    {
    action: "list_webhook_deliveries",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort })
  },
    config.timeout,
    config
  );
}


export type CreateWebhookSubscriptionInput = {
  origin_entity_id: UUID;
  app_id?: UUID | null;
  target_url: string;
  event_types?: Array<string>;
  active?: boolean;
  description?: string | null;
};

export type CreateWebhookSubscriptionFields = UnifiedFieldSelection<WebhookSubscriptionResourceSchema>[];

export type InferCreateWebhookSubscriptionResult<
  Fields extends CreateWebhookSubscriptionFields | undefined,
> = InferResult<WebhookSubscriptionResourceSchema, Fields>;

export type CreateWebhookSubscriptionResult<Fields extends CreateWebhookSubscriptionFields | undefined = undefined> = | { success: true; data: InferCreateWebhookSubscriptionResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Create a new WebhookSubscription
 *
 * @ashActionType :create
 */
export async function create_webhook_subscription<Fields extends CreateWebhookSubscriptionFields | undefined = undefined>(
  config: {
  tenant?: string;
  input: CreateWebhookSubscriptionInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<CreateWebhookSubscriptionResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "create_webhook_subscription",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<CreateWebhookSubscriptionResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


/**
 * Validate: Create a new WebhookSubscription
 *
 * @ashActionType :create
 * @validation true
 */
export async function validate_create_webhook_subscription(
  config: {
  tenant?: string;
  input: CreateWebhookSubscriptionInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "create_webhook_subscription",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Create a new WebhookSubscription
 *
 * @ashActionType :create
 * @validation true
 */
export async function validate_create_webhook_subscription_channel(config: {
  channel: Channel;
  tenant?: string;
  input: CreateWebhookSubscriptionInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "create_webhook_subscription",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Create a new WebhookSubscription
 *
 * @ashActionType :create
 */
export async function create_webhook_subscription_channel<Fields extends CreateWebhookSubscriptionFields | undefined = undefined>(config: {
  channel: Channel;
  tenant?: string;
  input: CreateWebhookSubscriptionInput;
  fields?: Fields;
  result_handler: (result: CreateWebhookSubscriptionResult<Fields>) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<CreateWebhookSubscriptionResult<Fields>>(
    config.channel,
    {
    action: "create_webhook_subscription",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  },
    config.timeout,
    config
  );
}


export type DestroyWebhookSubscriptionResult = | { success: true; data: {}; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Delete a WebhookSubscription
 *
 * @ashActionType :destroy
 */
export async function destroy_webhook_subscription(
  config: {
  tenant?: string;
  identity: UUID;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<DestroyWebhookSubscriptionResult> {
  const payload = {
    action: "destroy_webhook_subscription",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity
  };

  return executeActionRpcRequest<DestroyWebhookSubscriptionResult>(
    payload,
    config
  );
}


/**
 * Validate: Delete a WebhookSubscription
 *
 * @ashActionType :destroy
 * @validation true
 */
export async function validate_destroy_webhook_subscription(
  config: {
  tenant?: string;
  identity: UUID | string;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "destroy_webhook_subscription",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Delete a WebhookSubscription
 *
 * @ashActionType :destroy
 * @validation true
 */
export async function validate_destroy_webhook_subscription_channel(config: {
  channel: Channel;
  tenant?: string;
  identity: UUID | string;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "destroy_webhook_subscription",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity
  },
    config.timeout,
    config
  );
}


/**
 * Delete a WebhookSubscription
 *
 * @ashActionType :destroy
 */
export async function destroy_webhook_subscription_channel(config: {
  channel: Channel;
  tenant?: string;
  identity: UUID;
  result_handler: (result: DestroyWebhookSubscriptionResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<DestroyWebhookSubscriptionResult>(
    config.channel,
    {
    action: "destroy_webhook_subscription",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity
  },
    config.timeout,
    config
  );
}


export type ListWebhookSubscriptionsInput = {
  origin_entity_id: UUID;
};

export type ListWebhookSubscriptionsFields = UnifiedFieldSelection<WebhookSubscriptionResourceSchema>[];
export type InferListWebhookSubscriptionsResult<
  Fields extends ListWebhookSubscriptionsFields,
> = Array<InferResult<WebhookSubscriptionResourceSchema, Fields>>;

export type ListWebhookSubscriptionsResult<Fields extends ListWebhookSubscriptionsFields> = | { success: true; data: InferListWebhookSubscriptionsResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read WebhookSubscription records
 *
 * @ashActionType :read
 */
export async function list_webhook_subscriptions<Fields extends ListWebhookSubscriptionsFields>(
  config: {
  tenant?: string;
  input: ListWebhookSubscriptionsInput;
  fields: Fields;
  filter?: WebhookSubscriptionFilterInput;
  sort?: SortString<WebhookSubscriptionSortField> | SortString<WebhookSubscriptionSortField>[];
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ListWebhookSubscriptionsResult<Fields>> {
  const payload = {
    action: "list_webhook_subscriptions",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort })
  };

  return executeActionRpcRequest<ListWebhookSubscriptionsResult<Fields>>(
    payload,
    config
  );
}


/**
 * Validate: Read WebhookSubscription records
 *
 * @ashActionType :read
 * @validation true
 */
export async function validate_list_webhook_subscriptions(
  config: {
  tenant?: string;
  input: ListWebhookSubscriptionsInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "list_webhook_subscriptions",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Read WebhookSubscription records
 *
 * @ashActionType :read
 * @validation true
 */
export async function validate_list_webhook_subscriptions_channel(config: {
  channel: Channel;
  tenant?: string;
  input: ListWebhookSubscriptionsInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "list_webhook_subscriptions",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Read WebhookSubscription records
 *
 * @ashActionType :read
 */
export async function list_webhook_subscriptions_channel<Fields extends ListWebhookSubscriptionsFields>(config: {
  channel: Channel;
  tenant?: string;
  input: ListWebhookSubscriptionsInput;
  fields: Fields;
  filter?: WebhookSubscriptionFilterInput;
  sort?: SortString<WebhookSubscriptionSortField> | SortString<WebhookSubscriptionSortField>[];
  result_handler: (result: ListWebhookSubscriptionsResult<Fields>) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<ListWebhookSubscriptionsResult<Fields>>(
    config.channel,
    {
    action: "list_webhook_subscriptions",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort })
  },
    config.timeout,
    config
  );
}


export type SendTestWebhookInput = {
  id: UUID;
  origin_entity_id: UUID;
};

export type InferSendTestWebhookResult = Record<string, any>;

export type SendTestWebhookResult = | { success: true; data: InferSendTestWebhookResult; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Execute generic action on WebhookSubscription
 *
 * @ashActionType :action
 */
export async function send_test_webhook(
  config: {
  tenant?: string;
  input: SendTestWebhookInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<SendTestWebhookResult> {
  const payload = {
    action: "send_test_webhook",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeActionRpcRequest<SendTestWebhookResult>(
    payload,
    config
  );
}


/**
 * Validate: Execute generic action on WebhookSubscription
 *
 * @ashActionType :action
 * @validation true
 */
export async function validate_send_test_webhook(
  config: {
  tenant?: string;
  input: SendTestWebhookInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "send_test_webhook",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Execute generic action on WebhookSubscription
 *
 * @ashActionType :action
 * @validation true
 */
export async function validate_send_test_webhook_channel(config: {
  channel: Channel;
  tenant?: string;
  input: SendTestWebhookInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "send_test_webhook",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Execute generic action on WebhookSubscription
 *
 * @ashActionType :action
 */
export async function send_test_webhook_channel(config: {
  channel: Channel;
  tenant?: string;
  input: SendTestWebhookInput;
  result_handler: (result: SendTestWebhookResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<SendTestWebhookResult>(
    config.channel,
    {
    action: "send_test_webhook",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


export type UpdateWebhookSubscriptionInput = {
  target_url?: string;
  event_types?: Array<string>;
  active?: boolean;
  description?: string | null;
};

export type UpdateWebhookSubscriptionFields = UnifiedFieldSelection<WebhookSubscriptionResourceSchema>[];

export type InferUpdateWebhookSubscriptionResult<
  Fields extends UpdateWebhookSubscriptionFields | undefined,
> = InferResult<WebhookSubscriptionResourceSchema, Fields>;

export type UpdateWebhookSubscriptionResult<Fields extends UpdateWebhookSubscriptionFields | undefined = undefined> = | { success: true; data: InferUpdateWebhookSubscriptionResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing WebhookSubscription
 *
 * @ashActionType :update
 */
export async function update_webhook_subscription<Fields extends UpdateWebhookSubscriptionFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUID;
  input: UpdateWebhookSubscriptionInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<UpdateWebhookSubscriptionResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "update_webhook_subscription",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<UpdateWebhookSubscriptionResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


/**
 * Validate: Update an existing WebhookSubscription
 *
 * @ashActionType :update
 * @validation true
 */
export async function validate_update_webhook_subscription(
  config: {
  tenant?: string;
  identity: UUID | string;
  input: UpdateWebhookSubscriptionInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "update_webhook_subscription",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Update an existing WebhookSubscription
 *
 * @ashActionType :update
 * @validation true
 */
export async function validate_update_webhook_subscription_channel(config: {
  channel: Channel;
  tenant?: string;
  identity: UUID | string;
  input: UpdateWebhookSubscriptionInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "update_webhook_subscription",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Update an existing WebhookSubscription
 *
 * @ashActionType :update
 */
export async function update_webhook_subscription_channel<Fields extends UpdateWebhookSubscriptionFields | undefined = undefined>(config: {
  channel: Channel;
  tenant?: string;
  identity: UUID;
  input: UpdateWebhookSubscriptionInput;
  fields?: Fields;
  result_handler: (result: UpdateWebhookSubscriptionResult<Fields>) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<UpdateWebhookSubscriptionResult<Fields>>(
    config.channel,
    {
    action: "update_webhook_subscription",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  },
    config.timeout,
    config
  );
}


export type CreateFeatureFlagInput = {
  feature_name: string;
  enabled?: boolean;
  origin_entity_id: UUID;
};

export type CreateFeatureFlagFields = UnifiedFieldSelection<FeatureFlagResourceSchema>[];

export type InferCreateFeatureFlagResult<
  Fields extends CreateFeatureFlagFields | undefined,
> = InferResult<FeatureFlagResourceSchema, Fields>;

export type CreateFeatureFlagResult<Fields extends CreateFeatureFlagFields | undefined = undefined> = | { success: true; data: InferCreateFeatureFlagResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Create a new FeatureFlag
 *
 * @ashActionType :create
 */
export async function create_feature_flag<Fields extends CreateFeatureFlagFields | undefined = undefined>(
  config: {
  tenant?: string;
  input: CreateFeatureFlagInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<CreateFeatureFlagResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "create_feature_flag",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<CreateFeatureFlagResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


/**
 * Validate: Create a new FeatureFlag
 *
 * @ashActionType :create
 * @validation true
 */
export async function validate_create_feature_flag(
  config: {
  tenant?: string;
  input: CreateFeatureFlagInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "create_feature_flag",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Create a new FeatureFlag
 *
 * @ashActionType :create
 * @validation true
 */
export async function validate_create_feature_flag_channel(config: {
  channel: Channel;
  tenant?: string;
  input: CreateFeatureFlagInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "create_feature_flag",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Create a new FeatureFlag
 *
 * @ashActionType :create
 */
export async function create_feature_flag_channel<Fields extends CreateFeatureFlagFields | undefined = undefined>(config: {
  channel: Channel;
  tenant?: string;
  input: CreateFeatureFlagInput;
  fields?: Fields;
  result_handler: (result: CreateFeatureFlagResult<Fields>) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<CreateFeatureFlagResult<Fields>>(
    config.channel,
    {
    action: "create_feature_flag",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  },
    config.timeout,
    config
  );
}


export type DeleteFeatureFlagResult = | { success: true; data: {}; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Delete a FeatureFlag
 *
 * @ashActionType :destroy
 */
export async function delete_feature_flag(
  config: {
  tenant?: string;
  identity: UUID;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<DeleteFeatureFlagResult> {
  const payload = {
    action: "delete_feature_flag",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity
  };

  return executeActionRpcRequest<DeleteFeatureFlagResult>(
    payload,
    config
  );
}


/**
 * Validate: Delete a FeatureFlag
 *
 * @ashActionType :destroy
 * @validation true
 */
export async function validate_delete_feature_flag(
  config: {
  tenant?: string;
  identity: UUID | string;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "delete_feature_flag",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Delete a FeatureFlag
 *
 * @ashActionType :destroy
 * @validation true
 */
export async function validate_delete_feature_flag_channel(config: {
  channel: Channel;
  tenant?: string;
  identity: UUID | string;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "delete_feature_flag",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity
  },
    config.timeout,
    config
  );
}


/**
 * Delete a FeatureFlag
 *
 * @ashActionType :destroy
 */
export async function delete_feature_flag_channel(config: {
  channel: Channel;
  tenant?: string;
  identity: UUID;
  result_handler: (result: DeleteFeatureFlagResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<DeleteFeatureFlagResult>(
    config.channel,
    {
    action: "delete_feature_flag",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity
  },
    config.timeout,
    config
  );
}


export type ListFeatureFlagsFields = UnifiedFieldSelection<FeatureFlagResourceSchema>[];
export type InferListFeatureFlagsResult<
  Fields extends ListFeatureFlagsFields,
> = Array<InferResult<FeatureFlagResourceSchema, Fields>>;

export type ListFeatureFlagsResult<Fields extends ListFeatureFlagsFields> = | { success: true; data: InferListFeatureFlagsResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read FeatureFlag records
 *
 * @ashActionType :read
 */
export async function list_feature_flags<Fields extends ListFeatureFlagsFields>(
  config: {
  tenant?: string;
  fields: Fields;
  filter?: FeatureFlagFilterInput;
  sort?: SortString<FeatureFlagSortField> | SortString<FeatureFlagSortField>[];
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ListFeatureFlagsResult<Fields>> {
  const payload = {
    action: "list_feature_flags",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort })
  };

  return executeActionRpcRequest<ListFeatureFlagsResult<Fields>>(
    payload,
    config
  );
}


/**
 * Validate: Read FeatureFlag records
 *
 * @ashActionType :read
 * @validation true
 */
export async function validate_list_feature_flags(
  config: {
  tenant?: string;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "list_feature_flags",
    ...(config.tenant !== undefined && { tenant: config.tenant })
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Read FeatureFlag records
 *
 * @ashActionType :read
 * @validation true
 */
export async function validate_list_feature_flags_channel(config: {
  channel: Channel;
  tenant?: string;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "list_feature_flags",
    ...(config.tenant !== undefined && { tenant: config.tenant })
  },
    config.timeout,
    config
  );
}


/**
 * Read FeatureFlag records
 *
 * @ashActionType :read
 */
export async function list_feature_flags_channel<Fields extends ListFeatureFlagsFields>(config: {
  channel: Channel;
  tenant?: string;
  fields: Fields;
  filter?: FeatureFlagFilterInput;
  sort?: SortString<FeatureFlagSortField> | SortString<FeatureFlagSortField>[];
  result_handler: (result: ListFeatureFlagsResult<Fields>) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<ListFeatureFlagsResult<Fields>>(
    config.channel,
    {
    action: "list_feature_flags",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort })
  },
    config.timeout,
    config
  );
}


export type UpdateFeatureFlagInput = {
  enabled?: boolean;
};

export type UpdateFeatureFlagFields = UnifiedFieldSelection<FeatureFlagResourceSchema>[];

export type InferUpdateFeatureFlagResult<
  Fields extends UpdateFeatureFlagFields | undefined,
> = InferResult<FeatureFlagResourceSchema, Fields>;

export type UpdateFeatureFlagResult<Fields extends UpdateFeatureFlagFields | undefined = undefined> = | { success: true; data: InferUpdateFeatureFlagResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing FeatureFlag
 *
 * @ashActionType :update
 */
export async function update_feature_flag<Fields extends UpdateFeatureFlagFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUID;
  input?: UpdateFeatureFlagInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<UpdateFeatureFlagResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "update_feature_flag",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<UpdateFeatureFlagResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


/**
 * Validate: Update an existing FeatureFlag
 *
 * @ashActionType :update
 * @validation true
 */
export async function validate_update_feature_flag(
  config: {
  tenant?: string;
  identity: UUID | string;
  input?: UpdateFeatureFlagInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "update_feature_flag",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Update an existing FeatureFlag
 *
 * @ashActionType :update
 * @validation true
 */
export async function validate_update_feature_flag_channel(config: {
  channel: Channel;
  tenant?: string;
  identity: UUID | string;
  input?: UpdateFeatureFlagInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "update_feature_flag",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Update an existing FeatureFlag
 *
 * @ashActionType :update
 */
export async function update_feature_flag_channel<Fields extends UpdateFeatureFlagFields | undefined = undefined>(config: {
  channel: Channel;
  tenant?: string;
  identity: UUID;
  input?: UpdateFeatureFlagInput;
  fields?: Fields;
  result_handler: (result: UpdateFeatureFlagResult<Fields>) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<UpdateFeatureFlagResult<Fields>>(
    config.channel,
    {
    action: "update_feature_flag",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  },
    config.timeout,
    config
  );
}


export type CreateFeedbackInput = {
  feedback_type: string;
  subject: string;
  message: string;
  screenshot_url?: string | null;
  admin_notes?: string | null;
  user_email?: string | null;
  user_name?: string | null;
  context_type?: string | null;
  context_name?: string | null;
  resolved_at?: UtcDateTimeUsec | null;
  status?: string;
  origin_entity_id?: UUID | null;
};

export type CreateFeedbackFields = UnifiedFieldSelection<FeedbackResourceSchema>[];

export type InferCreateFeedbackResult<
  Fields extends CreateFeedbackFields | undefined,
> = InferResult<FeedbackResourceSchema, Fields>;

export type CreateFeedbackResult<Fields extends CreateFeedbackFields | undefined = undefined> = | { success: true; data: InferCreateFeedbackResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Create a new Feedback
 *
 * @ashActionType :create
 */
export async function create_feedback<Fields extends CreateFeedbackFields | undefined = undefined>(
  config: {
  tenant?: string;
  input: CreateFeedbackInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<CreateFeedbackResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "create_feedback",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<CreateFeedbackResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


/**
 * Validate: Create a new Feedback
 *
 * @ashActionType :create
 * @validation true
 */
export async function validate_create_feedback(
  config: {
  tenant?: string;
  input: CreateFeedbackInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "create_feedback",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Create a new Feedback
 *
 * @ashActionType :create
 * @validation true
 */
export async function validate_create_feedback_channel(config: {
  channel: Channel;
  tenant?: string;
  input: CreateFeedbackInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "create_feedback",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Create a new Feedback
 *
 * @ashActionType :create
 */
export async function create_feedback_channel<Fields extends CreateFeedbackFields | undefined = undefined>(config: {
  channel: Channel;
  tenant?: string;
  input: CreateFeedbackInput;
  fields?: Fields;
  result_handler: (result: CreateFeedbackResult<Fields>) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<CreateFeedbackResult<Fields>>(
    config.channel,
    {
    action: "create_feedback",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  },
    config.timeout,
    config
  );
}


export type ListFeedbackFields = UnifiedFieldSelection<FeedbackResourceSchema>[];


export type InferListFeedbackResult<
  Fields extends ListFeedbackFields | undefined,
  Page extends ListFeedbackConfig["page"] = undefined
> = ConditionalPaginatedResultMixed<Page, Array<InferResult<FeedbackResourceSchema, Fields>>, {
  results: Array<InferResult<FeedbackResourceSchema, Fields>>;
  has_more: boolean;
  limit: number;
  offset: number;
  count?: number | null;
  type: "offset";
}, {
  results: Array<InferResult<FeedbackResourceSchema, Fields>>;
  has_more: boolean;
  limit: number;
  after: string | null;
  before: string | null;
  previous_page: string;
  next_page: string;
  count?: number | null;
  type: "keyset";
}>;

export type ListFeedbackConfig = {
  tenant?: string;
  fields: ListFeedbackFields;
  filter?: FeedbackFilterInput;
  sort?: SortString<FeedbackSortField> | SortString<FeedbackSortField>[];
  page?: (
    {
      limit?: number;
      offset?: number;
      count?: boolean;
    } | {
      limit?: number;
      after?: string;
      before?: string;
    }
  );
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
};

export type ListFeedbackResult<Fields extends ListFeedbackFields, Page extends ListFeedbackConfig["page"] = undefined> = | { success: true; data: InferListFeedbackResult<Fields, Page>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read Feedback records
 *
 * @ashActionType :read
 */
export async function list_feedback<Fields extends ListFeedbackFields, Config extends ListFeedbackConfig = ListFeedbackConfig>(
  config: Config & { fields: Fields }
): Promise<ListFeedbackResult<Fields, Config["page"]>> {
  const payload = {
    action: "list_feedback",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort }),
    ...(config.page && { page: config.page })
  };

  return executeActionRpcRequest<ListFeedbackResult<Fields, Config["page"]>>(
    payload,
    config
  );
}


/**
 * Validate: Read Feedback records
 *
 * @ashActionType :read
 * @validation true
 */
export async function validate_list_feedback(
  config: {
  tenant?: string;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "list_feedback",
    ...(config.tenant !== undefined && { tenant: config.tenant })
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Read Feedback records
 *
 * @ashActionType :read
 * @validation true
 */
export async function validate_list_feedback_channel(config: {
  channel: Channel;
  tenant?: string;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "list_feedback",
    ...(config.tenant !== undefined && { tenant: config.tenant })
  },
    config.timeout,
    config
  );
}


/**
 * Read Feedback records
 *
 * @ashActionType :read
 */
export async function list_feedback_channel<Fields extends ListFeedbackFields>(config: {
  channel: Channel;
  tenant?: string;
  fields: Fields;
  filter?: FeedbackFilterInput;
  sort?: SortString<FeedbackSortField> | SortString<FeedbackSortField>[];
  page?: (
    {
      limit?: number;
      offset?: number;
      count?: boolean;
    } | {
      limit?: number;
      after?: string;
      before?: string;
    }
  );
  result_handler: (result: ListFeedbackResult<Fields>) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<ListFeedbackResult<Fields>>(
    config.channel,
    {
    action: "list_feedback",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort }),
    ...(config.page && { page: config.page })
  },
    config.timeout,
    config
  );
}


export type UpdateFeedbackInput = {
  feedback_type?: string;
  subject?: string;
  message?: string;
  screenshot_url?: string | null;
  admin_notes?: string | null;
  user_email?: string | null;
  user_name?: string | null;
  context_type?: string | null;
  context_name?: string | null;
  resolved_at?: UtcDateTimeUsec | null;
  status?: string;
  origin_entity_id?: UUID | null;
};

export type UpdateFeedbackFields = UnifiedFieldSelection<FeedbackResourceSchema>[];

export type InferUpdateFeedbackResult<
  Fields extends UpdateFeedbackFields | undefined,
> = InferResult<FeedbackResourceSchema, Fields>;

export type UpdateFeedbackResult<Fields extends UpdateFeedbackFields | undefined = undefined> = | { success: true; data: InferUpdateFeedbackResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing Feedback
 *
 * @ashActionType :update
 */
export async function update_feedback<Fields extends UpdateFeedbackFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUID;
  input: UpdateFeedbackInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<UpdateFeedbackResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "update_feedback",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<UpdateFeedbackResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


/**
 * Validate: Update an existing Feedback
 *
 * @ashActionType :update
 * @validation true
 */
export async function validate_update_feedback(
  config: {
  tenant?: string;
  identity: UUID | string;
  input: UpdateFeedbackInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "update_feedback",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Update an existing Feedback
 *
 * @ashActionType :update
 * @validation true
 */
export async function validate_update_feedback_channel(config: {
  channel: Channel;
  tenant?: string;
  identity: UUID | string;
  input: UpdateFeedbackInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "update_feedback",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Update an existing Feedback
 *
 * @ashActionType :update
 */
export async function update_feedback_channel<Fields extends UpdateFeedbackFields | undefined = undefined>(config: {
  channel: Channel;
  tenant?: string;
  identity: UUID;
  input: UpdateFeedbackInput;
  fields?: Fields;
  result_handler: (result: UpdateFeedbackResult<Fields>) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<UpdateFeedbackResult<Fields>>(
    config.channel,
    {
    action: "update_feedback",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  },
    config.timeout,
    config
  );
}


export type CreateHomepageCardInput = {
  name: string;
  role: string;
  gradient: string;
  is_active: boolean;
  custom_url?: string | null;
  avatar_url?: string | null;
  avatar_key?: string | null;
  ai_enabled: boolean;
  description?: string | null;
  tags?: Array<string> | null;
  use_gradient?: boolean | null;
  order_index?: number;
  destination_type?: string;
  avatar_size?: string | null;
  avatar_shape?: string | null;
  avatar_vertical_position?: Decimal | null;
  background_color?: string | null;
  origin_entity_id?: UUID | null;
};

export type CreateHomepageCardFields = UnifiedFieldSelection<HomepageCardResourceSchema>[];

export type InferCreateHomepageCardResult<
  Fields extends CreateHomepageCardFields | undefined,
> = InferResult<HomepageCardResourceSchema, Fields>;

export type CreateHomepageCardResult<Fields extends CreateHomepageCardFields | undefined = undefined> = | { success: true; data: InferCreateHomepageCardResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Create a new HomepageCard
 *
 * @ashActionType :create
 */
export async function create_homepage_card<Fields extends CreateHomepageCardFields | undefined = undefined>(
  config: {
  tenant?: string;
  input: CreateHomepageCardInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<CreateHomepageCardResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "create_homepage_card",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<CreateHomepageCardResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


/**
 * Validate: Create a new HomepageCard
 *
 * @ashActionType :create
 * @validation true
 */
export async function validate_create_homepage_card(
  config: {
  tenant?: string;
  input: CreateHomepageCardInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "create_homepage_card",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Create a new HomepageCard
 *
 * @ashActionType :create
 * @validation true
 */
export async function validate_create_homepage_card_channel(config: {
  channel: Channel;
  tenant?: string;
  input: CreateHomepageCardInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "create_homepage_card",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Create a new HomepageCard
 *
 * @ashActionType :create
 */
export async function create_homepage_card_channel<Fields extends CreateHomepageCardFields | undefined = undefined>(config: {
  channel: Channel;
  tenant?: string;
  input: CreateHomepageCardInput;
  fields?: Fields;
  result_handler: (result: CreateHomepageCardResult<Fields>) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<CreateHomepageCardResult<Fields>>(
    config.channel,
    {
    action: "create_homepage_card",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  },
    config.timeout,
    config
  );
}


export type DeleteHomepageCardResult = | { success: true; data: {}; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Delete a HomepageCard
 *
 * @ashActionType :destroy
 */
export async function delete_homepage_card(
  config: {
  tenant?: string;
  identity: UUID;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<DeleteHomepageCardResult> {
  const payload = {
    action: "delete_homepage_card",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity
  };

  return executeActionRpcRequest<DeleteHomepageCardResult>(
    payload,
    config
  );
}


/**
 * Validate: Delete a HomepageCard
 *
 * @ashActionType :destroy
 * @validation true
 */
export async function validate_delete_homepage_card(
  config: {
  tenant?: string;
  identity: UUID | string;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "delete_homepage_card",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Delete a HomepageCard
 *
 * @ashActionType :destroy
 * @validation true
 */
export async function validate_delete_homepage_card_channel(config: {
  channel: Channel;
  tenant?: string;
  identity: UUID | string;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "delete_homepage_card",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity
  },
    config.timeout,
    config
  );
}


/**
 * Delete a HomepageCard
 *
 * @ashActionType :destroy
 */
export async function delete_homepage_card_channel(config: {
  channel: Channel;
  tenant?: string;
  identity: UUID;
  result_handler: (result: DeleteHomepageCardResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<DeleteHomepageCardResult>(
    config.channel,
    {
    action: "delete_homepage_card",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity
  },
    config.timeout,
    config
  );
}


export type ListHomepageCardsFields = UnifiedFieldSelection<HomepageCardResourceSchema>[];
export type InferListHomepageCardsResult<
  Fields extends ListHomepageCardsFields,
> = Array<InferResult<HomepageCardResourceSchema, Fields>>;

export type ListHomepageCardsResult<Fields extends ListHomepageCardsFields> = | { success: true; data: InferListHomepageCardsResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read HomepageCard records
 *
 * @ashActionType :read
 */
export async function list_homepage_cards<Fields extends ListHomepageCardsFields>(
  config: {
  tenant?: string;
  fields: Fields;
  filter?: HomepageCardFilterInput;
  sort?: SortString<HomepageCardSortField> | SortString<HomepageCardSortField>[];
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ListHomepageCardsResult<Fields>> {
  const payload = {
    action: "list_homepage_cards",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort })
  };

  return executeActionRpcRequest<ListHomepageCardsResult<Fields>>(
    payload,
    config
  );
}


/**
 * Validate: Read HomepageCard records
 *
 * @ashActionType :read
 * @validation true
 */
export async function validate_list_homepage_cards(
  config: {
  tenant?: string;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "list_homepage_cards",
    ...(config.tenant !== undefined && { tenant: config.tenant })
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Read HomepageCard records
 *
 * @ashActionType :read
 * @validation true
 */
export async function validate_list_homepage_cards_channel(config: {
  channel: Channel;
  tenant?: string;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "list_homepage_cards",
    ...(config.tenant !== undefined && { tenant: config.tenant })
  },
    config.timeout,
    config
  );
}


/**
 * Read HomepageCard records
 *
 * @ashActionType :read
 */
export async function list_homepage_cards_channel<Fields extends ListHomepageCardsFields>(config: {
  channel: Channel;
  tenant?: string;
  fields: Fields;
  filter?: HomepageCardFilterInput;
  sort?: SortString<HomepageCardSortField> | SortString<HomepageCardSortField>[];
  result_handler: (result: ListHomepageCardsResult<Fields>) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<ListHomepageCardsResult<Fields>>(
    config.channel,
    {
    action: "list_homepage_cards",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort })
  },
    config.timeout,
    config
  );
}


export type UpdateHomepageCardInput = {
  name?: string;
  role?: string;
  gradient?: string;
  is_active?: boolean;
  custom_url?: string | null;
  avatar_url?: string | null;
  avatar_key?: string | null;
  ai_enabled?: boolean;
  description?: string | null;
  tags?: Array<string> | null;
  use_gradient?: boolean | null;
  order_index?: number;
  destination_type?: string;
  avatar_size?: string | null;
  avatar_shape?: string | null;
  avatar_vertical_position?: Decimal | null;
  background_color?: string | null;
  origin_entity_id?: UUID | null;
};

export type UpdateHomepageCardFields = UnifiedFieldSelection<HomepageCardResourceSchema>[];

export type InferUpdateHomepageCardResult<
  Fields extends UpdateHomepageCardFields | undefined,
> = InferResult<HomepageCardResourceSchema, Fields>;

export type UpdateHomepageCardResult<Fields extends UpdateHomepageCardFields | undefined = undefined> = | { success: true; data: InferUpdateHomepageCardResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing HomepageCard
 *
 * @ashActionType :update
 */
export async function update_homepage_card<Fields extends UpdateHomepageCardFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUID;
  input: UpdateHomepageCardInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<UpdateHomepageCardResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "update_homepage_card",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<UpdateHomepageCardResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


/**
 * Validate: Update an existing HomepageCard
 *
 * @ashActionType :update
 * @validation true
 */
export async function validate_update_homepage_card(
  config: {
  tenant?: string;
  identity: UUID | string;
  input: UpdateHomepageCardInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "update_homepage_card",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Update an existing HomepageCard
 *
 * @ashActionType :update
 * @validation true
 */
export async function validate_update_homepage_card_channel(config: {
  channel: Channel;
  tenant?: string;
  identity: UUID | string;
  input: UpdateHomepageCardInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "update_homepage_card",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Update an existing HomepageCard
 *
 * @ashActionType :update
 */
export async function update_homepage_card_channel<Fields extends UpdateHomepageCardFields | undefined = undefined>(config: {
  channel: Channel;
  tenant?: string;
  identity: UUID;
  input: UpdateHomepageCardInput;
  fields?: Fields;
  result_handler: (result: UpdateHomepageCardResult<Fields>) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<UpdateHomepageCardResult<Fields>>(
    config.channel,
    {
    action: "update_homepage_card",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  },
    config.timeout,
    config
  );
}


export type CreateChatConfigInput = {
  chat_button_color?: string | null;
  welcome_message?: string | null;
  show_close_button?: boolean | null;
  show_powered_by?: boolean | null;
  show_header_avatar?: boolean | null;
  show_ai_message_avatar?: boolean | null;
  chat_button_show_icon?: boolean | null;
  public_profile_button_show_icon?: boolean | null;
  podcast_button_show_icon?: boolean | null;
  public_profile_show_brand_logo?: boolean | null;
  podcast_show_brand_logo?: boolean | null;
  use_chat_button_for_public_profile?: boolean | null;
  use_chat_button_for_podcast?: boolean | null;
  available_avatar_ids?: Array<string> | null;
  chat_button_text?: string | null;
  show_brand_logo?: boolean | null;
  chat_window_title?: string | null;
  chat_bubble_color?: string | null;
  chat_bubble_text_color?: string | null;
  user_bubble_color?: string | null;
  user_bubble_text_color?: string | null;
  enable_sound_notifications?: boolean | null;
  show_typing_indicator?: boolean | null;
  enable_chat_history?: boolean | null;
  inherits_descendant_knowledge?: boolean;
  button_shape?: string | null;
  window_size?: string | null;
  header_style?: string | null;
  window_border_radius?: string | null;
  window_animation?: string | null;
  send_button_style?: string | null;
  input_placeholder?: string | null;
  window_position_mode?: string | null;
  chat_button_icon?: string | null;
  chat_button_text_color?: string | null;
  public_profile_button_icon?: string | null;
  public_profile_button_text_color?: string | null;
  podcast_button_icon?: string | null;
  podcast_button_text_color?: string | null;
  public_profile_button_color?: string | null;
  podcast_button_color?: string | null;
  chat_button_border_color?: string | null;
  public_profile_button_border_color?: string | null;
  podcast_button_border_color?: string | null;
  public_profile_button_shape?: string | null;
  podcast_button_shape?: string | null;
  public_profile_button_text?: string | null;
  podcast_button_text?: string | null;
  input_style?: string | null;
  chat_button_position?: string | null;
  response_delay?: number | null;
  origin_entity_id?: UUID | null;
};

export type CreateChatConfigFields = UnifiedFieldSelection<ChatConfigResourceSchema>[];

export type InferCreateChatConfigResult<
  Fields extends CreateChatConfigFields | undefined,
> = InferResult<ChatConfigResourceSchema, Fields>;

export type CreateChatConfigResult<Fields extends CreateChatConfigFields | undefined = undefined> = | { success: true; data: InferCreateChatConfigResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Create a new ChatConfig
 *
 * @ashActionType :create
 */
export async function create_chat_config<Fields extends CreateChatConfigFields | undefined = undefined>(
  config: {
  tenant?: string;
  input?: CreateChatConfigInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<CreateChatConfigResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "create_chat_config",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<CreateChatConfigResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


/**
 * Validate: Create a new ChatConfig
 *
 * @ashActionType :create
 * @validation true
 */
export async function validate_create_chat_config(
  config: {
  tenant?: string;
  input?: CreateChatConfigInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "create_chat_config",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Create a new ChatConfig
 *
 * @ashActionType :create
 * @validation true
 */
export async function validate_create_chat_config_channel(config: {
  channel: Channel;
  tenant?: string;
  input?: CreateChatConfigInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "create_chat_config",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Create a new ChatConfig
 *
 * @ashActionType :create
 */
export async function create_chat_config_channel<Fields extends CreateChatConfigFields | undefined = undefined>(config: {
  channel: Channel;
  tenant?: string;
  input?: CreateChatConfigInput;
  fields?: Fields;
  result_handler: (result: CreateChatConfigResult<Fields>) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<CreateChatConfigResult<Fields>>(
    config.channel,
    {
    action: "create_chat_config",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  },
    config.timeout,
    config
  );
}


export type ListChatConfigsFields = UnifiedFieldSelection<ChatConfigResourceSchema>[];


export type InferListChatConfigsResult<
  Fields extends ListChatConfigsFields | undefined,
  Page extends ListChatConfigsConfig["page"] = undefined
> = ConditionalPaginatedResultMixed<Page, Array<InferResult<ChatConfigResourceSchema, Fields>>, {
  results: Array<InferResult<ChatConfigResourceSchema, Fields>>;
  has_more: boolean;
  limit: number;
  offset: number;
  count?: number | null;
  type: "offset";
}, {
  results: Array<InferResult<ChatConfigResourceSchema, Fields>>;
  has_more: boolean;
  limit: number;
  after: string | null;
  before: string | null;
  previous_page: string;
  next_page: string;
  count?: number | null;
  type: "keyset";
}>;

export type ListChatConfigsConfig = {
  tenant?: string;
  fields: ListChatConfigsFields;
  filter?: ChatConfigFilterInput;
  sort?: SortString<ChatConfigSortField> | SortString<ChatConfigSortField>[];
  page?: (
    {
      limit?: number;
      offset?: number;
      count?: boolean;
    } | {
      limit?: number;
      after?: string;
      before?: string;
    }
  );
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
};

export type ListChatConfigsResult<Fields extends ListChatConfigsFields, Page extends ListChatConfigsConfig["page"] = undefined> = | { success: true; data: InferListChatConfigsResult<Fields, Page>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read ChatConfig records
 *
 * @ashActionType :read
 */
export async function list_chat_configs<Fields extends ListChatConfigsFields, Config extends ListChatConfigsConfig = ListChatConfigsConfig>(
  config: Config & { fields: Fields }
): Promise<ListChatConfigsResult<Fields, Config["page"]>> {
  const payload = {
    action: "list_chat_configs",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort }),
    ...(config.page && { page: config.page })
  };

  return executeActionRpcRequest<ListChatConfigsResult<Fields, Config["page"]>>(
    payload,
    config
  );
}


/**
 * Validate: Read ChatConfig records
 *
 * @ashActionType :read
 * @validation true
 */
export async function validate_list_chat_configs(
  config: {
  tenant?: string;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "list_chat_configs",
    ...(config.tenant !== undefined && { tenant: config.tenant })
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Read ChatConfig records
 *
 * @ashActionType :read
 * @validation true
 */
export async function validate_list_chat_configs_channel(config: {
  channel: Channel;
  tenant?: string;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "list_chat_configs",
    ...(config.tenant !== undefined && { tenant: config.tenant })
  },
    config.timeout,
    config
  );
}


/**
 * Read ChatConfig records
 *
 * @ashActionType :read
 */
export async function list_chat_configs_channel<Fields extends ListChatConfigsFields>(config: {
  channel: Channel;
  tenant?: string;
  fields: Fields;
  filter?: ChatConfigFilterInput;
  sort?: SortString<ChatConfigSortField> | SortString<ChatConfigSortField>[];
  page?: (
    {
      limit?: number;
      offset?: number;
      count?: boolean;
    } | {
      limit?: number;
      after?: string;
      before?: string;
    }
  );
  result_handler: (result: ListChatConfigsResult<Fields>) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<ListChatConfigsResult<Fields>>(
    config.channel,
    {
    action: "list_chat_configs",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort }),
    ...(config.page && { page: config.page })
  },
    config.timeout,
    config
  );
}


export type UpdateChatConfigInput = {
  chat_button_color?: string | null;
  welcome_message?: string | null;
  show_close_button?: boolean | null;
  show_powered_by?: boolean | null;
  show_header_avatar?: boolean | null;
  show_ai_message_avatar?: boolean | null;
  chat_button_show_icon?: boolean | null;
  public_profile_button_show_icon?: boolean | null;
  podcast_button_show_icon?: boolean | null;
  public_profile_show_brand_logo?: boolean | null;
  podcast_show_brand_logo?: boolean | null;
  use_chat_button_for_public_profile?: boolean | null;
  use_chat_button_for_podcast?: boolean | null;
  available_avatar_ids?: Array<string> | null;
  chat_button_text?: string | null;
  show_brand_logo?: boolean | null;
  chat_window_title?: string | null;
  chat_bubble_color?: string | null;
  chat_bubble_text_color?: string | null;
  user_bubble_color?: string | null;
  user_bubble_text_color?: string | null;
  enable_sound_notifications?: boolean | null;
  show_typing_indicator?: boolean | null;
  enable_chat_history?: boolean | null;
  inherits_descendant_knowledge?: boolean;
  button_shape?: string | null;
  window_size?: string | null;
  header_style?: string | null;
  window_border_radius?: string | null;
  window_animation?: string | null;
  send_button_style?: string | null;
  input_placeholder?: string | null;
  window_position_mode?: string | null;
  chat_button_icon?: string | null;
  chat_button_text_color?: string | null;
  public_profile_button_icon?: string | null;
  public_profile_button_text_color?: string | null;
  podcast_button_icon?: string | null;
  podcast_button_text_color?: string | null;
  public_profile_button_color?: string | null;
  podcast_button_color?: string | null;
  chat_button_border_color?: string | null;
  public_profile_button_border_color?: string | null;
  podcast_button_border_color?: string | null;
  public_profile_button_shape?: string | null;
  podcast_button_shape?: string | null;
  public_profile_button_text?: string | null;
  podcast_button_text?: string | null;
  input_style?: string | null;
  chat_button_position?: string | null;
  response_delay?: number | null;
  origin_entity_id?: UUID | null;
};

export type UpdateChatConfigFields = UnifiedFieldSelection<ChatConfigResourceSchema>[];

export type InferUpdateChatConfigResult<
  Fields extends UpdateChatConfigFields | undefined,
> = InferResult<ChatConfigResourceSchema, Fields>;

export type UpdateChatConfigResult<Fields extends UpdateChatConfigFields | undefined = undefined> = | { success: true; data: InferUpdateChatConfigResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing ChatConfig
 *
 * @ashActionType :update
 */
export async function update_chat_config<Fields extends UpdateChatConfigFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUID;
  input?: UpdateChatConfigInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<UpdateChatConfigResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "update_chat_config",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<UpdateChatConfigResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


/**
 * Validate: Update an existing ChatConfig
 *
 * @ashActionType :update
 * @validation true
 */
export async function validate_update_chat_config(
  config: {
  tenant?: string;
  identity: UUID | string;
  input?: UpdateChatConfigInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "update_chat_config",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Update an existing ChatConfig
 *
 * @ashActionType :update
 * @validation true
 */
export async function validate_update_chat_config_channel(config: {
  channel: Channel;
  tenant?: string;
  identity: UUID | string;
  input?: UpdateChatConfigInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "update_chat_config",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Update an existing ChatConfig
 *
 * @ashActionType :update
 */
export async function update_chat_config_channel<Fields extends UpdateChatConfigFields | undefined = undefined>(config: {
  channel: Channel;
  tenant?: string;
  identity: UUID;
  input?: UpdateChatConfigInput;
  fields?: Fields;
  result_handler: (result: UpdateChatConfigResult<Fields>) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<UpdateChatConfigResult<Fields>>(
    config.channel,
    {
    action: "update_chat_config",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  },
    config.timeout,
    config
  );
}


export type CreateOriginAssetInput = {
  asset_type: string;
  asset_url: string;
  asset_key?: string | null;
  asset_name?: string | null;
  asset_metadata?: Record<string, any> | null;
  source_platform?: string | null;
  shared_with_origin_entity_ids?: Array<UUID> | null;
  is_shared_globally?: boolean | null;
  origin_entity_id: UUID;
};

export type CreateOriginAssetFields = UnifiedFieldSelection<OriginAssetResourceSchema>[];

export type InferCreateOriginAssetResult<
  Fields extends CreateOriginAssetFields | undefined,
> = InferResult<OriginAssetResourceSchema, Fields>;

export type CreateOriginAssetResult<Fields extends CreateOriginAssetFields | undefined = undefined> = | { success: true; data: InferCreateOriginAssetResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Create a new OriginAsset
 *
 * @ashActionType :create
 */
export async function create_origin_asset<Fields extends CreateOriginAssetFields | undefined = undefined>(
  config: {
  tenant?: string;
  input: CreateOriginAssetInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<CreateOriginAssetResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "create_origin_asset",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<CreateOriginAssetResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


/**
 * Validate: Create a new OriginAsset
 *
 * @ashActionType :create
 * @validation true
 */
export async function validate_create_origin_asset(
  config: {
  tenant?: string;
  input: CreateOriginAssetInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "create_origin_asset",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Create a new OriginAsset
 *
 * @ashActionType :create
 * @validation true
 */
export async function validate_create_origin_asset_channel(config: {
  channel: Channel;
  tenant?: string;
  input: CreateOriginAssetInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "create_origin_asset",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Create a new OriginAsset
 *
 * @ashActionType :create
 */
export async function create_origin_asset_channel<Fields extends CreateOriginAssetFields | undefined = undefined>(config: {
  channel: Channel;
  tenant?: string;
  input: CreateOriginAssetInput;
  fields?: Fields;
  result_handler: (result: CreateOriginAssetResult<Fields>) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<CreateOriginAssetResult<Fields>>(
    config.channel,
    {
    action: "create_origin_asset",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  },
    config.timeout,
    config
  );
}


export type EditAssetImageInput = {
  image_url: string;
  prompt: string;
  edit_mode?: string | null;
  asset_name?: string | null;
  asset_type?: string | null;
  origin_entity_id: UUID;
};

export type InferEditAssetImageResult = Record<string, any>;

export type EditAssetImageResult = | { success: true; data: InferEditAssetImageResult; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Execute generic action on OriginAsset
 *
 * @ashActionType :action
 */
export async function edit_asset_image(
  config: {
  tenant?: string;
  input: EditAssetImageInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<EditAssetImageResult> {
  const payload = {
    action: "edit_asset_image",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeActionRpcRequest<EditAssetImageResult>(
    payload,
    config
  );
}


/**
 * Validate: Execute generic action on OriginAsset
 *
 * @ashActionType :action
 * @validation true
 */
export async function validate_edit_asset_image(
  config: {
  tenant?: string;
  input: EditAssetImageInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "edit_asset_image",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Execute generic action on OriginAsset
 *
 * @ashActionType :action
 * @validation true
 */
export async function validate_edit_asset_image_channel(config: {
  channel: Channel;
  tenant?: string;
  input: EditAssetImageInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "edit_asset_image",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Execute generic action on OriginAsset
 *
 * @ashActionType :action
 */
export async function edit_asset_image_channel(config: {
  channel: Channel;
  tenant?: string;
  input: EditAssetImageInput;
  result_handler: (result: EditAssetImageResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<EditAssetImageResult>(
    config.channel,
    {
    action: "edit_asset_image",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


export type ListOriginAssetsFields = UnifiedFieldSelection<OriginAssetResourceSchema>[];


export type InferListOriginAssetsResult<
  Fields extends ListOriginAssetsFields | undefined,
  Page extends ListOriginAssetsConfig["page"] = undefined
> = ConditionalPaginatedResult<Page, Array<InferResult<OriginAssetResourceSchema, Fields>>, {
  results: Array<InferResult<OriginAssetResourceSchema, Fields>>;
  has_more: boolean;
  limit: number;
  after: string | null;
  before: string | null;
  previous_page: string;
  next_page: string;
  count?: number | null;
  type: "keyset";
}>;

export type ListOriginAssetsConfig = {
  tenant?: string;
  fields: ListOriginAssetsFields;
  filter?: OriginAssetFilterInput;
  sort?: SortString<OriginAssetSortField> | SortString<OriginAssetSortField>[];
  page?: {
    limit?: number;
    after?: string;
    before?: string;
    offset?: never;
    count?: never;
  };
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
};

export type ListOriginAssetsResult<Fields extends ListOriginAssetsFields, Page extends ListOriginAssetsConfig["page"] = undefined> = | { success: true; data: InferListOriginAssetsResult<Fields, Page>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read OriginAsset records
 *
 * @ashActionType :read
 */
export async function list_origin_assets<Fields extends ListOriginAssetsFields, Config extends ListOriginAssetsConfig = ListOriginAssetsConfig>(
  config: Config & { fields: Fields }
): Promise<ListOriginAssetsResult<Fields, Config["page"]>> {
  const payload = {
    action: "list_origin_assets",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort }),
    ...(config.page && { page: config.page })
  };

  return executeActionRpcRequest<ListOriginAssetsResult<Fields, Config["page"]>>(
    payload,
    config
  );
}


/**
 * Validate: Read OriginAsset records
 *
 * @ashActionType :read
 * @validation true
 */
export async function validate_list_origin_assets(
  config: {
  tenant?: string;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "list_origin_assets",
    ...(config.tenant !== undefined && { tenant: config.tenant })
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Read OriginAsset records
 *
 * @ashActionType :read
 * @validation true
 */
export async function validate_list_origin_assets_channel(config: {
  channel: Channel;
  tenant?: string;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "list_origin_assets",
    ...(config.tenant !== undefined && { tenant: config.tenant })
  },
    config.timeout,
    config
  );
}


/**
 * Read OriginAsset records
 *
 * @ashActionType :read
 */
export async function list_origin_assets_channel<Fields extends ListOriginAssetsFields>(config: {
  channel: Channel;
  tenant?: string;
  fields: Fields;
  filter?: OriginAssetFilterInput;
  sort?: SortString<OriginAssetSortField> | SortString<OriginAssetSortField>[];
  page?: {
    limit?: number;
    after?: string;
    before?: string;
    offset?: never;
    count?: never;
  };
  result_handler: (result: ListOriginAssetsResult<Fields>) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<ListOriginAssetsResult<Fields>>(
    config.channel,
    {
    action: "list_origin_assets",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort }),
    ...(config.page && { page: config.page })
  },
    config.timeout,
    config
  );
}


export type UpdateOriginAssetInput = {
  asset_type?: string;
  asset_url?: string;
  asset_key?: string | null;
  asset_name?: string | null;
  asset_metadata?: Record<string, any> | null;
  source_platform?: string | null;
  shared_with_origin_entity_ids?: Array<UUID> | null;
  is_shared_globally?: boolean | null;
  origin_entity_id?: UUID;
};

export type UpdateOriginAssetFields = UnifiedFieldSelection<OriginAssetResourceSchema>[];

export type InferUpdateOriginAssetResult<
  Fields extends UpdateOriginAssetFields | undefined,
> = InferResult<OriginAssetResourceSchema, Fields>;

export type UpdateOriginAssetResult<Fields extends UpdateOriginAssetFields | undefined = undefined> = | { success: true; data: InferUpdateOriginAssetResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing OriginAsset
 *
 * @ashActionType :update
 */
export async function update_origin_asset<Fields extends UpdateOriginAssetFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUID;
  input: UpdateOriginAssetInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<UpdateOriginAssetResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "update_origin_asset",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<UpdateOriginAssetResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


/**
 * Validate: Update an existing OriginAsset
 *
 * @ashActionType :update
 * @validation true
 */
export async function validate_update_origin_asset(
  config: {
  tenant?: string;
  identity: UUID | string;
  input: UpdateOriginAssetInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "update_origin_asset",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Update an existing OriginAsset
 *
 * @ashActionType :update
 * @validation true
 */
export async function validate_update_origin_asset_channel(config: {
  channel: Channel;
  tenant?: string;
  identity: UUID | string;
  input: UpdateOriginAssetInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "update_origin_asset",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Update an existing OriginAsset
 *
 * @ashActionType :update
 */
export async function update_origin_asset_channel<Fields extends UpdateOriginAssetFields | undefined = undefined>(config: {
  channel: Channel;
  tenant?: string;
  identity: UUID;
  input: UpdateOriginAssetInput;
  fields?: Fields;
  result_handler: (result: UpdateOriginAssetResult<Fields>) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<UpdateOriginAssetResult<Fields>>(
    config.channel,
    {
    action: "update_origin_asset",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  },
    config.timeout,
    config
  );
}


export type CloneCartesiaVoiceInput = {
  name: string;
  description?: string | null;
  audio_file_base64: string;
  audio_format: string;
  language?: string;
};

export type InferCloneCartesiaVoiceResult = Record<string, any>;

export type CloneCartesiaVoiceResult = | { success: true; data: InferCloneCartesiaVoiceResult; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Execute generic action on OriginEntity
 *
 * @ashActionType :action
 */
export async function clone_cartesia_voice(
  config: {
  tenant?: string;
  input: CloneCartesiaVoiceInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<CloneCartesiaVoiceResult> {
  const payload = {
    action: "clone_cartesia_voice",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeActionRpcRequest<CloneCartesiaVoiceResult>(
    payload,
    config
  );
}


/**
 * Validate: Execute generic action on OriginEntity
 *
 * @ashActionType :action
 * @validation true
 */
export async function validate_clone_cartesia_voice(
  config: {
  tenant?: string;
  input: CloneCartesiaVoiceInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "clone_cartesia_voice",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Execute generic action on OriginEntity
 *
 * @ashActionType :action
 * @validation true
 */
export async function validate_clone_cartesia_voice_channel(config: {
  channel: Channel;
  tenant?: string;
  input: CloneCartesiaVoiceInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "clone_cartesia_voice",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Execute generic action on OriginEntity
 *
 * @ashActionType :action
 */
export async function clone_cartesia_voice_channel(config: {
  channel: Channel;
  tenant?: string;
  input: CloneCartesiaVoiceInput;
  result_handler: (result: CloneCartesiaVoiceResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<CloneCartesiaVoiceResult>(
    config.channel,
    {
    action: "clone_cartesia_voice",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


export type CreateOriginEntityInput = {
  parent_origin_entity_id?: UUID | null;
  name: string;
  description?: string | null;
  title?: string | null;
  personality_traits?: Array<string> | null;
  mission_statement?: string | null;
  core_values?: Array<string> | null;
  accent_colors?: Array<string> | null;
  picture_url?: string | null;
  picture_key?: string | null;
  banner_url?: string | null;
  banner_key?: string | null;
  social_links?: Array<OriginsIdentitySocialLinkInputSchema> | null;
  content_themes?: Array<string> | null;
  target_audience?: string | null;
  tone_of_voice?: string | null;
  key_messaging?: Array<string> | null;
  engagement_metrics?: Record<string, any> | null;
  extracted_content?: Record<string, any> | null;
  ai_generated_summary?: string | null;
  voice_settings?: Record<string, any> | null;
  voice_config?: Record<string, any> | null;
  guardrails_settings?: Record<string, any> | null;
  appearance_config?: OriginsIdentityAppearanceConfigInputSchema | null;
  brand_conversation_config?: OriginsIdentityBrandConversationConfigInputSchema | null;
  public_profile_config?: OriginsIdentityPublicProfileConfigInputSchema | null;
  categories?: Array<string> | null;
  is_public?: boolean | null;
  is_active?: boolean | null;
  is_featured?: boolean | null;
  slug: string;
  website_content?: Record<string, any> | null;
  last_website_scrape?: UtcDateTimeUsec | null;
  page_sections_config?: Record<string, any> | null;
  youtube_channel_id?: string | null;
  youtube_channel_data?: Record<string, any> | null;
  youtube_content_settings?: Record<string, any> | null;
  content_tabs_config?: Record<string, any> | null;
  youtube_sync_settings?: Record<string, any> | null;
  podcast_content_settings?: Record<string, any> | null;
  podcast_button_visible?: boolean | null;
  podcast_button_link?: string | null;
  podcast_button_text?: string | null;
  podcast_button_color?: string | null;
  podcast_button_text_color?: string | null;
  setup_progress_id?: UUID | null;
};

export type CreateOriginEntityFields = UnifiedFieldSelection<OriginEntityResourceSchema>[];

export type InferCreateOriginEntityResult<
  Fields extends CreateOriginEntityFields | undefined,
> = InferResult<OriginEntityResourceSchema, Fields>;

export type CreateOriginEntityResult<Fields extends CreateOriginEntityFields | undefined = undefined> = | { success: true; data: InferCreateOriginEntityResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Create a new OriginEntity
 *
 * @ashActionType :create
 */
export async function create_origin_entity<Fields extends CreateOriginEntityFields | undefined = undefined>(
  config: {
  tenant?: string;
  input: CreateOriginEntityInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<CreateOriginEntityResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "create_origin_entity",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<CreateOriginEntityResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


/**
 * Validate: Create a new OriginEntity
 *
 * @ashActionType :create
 * @validation true
 */
export async function validate_create_origin_entity(
  config: {
  tenant?: string;
  input: CreateOriginEntityInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "create_origin_entity",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Create a new OriginEntity
 *
 * @ashActionType :create
 * @validation true
 */
export async function validate_create_origin_entity_channel(config: {
  channel: Channel;
  tenant?: string;
  input: CreateOriginEntityInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "create_origin_entity",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Create a new OriginEntity
 *
 * @ashActionType :create
 */
export async function create_origin_entity_channel<Fields extends CreateOriginEntityFields | undefined = undefined>(config: {
  channel: Channel;
  tenant?: string;
  input: CreateOriginEntityInput;
  fields?: Fields;
  result_handler: (result: CreateOriginEntityResult<Fields>) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<CreateOriginEntityResult<Fields>>(
    config.channel,
    {
    action: "create_origin_entity",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  },
    config.timeout,
    config
  );
}


export type DeleteOriginEntityResult = | { success: true; data: {}; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Delete a OriginEntity
 *
 * @ashActionType :destroy
 */
export async function delete_origin_entity(
  config: {
  tenant?: string;
  identity: UUID;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<DeleteOriginEntityResult> {
  const payload = {
    action: "delete_origin_entity",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity
  };

  return executeActionRpcRequest<DeleteOriginEntityResult>(
    payload,
    config
  );
}


/**
 * Validate: Delete a OriginEntity
 *
 * @ashActionType :destroy
 * @validation true
 */
export async function validate_delete_origin_entity(
  config: {
  tenant?: string;
  identity: UUID | string;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "delete_origin_entity",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Delete a OriginEntity
 *
 * @ashActionType :destroy
 * @validation true
 */
export async function validate_delete_origin_entity_channel(config: {
  channel: Channel;
  tenant?: string;
  identity: UUID | string;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "delete_origin_entity",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity
  },
    config.timeout,
    config
  );
}


/**
 * Delete a OriginEntity
 *
 * @ashActionType :destroy
 */
export async function delete_origin_entity_channel(config: {
  channel: Channel;
  tenant?: string;
  identity: UUID;
  result_handler: (result: DeleteOriginEntityResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<DeleteOriginEntityResult>(
    config.channel,
    {
    action: "delete_origin_entity",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity
  },
    config.timeout,
    config
  );
}


export type DiscoverAndExtractProfileInput = {
  entity_name: string;
  category_hint: string;
  website_url?: string | null;
};

export type InferDiscoverAndExtractProfileResult = Record<string, any>;

export type DiscoverAndExtractProfileResult = | { success: true; data: InferDiscoverAndExtractProfileResult; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Execute generic action on OriginEntity
 *
 * @ashActionType :action
 */
export async function discover_and_extract_profile(
  config: {
  tenant?: string;
  input: DiscoverAndExtractProfileInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<DiscoverAndExtractProfileResult> {
  const payload = {
    action: "discover_and_extract_profile",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeActionRpcRequest<DiscoverAndExtractProfileResult>(
    payload,
    config
  );
}


/**
 * Validate: Execute generic action on OriginEntity
 *
 * @ashActionType :action
 * @validation true
 */
export async function validate_discover_and_extract_profile(
  config: {
  tenant?: string;
  input: DiscoverAndExtractProfileInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "discover_and_extract_profile",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Execute generic action on OriginEntity
 *
 * @ashActionType :action
 * @validation true
 */
export async function validate_discover_and_extract_profile_channel(config: {
  channel: Channel;
  tenant?: string;
  input: DiscoverAndExtractProfileInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "discover_and_extract_profile",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Execute generic action on OriginEntity
 *
 * @ashActionType :action
 */
export async function discover_and_extract_profile_channel(config: {
  channel: Channel;
  tenant?: string;
  input: DiscoverAndExtractProfileInput;
  result_handler: (result: DiscoverAndExtractProfileResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<DiscoverAndExtractProfileResult>(
    config.channel,
    {
    action: "discover_and_extract_profile",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


export type DiscoverEntityLinksInput = {
  entity_name: string;
  category_hint: string;
};

export type InferDiscoverEntityLinksResult = Record<string, any>;

export type DiscoverEntityLinksResult = | { success: true; data: InferDiscoverEntityLinksResult; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Execute generic action on OriginEntity
 *
 * @ashActionType :action
 */
export async function discover_entity_links(
  config: {
  tenant?: string;
  input: DiscoverEntityLinksInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<DiscoverEntityLinksResult> {
  const payload = {
    action: "discover_entity_links",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeActionRpcRequest<DiscoverEntityLinksResult>(
    payload,
    config
  );
}


/**
 * Validate: Execute generic action on OriginEntity
 *
 * @ashActionType :action
 * @validation true
 */
export async function validate_discover_entity_links(
  config: {
  tenant?: string;
  input: DiscoverEntityLinksInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "discover_entity_links",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Execute generic action on OriginEntity
 *
 * @ashActionType :action
 * @validation true
 */
export async function validate_discover_entity_links_channel(config: {
  channel: Channel;
  tenant?: string;
  input: DiscoverEntityLinksInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "discover_entity_links",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Execute generic action on OriginEntity
 *
 * @ashActionType :action
 */
export async function discover_entity_links_channel(config: {
  channel: Channel;
  tenant?: string;
  input: DiscoverEntityLinksInput;
  result_handler: (result: DiscoverEntityLinksResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<DiscoverEntityLinksResult>(
    config.channel,
    {
    action: "discover_entity_links",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


export type GenerateProfileDescriptionInput = {
  origin_entity_id: UUID;
  mode: string;
  current_description?: string | null;
};

export type InferGenerateProfileDescriptionResult = Record<string, any>;

export type GenerateProfileDescriptionResult = | { success: true; data: InferGenerateProfileDescriptionResult; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Execute generic action on OriginEntity
 *
 * @ashActionType :action
 */
export async function generate_profile_description(
  config: {
  tenant?: string;
  input: GenerateProfileDescriptionInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<GenerateProfileDescriptionResult> {
  const payload = {
    action: "generate_profile_description",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeActionRpcRequest<GenerateProfileDescriptionResult>(
    payload,
    config
  );
}


/**
 * Validate: Execute generic action on OriginEntity
 *
 * @ashActionType :action
 * @validation true
 */
export async function validate_generate_profile_description(
  config: {
  tenant?: string;
  input: GenerateProfileDescriptionInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "generate_profile_description",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Execute generic action on OriginEntity
 *
 * @ashActionType :action
 * @validation true
 */
export async function validate_generate_profile_description_channel(config: {
  channel: Channel;
  tenant?: string;
  input: GenerateProfileDescriptionInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "generate_profile_description",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Execute generic action on OriginEntity
 *
 * @ashActionType :action
 */
export async function generate_profile_description_channel(config: {
  channel: Channel;
  tenant?: string;
  input: GenerateProfileDescriptionInput;
  result_handler: (result: GenerateProfileDescriptionResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<GenerateProfileDescriptionResult>(
    config.channel,
    {
    action: "generate_profile_description",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


export type GenerateSoulConfigInput = {
  config_type: string;
  origin_entity_id?: UUID | null;
  selected_objectives?: Array<Record<string, any>> | null;
  additional_context?: string | null;
};

export type InferGenerateSoulConfigResult = Record<string, any>;

export type GenerateSoulConfigResult = | { success: true; data: InferGenerateSoulConfigResult; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Execute generic action on OriginEntity
 *
 * @ashActionType :action
 */
export async function generate_soul_config(
  config: {
  tenant?: string;
  input: GenerateSoulConfigInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<GenerateSoulConfigResult> {
  const payload = {
    action: "generate_soul_config",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeActionRpcRequest<GenerateSoulConfigResult>(
    payload,
    config
  );
}


/**
 * Validate: Execute generic action on OriginEntity
 *
 * @ashActionType :action
 * @validation true
 */
export async function validate_generate_soul_config(
  config: {
  tenant?: string;
  input: GenerateSoulConfigInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "generate_soul_config",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Execute generic action on OriginEntity
 *
 * @ashActionType :action
 * @validation true
 */
export async function validate_generate_soul_config_channel(config: {
  channel: Channel;
  tenant?: string;
  input: GenerateSoulConfigInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "generate_soul_config",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Execute generic action on OriginEntity
 *
 * @ashActionType :action
 */
export async function generate_soul_config_channel(config: {
  channel: Channel;
  tenant?: string;
  input: GenerateSoulConfigInput;
  result_handler: (result: GenerateSoulConfigResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<GenerateSoulConfigResult>(
    config.channel,
    {
    action: "generate_soul_config",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


export type GetOriginEntityInput = {
  id: UUID;
};

export type GetOriginEntityFields = UnifiedFieldSelection<OriginEntityResourceSchema>[];
export type InferGetOriginEntityResult<
  Fields extends GetOriginEntityFields,
> = InferResult<OriginEntityResourceSchema, Fields>;

export type GetOriginEntityResult<Fields extends GetOriginEntityFields> = | { success: true; data: InferGetOriginEntityResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read OriginEntity records
 *
 * @ashActionType :read
 */
export async function get_origin_entity<Fields extends GetOriginEntityFields>(
  config: {
  tenant?: string;
  input: GetOriginEntityInput;
  fields: Fields;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<GetOriginEntityResult<Fields>> {
  const payload = {
    action: "get_origin_entity",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<GetOriginEntityResult<Fields>>(
    payload,
    config
  );
}


/**
 * Validate: Read OriginEntity records
 *
 * @ashActionType :read
 * @validation true
 */
export async function validate_get_origin_entity(
  config: {
  tenant?: string;
  input: GetOriginEntityInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "get_origin_entity",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Read OriginEntity records
 *
 * @ashActionType :read
 * @validation true
 */
export async function validate_get_origin_entity_channel(config: {
  channel: Channel;
  tenant?: string;
  input: GetOriginEntityInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "get_origin_entity",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Read OriginEntity records
 *
 * @ashActionType :read
 */
export async function get_origin_entity_channel<Fields extends GetOriginEntityFields>(config: {
  channel: Channel;
  tenant?: string;
  input: GetOriginEntityInput;
  fields: Fields;
  result_handler: (result: GetOriginEntityResult<Fields>) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<GetOriginEntityResult<Fields>>(
    config.channel,
    {
    action: "get_origin_entity",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  },
    config.timeout,
    config
  );
}


export type GetOriginEntityBySlugInput = {
  slug: string;
};

export type GetOriginEntityBySlugFields = UnifiedFieldSelection<OriginEntityResourceSchema>[];
export type InferGetOriginEntityBySlugResult<
  Fields extends GetOriginEntityBySlugFields,
> = InferResult<OriginEntityResourceSchema, Fields>;

export type GetOriginEntityBySlugResult<Fields extends GetOriginEntityBySlugFields> = | { success: true; data: InferGetOriginEntityBySlugResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read OriginEntity records
 *
 * @ashActionType :read
 */
export async function get_origin_entity_by_slug<Fields extends GetOriginEntityBySlugFields>(
  config: {
  tenant?: string;
  input: GetOriginEntityBySlugInput;
  fields: Fields;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<GetOriginEntityBySlugResult<Fields>> {
  const payload = {
    action: "get_origin_entity_by_slug",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<GetOriginEntityBySlugResult<Fields>>(
    payload,
    config
  );
}


/**
 * Validate: Read OriginEntity records
 *
 * @ashActionType :read
 * @validation true
 */
export async function validate_get_origin_entity_by_slug(
  config: {
  tenant?: string;
  input: GetOriginEntityBySlugInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "get_origin_entity_by_slug",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Read OriginEntity records
 *
 * @ashActionType :read
 * @validation true
 */
export async function validate_get_origin_entity_by_slug_channel(config: {
  channel: Channel;
  tenant?: string;
  input: GetOriginEntityBySlugInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "get_origin_entity_by_slug",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Read OriginEntity records
 *
 * @ashActionType :read
 */
export async function get_origin_entity_by_slug_channel<Fields extends GetOriginEntityBySlugFields>(config: {
  channel: Channel;
  tenant?: string;
  input: GetOriginEntityBySlugInput;
  fields: Fields;
  result_handler: (result: GetOriginEntityBySlugResult<Fields>) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<GetOriginEntityBySlugResult<Fields>>(
    config.channel,
    {
    action: "get_origin_entity_by_slug",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  },
    config.timeout,
    config
  );
}


export type GetPublicOriginEntityInput = {
  id: UUID;
};

export type GetPublicOriginEntityFields = UnifiedFieldSelection<OriginEntityResourceSchema>[];
export type InferGetPublicOriginEntityResult<
  Fields extends GetPublicOriginEntityFields,
> = InferResult<OriginEntityResourceSchema, Fields>;

export type GetPublicOriginEntityResult<Fields extends GetPublicOriginEntityFields> = | { success: true; data: InferGetPublicOriginEntityResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read OriginEntity records
 *
 * @ashActionType :read
 */
export async function get_public_origin_entity<Fields extends GetPublicOriginEntityFields>(
  config: {
  tenant?: string;
  input: GetPublicOriginEntityInput;
  fields: Fields;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<GetPublicOriginEntityResult<Fields>> {
  const payload = {
    action: "get_public_origin_entity",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<GetPublicOriginEntityResult<Fields>>(
    payload,
    config
  );
}


/**
 * Validate: Read OriginEntity records
 *
 * @ashActionType :read
 * @validation true
 */
export async function validate_get_public_origin_entity(
  config: {
  tenant?: string;
  input: GetPublicOriginEntityInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "get_public_origin_entity",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Read OriginEntity records
 *
 * @ashActionType :read
 * @validation true
 */
export async function validate_get_public_origin_entity_channel(config: {
  channel: Channel;
  tenant?: string;
  input: GetPublicOriginEntityInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "get_public_origin_entity",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Read OriginEntity records
 *
 * @ashActionType :read
 */
export async function get_public_origin_entity_channel<Fields extends GetPublicOriginEntityFields>(config: {
  channel: Channel;
  tenant?: string;
  input: GetPublicOriginEntityInput;
  fields: Fields;
  result_handler: (result: GetPublicOriginEntityResult<Fields>) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<GetPublicOriginEntityResult<Fields>>(
    config.channel,
    {
    action: "get_public_origin_entity",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  },
    config.timeout,
    config
  );
}


export type InferListCartesiaVoicesResult = Array<Record<string, any>>;

export type ListCartesiaVoicesResult = | { success: true; data: InferListCartesiaVoicesResult; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Execute generic action on OriginEntity
 *
 * @ashActionType :action
 */
export async function list_cartesia_voices(
  config: {
  tenant?: string;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ListCartesiaVoicesResult> {
  const payload = {
    action: "list_cartesia_voices",
    ...(config.tenant !== undefined && { tenant: config.tenant })
  };

  return executeActionRpcRequest<ListCartesiaVoicesResult>(
    payload,
    config
  );
}


/**
 * Validate: Execute generic action on OriginEntity
 *
 * @ashActionType :action
 * @validation true
 */
export async function validate_list_cartesia_voices(
  config: {
  tenant?: string;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "list_cartesia_voices",
    ...(config.tenant !== undefined && { tenant: config.tenant })
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Execute generic action on OriginEntity
 *
 * @ashActionType :action
 * @validation true
 */
export async function validate_list_cartesia_voices_channel(config: {
  channel: Channel;
  tenant?: string;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "list_cartesia_voices",
    ...(config.tenant !== undefined && { tenant: config.tenant })
  },
    config.timeout,
    config
  );
}


/**
 * Execute generic action on OriginEntity
 *
 * @ashActionType :action
 */
export async function list_cartesia_voices_channel(config: {
  channel: Channel;
  tenant?: string;
  result_handler: (result: ListCartesiaVoicesResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<ListCartesiaVoicesResult>(
    config.channel,
    {
    action: "list_cartesia_voices",
    ...(config.tenant !== undefined && { tenant: config.tenant })
  },
    config.timeout,
    config
  );
}


export type ListOriginEntitiesFields = UnifiedFieldSelection<OriginEntityResourceSchema>[];


export type InferListOriginEntitiesResult<
  Fields extends ListOriginEntitiesFields | undefined,
  Page extends ListOriginEntitiesConfig["page"] = undefined
> = ConditionalPaginatedResult<Page, Array<InferResult<OriginEntityResourceSchema, Fields>>, {
  results: Array<InferResult<OriginEntityResourceSchema, Fields>>;
  has_more: boolean;
  limit: number;
  after: string | null;
  before: string | null;
  previous_page: string;
  next_page: string;
  count?: number | null;
  type: "keyset";
}>;

export type ListOriginEntitiesConfig = {
  tenant?: string;
  fields: ListOriginEntitiesFields;
  filter?: OriginEntityFilterInput;
  sort?: SortString<OriginEntitySortField> | SortString<OriginEntitySortField>[];
  page?: {
    limit?: number;
    after?: string;
    before?: string;
    offset?: never;
    count?: never;
  };
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
};

export type ListOriginEntitiesResult<Fields extends ListOriginEntitiesFields, Page extends ListOriginEntitiesConfig["page"] = undefined> = | { success: true; data: InferListOriginEntitiesResult<Fields, Page>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read OriginEntity records
 *
 * @ashActionType :read
 */
export async function list_origin_entities<Fields extends ListOriginEntitiesFields, Config extends ListOriginEntitiesConfig = ListOriginEntitiesConfig>(
  config: Config & { fields: Fields }
): Promise<ListOriginEntitiesResult<Fields, Config["page"]>> {
  const payload = {
    action: "list_origin_entities",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort }),
    ...(config.page && { page: config.page })
  };

  return executeActionRpcRequest<ListOriginEntitiesResult<Fields, Config["page"]>>(
    payload,
    config
  );
}


/**
 * Validate: Read OriginEntity records
 *
 * @ashActionType :read
 * @validation true
 */
export async function validate_list_origin_entities(
  config: {
  tenant?: string;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "list_origin_entities",
    ...(config.tenant !== undefined && { tenant: config.tenant })
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Read OriginEntity records
 *
 * @ashActionType :read
 * @validation true
 */
export async function validate_list_origin_entities_channel(config: {
  channel: Channel;
  tenant?: string;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "list_origin_entities",
    ...(config.tenant !== undefined && { tenant: config.tenant })
  },
    config.timeout,
    config
  );
}


/**
 * Read OriginEntity records
 *
 * @ashActionType :read
 */
export async function list_origin_entities_channel<Fields extends ListOriginEntitiesFields>(config: {
  channel: Channel;
  tenant?: string;
  fields: Fields;
  filter?: OriginEntityFilterInput;
  sort?: SortString<OriginEntitySortField> | SortString<OriginEntitySortField>[];
  page?: {
    limit?: number;
    after?: string;
    before?: string;
    offset?: never;
    count?: never;
  };
  result_handler: (result: ListOriginEntitiesResult<Fields>) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<ListOriginEntitiesResult<Fields>>(
    config.channel,
    {
    action: "list_origin_entities",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort }),
    ...(config.page && { page: config.page })
  },
    config.timeout,
    config
  );
}


export type ListOriginEntitiesForSwitcherFields = UnifiedFieldSelection<OriginEntityResourceSchema>[];
export type InferListOriginEntitiesForSwitcherResult<
  Fields extends ListOriginEntitiesForSwitcherFields,
> = Array<InferResult<OriginEntityResourceSchema, Fields>>;

export type ListOriginEntitiesForSwitcherResult<Fields extends ListOriginEntitiesForSwitcherFields> = | { success: true; data: InferListOriginEntitiesForSwitcherResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read OriginEntity records
 *
 * @ashActionType :read
 */
export async function list_origin_entities_for_switcher<Fields extends ListOriginEntitiesForSwitcherFields>(
  config: {
  tenant?: string;
  fields: Fields;
  filter?: OriginEntityFilterInput;
  sort?: SortString<OriginEntitySortField> | SortString<OriginEntitySortField>[];
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ListOriginEntitiesForSwitcherResult<Fields>> {
  const payload = {
    action: "list_origin_entities_for_switcher",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort })
  };

  return executeActionRpcRequest<ListOriginEntitiesForSwitcherResult<Fields>>(
    payload,
    config
  );
}


/**
 * Validate: Read OriginEntity records
 *
 * @ashActionType :read
 * @validation true
 */
export async function validate_list_origin_entities_for_switcher(
  config: {
  tenant?: string;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "list_origin_entities_for_switcher",
    ...(config.tenant !== undefined && { tenant: config.tenant })
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Read OriginEntity records
 *
 * @ashActionType :read
 * @validation true
 */
export async function validate_list_origin_entities_for_switcher_channel(config: {
  channel: Channel;
  tenant?: string;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "list_origin_entities_for_switcher",
    ...(config.tenant !== undefined && { tenant: config.tenant })
  },
    config.timeout,
    config
  );
}


/**
 * Read OriginEntity records
 *
 * @ashActionType :read
 */
export async function list_origin_entities_for_switcher_channel<Fields extends ListOriginEntitiesForSwitcherFields>(config: {
  channel: Channel;
  tenant?: string;
  fields: Fields;
  filter?: OriginEntityFilterInput;
  sort?: SortString<OriginEntitySortField> | SortString<OriginEntitySortField>[];
  result_handler: (result: ListOriginEntitiesForSwitcherResult<Fields>) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<ListOriginEntitiesForSwitcherResult<Fields>>(
    config.channel,
    {
    action: "list_origin_entities_for_switcher",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort })
  },
    config.timeout,
    config
  );
}


export type ListOriginEntitiesForUserFields = UnifiedFieldSelection<OriginEntityResourceSchema>[];


export type InferListOriginEntitiesForUserResult<
  Fields extends ListOriginEntitiesForUserFields | undefined,
  Page extends ListOriginEntitiesForUserConfig["page"] = undefined
> = ConditionalPaginatedResult<Page, Array<InferResult<OriginEntityResourceSchema, Fields>>, {
  results: Array<InferResult<OriginEntityResourceSchema, Fields>>;
  has_more: boolean;
  limit: number;
  after: string | null;
  before: string | null;
  previous_page: string;
  next_page: string;
  count?: number | null;
  type: "keyset";
}>;

export type ListOriginEntitiesForUserConfig = {
  tenant?: string;
  fields: ListOriginEntitiesForUserFields;
  filter?: OriginEntityFilterInput;
  sort?: SortString<OriginEntitySortField> | SortString<OriginEntitySortField>[];
  page?: {
    limit?: number;
    after?: string;
    before?: string;
    offset?: never;
    count?: never;
  };
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
};

export type ListOriginEntitiesForUserResult<Fields extends ListOriginEntitiesForUserFields, Page extends ListOriginEntitiesForUserConfig["page"] = undefined> = | { success: true; data: InferListOriginEntitiesForUserResult<Fields, Page>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read OriginEntity records
 *
 * @ashActionType :read
 */
export async function list_origin_entities_for_user<Fields extends ListOriginEntitiesForUserFields, Config extends ListOriginEntitiesForUserConfig = ListOriginEntitiesForUserConfig>(
  config: Config & { fields: Fields }
): Promise<ListOriginEntitiesForUserResult<Fields, Config["page"]>> {
  const payload = {
    action: "list_origin_entities_for_user",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort }),
    ...(config.page && { page: config.page })
  };

  return executeActionRpcRequest<ListOriginEntitiesForUserResult<Fields, Config["page"]>>(
    payload,
    config
  );
}


/**
 * Validate: Read OriginEntity records
 *
 * @ashActionType :read
 * @validation true
 */
export async function validate_list_origin_entities_for_user(
  config: {
  tenant?: string;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "list_origin_entities_for_user",
    ...(config.tenant !== undefined && { tenant: config.tenant })
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Read OriginEntity records
 *
 * @ashActionType :read
 * @validation true
 */
export async function validate_list_origin_entities_for_user_channel(config: {
  channel: Channel;
  tenant?: string;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "list_origin_entities_for_user",
    ...(config.tenant !== undefined && { tenant: config.tenant })
  },
    config.timeout,
    config
  );
}


/**
 * Read OriginEntity records
 *
 * @ashActionType :read
 */
export async function list_origin_entities_for_user_channel<Fields extends ListOriginEntitiesForUserFields>(config: {
  channel: Channel;
  tenant?: string;
  fields: Fields;
  filter?: OriginEntityFilterInput;
  sort?: SortString<OriginEntitySortField> | SortString<OriginEntitySortField>[];
  page?: {
    limit?: number;
    after?: string;
    before?: string;
    offset?: never;
    count?: never;
  };
  result_handler: (result: ListOriginEntitiesForUserResult<Fields>) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<ListOriginEntitiesForUserResult<Fields>>(
    config.channel,
    {
    action: "list_origin_entities_for_user",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort }),
    ...(config.page && { page: config.page })
  },
    config.timeout,
    config
  );
}


export type ListPublicOriginEntitiesFields = UnifiedFieldSelection<OriginEntityResourceSchema>[];


export type InferListPublicOriginEntitiesResult<
  Fields extends ListPublicOriginEntitiesFields | undefined,
  Page extends ListPublicOriginEntitiesConfig["page"] = undefined
> = ConditionalPaginatedResult<Page, Array<InferResult<OriginEntityResourceSchema, Fields>>, {
  results: Array<InferResult<OriginEntityResourceSchema, Fields>>;
  has_more: boolean;
  limit: number;
  after: string | null;
  before: string | null;
  previous_page: string;
  next_page: string;
  count?: number | null;
  type: "keyset";
}>;

export type ListPublicOriginEntitiesConfig = {
  tenant?: string;
  fields: ListPublicOriginEntitiesFields;
  filter?: OriginEntityFilterInput;
  sort?: SortString<OriginEntitySortField> | SortString<OriginEntitySortField>[];
  page?: {
    limit?: number;
    after?: string;
    before?: string;
    offset?: never;
    count?: never;
  };
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
};

export type ListPublicOriginEntitiesResult<Fields extends ListPublicOriginEntitiesFields, Page extends ListPublicOriginEntitiesConfig["page"] = undefined> = | { success: true; data: InferListPublicOriginEntitiesResult<Fields, Page>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read OriginEntity records
 *
 * @ashActionType :read
 */
export async function list_public_origin_entities<Fields extends ListPublicOriginEntitiesFields, Config extends ListPublicOriginEntitiesConfig = ListPublicOriginEntitiesConfig>(
  config: Config & { fields: Fields }
): Promise<ListPublicOriginEntitiesResult<Fields, Config["page"]>> {
  const payload = {
    action: "list_public_origin_entities",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort }),
    ...(config.page && { page: config.page })
  };

  return executeActionRpcRequest<ListPublicOriginEntitiesResult<Fields, Config["page"]>>(
    payload,
    config
  );
}


/**
 * Validate: Read OriginEntity records
 *
 * @ashActionType :read
 * @validation true
 */
export async function validate_list_public_origin_entities(
  config: {
  tenant?: string;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "list_public_origin_entities",
    ...(config.tenant !== undefined && { tenant: config.tenant })
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Read OriginEntity records
 *
 * @ashActionType :read
 * @validation true
 */
export async function validate_list_public_origin_entities_channel(config: {
  channel: Channel;
  tenant?: string;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "list_public_origin_entities",
    ...(config.tenant !== undefined && { tenant: config.tenant })
  },
    config.timeout,
    config
  );
}


/**
 * Read OriginEntity records
 *
 * @ashActionType :read
 */
export async function list_public_origin_entities_channel<Fields extends ListPublicOriginEntitiesFields>(config: {
  channel: Channel;
  tenant?: string;
  fields: Fields;
  filter?: OriginEntityFilterInput;
  sort?: SortString<OriginEntitySortField> | SortString<OriginEntitySortField>[];
  page?: {
    limit?: number;
    after?: string;
    before?: string;
    offset?: never;
    count?: never;
  };
  result_handler: (result: ListPublicOriginEntitiesResult<Fields>) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<ListPublicOriginEntitiesResult<Fields>>(
    config.channel,
    {
    action: "list_public_origin_entities",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort }),
    ...(config.page && { page: config.page })
  },
    config.timeout,
    config
  );
}


export type ScrapeWebsiteContentInput = {
  url: string;
};

export type ScrapeWebsiteContentFields = UnifiedFieldSelection<ScrapedWebsiteContentResourceSchema>[];

export type InferScrapeWebsiteContentResult<
  Fields extends ScrapeWebsiteContentFields | undefined,
> = InferResult<ScrapedWebsiteContentResourceSchema, Fields>;

export type ScrapeWebsiteContentResult<Fields extends ScrapeWebsiteContentFields | undefined = undefined> = | { success: true; data: InferScrapeWebsiteContentResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Execute generic action on OriginEntity
 *
 * @ashActionType :action
 */
export async function scrape_website_content<Fields extends ScrapeWebsiteContentFields | undefined = undefined>(
  config: {
  tenant?: string;
  input: ScrapeWebsiteContentInput;
  fields: Fields;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ScrapeWebsiteContentResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "scrape_website_content",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<ScrapeWebsiteContentResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


/**
 * Validate: Execute generic action on OriginEntity
 *
 * @ashActionType :action
 * @validation true
 */
export async function validate_scrape_website_content(
  config: {
  tenant?: string;
  input: ScrapeWebsiteContentInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "scrape_website_content",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Execute generic action on OriginEntity
 *
 * @ashActionType :action
 * @validation true
 */
export async function validate_scrape_website_content_channel(config: {
  channel: Channel;
  tenant?: string;
  input: ScrapeWebsiteContentInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "scrape_website_content",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Execute generic action on OriginEntity
 *
 * @ashActionType :action
 */
export async function scrape_website_content_channel<Fields extends ScrapeWebsiteContentFields | undefined = undefined>(config: {
  channel: Channel;
  tenant?: string;
  input: ScrapeWebsiteContentInput;
  fields: Fields;
  result_handler: (result: ScrapeWebsiteContentResult<Fields>) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<ScrapeWebsiteContentResult<Fields>>(
    config.channel,
    {
    action: "scrape_website_content",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  },
    config.timeout,
    config
  );
}


export type SpeechToTextInput = {
  audio_data: string;
};

export type InferSpeechToTextResult = Record<string, any>;

export type SpeechToTextResult = | { success: true; data: InferSpeechToTextResult; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Execute generic action on OriginEntity
 *
 * @ashActionType :action
 */
export async function speech_to_text(
  config: {
  tenant?: string;
  input: SpeechToTextInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<SpeechToTextResult> {
  const payload = {
    action: "speech_to_text",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeActionRpcRequest<SpeechToTextResult>(
    payload,
    config
  );
}


/**
 * Validate: Execute generic action on OriginEntity
 *
 * @ashActionType :action
 * @validation true
 */
export async function validate_speech_to_text(
  config: {
  tenant?: string;
  input: SpeechToTextInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "speech_to_text",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Execute generic action on OriginEntity
 *
 * @ashActionType :action
 * @validation true
 */
export async function validate_speech_to_text_channel(config: {
  channel: Channel;
  tenant?: string;
  input: SpeechToTextInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "speech_to_text",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Execute generic action on OriginEntity
 *
 * @ashActionType :action
 */
export async function speech_to_text_channel(config: {
  channel: Channel;
  tenant?: string;
  input: SpeechToTextInput;
  result_handler: (result: SpeechToTextResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<SpeechToTextResult>(
    config.channel,
    {
    action: "speech_to_text",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


export type TextToSpeechInput = {
  text: string;
  voice_id?: string | null;
  output_format?: string | null;
  settings?: Record<string, any> | null;
};

export type InferTextToSpeechResult = Record<string, any>;

export type TextToSpeechResult = | { success: true; data: InferTextToSpeechResult; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Execute generic action on OriginEntity
 *
 * @ashActionType :action
 */
export async function text_to_speech(
  config: {
  tenant?: string;
  input: TextToSpeechInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<TextToSpeechResult> {
  const payload = {
    action: "text_to_speech",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeActionRpcRequest<TextToSpeechResult>(
    payload,
    config
  );
}


/**
 * Validate: Execute generic action on OriginEntity
 *
 * @ashActionType :action
 * @validation true
 */
export async function validate_text_to_speech(
  config: {
  tenant?: string;
  input: TextToSpeechInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "text_to_speech",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Execute generic action on OriginEntity
 *
 * @ashActionType :action
 * @validation true
 */
export async function validate_text_to_speech_channel(config: {
  channel: Channel;
  tenant?: string;
  input: TextToSpeechInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "text_to_speech",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Execute generic action on OriginEntity
 *
 * @ashActionType :action
 */
export async function text_to_speech_channel(config: {
  channel: Channel;
  tenant?: string;
  input: TextToSpeechInput;
  result_handler: (result: TextToSpeechResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<TextToSpeechResult>(
    config.channel,
    {
    action: "text_to_speech",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


export type UpdateOriginEntityInput = {
  parent_origin_entity_id?: UUID | null;
  name?: string;
  description?: string | null;
  title?: string | null;
  personality_traits?: Array<string> | null;
  mission_statement?: string | null;
  core_values?: Array<string> | null;
  accent_colors?: Array<string> | null;
  picture_url?: string | null;
  picture_key?: string | null;
  banner_url?: string | null;
  banner_key?: string | null;
  social_links?: Array<OriginsIdentitySocialLinkInputSchema> | null;
  content_themes?: Array<string> | null;
  target_audience?: string | null;
  tone_of_voice?: string | null;
  key_messaging?: Array<string> | null;
  engagement_metrics?: Record<string, any> | null;
  extracted_content?: Record<string, any> | null;
  ai_generated_summary?: string | null;
  voice_settings?: Record<string, any> | null;
  voice_config?: Record<string, any> | null;
  guardrails_settings?: Record<string, any> | null;
  appearance_config?: OriginsIdentityAppearanceConfigInputSchema | null;
  brand_conversation_config?: OriginsIdentityBrandConversationConfigInputSchema | null;
  public_profile_config?: OriginsIdentityPublicProfileConfigInputSchema | null;
  categories?: Array<string> | null;
  is_public?: boolean | null;
  is_active?: boolean | null;
  is_featured?: boolean | null;
  slug?: string;
  website_content?: Record<string, any> | null;
  last_website_scrape?: UtcDateTimeUsec | null;
  page_sections_config?: Record<string, any> | null;
  youtube_channel_id?: string | null;
  youtube_channel_data?: Record<string, any> | null;
  youtube_content_settings?: Record<string, any> | null;
  content_tabs_config?: Record<string, any> | null;
  youtube_sync_settings?: Record<string, any> | null;
  podcast_content_settings?: Record<string, any> | null;
  podcast_button_visible?: boolean | null;
  podcast_button_link?: string | null;
  podcast_button_text?: string | null;
  podcast_button_color?: string | null;
  podcast_button_text_color?: string | null;
  setup_progress_id?: UUID | null;
};

export type UpdateOriginEntityFields = UnifiedFieldSelection<OriginEntityResourceSchema>[];

export type InferUpdateOriginEntityResult<
  Fields extends UpdateOriginEntityFields | undefined,
> = InferResult<OriginEntityResourceSchema, Fields>;

export type UpdateOriginEntityResult<Fields extends UpdateOriginEntityFields | undefined = undefined> = | { success: true; data: InferUpdateOriginEntityResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing OriginEntity
 *
 * @ashActionType :update
 */
export async function update_origin_entity<Fields extends UpdateOriginEntityFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUID;
  input: UpdateOriginEntityInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<UpdateOriginEntityResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "update_origin_entity",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<UpdateOriginEntityResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


/**
 * Validate: Update an existing OriginEntity
 *
 * @ashActionType :update
 * @validation true
 */
export async function validate_update_origin_entity(
  config: {
  tenant?: string;
  identity: UUID | string;
  input: UpdateOriginEntityInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "update_origin_entity",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Update an existing OriginEntity
 *
 * @ashActionType :update
 * @validation true
 */
export async function validate_update_origin_entity_channel(config: {
  channel: Channel;
  tenant?: string;
  identity: UUID | string;
  input: UpdateOriginEntityInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "update_origin_entity",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Update an existing OriginEntity
 *
 * @ashActionType :update
 */
export async function update_origin_entity_channel<Fields extends UpdateOriginEntityFields | undefined = undefined>(config: {
  channel: Channel;
  tenant?: string;
  identity: UUID;
  input: UpdateOriginEntityInput;
  fields?: Fields;
  result_handler: (result: UpdateOriginEntityResult<Fields>) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<UpdateOriginEntityResult<Fields>>(
    config.channel,
    {
    action: "update_origin_entity",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  },
    config.timeout,
    config
  );
}


export type UpdateOriginEntityProfileConfigInput = {
  public_profile_config?: OriginsIdentityPublicProfileConfigInputSchema | null;
  appearance_config?: OriginsIdentityAppearanceConfigInputSchema | null;
  brand_conversation_config?: OriginsIdentityBrandConversationConfigInputSchema | null;
  is_public?: boolean | null;
  is_featured?: boolean | null;
};

export type UpdateOriginEntityProfileConfigFields = UnifiedFieldSelection<OriginEntityResourceSchema>[];

export type InferUpdateOriginEntityProfileConfigResult<
  Fields extends UpdateOriginEntityProfileConfigFields | undefined,
> = InferResult<OriginEntityResourceSchema, Fields>;

export type UpdateOriginEntityProfileConfigResult<Fields extends UpdateOriginEntityProfileConfigFields | undefined = undefined> = | { success: true; data: InferUpdateOriginEntityProfileConfigResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing OriginEntity
 *
 * @ashActionType :update
 */
export async function update_origin_entity_profile_config<Fields extends UpdateOriginEntityProfileConfigFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUID;
  input?: UpdateOriginEntityProfileConfigInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<UpdateOriginEntityProfileConfigResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "update_origin_entity_profile_config",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<UpdateOriginEntityProfileConfigResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


/**
 * Validate: Update an existing OriginEntity
 *
 * @ashActionType :update
 * @validation true
 */
export async function validate_update_origin_entity_profile_config(
  config: {
  tenant?: string;
  identity: UUID | string;
  input?: UpdateOriginEntityProfileConfigInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "update_origin_entity_profile_config",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Update an existing OriginEntity
 *
 * @ashActionType :update
 * @validation true
 */
export async function validate_update_origin_entity_profile_config_channel(config: {
  channel: Channel;
  tenant?: string;
  identity: UUID | string;
  input?: UpdateOriginEntityProfileConfigInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "update_origin_entity_profile_config",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Update an existing OriginEntity
 *
 * @ashActionType :update
 */
export async function update_origin_entity_profile_config_channel<Fields extends UpdateOriginEntityProfileConfigFields | undefined = undefined>(config: {
  channel: Channel;
  tenant?: string;
  identity: UUID;
  input?: UpdateOriginEntityProfileConfigInput;
  fields?: Fields;
  result_handler: (result: UpdateOriginEntityProfileConfigResult<Fields>) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<UpdateOriginEntityProfileConfigResult<Fields>>(
    config.channel,
    {
    action: "update_origin_entity_profile_config",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  },
    config.timeout,
    config
  );
}


export type AcceptMembershipInvitationInput = {
  user_id: UUID;
};

export type AcceptMembershipInvitationFields = UnifiedFieldSelection<OriginEntityMembershipResourceSchema>[];

export type InferAcceptMembershipInvitationResult<
  Fields extends AcceptMembershipInvitationFields | undefined,
> = InferResult<OriginEntityMembershipResourceSchema, Fields>;

export type AcceptMembershipInvitationResult<Fields extends AcceptMembershipInvitationFields | undefined = undefined> = | { success: true; data: InferAcceptMembershipInvitationResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing OriginEntityMembership
 *
 * @ashActionType :update
 */
export async function accept_membership_invitation<Fields extends AcceptMembershipInvitationFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUID;
  input: AcceptMembershipInvitationInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<AcceptMembershipInvitationResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "accept_membership_invitation",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<AcceptMembershipInvitationResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


/**
 * Validate: Update an existing OriginEntityMembership
 *
 * @ashActionType :update
 * @validation true
 */
export async function validate_accept_membership_invitation(
  config: {
  tenant?: string;
  identity: UUID | string;
  input: AcceptMembershipInvitationInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "accept_membership_invitation",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Update an existing OriginEntityMembership
 *
 * @ashActionType :update
 * @validation true
 */
export async function validate_accept_membership_invitation_channel(config: {
  channel: Channel;
  tenant?: string;
  identity: UUID | string;
  input: AcceptMembershipInvitationInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "accept_membership_invitation",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Update an existing OriginEntityMembership
 *
 * @ashActionType :update
 */
export async function accept_membership_invitation_channel<Fields extends AcceptMembershipInvitationFields | undefined = undefined>(config: {
  channel: Channel;
  tenant?: string;
  identity: UUID;
  input: AcceptMembershipInvitationInput;
  fields?: Fields;
  result_handler: (result: AcceptMembershipInvitationResult<Fields>) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<AcceptMembershipInvitationResult<Fields>>(
    config.channel,
    {
    action: "accept_membership_invitation",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  },
    config.timeout,
    config
  );
}


export type ActivateMembershipFields = UnifiedFieldSelection<OriginEntityMembershipResourceSchema>[];

export type InferActivateMembershipResult<
  Fields extends ActivateMembershipFields | undefined,
> = InferResult<OriginEntityMembershipResourceSchema, Fields>;

export type ActivateMembershipResult<Fields extends ActivateMembershipFields | undefined = undefined> = | { success: true; data: InferActivateMembershipResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing OriginEntityMembership
 *
 * @ashActionType :update
 */
export async function activate_membership<Fields extends ActivateMembershipFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUID;
  fields?: Fields;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ActivateMembershipResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "activate_membership",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<ActivateMembershipResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


/**
 * Validate: Update an existing OriginEntityMembership
 *
 * @ashActionType :update
 * @validation true
 */
export async function validate_activate_membership(
  config: {
  tenant?: string;
  identity: UUID | string;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "activate_membership",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Update an existing OriginEntityMembership
 *
 * @ashActionType :update
 * @validation true
 */
export async function validate_activate_membership_channel(config: {
  channel: Channel;
  tenant?: string;
  identity: UUID | string;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "activate_membership",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity
  },
    config.timeout,
    config
  );
}


/**
 * Update an existing OriginEntityMembership
 *
 * @ashActionType :update
 */
export async function activate_membership_channel<Fields extends ActivateMembershipFields | undefined = undefined>(config: {
  channel: Channel;
  tenant?: string;
  identity: UUID;
  fields?: Fields;
  result_handler: (result: ActivateMembershipResult<Fields>) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<ActivateMembershipResult<Fields>>(
    config.channel,
    {
    action: "activate_membership",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    ...(config.fields !== undefined && { fields: config.fields })
  },
    config.timeout,
    config
  );
}


export type CreateOriginEntityMembershipInput = {
  user_id?: UUID | null;
  role?: "admin" | "member" | "owner" | "viewer";
  status?: "active" | "pending" | "suspended";
  invitation_token?: UUID | null;
  invited_email?: string | null;
  expires_at?: UtcDateTimeUsec | null;
  invited_by?: UUID | null;
  accepted_at?: UtcDateTimeUsec | null;
  origin_entity_id: UUID;
};

export type CreateOriginEntityMembershipFields = UnifiedFieldSelection<OriginEntityMembershipResourceSchema>[];

export type InferCreateOriginEntityMembershipResult<
  Fields extends CreateOriginEntityMembershipFields | undefined,
> = InferResult<OriginEntityMembershipResourceSchema, Fields>;

export type CreateOriginEntityMembershipResult<Fields extends CreateOriginEntityMembershipFields | undefined = undefined> = | { success: true; data: InferCreateOriginEntityMembershipResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Create a new OriginEntityMembership
 *
 * @ashActionType :create
 */
export async function create_origin_entity_membership<Fields extends CreateOriginEntityMembershipFields | undefined = undefined>(
  config: {
  tenant?: string;
  input: CreateOriginEntityMembershipInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<CreateOriginEntityMembershipResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "create_origin_entity_membership",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<CreateOriginEntityMembershipResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


/**
 * Validate: Create a new OriginEntityMembership
 *
 * @ashActionType :create
 * @validation true
 */
export async function validate_create_origin_entity_membership(
  config: {
  tenant?: string;
  input: CreateOriginEntityMembershipInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "create_origin_entity_membership",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Create a new OriginEntityMembership
 *
 * @ashActionType :create
 * @validation true
 */
export async function validate_create_origin_entity_membership_channel(config: {
  channel: Channel;
  tenant?: string;
  input: CreateOriginEntityMembershipInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "create_origin_entity_membership",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Create a new OriginEntityMembership
 *
 * @ashActionType :create
 */
export async function create_origin_entity_membership_channel<Fields extends CreateOriginEntityMembershipFields | undefined = undefined>(config: {
  channel: Channel;
  tenant?: string;
  input: CreateOriginEntityMembershipInput;
  fields?: Fields;
  result_handler: (result: CreateOriginEntityMembershipResult<Fields>) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<CreateOriginEntityMembershipResult<Fields>>(
    config.channel,
    {
    action: "create_origin_entity_membership",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  },
    config.timeout,
    config
  );
}


export type DeleteOriginEntityMembershipResult = | { success: true; data: {}; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Delete a OriginEntityMembership
 *
 * @ashActionType :destroy
 */
export async function delete_origin_entity_membership(
  config: {
  tenant?: string;
  identity: UUID;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<DeleteOriginEntityMembershipResult> {
  const payload = {
    action: "delete_origin_entity_membership",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity
  };

  return executeActionRpcRequest<DeleteOriginEntityMembershipResult>(
    payload,
    config
  );
}


/**
 * Validate: Delete a OriginEntityMembership
 *
 * @ashActionType :destroy
 * @validation true
 */
export async function validate_delete_origin_entity_membership(
  config: {
  tenant?: string;
  identity: UUID | string;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "delete_origin_entity_membership",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Delete a OriginEntityMembership
 *
 * @ashActionType :destroy
 * @validation true
 */
export async function validate_delete_origin_entity_membership_channel(config: {
  channel: Channel;
  tenant?: string;
  identity: UUID | string;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "delete_origin_entity_membership",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity
  },
    config.timeout,
    config
  );
}


/**
 * Delete a OriginEntityMembership
 *
 * @ashActionType :destroy
 */
export async function delete_origin_entity_membership_channel(config: {
  channel: Channel;
  tenant?: string;
  identity: UUID;
  result_handler: (result: DeleteOriginEntityMembershipResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<DeleteOriginEntityMembershipResult>(
    config.channel,
    {
    action: "delete_origin_entity_membership",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity
  },
    config.timeout,
    config
  );
}


export type GetInheritedMembershipsInput = {
  origin_entity_id: UUID;
};

export type GetInheritedMembershipsFields = UnifiedFieldSelection<OriginEntityMembershipResourceSchema>[];
export type InferGetInheritedMembershipsResult<
  Fields extends GetInheritedMembershipsFields,
> = Array<InferResult<OriginEntityMembershipResourceSchema, Fields>>;

export type GetInheritedMembershipsResult<Fields extends GetInheritedMembershipsFields> = | { success: true; data: InferGetInheritedMembershipsResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read OriginEntityMembership records
 *
 * @ashActionType :read
 */
export async function get_inherited_memberships<Fields extends GetInheritedMembershipsFields>(
  config: {
  tenant?: string;
  input: GetInheritedMembershipsInput;
  fields: Fields;
  filter?: OriginEntityMembershipFilterInput;
  sort?: SortString<OriginEntityMembershipSortField> | SortString<OriginEntityMembershipSortField>[];
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<GetInheritedMembershipsResult<Fields>> {
  const payload = {
    action: "get_inherited_memberships",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort })
  };

  return executeActionRpcRequest<GetInheritedMembershipsResult<Fields>>(
    payload,
    config
  );
}


/**
 * Validate: Read OriginEntityMembership records
 *
 * @ashActionType :read
 * @validation true
 */
export async function validate_get_inherited_memberships(
  config: {
  tenant?: string;
  input: GetInheritedMembershipsInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "get_inherited_memberships",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Read OriginEntityMembership records
 *
 * @ashActionType :read
 * @validation true
 */
export async function validate_get_inherited_memberships_channel(config: {
  channel: Channel;
  tenant?: string;
  input: GetInheritedMembershipsInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "get_inherited_memberships",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Read OriginEntityMembership records
 *
 * @ashActionType :read
 */
export async function get_inherited_memberships_channel<Fields extends GetInheritedMembershipsFields>(config: {
  channel: Channel;
  tenant?: string;
  input: GetInheritedMembershipsInput;
  fields: Fields;
  filter?: OriginEntityMembershipFilterInput;
  sort?: SortString<OriginEntityMembershipSortField> | SortString<OriginEntityMembershipSortField>[];
  result_handler: (result: GetInheritedMembershipsResult<Fields>) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<GetInheritedMembershipsResult<Fields>>(
    config.channel,
    {
    action: "get_inherited_memberships",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort })
  },
    config.timeout,
    config
  );
}


export type GetMembershipByTokenInput = {
  invitation_token: UUID;
};

export type GetMembershipByTokenFields = UnifiedFieldSelection<OriginEntityMembershipResourceSchema>[];
export type InferGetMembershipByTokenResult<
  Fields extends GetMembershipByTokenFields,
> = InferResult<OriginEntityMembershipResourceSchema, Fields>;

export type GetMembershipByTokenResult<Fields extends GetMembershipByTokenFields> = | { success: true; data: InferGetMembershipByTokenResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read OriginEntityMembership records
 *
 * @ashActionType :read
 */
export async function get_membership_by_token<Fields extends GetMembershipByTokenFields>(
  config: {
  tenant?: string;
  input: GetMembershipByTokenInput;
  fields: Fields;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<GetMembershipByTokenResult<Fields>> {
  const payload = {
    action: "get_membership_by_token",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<GetMembershipByTokenResult<Fields>>(
    payload,
    config
  );
}


/**
 * Validate: Read OriginEntityMembership records
 *
 * @ashActionType :read
 * @validation true
 */
export async function validate_get_membership_by_token(
  config: {
  tenant?: string;
  input: GetMembershipByTokenInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "get_membership_by_token",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Read OriginEntityMembership records
 *
 * @ashActionType :read
 * @validation true
 */
export async function validate_get_membership_by_token_channel(config: {
  channel: Channel;
  tenant?: string;
  input: GetMembershipByTokenInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "get_membership_by_token",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Read OriginEntityMembership records
 *
 * @ashActionType :read
 */
export async function get_membership_by_token_channel<Fields extends GetMembershipByTokenFields>(config: {
  channel: Channel;
  tenant?: string;
  input: GetMembershipByTokenInput;
  fields: Fields;
  result_handler: (result: GetMembershipByTokenResult<Fields>) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<GetMembershipByTokenResult<Fields>>(
    config.channel,
    {
    action: "get_membership_by_token",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  },
    config.timeout,
    config
  );
}


export type GetMembershipsForOriginInput = {
  origin_entity_id: UUID;
};

export type GetMembershipsForOriginFields = UnifiedFieldSelection<OriginEntityMembershipResourceSchema>[];
export type InferGetMembershipsForOriginResult<
  Fields extends GetMembershipsForOriginFields,
> = Array<InferResult<OriginEntityMembershipResourceSchema, Fields>>;

export type GetMembershipsForOriginResult<Fields extends GetMembershipsForOriginFields> = | { success: true; data: InferGetMembershipsForOriginResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read OriginEntityMembership records
 *
 * @ashActionType :read
 */
export async function get_memberships_for_origin<Fields extends GetMembershipsForOriginFields>(
  config: {
  tenant?: string;
  input: GetMembershipsForOriginInput;
  fields: Fields;
  filter?: OriginEntityMembershipFilterInput;
  sort?: SortString<OriginEntityMembershipSortField> | SortString<OriginEntityMembershipSortField>[];
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<GetMembershipsForOriginResult<Fields>> {
  const payload = {
    action: "get_memberships_for_origin",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort })
  };

  return executeActionRpcRequest<GetMembershipsForOriginResult<Fields>>(
    payload,
    config
  );
}


/**
 * Validate: Read OriginEntityMembership records
 *
 * @ashActionType :read
 * @validation true
 */
export async function validate_get_memberships_for_origin(
  config: {
  tenant?: string;
  input: GetMembershipsForOriginInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "get_memberships_for_origin",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Read OriginEntityMembership records
 *
 * @ashActionType :read
 * @validation true
 */
export async function validate_get_memberships_for_origin_channel(config: {
  channel: Channel;
  tenant?: string;
  input: GetMembershipsForOriginInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "get_memberships_for_origin",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Read OriginEntityMembership records
 *
 * @ashActionType :read
 */
export async function get_memberships_for_origin_channel<Fields extends GetMembershipsForOriginFields>(config: {
  channel: Channel;
  tenant?: string;
  input: GetMembershipsForOriginInput;
  fields: Fields;
  filter?: OriginEntityMembershipFilterInput;
  sort?: SortString<OriginEntityMembershipSortField> | SortString<OriginEntityMembershipSortField>[];
  result_handler: (result: GetMembershipsForOriginResult<Fields>) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<GetMembershipsForOriginResult<Fields>>(
    config.channel,
    {
    action: "get_memberships_for_origin",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort })
  },
    config.timeout,
    config
  );
}


export type GetMembershipsForUserInput = {
  user_id: UUID;
};

export type GetMembershipsForUserFields = UnifiedFieldSelection<OriginEntityMembershipResourceSchema>[];
export type InferGetMembershipsForUserResult<
  Fields extends GetMembershipsForUserFields,
> = Array<InferResult<OriginEntityMembershipResourceSchema, Fields>>;

export type GetMembershipsForUserResult<Fields extends GetMembershipsForUserFields> = | { success: true; data: InferGetMembershipsForUserResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read OriginEntityMembership records
 *
 * @ashActionType :read
 */
export async function get_memberships_for_user<Fields extends GetMembershipsForUserFields>(
  config: {
  tenant?: string;
  input: GetMembershipsForUserInput;
  fields: Fields;
  filter?: OriginEntityMembershipFilterInput;
  sort?: SortString<OriginEntityMembershipSortField> | SortString<OriginEntityMembershipSortField>[];
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<GetMembershipsForUserResult<Fields>> {
  const payload = {
    action: "get_memberships_for_user",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort })
  };

  return executeActionRpcRequest<GetMembershipsForUserResult<Fields>>(
    payload,
    config
  );
}


/**
 * Validate: Read OriginEntityMembership records
 *
 * @ashActionType :read
 * @validation true
 */
export async function validate_get_memberships_for_user(
  config: {
  tenant?: string;
  input: GetMembershipsForUserInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "get_memberships_for_user",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Read OriginEntityMembership records
 *
 * @ashActionType :read
 * @validation true
 */
export async function validate_get_memberships_for_user_channel(config: {
  channel: Channel;
  tenant?: string;
  input: GetMembershipsForUserInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "get_memberships_for_user",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Read OriginEntityMembership records
 *
 * @ashActionType :read
 */
export async function get_memberships_for_user_channel<Fields extends GetMembershipsForUserFields>(config: {
  channel: Channel;
  tenant?: string;
  input: GetMembershipsForUserInput;
  fields: Fields;
  filter?: OriginEntityMembershipFilterInput;
  sort?: SortString<OriginEntityMembershipSortField> | SortString<OriginEntityMembershipSortField>[];
  result_handler: (result: GetMembershipsForUserResult<Fields>) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<GetMembershipsForUserResult<Fields>>(
    config.channel,
    {
    action: "get_memberships_for_user",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort })
  },
    config.timeout,
    config
  );
}


export type GetOriginEntityMembershipInput = {
  id: UUID;
};

export type GetOriginEntityMembershipFields = UnifiedFieldSelection<OriginEntityMembershipResourceSchema>[];
export type InferGetOriginEntityMembershipResult<
  Fields extends GetOriginEntityMembershipFields,
> = InferResult<OriginEntityMembershipResourceSchema, Fields>;

export type GetOriginEntityMembershipResult<Fields extends GetOriginEntityMembershipFields> = | { success: true; data: InferGetOriginEntityMembershipResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read OriginEntityMembership records
 *
 * @ashActionType :read
 */
export async function get_origin_entity_membership<Fields extends GetOriginEntityMembershipFields>(
  config: {
  tenant?: string;
  input: GetOriginEntityMembershipInput;
  fields: Fields;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<GetOriginEntityMembershipResult<Fields>> {
  const payload = {
    action: "get_origin_entity_membership",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<GetOriginEntityMembershipResult<Fields>>(
    payload,
    config
  );
}


/**
 * Validate: Read OriginEntityMembership records
 *
 * @ashActionType :read
 * @validation true
 */
export async function validate_get_origin_entity_membership(
  config: {
  tenant?: string;
  input: GetOriginEntityMembershipInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "get_origin_entity_membership",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Read OriginEntityMembership records
 *
 * @ashActionType :read
 * @validation true
 */
export async function validate_get_origin_entity_membership_channel(config: {
  channel: Channel;
  tenant?: string;
  input: GetOriginEntityMembershipInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "get_origin_entity_membership",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Read OriginEntityMembership records
 *
 * @ashActionType :read
 */
export async function get_origin_entity_membership_channel<Fields extends GetOriginEntityMembershipFields>(config: {
  channel: Channel;
  tenant?: string;
  input: GetOriginEntityMembershipInput;
  fields: Fields;
  result_handler: (result: GetOriginEntityMembershipResult<Fields>) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<GetOriginEntityMembershipResult<Fields>>(
    config.channel,
    {
    action: "get_origin_entity_membership",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  },
    config.timeout,
    config
  );
}


export type GetPendingMembershipsInput = {
  origin_entity_id: UUID;
};

export type GetPendingMembershipsFields = UnifiedFieldSelection<OriginEntityMembershipResourceSchema>[];
export type InferGetPendingMembershipsResult<
  Fields extends GetPendingMembershipsFields,
> = Array<InferResult<OriginEntityMembershipResourceSchema, Fields>>;

export type GetPendingMembershipsResult<Fields extends GetPendingMembershipsFields> = | { success: true; data: InferGetPendingMembershipsResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read OriginEntityMembership records
 *
 * @ashActionType :read
 */
export async function get_pending_memberships<Fields extends GetPendingMembershipsFields>(
  config: {
  tenant?: string;
  input: GetPendingMembershipsInput;
  fields: Fields;
  filter?: OriginEntityMembershipFilterInput;
  sort?: SortString<OriginEntityMembershipSortField> | SortString<OriginEntityMembershipSortField>[];
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<GetPendingMembershipsResult<Fields>> {
  const payload = {
    action: "get_pending_memberships",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort })
  };

  return executeActionRpcRequest<GetPendingMembershipsResult<Fields>>(
    payload,
    config
  );
}


/**
 * Validate: Read OriginEntityMembership records
 *
 * @ashActionType :read
 * @validation true
 */
export async function validate_get_pending_memberships(
  config: {
  tenant?: string;
  input: GetPendingMembershipsInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "get_pending_memberships",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Read OriginEntityMembership records
 *
 * @ashActionType :read
 * @validation true
 */
export async function validate_get_pending_memberships_channel(config: {
  channel: Channel;
  tenant?: string;
  input: GetPendingMembershipsInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "get_pending_memberships",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Read OriginEntityMembership records
 *
 * @ashActionType :read
 */
export async function get_pending_memberships_channel<Fields extends GetPendingMembershipsFields>(config: {
  channel: Channel;
  tenant?: string;
  input: GetPendingMembershipsInput;
  fields: Fields;
  filter?: OriginEntityMembershipFilterInput;
  sort?: SortString<OriginEntityMembershipSortField> | SortString<OriginEntityMembershipSortField>[];
  result_handler: (result: GetPendingMembershipsResult<Fields>) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<GetPendingMembershipsResult<Fields>>(
    config.channel,
    {
    action: "get_pending_memberships",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort })
  },
    config.timeout,
    config
  );
}


export type InviteToOriginInput = {
  role?: "admin" | "member" | "owner" | "viewer";
  invited_email?: string | null;
  origin_entity_id: UUID;
};

export type InviteToOriginFields = UnifiedFieldSelection<OriginEntityMembershipResourceSchema>[];

export type InferInviteToOriginResult<
  Fields extends InviteToOriginFields | undefined,
> = InferResult<OriginEntityMembershipResourceSchema, Fields>;

export type InviteToOriginResult<Fields extends InviteToOriginFields | undefined = undefined> = | { success: true; data: InferInviteToOriginResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Create a new OriginEntityMembership
 *
 * @ashActionType :create
 */
export async function invite_to_origin<Fields extends InviteToOriginFields | undefined = undefined>(
  config: {
  tenant?: string;
  input: InviteToOriginInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<InviteToOriginResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "invite_to_origin",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<InviteToOriginResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


/**
 * Validate: Create a new OriginEntityMembership
 *
 * @ashActionType :create
 * @validation true
 */
export async function validate_invite_to_origin(
  config: {
  tenant?: string;
  input: InviteToOriginInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "invite_to_origin",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Create a new OriginEntityMembership
 *
 * @ashActionType :create
 * @validation true
 */
export async function validate_invite_to_origin_channel(config: {
  channel: Channel;
  tenant?: string;
  input: InviteToOriginInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "invite_to_origin",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Create a new OriginEntityMembership
 *
 * @ashActionType :create
 */
export async function invite_to_origin_channel<Fields extends InviteToOriginFields | undefined = undefined>(config: {
  channel: Channel;
  tenant?: string;
  input: InviteToOriginInput;
  fields?: Fields;
  result_handler: (result: InviteToOriginResult<Fields>) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<InviteToOriginResult<Fields>>(
    config.channel,
    {
    action: "invite_to_origin",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  },
    config.timeout,
    config
  );
}


export type ListOriginEntityMembershipsFields = UnifiedFieldSelection<OriginEntityMembershipResourceSchema>[];


export type InferListOriginEntityMembershipsResult<
  Fields extends ListOriginEntityMembershipsFields | undefined,
  Page extends ListOriginEntityMembershipsConfig["page"] = undefined
> = ConditionalPaginatedResultMixed<Page, Array<InferResult<OriginEntityMembershipResourceSchema, Fields>>, {
  results: Array<InferResult<OriginEntityMembershipResourceSchema, Fields>>;
  has_more: boolean;
  limit: number;
  offset: number;
  count?: number | null;
  type: "offset";
}, {
  results: Array<InferResult<OriginEntityMembershipResourceSchema, Fields>>;
  has_more: boolean;
  limit: number;
  after: string | null;
  before: string | null;
  previous_page: string;
  next_page: string;
  count?: number | null;
  type: "keyset";
}>;

export type ListOriginEntityMembershipsConfig = {
  tenant?: string;
  fields: ListOriginEntityMembershipsFields;
  filter?: OriginEntityMembershipFilterInput;
  sort?: SortString<OriginEntityMembershipSortField> | SortString<OriginEntityMembershipSortField>[];
  page?: (
    {
      limit?: number;
      offset?: number;
      count?: boolean;
    } | {
      limit?: number;
      after?: string;
      before?: string;
    }
  );
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
};

export type ListOriginEntityMembershipsResult<Fields extends ListOriginEntityMembershipsFields, Page extends ListOriginEntityMembershipsConfig["page"] = undefined> = | { success: true; data: InferListOriginEntityMembershipsResult<Fields, Page>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read OriginEntityMembership records
 *
 * @ashActionType :read
 */
export async function list_origin_entity_memberships<Fields extends ListOriginEntityMembershipsFields, Config extends ListOriginEntityMembershipsConfig = ListOriginEntityMembershipsConfig>(
  config: Config & { fields: Fields }
): Promise<ListOriginEntityMembershipsResult<Fields, Config["page"]>> {
  const payload = {
    action: "list_origin_entity_memberships",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort }),
    ...(config.page && { page: config.page })
  };

  return executeActionRpcRequest<ListOriginEntityMembershipsResult<Fields, Config["page"]>>(
    payload,
    config
  );
}


/**
 * Validate: Read OriginEntityMembership records
 *
 * @ashActionType :read
 * @validation true
 */
export async function validate_list_origin_entity_memberships(
  config: {
  tenant?: string;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "list_origin_entity_memberships",
    ...(config.tenant !== undefined && { tenant: config.tenant })
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Read OriginEntityMembership records
 *
 * @ashActionType :read
 * @validation true
 */
export async function validate_list_origin_entity_memberships_channel(config: {
  channel: Channel;
  tenant?: string;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "list_origin_entity_memberships",
    ...(config.tenant !== undefined && { tenant: config.tenant })
  },
    config.timeout,
    config
  );
}


/**
 * Read OriginEntityMembership records
 *
 * @ashActionType :read
 */
export async function list_origin_entity_memberships_channel<Fields extends ListOriginEntityMembershipsFields>(config: {
  channel: Channel;
  tenant?: string;
  fields: Fields;
  filter?: OriginEntityMembershipFilterInput;
  sort?: SortString<OriginEntityMembershipSortField> | SortString<OriginEntityMembershipSortField>[];
  page?: (
    {
      limit?: number;
      offset?: number;
      count?: boolean;
    } | {
      limit?: number;
      after?: string;
      before?: string;
    }
  );
  result_handler: (result: ListOriginEntityMembershipsResult<Fields>) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<ListOriginEntityMembershipsResult<Fields>>(
    config.channel,
    {
    action: "list_origin_entity_memberships",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort }),
    ...(config.page && { page: config.page })
  },
    config.timeout,
    config
  );
}


export type SuspendMembershipFields = UnifiedFieldSelection<OriginEntityMembershipResourceSchema>[];

export type InferSuspendMembershipResult<
  Fields extends SuspendMembershipFields | undefined,
> = InferResult<OriginEntityMembershipResourceSchema, Fields>;

export type SuspendMembershipResult<Fields extends SuspendMembershipFields | undefined = undefined> = | { success: true; data: InferSuspendMembershipResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing OriginEntityMembership
 *
 * @ashActionType :update
 */
export async function suspend_membership<Fields extends SuspendMembershipFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUID;
  fields?: Fields;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<SuspendMembershipResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "suspend_membership",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<SuspendMembershipResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


/**
 * Validate: Update an existing OriginEntityMembership
 *
 * @ashActionType :update
 * @validation true
 */
export async function validate_suspend_membership(
  config: {
  tenant?: string;
  identity: UUID | string;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "suspend_membership",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Update an existing OriginEntityMembership
 *
 * @ashActionType :update
 * @validation true
 */
export async function validate_suspend_membership_channel(config: {
  channel: Channel;
  tenant?: string;
  identity: UUID | string;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "suspend_membership",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity
  },
    config.timeout,
    config
  );
}


/**
 * Update an existing OriginEntityMembership
 *
 * @ashActionType :update
 */
export async function suspend_membership_channel<Fields extends SuspendMembershipFields | undefined = undefined>(config: {
  channel: Channel;
  tenant?: string;
  identity: UUID;
  fields?: Fields;
  result_handler: (result: SuspendMembershipResult<Fields>) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<SuspendMembershipResult<Fields>>(
    config.channel,
    {
    action: "suspend_membership",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    ...(config.fields !== undefined && { fields: config.fields })
  },
    config.timeout,
    config
  );
}


export type UpdateOriginEntityMembershipInput = {
  user_id?: UUID | null;
  role?: "admin" | "member" | "owner" | "viewer";
  status?: "active" | "pending" | "suspended";
  invitation_token?: UUID | null;
  invited_email?: string | null;
  expires_at?: UtcDateTimeUsec | null;
  invited_by?: UUID | null;
  accepted_at?: UtcDateTimeUsec | null;
  origin_entity_id?: UUID;
};

export type UpdateOriginEntityMembershipFields = UnifiedFieldSelection<OriginEntityMembershipResourceSchema>[];

export type InferUpdateOriginEntityMembershipResult<
  Fields extends UpdateOriginEntityMembershipFields | undefined,
> = InferResult<OriginEntityMembershipResourceSchema, Fields>;

export type UpdateOriginEntityMembershipResult<Fields extends UpdateOriginEntityMembershipFields | undefined = undefined> = | { success: true; data: InferUpdateOriginEntityMembershipResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing OriginEntityMembership
 *
 * @ashActionType :update
 */
export async function update_origin_entity_membership<Fields extends UpdateOriginEntityMembershipFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUID;
  input: UpdateOriginEntityMembershipInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<UpdateOriginEntityMembershipResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "update_origin_entity_membership",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<UpdateOriginEntityMembershipResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


/**
 * Validate: Update an existing OriginEntityMembership
 *
 * @ashActionType :update
 * @validation true
 */
export async function validate_update_origin_entity_membership(
  config: {
  tenant?: string;
  identity: UUID | string;
  input: UpdateOriginEntityMembershipInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "update_origin_entity_membership",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Update an existing OriginEntityMembership
 *
 * @ashActionType :update
 * @validation true
 */
export async function validate_update_origin_entity_membership_channel(config: {
  channel: Channel;
  tenant?: string;
  identity: UUID | string;
  input: UpdateOriginEntityMembershipInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "update_origin_entity_membership",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Update an existing OriginEntityMembership
 *
 * @ashActionType :update
 */
export async function update_origin_entity_membership_channel<Fields extends UpdateOriginEntityMembershipFields | undefined = undefined>(config: {
  channel: Channel;
  tenant?: string;
  identity: UUID;
  input: UpdateOriginEntityMembershipInput;
  fields?: Fields;
  result_handler: (result: UpdateOriginEntityMembershipResult<Fields>) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<UpdateOriginEntityMembershipResult<Fields>>(
    config.channel,
    {
    action: "update_origin_entity_membership",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  },
    config.timeout,
    config
  );
}


export type CreatePromptContextInput = {
  name: string;
  public_title?: string | null;
  public_description?: string | null;
  context_text: string;
  prompt_tool_ids?: Array<UUID> | null;
  is_active?: boolean | null;
  is_default?: boolean | null;
  origin_entity_id: UUID;
  guardrail_config?: OriginsIdentityPromptContextGuardrailConfigInputSchema | null;
};

export type CreatePromptContextFields = UnifiedFieldSelection<PromptContextResourceSchema>[];

export type InferCreatePromptContextResult<
  Fields extends CreatePromptContextFields | undefined,
> = InferResult<PromptContextResourceSchema, Fields>;

export type CreatePromptContextResult<Fields extends CreatePromptContextFields | undefined = undefined> = | { success: true; data: InferCreatePromptContextResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Create a new PromptContext
 *
 * @ashActionType :create
 */
export async function create_prompt_context<Fields extends CreatePromptContextFields | undefined = undefined>(
  config: {
  tenant?: string;
  input: CreatePromptContextInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<CreatePromptContextResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "create_prompt_context",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<CreatePromptContextResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


/**
 * Validate: Create a new PromptContext
 *
 * @ashActionType :create
 * @validation true
 */
export async function validate_create_prompt_context(
  config: {
  tenant?: string;
  input: CreatePromptContextInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "create_prompt_context",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Create a new PromptContext
 *
 * @ashActionType :create
 * @validation true
 */
export async function validate_create_prompt_context_channel(config: {
  channel: Channel;
  tenant?: string;
  input: CreatePromptContextInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "create_prompt_context",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Create a new PromptContext
 *
 * @ashActionType :create
 */
export async function create_prompt_context_channel<Fields extends CreatePromptContextFields | undefined = undefined>(config: {
  channel: Channel;
  tenant?: string;
  input: CreatePromptContextInput;
  fields?: Fields;
  result_handler: (result: CreatePromptContextResult<Fields>) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<CreatePromptContextResult<Fields>>(
    config.channel,
    {
    action: "create_prompt_context",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  },
    config.timeout,
    config
  );
}


export type DeletePromptContextResult = | { success: true; data: {}; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Delete a PromptContext
 *
 * @ashActionType :destroy
 */
export async function delete_prompt_context(
  config: {
  tenant?: string;
  identity: UUID;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<DeletePromptContextResult> {
  const payload = {
    action: "delete_prompt_context",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity
  };

  return executeActionRpcRequest<DeletePromptContextResult>(
    payload,
    config
  );
}


/**
 * Validate: Delete a PromptContext
 *
 * @ashActionType :destroy
 * @validation true
 */
export async function validate_delete_prompt_context(
  config: {
  tenant?: string;
  identity: UUID | string;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "delete_prompt_context",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Delete a PromptContext
 *
 * @ashActionType :destroy
 * @validation true
 */
export async function validate_delete_prompt_context_channel(config: {
  channel: Channel;
  tenant?: string;
  identity: UUID | string;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "delete_prompt_context",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity
  },
    config.timeout,
    config
  );
}


/**
 * Delete a PromptContext
 *
 * @ashActionType :destroy
 */
export async function delete_prompt_context_channel(config: {
  channel: Channel;
  tenant?: string;
  identity: UUID;
  result_handler: (result: DeletePromptContextResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<DeletePromptContextResult>(
    config.channel,
    {
    action: "delete_prompt_context",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity
  },
    config.timeout,
    config
  );
}


export type GetPromptContextPublicTitleInput = {
  context_id: UUID;
  origin_entity_id: UUID;
};

export type InferGetPromptContextPublicTitleResult = Record<string, any>;

export type GetPromptContextPublicTitleResult = | { success: true; data: InferGetPromptContextPublicTitleResult; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Execute generic action on PromptContext
 *
 * @ashActionType :action
 */
export async function get_prompt_context_public_title(
  config: {
  tenant?: string;
  input: GetPromptContextPublicTitleInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<GetPromptContextPublicTitleResult> {
  const payload = {
    action: "get_prompt_context_public_title",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeActionRpcRequest<GetPromptContextPublicTitleResult>(
    payload,
    config
  );
}


/**
 * Validate: Execute generic action on PromptContext
 *
 * @ashActionType :action
 * @validation true
 */
export async function validate_get_prompt_context_public_title(
  config: {
  tenant?: string;
  input: GetPromptContextPublicTitleInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "get_prompt_context_public_title",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Execute generic action on PromptContext
 *
 * @ashActionType :action
 * @validation true
 */
export async function validate_get_prompt_context_public_title_channel(config: {
  channel: Channel;
  tenant?: string;
  input: GetPromptContextPublicTitleInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "get_prompt_context_public_title",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Execute generic action on PromptContext
 *
 * @ashActionType :action
 */
export async function get_prompt_context_public_title_channel(config: {
  channel: Channel;
  tenant?: string;
  input: GetPromptContextPublicTitleInput;
  result_handler: (result: GetPromptContextPublicTitleResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<GetPromptContextPublicTitleResult>(
    config.channel,
    {
    action: "get_prompt_context_public_title",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


export type ListPromptContextsForOriginInput = {
  origin_entity_id: UUID;
};

export type ListPromptContextsForOriginFields = UnifiedFieldSelection<PromptContextResourceSchema>[];
export type InferListPromptContextsForOriginResult<
  Fields extends ListPromptContextsForOriginFields,
> = Array<InferResult<PromptContextResourceSchema, Fields>>;

export type ListPromptContextsForOriginResult<Fields extends ListPromptContextsForOriginFields> = | { success: true; data: InferListPromptContextsForOriginResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read PromptContext records
 *
 * @ashActionType :read
 */
export async function list_prompt_contexts_for_origin<Fields extends ListPromptContextsForOriginFields>(
  config: {
  tenant?: string;
  input: ListPromptContextsForOriginInput;
  fields: Fields;
  filter?: PromptContextFilterInput;
  sort?: SortString<PromptContextSortField> | SortString<PromptContextSortField>[];
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ListPromptContextsForOriginResult<Fields>> {
  const payload = {
    action: "list_prompt_contexts_for_origin",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort })
  };

  return executeActionRpcRequest<ListPromptContextsForOriginResult<Fields>>(
    payload,
    config
  );
}


/**
 * Validate: Read PromptContext records
 *
 * @ashActionType :read
 * @validation true
 */
export async function validate_list_prompt_contexts_for_origin(
  config: {
  tenant?: string;
  input: ListPromptContextsForOriginInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "list_prompt_contexts_for_origin",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Read PromptContext records
 *
 * @ashActionType :read
 * @validation true
 */
export async function validate_list_prompt_contexts_for_origin_channel(config: {
  channel: Channel;
  tenant?: string;
  input: ListPromptContextsForOriginInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "list_prompt_contexts_for_origin",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Read PromptContext records
 *
 * @ashActionType :read
 */
export async function list_prompt_contexts_for_origin_channel<Fields extends ListPromptContextsForOriginFields>(config: {
  channel: Channel;
  tenant?: string;
  input: ListPromptContextsForOriginInput;
  fields: Fields;
  filter?: PromptContextFilterInput;
  sort?: SortString<PromptContextSortField> | SortString<PromptContextSortField>[];
  result_handler: (result: ListPromptContextsForOriginResult<Fields>) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<ListPromptContextsForOriginResult<Fields>>(
    config.channel,
    {
    action: "list_prompt_contexts_for_origin",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort })
  },
    config.timeout,
    config
  );
}


export type UpdatePromptContextInput = {
  name?: string;
  public_title?: string | null;
  public_description?: string | null;
  context_text?: string;
  prompt_tool_ids?: Array<UUID> | null;
  is_active?: boolean | null;
  is_default?: boolean | null;
  guardrail_config?: OriginsIdentityPromptContextGuardrailConfigInputSchema | null;
};

export type UpdatePromptContextFields = UnifiedFieldSelection<PromptContextResourceSchema>[];

export type InferUpdatePromptContextResult<
  Fields extends UpdatePromptContextFields | undefined,
> = InferResult<PromptContextResourceSchema, Fields>;

export type UpdatePromptContextResult<Fields extends UpdatePromptContextFields | undefined = undefined> = | { success: true; data: InferUpdatePromptContextResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing PromptContext
 *
 * @ashActionType :update
 */
export async function update_prompt_context<Fields extends UpdatePromptContextFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUID;
  input: UpdatePromptContextInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<UpdatePromptContextResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "update_prompt_context",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<UpdatePromptContextResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


/**
 * Validate: Update an existing PromptContext
 *
 * @ashActionType :update
 * @validation true
 */
export async function validate_update_prompt_context(
  config: {
  tenant?: string;
  identity: UUID | string;
  input: UpdatePromptContextInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "update_prompt_context",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Update an existing PromptContext
 *
 * @ashActionType :update
 * @validation true
 */
export async function validate_update_prompt_context_channel(config: {
  channel: Channel;
  tenant?: string;
  identity: UUID | string;
  input: UpdatePromptContextInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "update_prompt_context",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Update an existing PromptContext
 *
 * @ashActionType :update
 */
export async function update_prompt_context_channel<Fields extends UpdatePromptContextFields | undefined = undefined>(config: {
  channel: Channel;
  tenant?: string;
  identity: UUID;
  input: UpdatePromptContextInput;
  fields?: Fields;
  result_handler: (result: UpdatePromptContextResult<Fields>) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<UpdatePromptContextResult<Fields>>(
    config.channel,
    {
    action: "update_prompt_context",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  },
    config.timeout,
    config
  );
}


export type CreatePromptToolInput = {
  name: string;
  description: string;
  prompt_template: string;
  model?: string | null;
  temperature?: number | null;
  max_tokens?: number | null;
  is_active?: boolean | null;
  auto_approve?: boolean | null;
  letta_tools?: Array<string> | null;
  origin_entity_id: UUID;
  source_origin_entity_id?: UUID | null;
};

export type CreatePromptToolFields = UnifiedFieldSelection<PromptToolResourceSchema>[];

export type InferCreatePromptToolResult<
  Fields extends CreatePromptToolFields | undefined,
> = InferResult<PromptToolResourceSchema, Fields>;

export type CreatePromptToolResult<Fields extends CreatePromptToolFields | undefined = undefined> = | { success: true; data: InferCreatePromptToolResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Create a new PromptTool
 *
 * @ashActionType :create
 */
export async function create_prompt_tool<Fields extends CreatePromptToolFields | undefined = undefined>(
  config: {
  tenant?: string;
  input: CreatePromptToolInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<CreatePromptToolResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "create_prompt_tool",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<CreatePromptToolResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


/**
 * Validate: Create a new PromptTool
 *
 * @ashActionType :create
 * @validation true
 */
export async function validate_create_prompt_tool(
  config: {
  tenant?: string;
  input: CreatePromptToolInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "create_prompt_tool",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Create a new PromptTool
 *
 * @ashActionType :create
 * @validation true
 */
export async function validate_create_prompt_tool_channel(config: {
  channel: Channel;
  tenant?: string;
  input: CreatePromptToolInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "create_prompt_tool",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Create a new PromptTool
 *
 * @ashActionType :create
 */
export async function create_prompt_tool_channel<Fields extends CreatePromptToolFields | undefined = undefined>(config: {
  channel: Channel;
  tenant?: string;
  input: CreatePromptToolInput;
  fields?: Fields;
  result_handler: (result: CreatePromptToolResult<Fields>) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<CreatePromptToolResult<Fields>>(
    config.channel,
    {
    action: "create_prompt_tool",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  },
    config.timeout,
    config
  );
}


export type DeletePromptToolResult = | { success: true; data: {}; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Delete a PromptTool
 *
 * @ashActionType :destroy
 */
export async function delete_prompt_tool(
  config: {
  tenant?: string;
  identity: UUID;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<DeletePromptToolResult> {
  const payload = {
    action: "delete_prompt_tool",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity
  };

  return executeActionRpcRequest<DeletePromptToolResult>(
    payload,
    config
  );
}


/**
 * Validate: Delete a PromptTool
 *
 * @ashActionType :destroy
 * @validation true
 */
export async function validate_delete_prompt_tool(
  config: {
  tenant?: string;
  identity: UUID | string;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "delete_prompt_tool",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Delete a PromptTool
 *
 * @ashActionType :destroy
 * @validation true
 */
export async function validate_delete_prompt_tool_channel(config: {
  channel: Channel;
  tenant?: string;
  identity: UUID | string;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "delete_prompt_tool",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity
  },
    config.timeout,
    config
  );
}


/**
 * Delete a PromptTool
 *
 * @ashActionType :destroy
 */
export async function delete_prompt_tool_channel(config: {
  channel: Channel;
  tenant?: string;
  identity: UUID;
  result_handler: (result: DeletePromptToolResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<DeletePromptToolResult>(
    config.channel,
    {
    action: "delete_prompt_tool",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity
  },
    config.timeout,
    config
  );
}


export type ListPromptToolsForOriginInput = {
  origin_entity_id: UUID;
};

export type ListPromptToolsForOriginFields = UnifiedFieldSelection<PromptToolResourceSchema>[];
export type InferListPromptToolsForOriginResult<
  Fields extends ListPromptToolsForOriginFields,
> = Array<InferResult<PromptToolResourceSchema, Fields>>;

export type ListPromptToolsForOriginResult<Fields extends ListPromptToolsForOriginFields> = | { success: true; data: InferListPromptToolsForOriginResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read PromptTool records
 *
 * @ashActionType :read
 */
export async function list_prompt_tools_for_origin<Fields extends ListPromptToolsForOriginFields>(
  config: {
  tenant?: string;
  input: ListPromptToolsForOriginInput;
  fields: Fields;
  filter?: PromptToolFilterInput;
  sort?: SortString<PromptToolSortField> | SortString<PromptToolSortField>[];
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ListPromptToolsForOriginResult<Fields>> {
  const payload = {
    action: "list_prompt_tools_for_origin",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort })
  };

  return executeActionRpcRequest<ListPromptToolsForOriginResult<Fields>>(
    payload,
    config
  );
}


/**
 * Validate: Read PromptTool records
 *
 * @ashActionType :read
 * @validation true
 */
export async function validate_list_prompt_tools_for_origin(
  config: {
  tenant?: string;
  input: ListPromptToolsForOriginInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "list_prompt_tools_for_origin",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Read PromptTool records
 *
 * @ashActionType :read
 * @validation true
 */
export async function validate_list_prompt_tools_for_origin_channel(config: {
  channel: Channel;
  tenant?: string;
  input: ListPromptToolsForOriginInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "list_prompt_tools_for_origin",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Read PromptTool records
 *
 * @ashActionType :read
 */
export async function list_prompt_tools_for_origin_channel<Fields extends ListPromptToolsForOriginFields>(config: {
  channel: Channel;
  tenant?: string;
  input: ListPromptToolsForOriginInput;
  fields: Fields;
  filter?: PromptToolFilterInput;
  sort?: SortString<PromptToolSortField> | SortString<PromptToolSortField>[];
  result_handler: (result: ListPromptToolsForOriginResult<Fields>) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<ListPromptToolsForOriginResult<Fields>>(
    config.channel,
    {
    action: "list_prompt_tools_for_origin",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort })
  },
    config.timeout,
    config
  );
}


export type UpdatePromptToolInput = {
  name?: string;
  description?: string;
  prompt_template?: string;
  model?: string | null;
  temperature?: number | null;
  max_tokens?: number | null;
  is_active?: boolean | null;
  auto_approve?: boolean | null;
  letta_tools?: Array<string> | null;
  source_origin_entity_id?: UUID | null;
};

export type UpdatePromptToolFields = UnifiedFieldSelection<PromptToolResourceSchema>[];

export type InferUpdatePromptToolResult<
  Fields extends UpdatePromptToolFields | undefined,
> = InferResult<PromptToolResourceSchema, Fields>;

export type UpdatePromptToolResult<Fields extends UpdatePromptToolFields | undefined = undefined> = | { success: true; data: InferUpdatePromptToolResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing PromptTool
 *
 * @ashActionType :update
 */
export async function update_prompt_tool<Fields extends UpdatePromptToolFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUID;
  input: UpdatePromptToolInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<UpdatePromptToolResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "update_prompt_tool",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<UpdatePromptToolResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


/**
 * Validate: Update an existing PromptTool
 *
 * @ashActionType :update
 * @validation true
 */
export async function validate_update_prompt_tool(
  config: {
  tenant?: string;
  identity: UUID | string;
  input: UpdatePromptToolInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "update_prompt_tool",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Update an existing PromptTool
 *
 * @ashActionType :update
 * @validation true
 */
export async function validate_update_prompt_tool_channel(config: {
  channel: Channel;
  tenant?: string;
  identity: UUID | string;
  input: UpdatePromptToolInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "update_prompt_tool",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Update an existing PromptTool
 *
 * @ashActionType :update
 */
export async function update_prompt_tool_channel<Fields extends UpdatePromptToolFields | undefined = undefined>(config: {
  channel: Channel;
  tenant?: string;
  identity: UUID;
  input: UpdatePromptToolInput;
  fields?: Fields;
  result_handler: (result: UpdatePromptToolResult<Fields>) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<UpdatePromptToolResult<Fields>>(
    config.channel,
    {
    action: "update_prompt_tool",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  },
    config.timeout,
    config
  );
}


export type CreateSetupProgressInput = {
  entity_name: string;
  completed_steps?: Array<string> | null;
  social_media_links?: Record<string, any> | null;
  ai_suggested_links?: Record<string, any> | null;
  api_extracted_data?: Record<string, any> | null;
  metadata?: Record<string, any> | null;
  is_completed?: boolean | null;
  current_step?: number | null;
  onboarding_mode?: string | null;
  origin_entity_id?: UUID | null;
};

export type CreateSetupProgressFields = UnifiedFieldSelection<SetupProgressResourceSchema>[];

export type InferCreateSetupProgressResult<
  Fields extends CreateSetupProgressFields | undefined,
> = InferResult<SetupProgressResourceSchema, Fields>;

export type CreateSetupProgressResult<Fields extends CreateSetupProgressFields | undefined = undefined> = | { success: true; data: InferCreateSetupProgressResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Create a new SetupProgress
 *
 * @ashActionType :create
 */
export async function create_setup_progress<Fields extends CreateSetupProgressFields | undefined = undefined>(
  config: {
  tenant?: string;
  input: CreateSetupProgressInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<CreateSetupProgressResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "create_setup_progress",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<CreateSetupProgressResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


/**
 * Validate: Create a new SetupProgress
 *
 * @ashActionType :create
 * @validation true
 */
export async function validate_create_setup_progress(
  config: {
  tenant?: string;
  input: CreateSetupProgressInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "create_setup_progress",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Create a new SetupProgress
 *
 * @ashActionType :create
 * @validation true
 */
export async function validate_create_setup_progress_channel(config: {
  channel: Channel;
  tenant?: string;
  input: CreateSetupProgressInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "create_setup_progress",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Create a new SetupProgress
 *
 * @ashActionType :create
 */
export async function create_setup_progress_channel<Fields extends CreateSetupProgressFields | undefined = undefined>(config: {
  channel: Channel;
  tenant?: string;
  input: CreateSetupProgressInput;
  fields?: Fields;
  result_handler: (result: CreateSetupProgressResult<Fields>) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<CreateSetupProgressResult<Fields>>(
    config.channel,
    {
    action: "create_setup_progress",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  },
    config.timeout,
    config
  );
}


export type ListSetupProgressFields = UnifiedFieldSelection<SetupProgressResourceSchema>[];


export type InferListSetupProgressResult<
  Fields extends ListSetupProgressFields | undefined,
  Page extends ListSetupProgressConfig["page"] = undefined
> = ConditionalPaginatedResultMixed<Page, Array<InferResult<SetupProgressResourceSchema, Fields>>, {
  results: Array<InferResult<SetupProgressResourceSchema, Fields>>;
  has_more: boolean;
  limit: number;
  offset: number;
  count?: number | null;
  type: "offset";
}, {
  results: Array<InferResult<SetupProgressResourceSchema, Fields>>;
  has_more: boolean;
  limit: number;
  after: string | null;
  before: string | null;
  previous_page: string;
  next_page: string;
  count?: number | null;
  type: "keyset";
}>;

export type ListSetupProgressConfig = {
  tenant?: string;
  fields: ListSetupProgressFields;
  filter?: SetupProgressFilterInput;
  sort?: SortString<SetupProgressSortField> | SortString<SetupProgressSortField>[];
  page?: (
    {
      limit?: number;
      offset?: number;
      count?: boolean;
    } | {
      limit?: number;
      after?: string;
      before?: string;
    }
  );
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
};

export type ListSetupProgressResult<Fields extends ListSetupProgressFields, Page extends ListSetupProgressConfig["page"] = undefined> = | { success: true; data: InferListSetupProgressResult<Fields, Page>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read SetupProgress records
 *
 * @ashActionType :read
 */
export async function list_setup_progress<Fields extends ListSetupProgressFields, Config extends ListSetupProgressConfig = ListSetupProgressConfig>(
  config: Config & { fields: Fields }
): Promise<ListSetupProgressResult<Fields, Config["page"]>> {
  const payload = {
    action: "list_setup_progress",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort }),
    ...(config.page && { page: config.page })
  };

  return executeActionRpcRequest<ListSetupProgressResult<Fields, Config["page"]>>(
    payload,
    config
  );
}


/**
 * Validate: Read SetupProgress records
 *
 * @ashActionType :read
 * @validation true
 */
export async function validate_list_setup_progress(
  config: {
  tenant?: string;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "list_setup_progress",
    ...(config.tenant !== undefined && { tenant: config.tenant })
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Read SetupProgress records
 *
 * @ashActionType :read
 * @validation true
 */
export async function validate_list_setup_progress_channel(config: {
  channel: Channel;
  tenant?: string;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "list_setup_progress",
    ...(config.tenant !== undefined && { tenant: config.tenant })
  },
    config.timeout,
    config
  );
}


/**
 * Read SetupProgress records
 *
 * @ashActionType :read
 */
export async function list_setup_progress_channel<Fields extends ListSetupProgressFields>(config: {
  channel: Channel;
  tenant?: string;
  fields: Fields;
  filter?: SetupProgressFilterInput;
  sort?: SortString<SetupProgressSortField> | SortString<SetupProgressSortField>[];
  page?: (
    {
      limit?: number;
      offset?: number;
      count?: boolean;
    } | {
      limit?: number;
      after?: string;
      before?: string;
    }
  );
  result_handler: (result: ListSetupProgressResult<Fields>) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<ListSetupProgressResult<Fields>>(
    config.channel,
    {
    action: "list_setup_progress",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort }),
    ...(config.page && { page: config.page })
  },
    config.timeout,
    config
  );
}


export type UpdateSetupProgressInput = {
  entity_name?: string;
  completed_steps?: Array<string> | null;
  social_media_links?: Record<string, any> | null;
  ai_suggested_links?: Record<string, any> | null;
  api_extracted_data?: Record<string, any> | null;
  metadata?: Record<string, any> | null;
  is_completed?: boolean | null;
  current_step?: number | null;
  onboarding_mode?: string | null;
  origin_entity_id?: UUID | null;
};

export type UpdateSetupProgressFields = UnifiedFieldSelection<SetupProgressResourceSchema>[];

export type InferUpdateSetupProgressResult<
  Fields extends UpdateSetupProgressFields | undefined,
> = InferResult<SetupProgressResourceSchema, Fields>;

export type UpdateSetupProgressResult<Fields extends UpdateSetupProgressFields | undefined = undefined> = | { success: true; data: InferUpdateSetupProgressResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing SetupProgress
 *
 * @ashActionType :update
 */
export async function update_setup_progress<Fields extends UpdateSetupProgressFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUID;
  input: UpdateSetupProgressInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<UpdateSetupProgressResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "update_setup_progress",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<UpdateSetupProgressResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


/**
 * Validate: Update an existing SetupProgress
 *
 * @ashActionType :update
 * @validation true
 */
export async function validate_update_setup_progress(
  config: {
  tenant?: string;
  identity: UUID | string;
  input: UpdateSetupProgressInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "update_setup_progress",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Update an existing SetupProgress
 *
 * @ashActionType :update
 * @validation true
 */
export async function validate_update_setup_progress_channel(config: {
  channel: Channel;
  tenant?: string;
  identity: UUID | string;
  input: UpdateSetupProgressInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "update_setup_progress",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Update an existing SetupProgress
 *
 * @ashActionType :update
 */
export async function update_setup_progress_channel<Fields extends UpdateSetupProgressFields | undefined = undefined>(config: {
  channel: Channel;
  tenant?: string;
  identity: UUID;
  input: UpdateSetupProgressInput;
  fields?: Fields;
  result_handler: (result: UpdateSetupProgressResult<Fields>) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<UpdateSetupProgressResult<Fields>>(
    config.channel,
    {
    action: "update_setup_progress",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  },
    config.timeout,
    config
  );
}


export type CreateSoulConfigInput = {
  personality_traits?: Array<Record<string, any>> | null;
  purpose_statement?: string | null;
  core_instructions?: Array<string> | null;
  conversation_starters?: Array<Record<string, any>> | null;
  speaking_tone?: string | null;
  communication_style?: OriginsIdentitySoulConfigCommunicationStyleInputSchema | null;
  vocabulary_preferences?: Array<string> | null;
  response_format_preferences?: OriginsIdentitySoulConfigResponseFormatPreferencesInputSchema | null;
  experience_settings?: OriginsIdentitySoulConfigExperienceSettingsInputSchema | null;
  guardrails_settings?: Record<string, any> | null;
  response_length?: string | null;
  origin_entity_id?: UUID | null;
};

export type CreateSoulConfigFields = UnifiedFieldSelection<SoulConfigResourceSchema>[];

export type InferCreateSoulConfigResult<
  Fields extends CreateSoulConfigFields | undefined,
> = InferResult<SoulConfigResourceSchema, Fields>;

export type CreateSoulConfigResult<Fields extends CreateSoulConfigFields | undefined = undefined> = | { success: true; data: InferCreateSoulConfigResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Create a new SoulConfig
 *
 * @ashActionType :create
 */
export async function create_soul_config<Fields extends CreateSoulConfigFields | undefined = undefined>(
  config: {
  tenant?: string;
  input?: CreateSoulConfigInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<CreateSoulConfigResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "create_soul_config",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<CreateSoulConfigResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


/**
 * Validate: Create a new SoulConfig
 *
 * @ashActionType :create
 * @validation true
 */
export async function validate_create_soul_config(
  config: {
  tenant?: string;
  input?: CreateSoulConfigInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "create_soul_config",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Create a new SoulConfig
 *
 * @ashActionType :create
 * @validation true
 */
export async function validate_create_soul_config_channel(config: {
  channel: Channel;
  tenant?: string;
  input?: CreateSoulConfigInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "create_soul_config",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Create a new SoulConfig
 *
 * @ashActionType :create
 */
export async function create_soul_config_channel<Fields extends CreateSoulConfigFields | undefined = undefined>(config: {
  channel: Channel;
  tenant?: string;
  input?: CreateSoulConfigInput;
  fields?: Fields;
  result_handler: (result: CreateSoulConfigResult<Fields>) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<CreateSoulConfigResult<Fields>>(
    config.channel,
    {
    action: "create_soul_config",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  },
    config.timeout,
    config
  );
}


export type GenerateConversationStartersInput = {
  transcript?: string | null;
  config?: Record<string, any> | null;
  episode_metadata?: Record<string, any> | null;
  brand_personality?: Record<string, any> | null;
  origin_entity_id?: UUID | null;
};

export type InferGenerateConversationStartersResult = Record<string, any>;

export type GenerateConversationStartersResult = | { success: true; data: InferGenerateConversationStartersResult; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Execute generic action on SoulConfig
 *
 * @ashActionType :action
 */
export async function generate_conversation_starters(
  config: {
  tenant?: string;
  input?: GenerateConversationStartersInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<GenerateConversationStartersResult> {
  const payload = {
    action: "generate_conversation_starters",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeActionRpcRequest<GenerateConversationStartersResult>(
    payload,
    config
  );
}


/**
 * Validate: Execute generic action on SoulConfig
 *
 * @ashActionType :action
 * @validation true
 */
export async function validate_generate_conversation_starters(
  config: {
  tenant?: string;
  input?: GenerateConversationStartersInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "generate_conversation_starters",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Execute generic action on SoulConfig
 *
 * @ashActionType :action
 * @validation true
 */
export async function validate_generate_conversation_starters_channel(config: {
  channel: Channel;
  tenant?: string;
  input?: GenerateConversationStartersInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "generate_conversation_starters",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Execute generic action on SoulConfig
 *
 * @ashActionType :action
 */
export async function generate_conversation_starters_channel(config: {
  channel: Channel;
  tenant?: string;
  input?: GenerateConversationStartersInput;
  result_handler: (result: GenerateConversationStartersResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<GenerateConversationStartersResult>(
    config.channel,
    {
    action: "generate_conversation_starters",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


export type ListSoulConfigsFields = UnifiedFieldSelection<SoulConfigResourceSchema>[];


export type InferListSoulConfigsResult<
  Fields extends ListSoulConfigsFields | undefined,
  Page extends ListSoulConfigsConfig["page"] = undefined
> = ConditionalPaginatedResultMixed<Page, Array<InferResult<SoulConfigResourceSchema, Fields>>, {
  results: Array<InferResult<SoulConfigResourceSchema, Fields>>;
  has_more: boolean;
  limit: number;
  offset: number;
  count?: number | null;
  type: "offset";
}, {
  results: Array<InferResult<SoulConfigResourceSchema, Fields>>;
  has_more: boolean;
  limit: number;
  after: string | null;
  before: string | null;
  previous_page: string;
  next_page: string;
  count?: number | null;
  type: "keyset";
}>;

export type ListSoulConfigsConfig = {
  tenant?: string;
  fields: ListSoulConfigsFields;
  filter?: SoulConfigFilterInput;
  sort?: SortString<SoulConfigSortField> | SortString<SoulConfigSortField>[];
  page?: (
    {
      limit?: number;
      offset?: number;
      count?: boolean;
    } | {
      limit?: number;
      after?: string;
      before?: string;
    }
  );
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
};

export type ListSoulConfigsResult<Fields extends ListSoulConfigsFields, Page extends ListSoulConfigsConfig["page"] = undefined> = | { success: true; data: InferListSoulConfigsResult<Fields, Page>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read SoulConfig records
 *
 * @ashActionType :read
 */
export async function list_soul_configs<Fields extends ListSoulConfigsFields, Config extends ListSoulConfigsConfig = ListSoulConfigsConfig>(
  config: Config & { fields: Fields }
): Promise<ListSoulConfigsResult<Fields, Config["page"]>> {
  const payload = {
    action: "list_soul_configs",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort }),
    ...(config.page && { page: config.page })
  };

  return executeActionRpcRequest<ListSoulConfigsResult<Fields, Config["page"]>>(
    payload,
    config
  );
}


/**
 * Validate: Read SoulConfig records
 *
 * @ashActionType :read
 * @validation true
 */
export async function validate_list_soul_configs(
  config: {
  tenant?: string;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "list_soul_configs",
    ...(config.tenant !== undefined && { tenant: config.tenant })
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Read SoulConfig records
 *
 * @ashActionType :read
 * @validation true
 */
export async function validate_list_soul_configs_channel(config: {
  channel: Channel;
  tenant?: string;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "list_soul_configs",
    ...(config.tenant !== undefined && { tenant: config.tenant })
  },
    config.timeout,
    config
  );
}


/**
 * Read SoulConfig records
 *
 * @ashActionType :read
 */
export async function list_soul_configs_channel<Fields extends ListSoulConfigsFields>(config: {
  channel: Channel;
  tenant?: string;
  fields: Fields;
  filter?: SoulConfigFilterInput;
  sort?: SortString<SoulConfigSortField> | SortString<SoulConfigSortField>[];
  page?: (
    {
      limit?: number;
      offset?: number;
      count?: boolean;
    } | {
      limit?: number;
      after?: string;
      before?: string;
    }
  );
  result_handler: (result: ListSoulConfigsResult<Fields>) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<ListSoulConfigsResult<Fields>>(
    config.channel,
    {
    action: "list_soul_configs",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort }),
    ...(config.page && { page: config.page })
  },
    config.timeout,
    config
  );
}


export type UpdateSoulConfigInput = {
  personality_traits?: Array<Record<string, any>> | null;
  purpose_statement?: string | null;
  core_instructions?: Array<string> | null;
  conversation_starters?: Array<Record<string, any>> | null;
  speaking_tone?: string | null;
  communication_style?: OriginsIdentitySoulConfigCommunicationStyleInputSchema | null;
  vocabulary_preferences?: Array<string> | null;
  response_format_preferences?: OriginsIdentitySoulConfigResponseFormatPreferencesInputSchema | null;
  experience_settings?: OriginsIdentitySoulConfigExperienceSettingsInputSchema | null;
  guardrails_settings?: Record<string, any> | null;
  response_length?: string | null;
  origin_entity_id?: UUID | null;
};

export type UpdateSoulConfigFields = UnifiedFieldSelection<SoulConfigResourceSchema>[];

export type InferUpdateSoulConfigResult<
  Fields extends UpdateSoulConfigFields | undefined,
> = InferResult<SoulConfigResourceSchema, Fields>;

export type UpdateSoulConfigResult<Fields extends UpdateSoulConfigFields | undefined = undefined> = | { success: true; data: InferUpdateSoulConfigResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing SoulConfig
 *
 * @ashActionType :update
 */
export async function update_soul_config<Fields extends UpdateSoulConfigFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUID;
  input?: UpdateSoulConfigInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<UpdateSoulConfigResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "update_soul_config",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<UpdateSoulConfigResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


/**
 * Validate: Update an existing SoulConfig
 *
 * @ashActionType :update
 * @validation true
 */
export async function validate_update_soul_config(
  config: {
  tenant?: string;
  identity: UUID | string;
  input?: UpdateSoulConfigInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "update_soul_config",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Update an existing SoulConfig
 *
 * @ashActionType :update
 * @validation true
 */
export async function validate_update_soul_config_channel(config: {
  channel: Channel;
  tenant?: string;
  identity: UUID | string;
  input?: UpdateSoulConfigInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "update_soul_config",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Update an existing SoulConfig
 *
 * @ashActionType :update
 */
export async function update_soul_config_channel<Fields extends UpdateSoulConfigFields | undefined = undefined>(config: {
  channel: Channel;
  tenant?: string;
  identity: UUID;
  input?: UpdateSoulConfigInput;
  fields?: Fields;
  result_handler: (result: UpdateSoulConfigResult<Fields>) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<UpdateSoulConfigResult<Fields>>(
    config.channel,
    {
    action: "update_soul_config",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  },
    config.timeout,
    config
  );
}


export type ListUserProfilesFields = UnifiedFieldSelection<UserProfileResourceSchema>[];


export type InferListUserProfilesResult<
  Fields extends ListUserProfilesFields | undefined,
  Page extends ListUserProfilesConfig["page"] = undefined
> = ConditionalPaginatedResultMixed<Page, Array<InferResult<UserProfileResourceSchema, Fields>>, {
  results: Array<InferResult<UserProfileResourceSchema, Fields>>;
  has_more: boolean;
  limit: number;
  offset: number;
  count?: number | null;
  type: "offset";
}, {
  results: Array<InferResult<UserProfileResourceSchema, Fields>>;
  has_more: boolean;
  limit: number;
  after: string | null;
  before: string | null;
  previous_page: string;
  next_page: string;
  count?: number | null;
  type: "keyset";
}>;

export type ListUserProfilesConfig = {
  tenant?: string;
  fields: ListUserProfilesFields;
  filter?: UserProfileFilterInput;
  sort?: SortString<UserProfileSortField> | SortString<UserProfileSortField>[];
  page?: (
    {
      limit?: number;
      offset?: number;
      count?: boolean;
    } | {
      limit?: number;
      after?: string;
      before?: string;
    }
  );
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
};

export type ListUserProfilesResult<Fields extends ListUserProfilesFields, Page extends ListUserProfilesConfig["page"] = undefined> = | { success: true; data: InferListUserProfilesResult<Fields, Page>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read UserProfile records
 *
 * @ashActionType :read
 */
export async function list_user_profiles<Fields extends ListUserProfilesFields, Config extends ListUserProfilesConfig = ListUserProfilesConfig>(
  config: Config & { fields: Fields }
): Promise<ListUserProfilesResult<Fields, Config["page"]>> {
  const payload = {
    action: "list_user_profiles",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort }),
    ...(config.page && { page: config.page })
  };

  return executeActionRpcRequest<ListUserProfilesResult<Fields, Config["page"]>>(
    payload,
    config
  );
}


/**
 * Validate: Read UserProfile records
 *
 * @ashActionType :read
 * @validation true
 */
export async function validate_list_user_profiles(
  config: {
  tenant?: string;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "list_user_profiles",
    ...(config.tenant !== undefined && { tenant: config.tenant })
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Read UserProfile records
 *
 * @ashActionType :read
 * @validation true
 */
export async function validate_list_user_profiles_channel(config: {
  channel: Channel;
  tenant?: string;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "list_user_profiles",
    ...(config.tenant !== undefined && { tenant: config.tenant })
  },
    config.timeout,
    config
  );
}


/**
 * Read UserProfile records
 *
 * @ashActionType :read
 */
export async function list_user_profiles_channel<Fields extends ListUserProfilesFields>(config: {
  channel: Channel;
  tenant?: string;
  fields: Fields;
  filter?: UserProfileFilterInput;
  sort?: SortString<UserProfileSortField> | SortString<UserProfileSortField>[];
  page?: (
    {
      limit?: number;
      offset?: number;
      count?: boolean;
    } | {
      limit?: number;
      after?: string;
      before?: string;
    }
  );
  result_handler: (result: ListUserProfilesResult<Fields>) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<ListUserProfilesResult<Fields>>(
    config.channel,
    {
    action: "list_user_profiles",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort }),
    ...(config.page && { page: config.page })
  },
    config.timeout,
    config
  );
}


export type UpdateUserProfileInput = {
  user_id?: UUID;
  display_name?: string | null;
  avatar_url?: string | null;
  bio?: string | null;
  notification_preferences?: Record<string, any> | null;
  privacy_settings?: Record<string, any> | null;
  email?: string | null;
  theme_preference?: string | null;
};

export type UpdateUserProfileFields = UnifiedFieldSelection<UserProfileResourceSchema>[];

export type InferUpdateUserProfileResult<
  Fields extends UpdateUserProfileFields | undefined,
> = InferResult<UserProfileResourceSchema, Fields>;

export type UpdateUserProfileResult<Fields extends UpdateUserProfileFields | undefined = undefined> = | { success: true; data: InferUpdateUserProfileResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing UserProfile
 *
 * @ashActionType :update
 */
export async function update_user_profile<Fields extends UpdateUserProfileFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: { user_id: UUID };
  input: UpdateUserProfileInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<UpdateUserProfileResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "update_user_profile",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<UpdateUserProfileResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


/**
 * Validate: Update an existing UserProfile
 *
 * @ashActionType :update
 * @validation true
 */
export async function validate_update_user_profile(
  config: {
  tenant?: string;
  identity: { user_id: UUID | string };
  input: UpdateUserProfileInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "update_user_profile",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Update an existing UserProfile
 *
 * @ashActionType :update
 * @validation true
 */
export async function validate_update_user_profile_channel(config: {
  channel: Channel;
  tenant?: string;
  identity: { user_id: UUID | string };
  input: UpdateUserProfileInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "update_user_profile",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Update an existing UserProfile
 *
 * @ashActionType :update
 */
export async function update_user_profile_channel<Fields extends UpdateUserProfileFields | undefined = undefined>(config: {
  channel: Channel;
  tenant?: string;
  identity: { user_id: UUID };
  input: UpdateUserProfileInput;
  fields?: Fields;
  result_handler: (result: UpdateUserProfileResult<Fields>) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<UpdateUserProfileResult<Fields>>(
    config.channel,
    {
    action: "update_user_profile",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  },
    config.timeout,
    config
  );
}


export type ListInterviewGeneratedContentsFields = UnifiedFieldSelection<InterviewGeneratedContentResourceSchema>[];


export type InferListInterviewGeneratedContentsResult<
  Fields extends ListInterviewGeneratedContentsFields | undefined,
  Page extends ListInterviewGeneratedContentsConfig["page"] = undefined
> = ConditionalPaginatedResultMixed<Page, Array<InferResult<InterviewGeneratedContentResourceSchema, Fields>>, {
  results: Array<InferResult<InterviewGeneratedContentResourceSchema, Fields>>;
  has_more: boolean;
  limit: number;
  offset: number;
  count?: number | null;
  type: "offset";
}, {
  results: Array<InferResult<InterviewGeneratedContentResourceSchema, Fields>>;
  has_more: boolean;
  limit: number;
  after: string | null;
  before: string | null;
  previous_page: string;
  next_page: string;
  count?: number | null;
  type: "keyset";
}>;

export type ListInterviewGeneratedContentsConfig = {
  tenant?: string;
  fields: ListInterviewGeneratedContentsFields;
  filter?: InterviewGeneratedContentFilterInput;
  sort?: SortString<InterviewGeneratedContentSortField> | SortString<InterviewGeneratedContentSortField>[];
  page?: (
    {
      limit?: number;
      offset?: number;
      count?: boolean;
    } | {
      limit?: number;
      after?: string;
      before?: string;
    }
  );
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
};

export type ListInterviewGeneratedContentsResult<Fields extends ListInterviewGeneratedContentsFields, Page extends ListInterviewGeneratedContentsConfig["page"] = undefined> = | { success: true; data: InferListInterviewGeneratedContentsResult<Fields, Page>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read InterviewGeneratedContent records
 *
 * @ashActionType :read
 */
export async function list_interview_generated_contents<Fields extends ListInterviewGeneratedContentsFields, Config extends ListInterviewGeneratedContentsConfig = ListInterviewGeneratedContentsConfig>(
  config: Config & { fields: Fields }
): Promise<ListInterviewGeneratedContentsResult<Fields, Config["page"]>> {
  const payload = {
    action: "list_interview_generated_contents",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort }),
    ...(config.page && { page: config.page })
  };

  return executeActionRpcRequest<ListInterviewGeneratedContentsResult<Fields, Config["page"]>>(
    payload,
    config
  );
}


/**
 * Validate: Read InterviewGeneratedContent records
 *
 * @ashActionType :read
 * @validation true
 */
export async function validate_list_interview_generated_contents(
  config: {
  tenant?: string;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "list_interview_generated_contents",
    ...(config.tenant !== undefined && { tenant: config.tenant })
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Read InterviewGeneratedContent records
 *
 * @ashActionType :read
 * @validation true
 */
export async function validate_list_interview_generated_contents_channel(config: {
  channel: Channel;
  tenant?: string;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "list_interview_generated_contents",
    ...(config.tenant !== undefined && { tenant: config.tenant })
  },
    config.timeout,
    config
  );
}


/**
 * Read InterviewGeneratedContent records
 *
 * @ashActionType :read
 */
export async function list_interview_generated_contents_channel<Fields extends ListInterviewGeneratedContentsFields>(config: {
  channel: Channel;
  tenant?: string;
  fields: Fields;
  filter?: InterviewGeneratedContentFilterInput;
  sort?: SortString<InterviewGeneratedContentSortField> | SortString<InterviewGeneratedContentSortField>[];
  page?: (
    {
      limit?: number;
      offset?: number;
      count?: boolean;
    } | {
      limit?: number;
      after?: string;
      before?: string;
    }
  );
  result_handler: (result: ListInterviewGeneratedContentsResult<Fields>) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<ListInterviewGeneratedContentsResult<Fields>>(
    config.channel,
    {
    action: "list_interview_generated_contents",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort }),
    ...(config.page && { page: config.page })
  },
    config.timeout,
    config
  );
}


export type UpdateInterviewGeneratedContentInput = {
  content_type?: "article" | "post" | "summary" | "thread" | "tweet";
  content?: string;
  title?: string | null;
  status?: "approved" | "draft" | "published" | "rejected";
  metadata?: Record<string, any> | null;
  interview_session_id?: UUID;
};

export type UpdateInterviewGeneratedContentFields = UnifiedFieldSelection<InterviewGeneratedContentResourceSchema>[];

export type InferUpdateInterviewGeneratedContentResult<
  Fields extends UpdateInterviewGeneratedContentFields | undefined,
> = InferResult<InterviewGeneratedContentResourceSchema, Fields>;

export type UpdateInterviewGeneratedContentResult<Fields extends UpdateInterviewGeneratedContentFields | undefined = undefined> = | { success: true; data: InferUpdateInterviewGeneratedContentResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing InterviewGeneratedContent
 *
 * @ashActionType :update
 */
export async function update_interview_generated_content<Fields extends UpdateInterviewGeneratedContentFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUID;
  input: UpdateInterviewGeneratedContentInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<UpdateInterviewGeneratedContentResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "update_interview_generated_content",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<UpdateInterviewGeneratedContentResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


/**
 * Validate: Update an existing InterviewGeneratedContent
 *
 * @ashActionType :update
 * @validation true
 */
export async function validate_update_interview_generated_content(
  config: {
  tenant?: string;
  identity: UUID | string;
  input: UpdateInterviewGeneratedContentInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "update_interview_generated_content",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Update an existing InterviewGeneratedContent
 *
 * @ashActionType :update
 * @validation true
 */
export async function validate_update_interview_generated_content_channel(config: {
  channel: Channel;
  tenant?: string;
  identity: UUID | string;
  input: UpdateInterviewGeneratedContentInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "update_interview_generated_content",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Update an existing InterviewGeneratedContent
 *
 * @ashActionType :update
 */
export async function update_interview_generated_content_channel<Fields extends UpdateInterviewGeneratedContentFields | undefined = undefined>(config: {
  channel: Channel;
  tenant?: string;
  identity: UUID;
  input: UpdateInterviewGeneratedContentInput;
  fields?: Fields;
  result_handler: (result: UpdateInterviewGeneratedContentResult<Fields>) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<UpdateInterviewGeneratedContentResult<Fields>>(
    config.channel,
    {
    action: "update_interview_generated_content",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  },
    config.timeout,
    config
  );
}


export type PresignRecordingDownloadUrlInput = {
  recording_id: UUID;
};

export type InferPresignRecordingDownloadUrlResult = string;

export type PresignRecordingDownloadUrlResult = | { success: true; data: InferPresignRecordingDownloadUrlResult; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Execute generic action on InterviewRecording
 *
 * @ashActionType :action
 */
export async function presign_recording_download_url(
  config: {
  tenant?: string;
  input: PresignRecordingDownloadUrlInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<PresignRecordingDownloadUrlResult> {
  const payload = {
    action: "presign_recording_download_url",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeActionRpcRequest<PresignRecordingDownloadUrlResult>(
    payload,
    config
  );
}


/**
 * Validate: Execute generic action on InterviewRecording
 *
 * @ashActionType :action
 * @validation true
 */
export async function validate_presign_recording_download_url(
  config: {
  tenant?: string;
  input: PresignRecordingDownloadUrlInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "presign_recording_download_url",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Execute generic action on InterviewRecording
 *
 * @ashActionType :action
 * @validation true
 */
export async function validate_presign_recording_download_url_channel(config: {
  channel: Channel;
  tenant?: string;
  input: PresignRecordingDownloadUrlInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "presign_recording_download_url",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Execute generic action on InterviewRecording
 *
 * @ashActionType :action
 */
export async function presign_recording_download_url_channel(config: {
  channel: Channel;
  tenant?: string;
  input: PresignRecordingDownloadUrlInput;
  result_handler: (result: PresignRecordingDownloadUrlResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<PresignRecordingDownloadUrlResult>(
    config.channel,
    {
    action: "presign_recording_download_url",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


export type CreateInterviewSessionInput = {
  status?: "cancelled" | "completed" | "finalized" | "in_progress" | "pending" | "processing";
  channel?: "video";
  metadata?: Record<string, any> | null;
  session_name?: string | null;
  summary?: string | null;
  key_quotes?: Array<Record<string, any>> | null;
  topics?: Array<string> | null;
  origin_entity_id: UUID;
};

export type CreateInterviewSessionFields = UnifiedFieldSelection<InterviewSessionResourceSchema>[];

export type InferCreateInterviewSessionResult<
  Fields extends CreateInterviewSessionFields | undefined,
> = InferResult<InterviewSessionResourceSchema, Fields>;

export type CreateInterviewSessionResult<Fields extends CreateInterviewSessionFields | undefined = undefined> = | { success: true; data: InferCreateInterviewSessionResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Create a new InterviewSession
 *
 * @ashActionType :create
 */
export async function create_interview_session<Fields extends CreateInterviewSessionFields | undefined = undefined>(
  config: {
  tenant?: string;
  input: CreateInterviewSessionInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<CreateInterviewSessionResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "create_interview_session",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<CreateInterviewSessionResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


/**
 * Validate: Create a new InterviewSession
 *
 * @ashActionType :create
 * @validation true
 */
export async function validate_create_interview_session(
  config: {
  tenant?: string;
  input: CreateInterviewSessionInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "create_interview_session",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Create a new InterviewSession
 *
 * @ashActionType :create
 * @validation true
 */
export async function validate_create_interview_session_channel(config: {
  channel: Channel;
  tenant?: string;
  input: CreateInterviewSessionInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "create_interview_session",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Create a new InterviewSession
 *
 * @ashActionType :create
 */
export async function create_interview_session_channel<Fields extends CreateInterviewSessionFields | undefined = undefined>(config: {
  channel: Channel;
  tenant?: string;
  input: CreateInterviewSessionInput;
  fields?: Fields;
  result_handler: (result: CreateInterviewSessionResult<Fields>) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<CreateInterviewSessionResult<Fields>>(
    config.channel,
    {
    action: "create_interview_session",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  },
    config.timeout,
    config
  );
}


export type FinishInterviewSessionFields = UnifiedFieldSelection<InterviewSessionResourceSchema>[];

export type InferFinishInterviewSessionResult<
  Fields extends FinishInterviewSessionFields | undefined,
> = InferResult<InterviewSessionResourceSchema, Fields>;

export type FinishInterviewSessionResult<Fields extends FinishInterviewSessionFields | undefined = undefined> = | { success: true; data: InferFinishInterviewSessionResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing InterviewSession
 *
 * @ashActionType :update
 */
export async function finish_interview_session<Fields extends FinishInterviewSessionFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUID;
  fields?: Fields;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<FinishInterviewSessionResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "finish_interview_session",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<FinishInterviewSessionResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


/**
 * Validate: Update an existing InterviewSession
 *
 * @ashActionType :update
 * @validation true
 */
export async function validate_finish_interview_session(
  config: {
  tenant?: string;
  identity: UUID | string;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "finish_interview_session",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Update an existing InterviewSession
 *
 * @ashActionType :update
 * @validation true
 */
export async function validate_finish_interview_session_channel(config: {
  channel: Channel;
  tenant?: string;
  identity: UUID | string;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "finish_interview_session",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity
  },
    config.timeout,
    config
  );
}


/**
 * Update an existing InterviewSession
 *
 * @ashActionType :update
 */
export async function finish_interview_session_channel<Fields extends FinishInterviewSessionFields | undefined = undefined>(config: {
  channel: Channel;
  tenant?: string;
  identity: UUID;
  fields?: Fields;
  result_handler: (result: FinishInterviewSessionResult<Fields>) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<FinishInterviewSessionResult<Fields>>(
    config.channel,
    {
    action: "finish_interview_session",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    ...(config.fields !== undefined && { fields: config.fields })
  },
    config.timeout,
    config
  );
}


export type GenerateInterviewQuestionInput = {
  session_id: UUID;
};

export type InferGenerateInterviewQuestionResult = Record<string, any>;

export type GenerateInterviewQuestionResult = | { success: true; data: InferGenerateInterviewQuestionResult; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Execute generic action on InterviewSession
 *
 * @ashActionType :action
 */
export async function generate_interview_question(
  config: {
  tenant?: string;
  input: GenerateInterviewQuestionInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<GenerateInterviewQuestionResult> {
  const payload = {
    action: "generate_interview_question",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeActionRpcRequest<GenerateInterviewQuestionResult>(
    payload,
    config
  );
}


/**
 * Validate: Execute generic action on InterviewSession
 *
 * @ashActionType :action
 * @validation true
 */
export async function validate_generate_interview_question(
  config: {
  tenant?: string;
  input: GenerateInterviewQuestionInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "generate_interview_question",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Execute generic action on InterviewSession
 *
 * @ashActionType :action
 * @validation true
 */
export async function validate_generate_interview_question_channel(config: {
  channel: Channel;
  tenant?: string;
  input: GenerateInterviewQuestionInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "generate_interview_question",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Execute generic action on InterviewSession
 *
 * @ashActionType :action
 */
export async function generate_interview_question_channel(config: {
  channel: Channel;
  tenant?: string;
  input: GenerateInterviewQuestionInput;
  result_handler: (result: GenerateInterviewQuestionResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<GenerateInterviewQuestionResult>(
    config.channel,
    {
    action: "generate_interview_question",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


export type GetInterviewSessionInput = {
  id: UUID;
};

export type GetInterviewSessionFields = UnifiedFieldSelection<InterviewSessionResourceSchema>[];
export type InferGetInterviewSessionResult<
  Fields extends GetInterviewSessionFields,
> = InferResult<InterviewSessionResourceSchema, Fields>;

export type GetInterviewSessionResult<Fields extends GetInterviewSessionFields> = | { success: true; data: InferGetInterviewSessionResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read InterviewSession records
 *
 * @ashActionType :read
 */
export async function get_interview_session<Fields extends GetInterviewSessionFields>(
  config: {
  tenant?: string;
  input: GetInterviewSessionInput;
  fields: Fields;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<GetInterviewSessionResult<Fields>> {
  const payload = {
    action: "get_interview_session",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<GetInterviewSessionResult<Fields>>(
    payload,
    config
  );
}


/**
 * Validate: Read InterviewSession records
 *
 * @ashActionType :read
 * @validation true
 */
export async function validate_get_interview_session(
  config: {
  tenant?: string;
  input: GetInterviewSessionInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "get_interview_session",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Read InterviewSession records
 *
 * @ashActionType :read
 * @validation true
 */
export async function validate_get_interview_session_channel(config: {
  channel: Channel;
  tenant?: string;
  input: GetInterviewSessionInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "get_interview_session",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Read InterviewSession records
 *
 * @ashActionType :read
 */
export async function get_interview_session_channel<Fields extends GetInterviewSessionFields>(config: {
  channel: Channel;
  tenant?: string;
  input: GetInterviewSessionInput;
  fields: Fields;
  result_handler: (result: GetInterviewSessionResult<Fields>) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<GetInterviewSessionResult<Fields>>(
    config.channel,
    {
    action: "get_interview_session",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  },
    config.timeout,
    config
  );
}


export type ListInterviewSessionsFields = UnifiedFieldSelection<InterviewSessionResourceSchema>[];


export type InferListInterviewSessionsResult<
  Fields extends ListInterviewSessionsFields | undefined,
  Page extends ListInterviewSessionsConfig["page"] = undefined
> = ConditionalPaginatedResultMixed<Page, Array<InferResult<InterviewSessionResourceSchema, Fields>>, {
  results: Array<InferResult<InterviewSessionResourceSchema, Fields>>;
  has_more: boolean;
  limit: number;
  offset: number;
  count?: number | null;
  type: "offset";
}, {
  results: Array<InferResult<InterviewSessionResourceSchema, Fields>>;
  has_more: boolean;
  limit: number;
  after: string | null;
  before: string | null;
  previous_page: string;
  next_page: string;
  count?: number | null;
  type: "keyset";
}>;

export type ListInterviewSessionsConfig = {
  tenant?: string;
  fields: ListInterviewSessionsFields;
  filter?: InterviewSessionFilterInput;
  sort?: SortString<InterviewSessionSortField> | SortString<InterviewSessionSortField>[];
  page?: (
    {
      limit?: number;
      offset?: number;
      count?: boolean;
    } | {
      limit?: number;
      after?: string;
      before?: string;
    }
  );
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
};

export type ListInterviewSessionsResult<Fields extends ListInterviewSessionsFields, Page extends ListInterviewSessionsConfig["page"] = undefined> = | { success: true; data: InferListInterviewSessionsResult<Fields, Page>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read InterviewSession records
 *
 * @ashActionType :read
 */
export async function list_interview_sessions<Fields extends ListInterviewSessionsFields, Config extends ListInterviewSessionsConfig = ListInterviewSessionsConfig>(
  config: Config & { fields: Fields }
): Promise<ListInterviewSessionsResult<Fields, Config["page"]>> {
  const payload = {
    action: "list_interview_sessions",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort }),
    ...(config.page && { page: config.page })
  };

  return executeActionRpcRequest<ListInterviewSessionsResult<Fields, Config["page"]>>(
    payload,
    config
  );
}


/**
 * Validate: Read InterviewSession records
 *
 * @ashActionType :read
 * @validation true
 */
export async function validate_list_interview_sessions(
  config: {
  tenant?: string;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "list_interview_sessions",
    ...(config.tenant !== undefined && { tenant: config.tenant })
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Read InterviewSession records
 *
 * @ashActionType :read
 * @validation true
 */
export async function validate_list_interview_sessions_channel(config: {
  channel: Channel;
  tenant?: string;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "list_interview_sessions",
    ...(config.tenant !== undefined && { tenant: config.tenant })
  },
    config.timeout,
    config
  );
}


/**
 * Read InterviewSession records
 *
 * @ashActionType :read
 */
export async function list_interview_sessions_channel<Fields extends ListInterviewSessionsFields>(config: {
  channel: Channel;
  tenant?: string;
  fields: Fields;
  filter?: InterviewSessionFilterInput;
  sort?: SortString<InterviewSessionSortField> | SortString<InterviewSessionSortField>[];
  page?: (
    {
      limit?: number;
      offset?: number;
      count?: boolean;
    } | {
      limit?: number;
      after?: string;
      before?: string;
    }
  );
  result_handler: (result: ListInterviewSessionsResult<Fields>) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<ListInterviewSessionsResult<Fields>>(
    config.channel,
    {
    action: "list_interview_sessions",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort }),
    ...(config.page && { page: config.page })
  },
    config.timeout,
    config
  );
}


export type UpdateInterviewSessionInput = {
  status?: "cancelled" | "completed" | "finalized" | "in_progress" | "pending" | "processing";
  channel?: "video";
  metadata?: Record<string, any> | null;
  session_name?: string | null;
  summary?: string | null;
  key_quotes?: Array<Record<string, any>> | null;
  topics?: Array<string> | null;
  origin_entity_id?: UUID;
};

export type UpdateInterviewSessionFields = UnifiedFieldSelection<InterviewSessionResourceSchema>[];

export type InferUpdateInterviewSessionResult<
  Fields extends UpdateInterviewSessionFields | undefined,
> = InferResult<InterviewSessionResourceSchema, Fields>;

export type UpdateInterviewSessionResult<Fields extends UpdateInterviewSessionFields | undefined = undefined> = | { success: true; data: InferUpdateInterviewSessionResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing InterviewSession
 *
 * @ashActionType :update
 */
export async function update_interview_session<Fields extends UpdateInterviewSessionFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUID;
  input: UpdateInterviewSessionInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<UpdateInterviewSessionResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "update_interview_session",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<UpdateInterviewSessionResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


/**
 * Validate: Update an existing InterviewSession
 *
 * @ashActionType :update
 * @validation true
 */
export async function validate_update_interview_session(
  config: {
  tenant?: string;
  identity: UUID | string;
  input: UpdateInterviewSessionInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "update_interview_session",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Update an existing InterviewSession
 *
 * @ashActionType :update
 * @validation true
 */
export async function validate_update_interview_session_channel(config: {
  channel: Channel;
  tenant?: string;
  identity: UUID | string;
  input: UpdateInterviewSessionInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "update_interview_session",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Update an existing InterviewSession
 *
 * @ashActionType :update
 */
export async function update_interview_session_channel<Fields extends UpdateInterviewSessionFields | undefined = undefined>(config: {
  channel: Channel;
  tenant?: string;
  identity: UUID;
  input: UpdateInterviewSessionInput;
  fields?: Fields;
  result_handler: (result: UpdateInterviewSessionResult<Fields>) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<UpdateInterviewSessionResult<Fields>>(
    config.channel,
    {
    action: "update_interview_session",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  },
    config.timeout,
    config
  );
}


export type CreateInterviewTurnInput = {
  turn_number: number;
  question: string;
  answer?: string | null;
  metadata?: Record<string, any> | null;
  interview_session_id: UUID;
};

export type CreateInterviewTurnFields = UnifiedFieldSelection<InterviewTurnResourceSchema>[];

export type InferCreateInterviewTurnResult<
  Fields extends CreateInterviewTurnFields | undefined,
> = InferResult<InterviewTurnResourceSchema, Fields>;

export type CreateInterviewTurnResult<Fields extends CreateInterviewTurnFields | undefined = undefined> = | { success: true; data: InferCreateInterviewTurnResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Create a new InterviewTurn
 *
 * @ashActionType :create
 */
export async function create_interview_turn<Fields extends CreateInterviewTurnFields | undefined = undefined>(
  config: {
  tenant?: string;
  input: CreateInterviewTurnInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<CreateInterviewTurnResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "create_interview_turn",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<CreateInterviewTurnResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


/**
 * Validate: Create a new InterviewTurn
 *
 * @ashActionType :create
 * @validation true
 */
export async function validate_create_interview_turn(
  config: {
  tenant?: string;
  input: CreateInterviewTurnInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "create_interview_turn",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Create a new InterviewTurn
 *
 * @ashActionType :create
 * @validation true
 */
export async function validate_create_interview_turn_channel(config: {
  channel: Channel;
  tenant?: string;
  input: CreateInterviewTurnInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "create_interview_turn",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Create a new InterviewTurn
 *
 * @ashActionType :create
 */
export async function create_interview_turn_channel<Fields extends CreateInterviewTurnFields | undefined = undefined>(config: {
  channel: Channel;
  tenant?: string;
  input: CreateInterviewTurnInput;
  fields?: Fields;
  result_handler: (result: CreateInterviewTurnResult<Fields>) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<CreateInterviewTurnResult<Fields>>(
    config.channel,
    {
    action: "create_interview_turn",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  },
    config.timeout,
    config
  );
}


export type GetDriveConnectionForEntityInput = {
  entity_profile_id: UUID;
};

export type GetDriveConnectionForEntityFields = UnifiedFieldSelection<DriveConnectionResourceSchema>[];
export type InferGetDriveConnectionForEntityResult<
  Fields extends GetDriveConnectionForEntityFields,
> = Array<InferResult<DriveConnectionResourceSchema, Fields>>;

export type GetDriveConnectionForEntityResult<Fields extends GetDriveConnectionForEntityFields> = | { success: true; data: InferGetDriveConnectionForEntityResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read DriveConnection records
 *
 * @ashActionType :read
 */
export async function get_drive_connection_for_entity<Fields extends GetDriveConnectionForEntityFields>(
  config: {
  tenant?: string;
  input: GetDriveConnectionForEntityInput;
  fields: Fields;
  filter?: DriveConnectionFilterInput;
  sort?: SortString<DriveConnectionSortField> | SortString<DriveConnectionSortField>[];
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<GetDriveConnectionForEntityResult<Fields>> {
  const payload = {
    action: "get_drive_connection_for_entity",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort })
  };

  return executeActionRpcRequest<GetDriveConnectionForEntityResult<Fields>>(
    payload,
    config
  );
}


/**
 * Validate: Read DriveConnection records
 *
 * @ashActionType :read
 * @validation true
 */
export async function validate_get_drive_connection_for_entity(
  config: {
  tenant?: string;
  input: GetDriveConnectionForEntityInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "get_drive_connection_for_entity",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Read DriveConnection records
 *
 * @ashActionType :read
 * @validation true
 */
export async function validate_get_drive_connection_for_entity_channel(config: {
  channel: Channel;
  tenant?: string;
  input: GetDriveConnectionForEntityInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "get_drive_connection_for_entity",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Read DriveConnection records
 *
 * @ashActionType :read
 */
export async function get_drive_connection_for_entity_channel<Fields extends GetDriveConnectionForEntityFields>(config: {
  channel: Channel;
  tenant?: string;
  input: GetDriveConnectionForEntityInput;
  fields: Fields;
  filter?: DriveConnectionFilterInput;
  sort?: SortString<DriveConnectionSortField> | SortString<DriveConnectionSortField>[];
  result_handler: (result: GetDriveConnectionForEntityResult<Fields>) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<GetDriveConnectionForEntityResult<Fields>>(
    config.channel,
    {
    action: "get_drive_connection_for_entity",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort })
  },
    config.timeout,
    config
  );
}


export type InitiateDriveOauthInput = {
  entity_profile_id: UUID;
  redirect_uri?: string | null;
};

export type InferInitiateDriveOauthResult = Record<string, any>;

export type InitiateDriveOauthResult = | { success: true; data: InferInitiateDriveOauthResult; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Execute generic action on DriveConnection
 *
 * @ashActionType :action
 */
export async function initiate_drive_oauth(
  config: {
  tenant?: string;
  input: InitiateDriveOauthInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<InitiateDriveOauthResult> {
  const payload = {
    action: "initiate_drive_oauth",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeActionRpcRequest<InitiateDriveOauthResult>(
    payload,
    config
  );
}


/**
 * Validate: Execute generic action on DriveConnection
 *
 * @ashActionType :action
 * @validation true
 */
export async function validate_initiate_drive_oauth(
  config: {
  tenant?: string;
  input: InitiateDriveOauthInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "initiate_drive_oauth",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Execute generic action on DriveConnection
 *
 * @ashActionType :action
 * @validation true
 */
export async function validate_initiate_drive_oauth_channel(config: {
  channel: Channel;
  tenant?: string;
  input: InitiateDriveOauthInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "initiate_drive_oauth",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Execute generic action on DriveConnection
 *
 * @ashActionType :action
 */
export async function initiate_drive_oauth_channel(config: {
  channel: Channel;
  tenant?: string;
  input: InitiateDriveOauthInput;
  result_handler: (result: InitiateDriveOauthResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<InitiateDriveOauthResult>(
    config.channel,
    {
    action: "initiate_drive_oauth",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


export type GetGithubConnectionForEntityInput = {
  entity_profile_id: UUID;
};

export type GetGithubConnectionForEntityFields = UnifiedFieldSelection<GithubConnectionResourceSchema>[];
export type InferGetGithubConnectionForEntityResult<
  Fields extends GetGithubConnectionForEntityFields,
> = Array<InferResult<GithubConnectionResourceSchema, Fields>>;

export type GetGithubConnectionForEntityResult<Fields extends GetGithubConnectionForEntityFields> = | { success: true; data: InferGetGithubConnectionForEntityResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read GithubConnection records
 *
 * @ashActionType :read
 */
export async function get_github_connection_for_entity<Fields extends GetGithubConnectionForEntityFields>(
  config: {
  tenant?: string;
  input: GetGithubConnectionForEntityInput;
  fields: Fields;
  filter?: GithubConnectionFilterInput;
  sort?: SortString<GithubConnectionSortField> | SortString<GithubConnectionSortField>[];
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<GetGithubConnectionForEntityResult<Fields>> {
  const payload = {
    action: "get_github_connection_for_entity",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort })
  };

  return executeActionRpcRequest<GetGithubConnectionForEntityResult<Fields>>(
    payload,
    config
  );
}


/**
 * Validate: Read GithubConnection records
 *
 * @ashActionType :read
 * @validation true
 */
export async function validate_get_github_connection_for_entity(
  config: {
  tenant?: string;
  input: GetGithubConnectionForEntityInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "get_github_connection_for_entity",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Read GithubConnection records
 *
 * @ashActionType :read
 * @validation true
 */
export async function validate_get_github_connection_for_entity_channel(config: {
  channel: Channel;
  tenant?: string;
  input: GetGithubConnectionForEntityInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "get_github_connection_for_entity",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Read GithubConnection records
 *
 * @ashActionType :read
 */
export async function get_github_connection_for_entity_channel<Fields extends GetGithubConnectionForEntityFields>(config: {
  channel: Channel;
  tenant?: string;
  input: GetGithubConnectionForEntityInput;
  fields: Fields;
  filter?: GithubConnectionFilterInput;
  sort?: SortString<GithubConnectionSortField> | SortString<GithubConnectionSortField>[];
  result_handler: (result: GetGithubConnectionForEntityResult<Fields>) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<GetGithubConnectionForEntityResult<Fields>>(
    config.channel,
    {
    action: "get_github_connection_for_entity",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort })
  },
    config.timeout,
    config
  );
}


export type InitiateGithubOauthInput = {
  entity_profile_id: UUID;
  redirect_uri?: string | null;
};

export type InferInitiateGithubOauthResult = Record<string, any>;

export type InitiateGithubOauthResult = | { success: true; data: InferInitiateGithubOauthResult; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Execute generic action on GithubConnection
 *
 * @ashActionType :action
 */
export async function initiate_github_oauth(
  config: {
  tenant?: string;
  input: InitiateGithubOauthInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<InitiateGithubOauthResult> {
  const payload = {
    action: "initiate_github_oauth",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeActionRpcRequest<InitiateGithubOauthResult>(
    payload,
    config
  );
}


/**
 * Validate: Execute generic action on GithubConnection
 *
 * @ashActionType :action
 * @validation true
 */
export async function validate_initiate_github_oauth(
  config: {
  tenant?: string;
  input: InitiateGithubOauthInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "initiate_github_oauth",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Execute generic action on GithubConnection
 *
 * @ashActionType :action
 * @validation true
 */
export async function validate_initiate_github_oauth_channel(config: {
  channel: Channel;
  tenant?: string;
  input: InitiateGithubOauthInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "initiate_github_oauth",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Execute generic action on GithubConnection
 *
 * @ashActionType :action
 */
export async function initiate_github_oauth_channel(config: {
  channel: Channel;
  tenant?: string;
  input: InitiateGithubOauthInput;
  result_handler: (result: InitiateGithubOauthResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<InitiateGithubOauthResult>(
    config.channel,
    {
    action: "initiate_github_oauth",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


export type ListGithubBranchesInput = {
  connection_id: UUID;
  owner: string;
  repo: string;
};

export type InferListGithubBranchesResult = Array<Record<string, any>>;

export type ListGithubBranchesResult = | { success: true; data: InferListGithubBranchesResult; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Execute generic action on GithubConnection
 *
 * @ashActionType :action
 */
export async function list_github_branches(
  config: {
  tenant?: string;
  input: ListGithubBranchesInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ListGithubBranchesResult> {
  const payload = {
    action: "list_github_branches",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeActionRpcRequest<ListGithubBranchesResult>(
    payload,
    config
  );
}


/**
 * Validate: Execute generic action on GithubConnection
 *
 * @ashActionType :action
 * @validation true
 */
export async function validate_list_github_branches(
  config: {
  tenant?: string;
  input: ListGithubBranchesInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "list_github_branches",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Execute generic action on GithubConnection
 *
 * @ashActionType :action
 * @validation true
 */
export async function validate_list_github_branches_channel(config: {
  channel: Channel;
  tenant?: string;
  input: ListGithubBranchesInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "list_github_branches",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Execute generic action on GithubConnection
 *
 * @ashActionType :action
 */
export async function list_github_branches_channel(config: {
  channel: Channel;
  tenant?: string;
  input: ListGithubBranchesInput;
  result_handler: (result: ListGithubBranchesResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<ListGithubBranchesResult>(
    config.channel,
    {
    action: "list_github_branches",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


export type ListGithubRepositoriesInput = {
  connection_id: UUID;
};

export type InferListGithubRepositoriesResult = Array<Record<string, any>>;

export type ListGithubRepositoriesResult = | { success: true; data: InferListGithubRepositoriesResult; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Execute generic action on GithubConnection
 *
 * @ashActionType :action
 */
export async function list_github_repositories(
  config: {
  tenant?: string;
  input: ListGithubRepositoriesInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ListGithubRepositoriesResult> {
  const payload = {
    action: "list_github_repositories",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeActionRpcRequest<ListGithubRepositoriesResult>(
    payload,
    config
  );
}


/**
 * Validate: Execute generic action on GithubConnection
 *
 * @ashActionType :action
 * @validation true
 */
export async function validate_list_github_repositories(
  config: {
  tenant?: string;
  input: ListGithubRepositoriesInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "list_github_repositories",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Execute generic action on GithubConnection
 *
 * @ashActionType :action
 * @validation true
 */
export async function validate_list_github_repositories_channel(config: {
  channel: Channel;
  tenant?: string;
  input: ListGithubRepositoriesInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "list_github_repositories",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Execute generic action on GithubConnection
 *
 * @ashActionType :action
 */
export async function list_github_repositories_channel(config: {
  channel: Channel;
  tenant?: string;
  input: ListGithubRepositoriesInput;
  result_handler: (result: ListGithubRepositoriesResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<ListGithubRepositoriesResult>(
    config.channel,
    {
    action: "list_github_repositories",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


export type CreateLibraryInput = {
  origin_entity_id: UUID;
  name: string;
  adapter_kind: "craft" | "curated" | "drive" | "github" | "raw_upload" | "s3" | "youtube";
  adapter_config?: Record<string, any> | null;
  inherits_to_descendants?: boolean;
  auto_resync_enabled?: boolean;
  auto_ingest_videos?: boolean;
  transcribe_media?: boolean;
};

export type CreateLibraryFields = UnifiedFieldSelection<LibraryResourceSchema>[];

export type InferCreateLibraryResult<
  Fields extends CreateLibraryFields | undefined,
> = InferResult<LibraryResourceSchema, Fields>;

export type CreateLibraryResult<Fields extends CreateLibraryFields | undefined = undefined> = | { success: true; data: InferCreateLibraryResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Create a new Library
 *
 * @ashActionType :create
 */
export async function create_library<Fields extends CreateLibraryFields | undefined = undefined>(
  config: {
  tenant?: string;
  input: CreateLibraryInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<CreateLibraryResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "create_library",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<CreateLibraryResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


/**
 * Validate: Create a new Library
 *
 * @ashActionType :create
 * @validation true
 */
export async function validate_create_library(
  config: {
  tenant?: string;
  input: CreateLibraryInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "create_library",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Create a new Library
 *
 * @ashActionType :create
 * @validation true
 */
export async function validate_create_library_channel(config: {
  channel: Channel;
  tenant?: string;
  input: CreateLibraryInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "create_library",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Create a new Library
 *
 * @ashActionType :create
 */
export async function create_library_channel<Fields extends CreateLibraryFields | undefined = undefined>(config: {
  channel: Channel;
  tenant?: string;
  input: CreateLibraryInput;
  fields?: Fields;
  result_handler: (result: CreateLibraryResult<Fields>) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<CreateLibraryResult<Fields>>(
    config.channel,
    {
    action: "create_library",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  },
    config.timeout,
    config
  );
}


export type DeleteLibraryResult = | { success: true; data: {}; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Delete a Library
 *
 * @ashActionType :destroy
 */
export async function delete_library(
  config: {
  tenant?: string;
  identity: UUID;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<DeleteLibraryResult> {
  const payload = {
    action: "delete_library",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity
  };

  return executeActionRpcRequest<DeleteLibraryResult>(
    payload,
    config
  );
}


/**
 * Validate: Delete a Library
 *
 * @ashActionType :destroy
 * @validation true
 */
export async function validate_delete_library(
  config: {
  tenant?: string;
  identity: UUID | string;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "delete_library",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Delete a Library
 *
 * @ashActionType :destroy
 * @validation true
 */
export async function validate_delete_library_channel(config: {
  channel: Channel;
  tenant?: string;
  identity: UUID | string;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "delete_library",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity
  },
    config.timeout,
    config
  );
}


/**
 * Delete a Library
 *
 * @ashActionType :destroy
 */
export async function delete_library_channel(config: {
  channel: Channel;
  tenant?: string;
  identity: UUID;
  result_handler: (result: DeleteLibraryResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<DeleteLibraryResult>(
    config.channel,
    {
    action: "delete_library",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity
  },
    config.timeout,
    config
  );
}


export type GetLibraryInput = {
  id: UUID;
};

export type GetLibraryFields = UnifiedFieldSelection<LibraryResourceSchema>[];
export type InferGetLibraryResult<
  Fields extends GetLibraryFields,
> = InferResult<LibraryResourceSchema, Fields>;

export type GetLibraryResult<Fields extends GetLibraryFields> = | { success: true; data: InferGetLibraryResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read Library records
 *
 * @ashActionType :read
 */
export async function get_library<Fields extends GetLibraryFields>(
  config: {
  tenant?: string;
  input: GetLibraryInput;
  fields: Fields;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<GetLibraryResult<Fields>> {
  const payload = {
    action: "get_library",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<GetLibraryResult<Fields>>(
    payload,
    config
  );
}


/**
 * Validate: Read Library records
 *
 * @ashActionType :read
 * @validation true
 */
export async function validate_get_library(
  config: {
  tenant?: string;
  input: GetLibraryInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "get_library",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Read Library records
 *
 * @ashActionType :read
 * @validation true
 */
export async function validate_get_library_channel(config: {
  channel: Channel;
  tenant?: string;
  input: GetLibraryInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "get_library",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Read Library records
 *
 * @ashActionType :read
 */
export async function get_library_channel<Fields extends GetLibraryFields>(config: {
  channel: Channel;
  tenant?: string;
  input: GetLibraryInput;
  fields: Fields;
  result_handler: (result: GetLibraryResult<Fields>) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<GetLibraryResult<Fields>>(
    config.channel,
    {
    action: "get_library",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  },
    config.timeout,
    config
  );
}


export type LibraryShellExecInput = {
  library_ids: Array<UUID>;
  command: string;
};

export type InferLibraryShellExecResult = Record<string, any>;

export type LibraryShellExecResult = | { success: true; data: InferLibraryShellExecResult; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Execute generic action on Library
 *
 * @ashActionType :action
 */
export async function library_shell_exec(
  config: {
  tenant?: string;
  input: LibraryShellExecInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<LibraryShellExecResult> {
  const payload = {
    action: "library_shell_exec",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeActionRpcRequest<LibraryShellExecResult>(
    payload,
    config
  );
}


/**
 * Validate: Execute generic action on Library
 *
 * @ashActionType :action
 * @validation true
 */
export async function validate_library_shell_exec(
  config: {
  tenant?: string;
  input: LibraryShellExecInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "library_shell_exec",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Execute generic action on Library
 *
 * @ashActionType :action
 * @validation true
 */
export async function validate_library_shell_exec_channel(config: {
  channel: Channel;
  tenant?: string;
  input: LibraryShellExecInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "library_shell_exec",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Execute generic action on Library
 *
 * @ashActionType :action
 */
export async function library_shell_exec_channel(config: {
  channel: Channel;
  tenant?: string;
  input: LibraryShellExecInput;
  result_handler: (result: LibraryShellExecResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<LibraryShellExecResult>(
    config.channel,
    {
    action: "library_shell_exec",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


export type LibraryShellListTreeInput = {
  library_ids: Array<UUID>;
};

export type InferLibraryShellListTreeResult = Record<string, any>;

export type LibraryShellListTreeResult = | { success: true; data: InferLibraryShellListTreeResult; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Execute generic action on Library
 *
 * @ashActionType :action
 */
export async function library_shell_list_tree(
  config: {
  tenant?: string;
  input: LibraryShellListTreeInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<LibraryShellListTreeResult> {
  const payload = {
    action: "library_shell_list_tree",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeActionRpcRequest<LibraryShellListTreeResult>(
    payload,
    config
  );
}


/**
 * Validate: Execute generic action on Library
 *
 * @ashActionType :action
 * @validation true
 */
export async function validate_library_shell_list_tree(
  config: {
  tenant?: string;
  input: LibraryShellListTreeInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "library_shell_list_tree",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Execute generic action on Library
 *
 * @ashActionType :action
 * @validation true
 */
export async function validate_library_shell_list_tree_channel(config: {
  channel: Channel;
  tenant?: string;
  input: LibraryShellListTreeInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "library_shell_list_tree",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Execute generic action on Library
 *
 * @ashActionType :action
 */
export async function library_shell_list_tree_channel(config: {
  channel: Channel;
  tenant?: string;
  input: LibraryShellListTreeInput;
  result_handler: (result: LibraryShellListTreeResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<LibraryShellListTreeResult>(
    config.channel,
    {
    action: "library_shell_list_tree",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


export type LibraryShellReadInput = {
  library_ids: Array<UUID>;
  path: string;
};

export type InferLibraryShellReadResult = Record<string, any>;

export type LibraryShellReadResult = | { success: true; data: InferLibraryShellReadResult; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Execute generic action on Library
 *
 * @ashActionType :action
 */
export async function library_shell_read(
  config: {
  tenant?: string;
  input: LibraryShellReadInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<LibraryShellReadResult> {
  const payload = {
    action: "library_shell_read",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeActionRpcRequest<LibraryShellReadResult>(
    payload,
    config
  );
}


/**
 * Validate: Execute generic action on Library
 *
 * @ashActionType :action
 * @validation true
 */
export async function validate_library_shell_read(
  config: {
  tenant?: string;
  input: LibraryShellReadInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "library_shell_read",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Execute generic action on Library
 *
 * @ashActionType :action
 * @validation true
 */
export async function validate_library_shell_read_channel(config: {
  channel: Channel;
  tenant?: string;
  input: LibraryShellReadInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "library_shell_read",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Execute generic action on Library
 *
 * @ashActionType :action
 */
export async function library_shell_read_channel(config: {
  channel: Channel;
  tenant?: string;
  input: LibraryShellReadInput;
  result_handler: (result: LibraryShellReadResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<LibraryShellReadResult>(
    config.channel,
    {
    action: "library_shell_read",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


export type ListLibrariesFields = UnifiedFieldSelection<LibraryResourceSchema>[];


export type InferListLibrariesResult<
  Fields extends ListLibrariesFields | undefined,
  Page extends ListLibrariesConfig["page"] = undefined
> = ConditionalPaginatedResult<Page, Array<InferResult<LibraryResourceSchema, Fields>>, {
  results: Array<InferResult<LibraryResourceSchema, Fields>>;
  has_more: boolean;
  limit: number;
  after: string | null;
  before: string | null;
  previous_page: string;
  next_page: string;
  count?: number | null;
  type: "keyset";
}>;

export type ListLibrariesConfig = {
  tenant?: string;
  fields: ListLibrariesFields;
  filter?: LibraryFilterInput;
  sort?: SortString<LibrarySortField> | SortString<LibrarySortField>[];
  page?: {
    limit?: number;
    after?: string;
    before?: string;
    offset?: never;
    count?: never;
  };
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
};

export type ListLibrariesResult<Fields extends ListLibrariesFields, Page extends ListLibrariesConfig["page"] = undefined> = | { success: true; data: InferListLibrariesResult<Fields, Page>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read Library records
 *
 * @ashActionType :read
 */
export async function list_libraries<Fields extends ListLibrariesFields, Config extends ListLibrariesConfig = ListLibrariesConfig>(
  config: Config & { fields: Fields }
): Promise<ListLibrariesResult<Fields, Config["page"]>> {
  const payload = {
    action: "list_libraries",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort }),
    ...(config.page && { page: config.page })
  };

  return executeActionRpcRequest<ListLibrariesResult<Fields, Config["page"]>>(
    payload,
    config
  );
}


/**
 * Validate: Read Library records
 *
 * @ashActionType :read
 * @validation true
 */
export async function validate_list_libraries(
  config: {
  tenant?: string;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "list_libraries",
    ...(config.tenant !== undefined && { tenant: config.tenant })
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Read Library records
 *
 * @ashActionType :read
 * @validation true
 */
export async function validate_list_libraries_channel(config: {
  channel: Channel;
  tenant?: string;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "list_libraries",
    ...(config.tenant !== undefined && { tenant: config.tenant })
  },
    config.timeout,
    config
  );
}


/**
 * Read Library records
 *
 * @ashActionType :read
 */
export async function list_libraries_channel<Fields extends ListLibrariesFields>(config: {
  channel: Channel;
  tenant?: string;
  fields: Fields;
  filter?: LibraryFilterInput;
  sort?: SortString<LibrarySortField> | SortString<LibrarySortField>[];
  page?: {
    limit?: number;
    after?: string;
    before?: string;
    offset?: never;
    count?: never;
  };
  result_handler: (result: ListLibrariesResult<Fields>) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<ListLibrariesResult<Fields>>(
    config.channel,
    {
    action: "list_libraries",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort }),
    ...(config.page && { page: config.page })
  },
    config.timeout,
    config
  );
}


export type ListLibrariesForOriginInput = {
  origin_entity_id: UUID;
};

export type ListLibrariesForOriginFields = UnifiedFieldSelection<LibraryResourceSchema>[];


export type InferListLibrariesForOriginResult<
  Fields extends ListLibrariesForOriginFields | undefined,
  Page extends ListLibrariesForOriginConfig["page"] = undefined
> = ConditionalPaginatedResult<Page, Array<InferResult<LibraryResourceSchema, Fields>>, {
  results: Array<InferResult<LibraryResourceSchema, Fields>>;
  has_more: boolean;
  limit: number;
  after: string | null;
  before: string | null;
  previous_page: string;
  next_page: string;
  count?: number | null;
  type: "keyset";
}>;

export type ListLibrariesForOriginConfig = {
  tenant?: string;
  input: ListLibrariesForOriginInput;
  fields: ListLibrariesForOriginFields;
  filter?: LibraryFilterInput;
  sort?: SortString<LibrarySortField> | SortString<LibrarySortField>[];
  page?: {
    limit?: number;
    after?: string;
    before?: string;
    offset?: never;
    count?: never;
  };
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
};

export type ListLibrariesForOriginResult<Fields extends ListLibrariesForOriginFields, Page extends ListLibrariesForOriginConfig["page"] = undefined> = | { success: true; data: InferListLibrariesForOriginResult<Fields, Page>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read Library records
 *
 * @ashActionType :read
 */
export async function list_libraries_for_origin<Fields extends ListLibrariesForOriginFields, Config extends ListLibrariesForOriginConfig = ListLibrariesForOriginConfig>(
  config: Config & { fields: Fields }
): Promise<ListLibrariesForOriginResult<Fields, Config["page"]>> {
  const payload = {
    action: "list_libraries_for_origin",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort }),
    ...(config.page && { page: config.page })
  };

  return executeActionRpcRequest<ListLibrariesForOriginResult<Fields, Config["page"]>>(
    payload,
    config
  );
}


/**
 * Validate: Read Library records
 *
 * @ashActionType :read
 * @validation true
 */
export async function validate_list_libraries_for_origin(
  config: {
  tenant?: string;
  input: ListLibrariesForOriginInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "list_libraries_for_origin",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Read Library records
 *
 * @ashActionType :read
 * @validation true
 */
export async function validate_list_libraries_for_origin_channel(config: {
  channel: Channel;
  tenant?: string;
  input: ListLibrariesForOriginInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "list_libraries_for_origin",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Read Library records
 *
 * @ashActionType :read
 */
export async function list_libraries_for_origin_channel<Fields extends ListLibrariesForOriginFields>(config: {
  channel: Channel;
  tenant?: string;
  input: ListLibrariesForOriginInput;
  fields: Fields;
  filter?: LibraryFilterInput;
  sort?: SortString<LibrarySortField> | SortString<LibrarySortField>[];
  page?: {
    limit?: number;
    after?: string;
    before?: string;
    offset?: never;
    count?: never;
  };
  result_handler: (result: ListLibrariesForOriginResult<Fields>) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<ListLibrariesForOriginResult<Fields>>(
    config.channel,
    {
    action: "list_libraries_for_origin",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort }),
    ...(config.page && { page: config.page })
  },
    config.timeout,
    config
  );
}


export type PreviewLibrarySyncFilterInput = {
  id: UUID;
  filters: Record<string, any>;
};

export type InferPreviewLibrarySyncFilterResult = Record<string, any>;

export type PreviewLibrarySyncFilterResult = | { success: true; data: InferPreviewLibrarySyncFilterResult; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Execute generic action on Library
 *
 * @ashActionType :action
 */
export async function preview_library_sync_filter(
  config: {
  tenant?: string;
  input: PreviewLibrarySyncFilterInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<PreviewLibrarySyncFilterResult> {
  const payload = {
    action: "preview_library_sync_filter",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeActionRpcRequest<PreviewLibrarySyncFilterResult>(
    payload,
    config
  );
}


/**
 * Validate: Execute generic action on Library
 *
 * @ashActionType :action
 * @validation true
 */
export async function validate_preview_library_sync_filter(
  config: {
  tenant?: string;
  input: PreviewLibrarySyncFilterInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "preview_library_sync_filter",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Execute generic action on Library
 *
 * @ashActionType :action
 * @validation true
 */
export async function validate_preview_library_sync_filter_channel(config: {
  channel: Channel;
  tenant?: string;
  input: PreviewLibrarySyncFilterInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "preview_library_sync_filter",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Execute generic action on Library
 *
 * @ashActionType :action
 */
export async function preview_library_sync_filter_channel(config: {
  channel: Channel;
  tenant?: string;
  input: PreviewLibrarySyncFilterInput;
  result_handler: (result: PreviewLibrarySyncFilterResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<PreviewLibrarySyncFilterResult>(
    config.channel,
    {
    action: "preview_library_sync_filter",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


export type SyncLibraryNowInput = {
  id: UUID;
};

export type InferSyncLibraryNowResult = Record<string, any>;

export type SyncLibraryNowResult = | { success: true; data: InferSyncLibraryNowResult; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Execute generic action on Library
 *
 * @ashActionType :action
 */
export async function sync_library_now(
  config: {
  tenant?: string;
  input: SyncLibraryNowInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<SyncLibraryNowResult> {
  const payload = {
    action: "sync_library_now",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeActionRpcRequest<SyncLibraryNowResult>(
    payload,
    config
  );
}


/**
 * Validate: Execute generic action on Library
 *
 * @ashActionType :action
 * @validation true
 */
export async function validate_sync_library_now(
  config: {
  tenant?: string;
  input: SyncLibraryNowInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "sync_library_now",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Execute generic action on Library
 *
 * @ashActionType :action
 * @validation true
 */
export async function validate_sync_library_now_channel(config: {
  channel: Channel;
  tenant?: string;
  input: SyncLibraryNowInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "sync_library_now",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Execute generic action on Library
 *
 * @ashActionType :action
 */
export async function sync_library_now_channel(config: {
  channel: Channel;
  tenant?: string;
  input: SyncLibraryNowInput;
  result_handler: (result: SyncLibraryNowResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<SyncLibraryNowResult>(
    config.channel,
    {
    action: "sync_library_now",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


export type UpdateLibraryInput = {
  name?: string;
  inherits_to_descendants?: boolean;
  auto_resync_enabled?: boolean;
  auto_ingest_videos?: boolean;
  transcribe_media?: boolean;
};

export type UpdateLibraryFields = UnifiedFieldSelection<LibraryResourceSchema>[];

export type InferUpdateLibraryResult<
  Fields extends UpdateLibraryFields | undefined,
> = InferResult<LibraryResourceSchema, Fields>;

export type UpdateLibraryResult<Fields extends UpdateLibraryFields | undefined = undefined> = | { success: true; data: InferUpdateLibraryResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing Library
 *
 * @ashActionType :update
 */
export async function update_library<Fields extends UpdateLibraryFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUID;
  input: UpdateLibraryInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<UpdateLibraryResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "update_library",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<UpdateLibraryResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


/**
 * Validate: Update an existing Library
 *
 * @ashActionType :update
 * @validation true
 */
export async function validate_update_library(
  config: {
  tenant?: string;
  identity: UUID | string;
  input: UpdateLibraryInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "update_library",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Update an existing Library
 *
 * @ashActionType :update
 * @validation true
 */
export async function validate_update_library_channel(config: {
  channel: Channel;
  tenant?: string;
  identity: UUID | string;
  input: UpdateLibraryInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "update_library",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Update an existing Library
 *
 * @ashActionType :update
 */
export async function update_library_channel<Fields extends UpdateLibraryFields | undefined = undefined>(config: {
  channel: Channel;
  tenant?: string;
  identity: UUID;
  input: UpdateLibraryInput;
  fields?: Fields;
  result_handler: (result: UpdateLibraryResult<Fields>) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<UpdateLibraryResult<Fields>>(
    config.channel,
    {
    action: "update_library",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  },
    config.timeout,
    config
  );
}


export type UpdateLibrarySyncFilterInput = {
  filters: Record<string, any>;
};

export type UpdateLibrarySyncFilterFields = UnifiedFieldSelection<LibraryResourceSchema>[];

export type InferUpdateLibrarySyncFilterResult<
  Fields extends UpdateLibrarySyncFilterFields | undefined,
> = InferResult<LibraryResourceSchema, Fields>;

export type UpdateLibrarySyncFilterResult<Fields extends UpdateLibrarySyncFilterFields | undefined = undefined> = | { success: true; data: InferUpdateLibrarySyncFilterResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing Library
 *
 * @ashActionType :update
 */
export async function update_library_sync_filter<Fields extends UpdateLibrarySyncFilterFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUID;
  input: UpdateLibrarySyncFilterInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<UpdateLibrarySyncFilterResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "update_library_sync_filter",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<UpdateLibrarySyncFilterResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


/**
 * Validate: Update an existing Library
 *
 * @ashActionType :update
 * @validation true
 */
export async function validate_update_library_sync_filter(
  config: {
  tenant?: string;
  identity: UUID | string;
  input: UpdateLibrarySyncFilterInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "update_library_sync_filter",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Update an existing Library
 *
 * @ashActionType :update
 * @validation true
 */
export async function validate_update_library_sync_filter_channel(config: {
  channel: Channel;
  tenant?: string;
  identity: UUID | string;
  input: UpdateLibrarySyncFilterInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "update_library_sync_filter",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Update an existing Library
 *
 * @ashActionType :update
 */
export async function update_library_sync_filter_channel<Fields extends UpdateLibrarySyncFilterFields | undefined = undefined>(config: {
  channel: Channel;
  tenant?: string;
  identity: UUID;
  input: UpdateLibrarySyncFilterInput;
  fields?: Fields;
  result_handler: (result: UpdateLibrarySyncFilterResult<Fields>) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<UpdateLibrarySyncFilterResult<Fields>>(
    config.channel,
    {
    action: "update_library_sync_filter",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  },
    config.timeout,
    config
  );
}


export type CreateLibraryAccessGrantInput = {
  library_id: UUID;
  grantee_origin_entity_id: UUID;
};

export type CreateLibraryAccessGrantFields = UnifiedFieldSelection<LibraryAccessGrantResourceSchema>[];

export type InferCreateLibraryAccessGrantResult<
  Fields extends CreateLibraryAccessGrantFields | undefined,
> = InferResult<LibraryAccessGrantResourceSchema, Fields>;

export type CreateLibraryAccessGrantResult<Fields extends CreateLibraryAccessGrantFields | undefined = undefined> = | { success: true; data: InferCreateLibraryAccessGrantResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Create a new LibraryAccessGrant
 *
 * @ashActionType :create
 */
export async function create_library_access_grant<Fields extends CreateLibraryAccessGrantFields | undefined = undefined>(
  config: {
  tenant?: string;
  input: CreateLibraryAccessGrantInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<CreateLibraryAccessGrantResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "create_library_access_grant",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<CreateLibraryAccessGrantResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


/**
 * Validate: Create a new LibraryAccessGrant
 *
 * @ashActionType :create
 * @validation true
 */
export async function validate_create_library_access_grant(
  config: {
  tenant?: string;
  input: CreateLibraryAccessGrantInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "create_library_access_grant",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Create a new LibraryAccessGrant
 *
 * @ashActionType :create
 * @validation true
 */
export async function validate_create_library_access_grant_channel(config: {
  channel: Channel;
  tenant?: string;
  input: CreateLibraryAccessGrantInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "create_library_access_grant",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Create a new LibraryAccessGrant
 *
 * @ashActionType :create
 */
export async function create_library_access_grant_channel<Fields extends CreateLibraryAccessGrantFields | undefined = undefined>(config: {
  channel: Channel;
  tenant?: string;
  input: CreateLibraryAccessGrantInput;
  fields?: Fields;
  result_handler: (result: CreateLibraryAccessGrantResult<Fields>) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<CreateLibraryAccessGrantResult<Fields>>(
    config.channel,
    {
    action: "create_library_access_grant",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  },
    config.timeout,
    config
  );
}


export type DeleteLibraryAccessGrantResult = | { success: true; data: {}; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Delete a LibraryAccessGrant
 *
 * @ashActionType :destroy
 */
export async function delete_library_access_grant(
  config: {
  tenant?: string;
  identity: UUID;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<DeleteLibraryAccessGrantResult> {
  const payload = {
    action: "delete_library_access_grant",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity
  };

  return executeActionRpcRequest<DeleteLibraryAccessGrantResult>(
    payload,
    config
  );
}


/**
 * Validate: Delete a LibraryAccessGrant
 *
 * @ashActionType :destroy
 * @validation true
 */
export async function validate_delete_library_access_grant(
  config: {
  tenant?: string;
  identity: UUID | string;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "delete_library_access_grant",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Delete a LibraryAccessGrant
 *
 * @ashActionType :destroy
 * @validation true
 */
export async function validate_delete_library_access_grant_channel(config: {
  channel: Channel;
  tenant?: string;
  identity: UUID | string;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "delete_library_access_grant",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity
  },
    config.timeout,
    config
  );
}


/**
 * Delete a LibraryAccessGrant
 *
 * @ashActionType :destroy
 */
export async function delete_library_access_grant_channel(config: {
  channel: Channel;
  tenant?: string;
  identity: UUID;
  result_handler: (result: DeleteLibraryAccessGrantResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<DeleteLibraryAccessGrantResult>(
    config.channel,
    {
    action: "delete_library_access_grant",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity
  },
    config.timeout,
    config
  );
}


export type ListLibraryAccessGrantsFields = UnifiedFieldSelection<LibraryAccessGrantResourceSchema>[];


export type InferListLibraryAccessGrantsResult<
  Fields extends ListLibraryAccessGrantsFields | undefined,
  Page extends ListLibraryAccessGrantsConfig["page"] = undefined
> = ConditionalPaginatedResultMixed<Page, Array<InferResult<LibraryAccessGrantResourceSchema, Fields>>, {
  results: Array<InferResult<LibraryAccessGrantResourceSchema, Fields>>;
  has_more: boolean;
  limit: number;
  offset: number;
  count?: number | null;
  type: "offset";
}, {
  results: Array<InferResult<LibraryAccessGrantResourceSchema, Fields>>;
  has_more: boolean;
  limit: number;
  after: string | null;
  before: string | null;
  previous_page: string;
  next_page: string;
  count?: number | null;
  type: "keyset";
}>;

export type ListLibraryAccessGrantsConfig = {
  tenant?: string;
  fields: ListLibraryAccessGrantsFields;
  filter?: LibraryAccessGrantFilterInput;
  sort?: SortString<LibraryAccessGrantSortField> | SortString<LibraryAccessGrantSortField>[];
  page?: (
    {
      limit?: number;
      offset?: number;
      count?: boolean;
    } | {
      limit?: number;
      after?: string;
      before?: string;
    }
  );
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
};

export type ListLibraryAccessGrantsResult<Fields extends ListLibraryAccessGrantsFields, Page extends ListLibraryAccessGrantsConfig["page"] = undefined> = | { success: true; data: InferListLibraryAccessGrantsResult<Fields, Page>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read LibraryAccessGrant records
 *
 * @ashActionType :read
 */
export async function list_library_access_grants<Fields extends ListLibraryAccessGrantsFields, Config extends ListLibraryAccessGrantsConfig = ListLibraryAccessGrantsConfig>(
  config: Config & { fields: Fields }
): Promise<ListLibraryAccessGrantsResult<Fields, Config["page"]>> {
  const payload = {
    action: "list_library_access_grants",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort }),
    ...(config.page && { page: config.page })
  };

  return executeActionRpcRequest<ListLibraryAccessGrantsResult<Fields, Config["page"]>>(
    payload,
    config
  );
}


/**
 * Validate: Read LibraryAccessGrant records
 *
 * @ashActionType :read
 * @validation true
 */
export async function validate_list_library_access_grants(
  config: {
  tenant?: string;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "list_library_access_grants",
    ...(config.tenant !== undefined && { tenant: config.tenant })
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Read LibraryAccessGrant records
 *
 * @ashActionType :read
 * @validation true
 */
export async function validate_list_library_access_grants_channel(config: {
  channel: Channel;
  tenant?: string;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "list_library_access_grants",
    ...(config.tenant !== undefined && { tenant: config.tenant })
  },
    config.timeout,
    config
  );
}


/**
 * Read LibraryAccessGrant records
 *
 * @ashActionType :read
 */
export async function list_library_access_grants_channel<Fields extends ListLibraryAccessGrantsFields>(config: {
  channel: Channel;
  tenant?: string;
  fields: Fields;
  filter?: LibraryAccessGrantFilterInput;
  sort?: SortString<LibraryAccessGrantSortField> | SortString<LibraryAccessGrantSortField>[];
  page?: (
    {
      limit?: number;
      offset?: number;
      count?: boolean;
    } | {
      limit?: number;
      after?: string;
      before?: string;
    }
  );
  result_handler: (result: ListLibraryAccessGrantsResult<Fields>) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<ListLibraryAccessGrantsResult<Fields>>(
    config.channel,
    {
    action: "list_library_access_grants",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort }),
    ...(config.page && { page: config.page })
  },
    config.timeout,
    config
  );
}


export type CreateLibraryFileInput = {
  library_id: UUID;
  path: string;
  item_type: string;
  item_name: string;
  item_content?: string | null;
  s3_key?: string | null;
  source_metadata?: Record<string, any> | null;
  sync_status?: "error" | "orphaned" | "synced" | null;
  letta_file_id?: string | null;
};

export type CreateLibraryFileFields = UnifiedFieldSelection<LibraryFileResourceSchema>[];

export type InferCreateLibraryFileResult<
  Fields extends CreateLibraryFileFields | undefined,
> = InferResult<LibraryFileResourceSchema, Fields>;

export type CreateLibraryFileResult<Fields extends CreateLibraryFileFields | undefined = undefined> = | { success: true; data: InferCreateLibraryFileResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Create a new LibraryFile
 *
 * @ashActionType :create
 */
export async function create_library_file<Fields extends CreateLibraryFileFields | undefined = undefined>(
  config: {
  tenant?: string;
  input: CreateLibraryFileInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<CreateLibraryFileResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "create_library_file",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<CreateLibraryFileResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


/**
 * Validate: Create a new LibraryFile
 *
 * @ashActionType :create
 * @validation true
 */
export async function validate_create_library_file(
  config: {
  tenant?: string;
  input: CreateLibraryFileInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "create_library_file",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Create a new LibraryFile
 *
 * @ashActionType :create
 * @validation true
 */
export async function validate_create_library_file_channel(config: {
  channel: Channel;
  tenant?: string;
  input: CreateLibraryFileInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "create_library_file",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Create a new LibraryFile
 *
 * @ashActionType :create
 */
export async function create_library_file_channel<Fields extends CreateLibraryFileFields | undefined = undefined>(config: {
  channel: Channel;
  tenant?: string;
  input: CreateLibraryFileInput;
  fields?: Fields;
  result_handler: (result: CreateLibraryFileResult<Fields>) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<CreateLibraryFileResult<Fields>>(
    config.channel,
    {
    action: "create_library_file",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  },
    config.timeout,
    config
  );
}


export type CreateLibraryFileInlineInput = {
  library_id: UUID;
  path: string;
  item_type: string;
  item_name: string;
  item_content?: string | null;
  source_metadata?: Record<string, any> | null;
};

export type CreateLibraryFileInlineFields = UnifiedFieldSelection<LibraryFileResourceSchema>[];

export type InferCreateLibraryFileInlineResult<
  Fields extends CreateLibraryFileInlineFields | undefined,
> = InferResult<LibraryFileResourceSchema, Fields>;

export type CreateLibraryFileInlineResult<Fields extends CreateLibraryFileInlineFields | undefined = undefined> = | { success: true; data: InferCreateLibraryFileInlineResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Create a new LibraryFile
 *
 * @ashActionType :create
 */
export async function create_library_file_inline<Fields extends CreateLibraryFileInlineFields | undefined = undefined>(
  config: {
  tenant?: string;
  input: CreateLibraryFileInlineInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<CreateLibraryFileInlineResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "create_library_file_inline",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<CreateLibraryFileInlineResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


/**
 * Validate: Create a new LibraryFile
 *
 * @ashActionType :create
 * @validation true
 */
export async function validate_create_library_file_inline(
  config: {
  tenant?: string;
  input: CreateLibraryFileInlineInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "create_library_file_inline",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Create a new LibraryFile
 *
 * @ashActionType :create
 * @validation true
 */
export async function validate_create_library_file_inline_channel(config: {
  channel: Channel;
  tenant?: string;
  input: CreateLibraryFileInlineInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "create_library_file_inline",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Create a new LibraryFile
 *
 * @ashActionType :create
 */
export async function create_library_file_inline_channel<Fields extends CreateLibraryFileInlineFields | undefined = undefined>(config: {
  channel: Channel;
  tenant?: string;
  input: CreateLibraryFileInlineInput;
  fields?: Fields;
  result_handler: (result: CreateLibraryFileInlineResult<Fields>) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<CreateLibraryFileInlineResult<Fields>>(
    config.channel,
    {
    action: "create_library_file_inline",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  },
    config.timeout,
    config
  );
}


export type DeleteLibraryFileResult = | { success: true; data: {}; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Delete a LibraryFile
 *
 * @ashActionType :destroy
 */
export async function delete_library_file(
  config: {
  tenant?: string;
  identity: UUID;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<DeleteLibraryFileResult> {
  const payload = {
    action: "delete_library_file",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity
  };

  return executeActionRpcRequest<DeleteLibraryFileResult>(
    payload,
    config
  );
}


/**
 * Validate: Delete a LibraryFile
 *
 * @ashActionType :destroy
 * @validation true
 */
export async function validate_delete_library_file(
  config: {
  tenant?: string;
  identity: UUID | string;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "delete_library_file",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Delete a LibraryFile
 *
 * @ashActionType :destroy
 * @validation true
 */
export async function validate_delete_library_file_channel(config: {
  channel: Channel;
  tenant?: string;
  identity: UUID | string;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "delete_library_file",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity
  },
    config.timeout,
    config
  );
}


/**
 * Delete a LibraryFile
 *
 * @ashActionType :destroy
 */
export async function delete_library_file_channel(config: {
  channel: Channel;
  tenant?: string;
  identity: UUID;
  result_handler: (result: DeleteLibraryFileResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<DeleteLibraryFileResult>(
    config.channel,
    {
    action: "delete_library_file",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity
  },
    config.timeout,
    config
  );
}


export type GetLibraryFileInput = {
  id: UUID;
};

export type GetLibraryFileFields = UnifiedFieldSelection<LibraryFileResourceSchema>[];
export type InferGetLibraryFileResult<
  Fields extends GetLibraryFileFields,
> = InferResult<LibraryFileResourceSchema, Fields>;

export type GetLibraryFileResult<Fields extends GetLibraryFileFields> = | { success: true; data: InferGetLibraryFileResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read LibraryFile records
 *
 * @ashActionType :read
 */
export async function get_library_file<Fields extends GetLibraryFileFields>(
  config: {
  tenant?: string;
  input: GetLibraryFileInput;
  fields: Fields;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<GetLibraryFileResult<Fields>> {
  const payload = {
    action: "get_library_file",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<GetLibraryFileResult<Fields>>(
    payload,
    config
  );
}


/**
 * Validate: Read LibraryFile records
 *
 * @ashActionType :read
 * @validation true
 */
export async function validate_get_library_file(
  config: {
  tenant?: string;
  input: GetLibraryFileInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "get_library_file",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Read LibraryFile records
 *
 * @ashActionType :read
 * @validation true
 */
export async function validate_get_library_file_channel(config: {
  channel: Channel;
  tenant?: string;
  input: GetLibraryFileInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "get_library_file",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Read LibraryFile records
 *
 * @ashActionType :read
 */
export async function get_library_file_channel<Fields extends GetLibraryFileFields>(config: {
  channel: Channel;
  tenant?: string;
  input: GetLibraryFileInput;
  fields: Fields;
  result_handler: (result: GetLibraryFileResult<Fields>) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<GetLibraryFileResult<Fields>>(
    config.channel,
    {
    action: "get_library_file",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  },
    config.timeout,
    config
  );
}


export type ListLibraryFilesFields = UnifiedFieldSelection<LibraryFileResourceSchema>[];


export type InferListLibraryFilesResult<
  Fields extends ListLibraryFilesFields | undefined,
  Page extends ListLibraryFilesConfig["page"] = undefined
> = ConditionalPaginatedResult<Page, Array<InferResult<LibraryFileResourceSchema, Fields>>, {
  results: Array<InferResult<LibraryFileResourceSchema, Fields>>;
  has_more: boolean;
  limit: number;
  after: string | null;
  before: string | null;
  previous_page: string;
  next_page: string;
  count?: number | null;
  type: "keyset";
}>;

export type ListLibraryFilesConfig = {
  tenant?: string;
  fields: ListLibraryFilesFields;
  filter?: LibraryFileFilterInput;
  sort?: SortString<LibraryFileSortField> | SortString<LibraryFileSortField>[];
  page?: {
    limit?: number;
    after?: string;
    before?: string;
    offset?: never;
    count?: never;
  };
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
};

export type ListLibraryFilesResult<Fields extends ListLibraryFilesFields, Page extends ListLibraryFilesConfig["page"] = undefined> = | { success: true; data: InferListLibraryFilesResult<Fields, Page>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read LibraryFile records
 *
 * @ashActionType :read
 */
export async function list_library_files<Fields extends ListLibraryFilesFields, Config extends ListLibraryFilesConfig = ListLibraryFilesConfig>(
  config: Config & { fields: Fields }
): Promise<ListLibraryFilesResult<Fields, Config["page"]>> {
  const payload = {
    action: "list_library_files",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort }),
    ...(config.page && { page: config.page })
  };

  return executeActionRpcRequest<ListLibraryFilesResult<Fields, Config["page"]>>(
    payload,
    config
  );
}


/**
 * Validate: Read LibraryFile records
 *
 * @ashActionType :read
 * @validation true
 */
export async function validate_list_library_files(
  config: {
  tenant?: string;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "list_library_files",
    ...(config.tenant !== undefined && { tenant: config.tenant })
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Read LibraryFile records
 *
 * @ashActionType :read
 * @validation true
 */
export async function validate_list_library_files_channel(config: {
  channel: Channel;
  tenant?: string;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "list_library_files",
    ...(config.tenant !== undefined && { tenant: config.tenant })
  },
    config.timeout,
    config
  );
}


/**
 * Read LibraryFile records
 *
 * @ashActionType :read
 */
export async function list_library_files_channel<Fields extends ListLibraryFilesFields>(config: {
  channel: Channel;
  tenant?: string;
  fields: Fields;
  filter?: LibraryFileFilterInput;
  sort?: SortString<LibraryFileSortField> | SortString<LibraryFileSortField>[];
  page?: {
    limit?: number;
    after?: string;
    before?: string;
    offset?: never;
    count?: never;
  };
  result_handler: (result: ListLibraryFilesResult<Fields>) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<ListLibraryFilesResult<Fields>>(
    config.channel,
    {
    action: "list_library_files",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort }),
    ...(config.page && { page: config.page })
  },
    config.timeout,
    config
  );
}


export type ListLibraryFilesForLibraryInput = {
  library_id: UUID;
};

export type ListLibraryFilesForLibraryFields = UnifiedFieldSelection<LibraryFileResourceSchema>[];


export type InferListLibraryFilesForLibraryResult<
  Fields extends ListLibraryFilesForLibraryFields | undefined,
  Page extends ListLibraryFilesForLibraryConfig["page"] = undefined
> = ConditionalPaginatedResult<Page, Array<InferResult<LibraryFileResourceSchema, Fields>>, {
  results: Array<InferResult<LibraryFileResourceSchema, Fields>>;
  has_more: boolean;
  limit: number;
  after: string | null;
  before: string | null;
  previous_page: string;
  next_page: string;
  count?: number | null;
  type: "keyset";
}>;

export type ListLibraryFilesForLibraryConfig = {
  tenant?: string;
  input: ListLibraryFilesForLibraryInput;
  fields: ListLibraryFilesForLibraryFields;
  filter?: LibraryFileFilterInput;
  sort?: SortString<LibraryFileSortField> | SortString<LibraryFileSortField>[];
  page?: {
    limit?: number;
    after?: string;
    before?: string;
    offset?: never;
    count?: never;
  };
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
};

export type ListLibraryFilesForLibraryResult<Fields extends ListLibraryFilesForLibraryFields, Page extends ListLibraryFilesForLibraryConfig["page"] = undefined> = | { success: true; data: InferListLibraryFilesForLibraryResult<Fields, Page>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read LibraryFile records
 *
 * @ashActionType :read
 */
export async function list_library_files_for_library<Fields extends ListLibraryFilesForLibraryFields, Config extends ListLibraryFilesForLibraryConfig = ListLibraryFilesForLibraryConfig>(
  config: Config & { fields: Fields }
): Promise<ListLibraryFilesForLibraryResult<Fields, Config["page"]>> {
  const payload = {
    action: "list_library_files_for_library",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort }),
    ...(config.page && { page: config.page })
  };

  return executeActionRpcRequest<ListLibraryFilesForLibraryResult<Fields, Config["page"]>>(
    payload,
    config
  );
}


/**
 * Validate: Read LibraryFile records
 *
 * @ashActionType :read
 * @validation true
 */
export async function validate_list_library_files_for_library(
  config: {
  tenant?: string;
  input: ListLibraryFilesForLibraryInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "list_library_files_for_library",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Read LibraryFile records
 *
 * @ashActionType :read
 * @validation true
 */
export async function validate_list_library_files_for_library_channel(config: {
  channel: Channel;
  tenant?: string;
  input: ListLibraryFilesForLibraryInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "list_library_files_for_library",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Read LibraryFile records
 *
 * @ashActionType :read
 */
export async function list_library_files_for_library_channel<Fields extends ListLibraryFilesForLibraryFields>(config: {
  channel: Channel;
  tenant?: string;
  input: ListLibraryFilesForLibraryInput;
  fields: Fields;
  filter?: LibraryFileFilterInput;
  sort?: SortString<LibraryFileSortField> | SortString<LibraryFileSortField>[];
  page?: {
    limit?: number;
    after?: string;
    before?: string;
    offset?: never;
    count?: never;
  };
  result_handler: (result: ListLibraryFilesForLibraryResult<Fields>) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<ListLibraryFilesForLibraryResult<Fields>>(
    config.channel,
    {
    action: "list_library_files_for_library",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort }),
    ...(config.page && { page: config.page })
  },
    config.timeout,
    config
  );
}


export type UpdateLibraryFileInput = {
  library_id?: UUID;
  path?: string;
  item_type?: string;
  item_name?: string;
  item_content?: string | null;
  s3_key?: string | null;
  source_metadata?: Record<string, any> | null;
  sync_status?: "error" | "orphaned" | "synced" | null;
  letta_file_id?: string | null;
};

export type UpdateLibraryFileFields = UnifiedFieldSelection<LibraryFileResourceSchema>[];

export type InferUpdateLibraryFileResult<
  Fields extends UpdateLibraryFileFields | undefined,
> = InferResult<LibraryFileResourceSchema, Fields>;

export type UpdateLibraryFileResult<Fields extends UpdateLibraryFileFields | undefined = undefined> = | { success: true; data: InferUpdateLibraryFileResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing LibraryFile
 *
 * @ashActionType :update
 */
export async function update_library_file<Fields extends UpdateLibraryFileFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUID;
  input: UpdateLibraryFileInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<UpdateLibraryFileResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "update_library_file",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<UpdateLibraryFileResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


/**
 * Validate: Update an existing LibraryFile
 *
 * @ashActionType :update
 * @validation true
 */
export async function validate_update_library_file(
  config: {
  tenant?: string;
  identity: UUID | string;
  input: UpdateLibraryFileInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "update_library_file",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Update an existing LibraryFile
 *
 * @ashActionType :update
 * @validation true
 */
export async function validate_update_library_file_channel(config: {
  channel: Channel;
  tenant?: string;
  identity: UUID | string;
  input: UpdateLibraryFileInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "update_library_file",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Update an existing LibraryFile
 *
 * @ashActionType :update
 */
export async function update_library_file_channel<Fields extends UpdateLibraryFileFields | undefined = undefined>(config: {
  channel: Channel;
  tenant?: string;
  identity: UUID;
  input: UpdateLibraryFileInput;
  fields?: Fields;
  result_handler: (result: UpdateLibraryFileResult<Fields>) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<UpdateLibraryFileResult<Fields>>(
    config.channel,
    {
    action: "update_library_file",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  },
    config.timeout,
    config
  );
}


export type UpsertWebsiteScrapeLibraryFileInput = {
  library_id: UUID;
  item_url: string;
  item_name: string;
  item_content: string;
  source_metadata?: Record<string, any> | null;
};

export type UpsertWebsiteScrapeLibraryFileFields = UnifiedFieldSelection<LibraryFileResourceSchema>[];

export type InferUpsertWebsiteScrapeLibraryFileResult<
  Fields extends UpsertWebsiteScrapeLibraryFileFields | undefined,
> = InferResult<LibraryFileResourceSchema, Fields>;

export type UpsertWebsiteScrapeLibraryFileResult<Fields extends UpsertWebsiteScrapeLibraryFileFields | undefined = undefined> = | { success: true; data: InferUpsertWebsiteScrapeLibraryFileResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Execute generic action on LibraryFile
 *
 * @ashActionType :action
 */
export async function upsert_website_scrape_library_file<Fields extends UpsertWebsiteScrapeLibraryFileFields | undefined = undefined>(
  config: {
  tenant?: string;
  input: UpsertWebsiteScrapeLibraryFileInput;
  fields: Fields;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<UpsertWebsiteScrapeLibraryFileResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "upsert_website_scrape_library_file",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<UpsertWebsiteScrapeLibraryFileResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


/**
 * Validate: Execute generic action on LibraryFile
 *
 * @ashActionType :action
 * @validation true
 */
export async function validate_upsert_website_scrape_library_file(
  config: {
  tenant?: string;
  input: UpsertWebsiteScrapeLibraryFileInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "upsert_website_scrape_library_file",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Execute generic action on LibraryFile
 *
 * @ashActionType :action
 * @validation true
 */
export async function validate_upsert_website_scrape_library_file_channel(config: {
  channel: Channel;
  tenant?: string;
  input: UpsertWebsiteScrapeLibraryFileInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "upsert_website_scrape_library_file",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Execute generic action on LibraryFile
 *
 * @ashActionType :action
 */
export async function upsert_website_scrape_library_file_channel<Fields extends UpsertWebsiteScrapeLibraryFileFields | undefined = undefined>(config: {
  channel: Channel;
  tenant?: string;
  input: UpsertWebsiteScrapeLibraryFileInput;
  fields: Fields;
  result_handler: (result: UpsertWebsiteScrapeLibraryFileResult<Fields>) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<UpsertWebsiteScrapeLibraryFileResult<Fields>>(
    config.channel,
    {
    action: "upsert_website_scrape_library_file",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  },
    config.timeout,
    config
  );
}


export type ResolveVideoPlaybackInput = {
  path: string;
  origin_entity_id?: UUID | null;
};

export type ResolveVideoPlaybackFields = UnifiedFieldSelection<ResolvedPlaybackResourceSchema>[];

export type InferResolveVideoPlaybackResult<
  Fields extends ResolveVideoPlaybackFields | undefined,
> = InferResult<ResolvedPlaybackResourceSchema, Fields>;

export type ResolveVideoPlaybackResult<Fields extends ResolveVideoPlaybackFields | undefined = undefined> = | { success: true; data: InferResolveVideoPlaybackResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Execute generic action on VideoAsset
 *
 * @ashActionType :action
 */
export async function resolve_video_playback<Fields extends ResolveVideoPlaybackFields | undefined = undefined>(
  config: {
  tenant?: string;
  input: ResolveVideoPlaybackInput;
  fields: Fields;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ResolveVideoPlaybackResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "resolve_video_playback",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<ResolveVideoPlaybackResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


/**
 * Validate: Execute generic action on VideoAsset
 *
 * @ashActionType :action
 * @validation true
 */
export async function validate_resolve_video_playback(
  config: {
  tenant?: string;
  input: ResolveVideoPlaybackInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "resolve_video_playback",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Execute generic action on VideoAsset
 *
 * @ashActionType :action
 * @validation true
 */
export async function validate_resolve_video_playback_channel(config: {
  channel: Channel;
  tenant?: string;
  input: ResolveVideoPlaybackInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "resolve_video_playback",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Execute generic action on VideoAsset
 *
 * @ashActionType :action
 */
export async function resolve_video_playback_channel<Fields extends ResolveVideoPlaybackFields | undefined = undefined>(config: {
  channel: Channel;
  tenant?: string;
  input: ResolveVideoPlaybackInput;
  fields: Fields;
  result_handler: (result: ResolveVideoPlaybackResult<Fields>) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<ResolveVideoPlaybackResult<Fields>>(
    config.channel,
    {
    action: "resolve_video_playback",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  },
    config.timeout,
    config
  );
}


export type DestroyChatBindingResult = | { success: true; data: {}; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Delete a ChatBinding
 *
 * @ashActionType :destroy
 */
export async function destroy_chat_binding(
  config: {
  tenant?: string;
  identity: UUID;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<DestroyChatBindingResult> {
  const payload = {
    action: "destroy_chat_binding",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity
  };

  return executeActionRpcRequest<DestroyChatBindingResult>(
    payload,
    config
  );
}


/**
 * Validate: Delete a ChatBinding
 *
 * @ashActionType :destroy
 * @validation true
 */
export async function validate_destroy_chat_binding(
  config: {
  tenant?: string;
  identity: UUID | string;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "destroy_chat_binding",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Delete a ChatBinding
 *
 * @ashActionType :destroy
 * @validation true
 */
export async function validate_destroy_chat_binding_channel(config: {
  channel: Channel;
  tenant?: string;
  identity: UUID | string;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "destroy_chat_binding",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity
  },
    config.timeout,
    config
  );
}


/**
 * Delete a ChatBinding
 *
 * @ashActionType :destroy
 */
export async function destroy_chat_binding_channel(config: {
  channel: Channel;
  tenant?: string;
  identity: UUID;
  result_handler: (result: DestroyChatBindingResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<DestroyChatBindingResult>(
    config.channel,
    {
    action: "destroy_chat_binding",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity
  },
    config.timeout,
    config
  );
}


export type GetBindingPreviewInput = {
  origin_entity_id: UUID;
};

export type InferGetBindingPreviewResult = Record<string, any>;

export type GetBindingPreviewResult = | { success: true; data: InferGetBindingPreviewResult; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Execute generic action on ChatBinding
 *
 * @ashActionType :action
 */
export async function get_binding_preview(
  config: {
  tenant?: string;
  input: GetBindingPreviewInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<GetBindingPreviewResult> {
  const payload = {
    action: "get_binding_preview",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeActionRpcRequest<GetBindingPreviewResult>(
    payload,
    config
  );
}


/**
 * Validate: Execute generic action on ChatBinding
 *
 * @ashActionType :action
 * @validation true
 */
export async function validate_get_binding_preview(
  config: {
  tenant?: string;
  input: GetBindingPreviewInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "get_binding_preview",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Execute generic action on ChatBinding
 *
 * @ashActionType :action
 * @validation true
 */
export async function validate_get_binding_preview_channel(config: {
  channel: Channel;
  tenant?: string;
  input: GetBindingPreviewInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "get_binding_preview",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Execute generic action on ChatBinding
 *
 * @ashActionType :action
 */
export async function get_binding_preview_channel(config: {
  channel: Channel;
  tenant?: string;
  input: GetBindingPreviewInput;
  result_handler: (result: GetBindingPreviewResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<GetBindingPreviewResult>(
    config.channel,
    {
    action: "get_binding_preview",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


export type ListAllChatBindingsFields = UnifiedFieldSelection<ChatBindingResourceSchema>[];


export type InferListAllChatBindingsResult<
  Fields extends ListAllChatBindingsFields | undefined,
  Page extends ListAllChatBindingsConfig["page"] = undefined
> = ConditionalPaginatedResultMixed<Page, Array<InferResult<ChatBindingResourceSchema, Fields>>, {
  results: Array<InferResult<ChatBindingResourceSchema, Fields>>;
  has_more: boolean;
  limit: number;
  offset: number;
  count?: number | null;
  type: "offset";
}, {
  results: Array<InferResult<ChatBindingResourceSchema, Fields>>;
  has_more: boolean;
  limit: number;
  after: string | null;
  before: string | null;
  previous_page: string;
  next_page: string;
  count?: number | null;
  type: "keyset";
}>;

export type ListAllChatBindingsConfig = {
  tenant?: string;
  fields: ListAllChatBindingsFields;
  filter?: ChatBindingFilterInput;
  sort?: SortString<ChatBindingSortField> | SortString<ChatBindingSortField>[];
  page?: (
    {
      limit?: number;
      offset?: number;
      count?: boolean;
    } | {
      limit?: number;
      after?: string;
      before?: string;
    }
  );
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
};

export type ListAllChatBindingsResult<Fields extends ListAllChatBindingsFields, Page extends ListAllChatBindingsConfig["page"] = undefined> = | { success: true; data: InferListAllChatBindingsResult<Fields, Page>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read ChatBinding records
 *
 * @ashActionType :read
 */
export async function list_all_chat_bindings<Fields extends ListAllChatBindingsFields, Config extends ListAllChatBindingsConfig = ListAllChatBindingsConfig>(
  config: Config & { fields: Fields }
): Promise<ListAllChatBindingsResult<Fields, Config["page"]>> {
  const payload = {
    action: "list_all_chat_bindings",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort }),
    ...(config.page && { page: config.page })
  };

  return executeActionRpcRequest<ListAllChatBindingsResult<Fields, Config["page"]>>(
    payload,
    config
  );
}


/**
 * Validate: Read ChatBinding records
 *
 * @ashActionType :read
 * @validation true
 */
export async function validate_list_all_chat_bindings(
  config: {
  tenant?: string;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "list_all_chat_bindings",
    ...(config.tenant !== undefined && { tenant: config.tenant })
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Read ChatBinding records
 *
 * @ashActionType :read
 * @validation true
 */
export async function validate_list_all_chat_bindings_channel(config: {
  channel: Channel;
  tenant?: string;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "list_all_chat_bindings",
    ...(config.tenant !== undefined && { tenant: config.tenant })
  },
    config.timeout,
    config
  );
}


/**
 * Read ChatBinding records
 *
 * @ashActionType :read
 */
export async function list_all_chat_bindings_channel<Fields extends ListAllChatBindingsFields>(config: {
  channel: Channel;
  tenant?: string;
  fields: Fields;
  filter?: ChatBindingFilterInput;
  sort?: SortString<ChatBindingSortField> | SortString<ChatBindingSortField>[];
  page?: (
    {
      limit?: number;
      offset?: number;
      count?: boolean;
    } | {
      limit?: number;
      after?: string;
      before?: string;
    }
  );
  result_handler: (result: ListAllChatBindingsResult<Fields>) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<ListAllChatBindingsResult<Fields>>(
    config.channel,
    {
    action: "list_all_chat_bindings",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort }),
    ...(config.page && { page: config.page })
  },
    config.timeout,
    config
  );
}


export type ListChatBindingsForOriginEntityInput = {
  origin_entity_id: UUID;
};

export type ListChatBindingsForOriginEntityFields = UnifiedFieldSelection<ChatBindingResourceSchema>[];
export type InferListChatBindingsForOriginEntityResult<
  Fields extends ListChatBindingsForOriginEntityFields,
> = Array<InferResult<ChatBindingResourceSchema, Fields>>;

export type ListChatBindingsForOriginEntityResult<Fields extends ListChatBindingsForOriginEntityFields> = | { success: true; data: InferListChatBindingsForOriginEntityResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read ChatBinding records
 *
 * @ashActionType :read
 */
export async function list_chat_bindings_for_origin_entity<Fields extends ListChatBindingsForOriginEntityFields>(
  config: {
  tenant?: string;
  input: ListChatBindingsForOriginEntityInput;
  fields: Fields;
  filter?: ChatBindingFilterInput;
  sort?: SortString<ChatBindingSortField> | SortString<ChatBindingSortField>[];
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ListChatBindingsForOriginEntityResult<Fields>> {
  const payload = {
    action: "list_chat_bindings_for_origin_entity",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort })
  };

  return executeActionRpcRequest<ListChatBindingsForOriginEntityResult<Fields>>(
    payload,
    config
  );
}


/**
 * Validate: Read ChatBinding records
 *
 * @ashActionType :read
 * @validation true
 */
export async function validate_list_chat_bindings_for_origin_entity(
  config: {
  tenant?: string;
  input: ListChatBindingsForOriginEntityInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "list_chat_bindings_for_origin_entity",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Read ChatBinding records
 *
 * @ashActionType :read
 * @validation true
 */
export async function validate_list_chat_bindings_for_origin_entity_channel(config: {
  channel: Channel;
  tenant?: string;
  input: ListChatBindingsForOriginEntityInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "list_chat_bindings_for_origin_entity",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Read ChatBinding records
 *
 * @ashActionType :read
 */
export async function list_chat_bindings_for_origin_entity_channel<Fields extends ListChatBindingsForOriginEntityFields>(config: {
  channel: Channel;
  tenant?: string;
  input: ListChatBindingsForOriginEntityInput;
  fields: Fields;
  filter?: ChatBindingFilterInput;
  sort?: SortString<ChatBindingSortField> | SortString<ChatBindingSortField>[];
  result_handler: (result: ListChatBindingsForOriginEntityResult<Fields>) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<ListChatBindingsForOriginEntityResult<Fields>>(
    config.channel,
    {
    action: "list_chat_bindings_for_origin_entity",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort })
  },
    config.timeout,
    config
  );
}


export type RegisterProspectInput = {
  origin_entity_id: UUID;
  prospect_labels?: Record<string, any>;
  notes?: string | null;
  prospect_phones: Array<string>;
};

export type RegisterProspectFields = UnifiedFieldSelection<ChatBindingResourceSchema>[];

export type InferRegisterProspectResult<
  Fields extends RegisterProspectFields | undefined,
> = InferResult<ChatBindingResourceSchema, Fields>;

export type RegisterProspectResult<Fields extends RegisterProspectFields | undefined = undefined> = | { success: true; data: InferRegisterProspectResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Create a new ChatBinding
 *
 * @ashActionType :create
 */
export async function register_prospect<Fields extends RegisterProspectFields | undefined = undefined>(
  config: {
  tenant?: string;
  input: RegisterProspectInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<RegisterProspectResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "register_prospect",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<RegisterProspectResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


/**
 * Validate: Create a new ChatBinding
 *
 * @ashActionType :create
 * @validation true
 */
export async function validate_register_prospect(
  config: {
  tenant?: string;
  input: RegisterProspectInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "register_prospect",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Create a new ChatBinding
 *
 * @ashActionType :create
 * @validation true
 */
export async function validate_register_prospect_channel(config: {
  channel: Channel;
  tenant?: string;
  input: RegisterProspectInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "register_prospect",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Create a new ChatBinding
 *
 * @ashActionType :create
 */
export async function register_prospect_channel<Fields extends RegisterProspectFields | undefined = undefined>(config: {
  channel: Channel;
  tenant?: string;
  input: RegisterProspectInput;
  fields?: Fields;
  result_handler: (result: RegisterProspectResult<Fields>) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<RegisterProspectResult<Fields>>(
    config.channel,
    {
    action: "register_prospect",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  },
    config.timeout,
    config
  );
}


export type ResolveMessagingLineInput = {
  origin_entity_id: UUID;
};

export type InferResolveMessagingLineResult = Record<string, any>;

export type ResolveMessagingLineResult = | { success: true; data: InferResolveMessagingLineResult; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Execute generic action on ChatBinding
 *
 * @ashActionType :action
 */
export async function resolve_messaging_line(
  config: {
  tenant?: string;
  input: ResolveMessagingLineInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ResolveMessagingLineResult> {
  const payload = {
    action: "resolve_messaging_line",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeActionRpcRequest<ResolveMessagingLineResult>(
    payload,
    config
  );
}


/**
 * Validate: Execute generic action on ChatBinding
 *
 * @ashActionType :action
 * @validation true
 */
export async function validate_resolve_messaging_line(
  config: {
  tenant?: string;
  input: ResolveMessagingLineInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "resolve_messaging_line",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Execute generic action on ChatBinding
 *
 * @ashActionType :action
 * @validation true
 */
export async function validate_resolve_messaging_line_channel(config: {
  channel: Channel;
  tenant?: string;
  input: ResolveMessagingLineInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "resolve_messaging_line",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Execute generic action on ChatBinding
 *
 * @ashActionType :action
 */
export async function resolve_messaging_line_channel(config: {
  channel: Channel;
  tenant?: string;
  input: ResolveMessagingLineInput;
  result_handler: (result: ResolveMessagingLineResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<ResolveMessagingLineResult>(
    config.channel,
    {
    action: "resolve_messaging_line",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


export type CreatePodcastConfigInput = {
  page_subtitle?: string | null;
  featured_video_url?: string | null;
  hero_cta_link?: string | null;
  playlist_id?: string | null;
  episodes?: Array<Record<string, any>> | null;
  appearance_config?: Record<string, any> | null;
  is_public?: boolean | null;
  trailer_url?: string | null;
  conversation_starter_config?: Record<string, any> | null;
  origins_button_visible?: boolean | null;
  page_title?: string | null;
  hero_cta_text?: string | null;
  logo_link_url?: string | null;
  cta_button_border_color?: string | null;
  origins_button_text?: string | null;
  origins_button_border_color?: string | null;
  origins_button_text_color?: string | null;
  logo_link_type?: string | null;
  entity_profile_id?: UUID | null;
};

export type CreatePodcastConfigFields = UnifiedFieldSelection<PodcastConfigResourceSchema>[];

export type InferCreatePodcastConfigResult<
  Fields extends CreatePodcastConfigFields | undefined,
> = InferResult<PodcastConfigResourceSchema, Fields>;

export type CreatePodcastConfigResult<Fields extends CreatePodcastConfigFields | undefined = undefined> = | { success: true; data: InferCreatePodcastConfigResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Create a new PodcastConfig
 *
 * @ashActionType :create
 */
export async function create_podcast_config<Fields extends CreatePodcastConfigFields | undefined = undefined>(
  config: {
  tenant?: string;
  input?: CreatePodcastConfigInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<CreatePodcastConfigResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "create_podcast_config",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<CreatePodcastConfigResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


/**
 * Validate: Create a new PodcastConfig
 *
 * @ashActionType :create
 * @validation true
 */
export async function validate_create_podcast_config(
  config: {
  tenant?: string;
  input?: CreatePodcastConfigInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "create_podcast_config",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Create a new PodcastConfig
 *
 * @ashActionType :create
 * @validation true
 */
export async function validate_create_podcast_config_channel(config: {
  channel: Channel;
  tenant?: string;
  input?: CreatePodcastConfigInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "create_podcast_config",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Create a new PodcastConfig
 *
 * @ashActionType :create
 */
export async function create_podcast_config_channel<Fields extends CreatePodcastConfigFields | undefined = undefined>(config: {
  channel: Channel;
  tenant?: string;
  input?: CreatePodcastConfigInput;
  fields?: Fields;
  result_handler: (result: CreatePodcastConfigResult<Fields>) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<CreatePodcastConfigResult<Fields>>(
    config.channel,
    {
    action: "create_podcast_config",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  },
    config.timeout,
    config
  );
}


export type ListPodcastConfigsFields = UnifiedFieldSelection<PodcastConfigResourceSchema>[];


export type InferListPodcastConfigsResult<
  Fields extends ListPodcastConfigsFields | undefined,
  Page extends ListPodcastConfigsConfig["page"] = undefined
> = ConditionalPaginatedResultMixed<Page, Array<InferResult<PodcastConfigResourceSchema, Fields>>, {
  results: Array<InferResult<PodcastConfigResourceSchema, Fields>>;
  has_more: boolean;
  limit: number;
  offset: number;
  count?: number | null;
  type: "offset";
}, {
  results: Array<InferResult<PodcastConfigResourceSchema, Fields>>;
  has_more: boolean;
  limit: number;
  after: string | null;
  before: string | null;
  previous_page: string;
  next_page: string;
  count?: number | null;
  type: "keyset";
}>;

export type ListPodcastConfigsConfig = {
  tenant?: string;
  fields: ListPodcastConfigsFields;
  filter?: PodcastConfigFilterInput;
  sort?: SortString<PodcastConfigSortField> | SortString<PodcastConfigSortField>[];
  page?: (
    {
      limit?: number;
      offset?: number;
      count?: boolean;
    } | {
      limit?: number;
      after?: string;
      before?: string;
    }
  );
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
};

export type ListPodcastConfigsResult<Fields extends ListPodcastConfigsFields, Page extends ListPodcastConfigsConfig["page"] = undefined> = | { success: true; data: InferListPodcastConfigsResult<Fields, Page>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read PodcastConfig records
 *
 * @ashActionType :read
 */
export async function list_podcast_configs<Fields extends ListPodcastConfigsFields, Config extends ListPodcastConfigsConfig = ListPodcastConfigsConfig>(
  config: Config & { fields: Fields }
): Promise<ListPodcastConfigsResult<Fields, Config["page"]>> {
  const payload = {
    action: "list_podcast_configs",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort }),
    ...(config.page && { page: config.page })
  };

  return executeActionRpcRequest<ListPodcastConfigsResult<Fields, Config["page"]>>(
    payload,
    config
  );
}


/**
 * Validate: Read PodcastConfig records
 *
 * @ashActionType :read
 * @validation true
 */
export async function validate_list_podcast_configs(
  config: {
  tenant?: string;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "list_podcast_configs",
    ...(config.tenant !== undefined && { tenant: config.tenant })
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Read PodcastConfig records
 *
 * @ashActionType :read
 * @validation true
 */
export async function validate_list_podcast_configs_channel(config: {
  channel: Channel;
  tenant?: string;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "list_podcast_configs",
    ...(config.tenant !== undefined && { tenant: config.tenant })
  },
    config.timeout,
    config
  );
}


/**
 * Read PodcastConfig records
 *
 * @ashActionType :read
 */
export async function list_podcast_configs_channel<Fields extends ListPodcastConfigsFields>(config: {
  channel: Channel;
  tenant?: string;
  fields: Fields;
  filter?: PodcastConfigFilterInput;
  sort?: SortString<PodcastConfigSortField> | SortString<PodcastConfigSortField>[];
  page?: (
    {
      limit?: number;
      offset?: number;
      count?: boolean;
    } | {
      limit?: number;
      after?: string;
      before?: string;
    }
  );
  result_handler: (result: ListPodcastConfigsResult<Fields>) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<ListPodcastConfigsResult<Fields>>(
    config.channel,
    {
    action: "list_podcast_configs",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort }),
    ...(config.page && { page: config.page })
  },
    config.timeout,
    config
  );
}


export type SyncPodcastEpisodesInput = {
  playlist_id: string;
  entity_profile_id: UUID;
};

export type InferSyncPodcastEpisodesResult = Record<string, any>;

export type SyncPodcastEpisodesResult = | { success: true; data: InferSyncPodcastEpisodesResult; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Execute generic action on PodcastConfig
 *
 * @ashActionType :action
 */
export async function sync_podcast_episodes(
  config: {
  tenant?: string;
  input: SyncPodcastEpisodesInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<SyncPodcastEpisodesResult> {
  const payload = {
    action: "sync_podcast_episodes",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeActionRpcRequest<SyncPodcastEpisodesResult>(
    payload,
    config
  );
}


/**
 * Validate: Execute generic action on PodcastConfig
 *
 * @ashActionType :action
 * @validation true
 */
export async function validate_sync_podcast_episodes(
  config: {
  tenant?: string;
  input: SyncPodcastEpisodesInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "sync_podcast_episodes",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Execute generic action on PodcastConfig
 *
 * @ashActionType :action
 * @validation true
 */
export async function validate_sync_podcast_episodes_channel(config: {
  channel: Channel;
  tenant?: string;
  input: SyncPodcastEpisodesInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "sync_podcast_episodes",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Execute generic action on PodcastConfig
 *
 * @ashActionType :action
 */
export async function sync_podcast_episodes_channel(config: {
  channel: Channel;
  tenant?: string;
  input: SyncPodcastEpisodesInput;
  result_handler: (result: SyncPodcastEpisodesResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<SyncPodcastEpisodesResult>(
    config.channel,
    {
    action: "sync_podcast_episodes",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


export type UpdatePodcastConfigInput = {
  page_subtitle?: string | null;
  featured_video_url?: string | null;
  hero_cta_link?: string | null;
  playlist_id?: string | null;
  episodes?: Array<Record<string, any>> | null;
  appearance_config?: Record<string, any> | null;
  is_public?: boolean | null;
  trailer_url?: string | null;
  conversation_starter_config?: Record<string, any> | null;
  origins_button_visible?: boolean | null;
  page_title?: string | null;
  hero_cta_text?: string | null;
  logo_link_url?: string | null;
  cta_button_border_color?: string | null;
  origins_button_text?: string | null;
  origins_button_border_color?: string | null;
  origins_button_text_color?: string | null;
  logo_link_type?: string | null;
  entity_profile_id?: UUID | null;
};

export type UpdatePodcastConfigFields = UnifiedFieldSelection<PodcastConfigResourceSchema>[];

export type InferUpdatePodcastConfigResult<
  Fields extends UpdatePodcastConfigFields | undefined,
> = InferResult<PodcastConfigResourceSchema, Fields>;

export type UpdatePodcastConfigResult<Fields extends UpdatePodcastConfigFields | undefined = undefined> = | { success: true; data: InferUpdatePodcastConfigResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing PodcastConfig
 *
 * @ashActionType :update
 */
export async function update_podcast_config<Fields extends UpdatePodcastConfigFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUID;
  input?: UpdatePodcastConfigInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<UpdatePodcastConfigResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "update_podcast_config",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<UpdatePodcastConfigResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


/**
 * Validate: Update an existing PodcastConfig
 *
 * @ashActionType :update
 * @validation true
 */
export async function validate_update_podcast_config(
  config: {
  tenant?: string;
  identity: UUID | string;
  input?: UpdatePodcastConfigInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "update_podcast_config",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Update an existing PodcastConfig
 *
 * @ashActionType :update
 * @validation true
 */
export async function validate_update_podcast_config_channel(config: {
  channel: Channel;
  tenant?: string;
  identity: UUID | string;
  input?: UpdatePodcastConfigInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "update_podcast_config",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Update an existing PodcastConfig
 *
 * @ashActionType :update
 */
export async function update_podcast_config_channel<Fields extends UpdatePodcastConfigFields | undefined = undefined>(config: {
  channel: Channel;
  tenant?: string;
  identity: UUID;
  input?: UpdatePodcastConfigInput;
  fields?: Fields;
  result_handler: (result: UpdatePodcastConfigResult<Fields>) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<UpdatePodcastConfigResult<Fields>>(
    config.channel,
    {
    action: "update_podcast_config",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  },
    config.timeout,
    config
  );
}


export type GetPodcastEpisodeSettingsForCharacterInput = {
  character_id: UUID;
};

export type GetPodcastEpisodeSettingsForCharacterFields = UnifiedFieldSelection<PodcastEpisodeSettingsResourceSchema>[];
export type InferGetPodcastEpisodeSettingsForCharacterResult<
  Fields extends GetPodcastEpisodeSettingsForCharacterFields,
> = Array<InferResult<PodcastEpisodeSettingsResourceSchema, Fields>>;

export type GetPodcastEpisodeSettingsForCharacterResult<Fields extends GetPodcastEpisodeSettingsForCharacterFields> = | { success: true; data: InferGetPodcastEpisodeSettingsForCharacterResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read PodcastEpisodeSettings records
 *
 * @ashActionType :read
 */
export async function get_podcast_episode_settings_for_character<Fields extends GetPodcastEpisodeSettingsForCharacterFields>(
  config: {
  tenant?: string;
  input: GetPodcastEpisodeSettingsForCharacterInput;
  fields: Fields;
  filter?: PodcastEpisodeSettingsFilterInput;
  sort?: SortString<PodcastEpisodeSettingsSortField> | SortString<PodcastEpisodeSettingsSortField>[];
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<GetPodcastEpisodeSettingsForCharacterResult<Fields>> {
  const payload = {
    action: "get_podcast_episode_settings_for_character",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort })
  };

  return executeActionRpcRequest<GetPodcastEpisodeSettingsForCharacterResult<Fields>>(
    payload,
    config
  );
}


/**
 * Validate: Read PodcastEpisodeSettings records
 *
 * @ashActionType :read
 * @validation true
 */
export async function validate_get_podcast_episode_settings_for_character(
  config: {
  tenant?: string;
  input: GetPodcastEpisodeSettingsForCharacterInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "get_podcast_episode_settings_for_character",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Read PodcastEpisodeSettings records
 *
 * @ashActionType :read
 * @validation true
 */
export async function validate_get_podcast_episode_settings_for_character_channel(config: {
  channel: Channel;
  tenant?: string;
  input: GetPodcastEpisodeSettingsForCharacterInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "get_podcast_episode_settings_for_character",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Read PodcastEpisodeSettings records
 *
 * @ashActionType :read
 */
export async function get_podcast_episode_settings_for_character_channel<Fields extends GetPodcastEpisodeSettingsForCharacterFields>(config: {
  channel: Channel;
  tenant?: string;
  input: GetPodcastEpisodeSettingsForCharacterInput;
  fields: Fields;
  filter?: PodcastEpisodeSettingsFilterInput;
  sort?: SortString<PodcastEpisodeSettingsSortField> | SortString<PodcastEpisodeSettingsSortField>[];
  result_handler: (result: GetPodcastEpisodeSettingsForCharacterResult<Fields>) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<GetPodcastEpisodeSettingsForCharacterResult<Fields>>(
    config.channel,
    {
    action: "get_podcast_episode_settings_for_character",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort })
  },
    config.timeout,
    config
  );
}


export type GetPodcastEpisodeSettingsForPodcastConfigInput = {
  podcast_config_id: UUID;
};

export type GetPodcastEpisodeSettingsForPodcastConfigFields = UnifiedFieldSelection<PodcastEpisodeSettingsResourceSchema>[];
export type InferGetPodcastEpisodeSettingsForPodcastConfigResult<
  Fields extends GetPodcastEpisodeSettingsForPodcastConfigFields,
> = Array<InferResult<PodcastEpisodeSettingsResourceSchema, Fields>>;

export type GetPodcastEpisodeSettingsForPodcastConfigResult<Fields extends GetPodcastEpisodeSettingsForPodcastConfigFields> = | { success: true; data: InferGetPodcastEpisodeSettingsForPodcastConfigResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read PodcastEpisodeSettings records
 *
 * @ashActionType :read
 */
export async function get_podcast_episode_settings_for_podcast_config<Fields extends GetPodcastEpisodeSettingsForPodcastConfigFields>(
  config: {
  tenant?: string;
  input: GetPodcastEpisodeSettingsForPodcastConfigInput;
  fields: Fields;
  filter?: PodcastEpisodeSettingsFilterInput;
  sort?: SortString<PodcastEpisodeSettingsSortField> | SortString<PodcastEpisodeSettingsSortField>[];
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<GetPodcastEpisodeSettingsForPodcastConfigResult<Fields>> {
  const payload = {
    action: "get_podcast_episode_settings_for_podcast_config",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort })
  };

  return executeActionRpcRequest<GetPodcastEpisodeSettingsForPodcastConfigResult<Fields>>(
    payload,
    config
  );
}


/**
 * Validate: Read PodcastEpisodeSettings records
 *
 * @ashActionType :read
 * @validation true
 */
export async function validate_get_podcast_episode_settings_for_podcast_config(
  config: {
  tenant?: string;
  input: GetPodcastEpisodeSettingsForPodcastConfigInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "get_podcast_episode_settings_for_podcast_config",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Read PodcastEpisodeSettings records
 *
 * @ashActionType :read
 * @validation true
 */
export async function validate_get_podcast_episode_settings_for_podcast_config_channel(config: {
  channel: Channel;
  tenant?: string;
  input: GetPodcastEpisodeSettingsForPodcastConfigInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "get_podcast_episode_settings_for_podcast_config",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Read PodcastEpisodeSettings records
 *
 * @ashActionType :read
 */
export async function get_podcast_episode_settings_for_podcast_config_channel<Fields extends GetPodcastEpisodeSettingsForPodcastConfigFields>(config: {
  channel: Channel;
  tenant?: string;
  input: GetPodcastEpisodeSettingsForPodcastConfigInput;
  fields: Fields;
  filter?: PodcastEpisodeSettingsFilterInput;
  sort?: SortString<PodcastEpisodeSettingsSortField> | SortString<PodcastEpisodeSettingsSortField>[];
  result_handler: (result: GetPodcastEpisodeSettingsForPodcastConfigResult<Fields>) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<GetPodcastEpisodeSettingsForPodcastConfigResult<Fields>>(
    config.channel,
    {
    action: "get_podcast_episode_settings_for_podcast_config",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort })
  },
    config.timeout,
    config
  );
}


export type ListPodcastEpisodeSettingsFields = UnifiedFieldSelection<PodcastEpisodeSettingsResourceSchema>[];


export type InferListPodcastEpisodeSettingsResult<
  Fields extends ListPodcastEpisodeSettingsFields | undefined,
  Page extends ListPodcastEpisodeSettingsConfig["page"] = undefined
> = ConditionalPaginatedResultMixed<Page, Array<InferResult<PodcastEpisodeSettingsResourceSchema, Fields>>, {
  results: Array<InferResult<PodcastEpisodeSettingsResourceSchema, Fields>>;
  has_more: boolean;
  limit: number;
  offset: number;
  count?: number | null;
  type: "offset";
}, {
  results: Array<InferResult<PodcastEpisodeSettingsResourceSchema, Fields>>;
  has_more: boolean;
  limit: number;
  after: string | null;
  before: string | null;
  previous_page: string;
  next_page: string;
  count?: number | null;
  type: "keyset";
}>;

export type ListPodcastEpisodeSettingsConfig = {
  tenant?: string;
  fields: ListPodcastEpisodeSettingsFields;
  filter?: PodcastEpisodeSettingsFilterInput;
  sort?: SortString<PodcastEpisodeSettingsSortField> | SortString<PodcastEpisodeSettingsSortField>[];
  page?: (
    {
      limit?: number;
      offset?: number;
      count?: boolean;
    } | {
      limit?: number;
      after?: string;
      before?: string;
    }
  );
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
};

export type ListPodcastEpisodeSettingsResult<Fields extends ListPodcastEpisodeSettingsFields, Page extends ListPodcastEpisodeSettingsConfig["page"] = undefined> = | { success: true; data: InferListPodcastEpisodeSettingsResult<Fields, Page>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read PodcastEpisodeSettings records
 *
 * @ashActionType :read
 */
export async function list_podcast_episode_settings<Fields extends ListPodcastEpisodeSettingsFields, Config extends ListPodcastEpisodeSettingsConfig = ListPodcastEpisodeSettingsConfig>(
  config: Config & { fields: Fields }
): Promise<ListPodcastEpisodeSettingsResult<Fields, Config["page"]>> {
  const payload = {
    action: "list_podcast_episode_settings",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort }),
    ...(config.page && { page: config.page })
  };

  return executeActionRpcRequest<ListPodcastEpisodeSettingsResult<Fields, Config["page"]>>(
    payload,
    config
  );
}


/**
 * Validate: Read PodcastEpisodeSettings records
 *
 * @ashActionType :read
 * @validation true
 */
export async function validate_list_podcast_episode_settings(
  config: {
  tenant?: string;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "list_podcast_episode_settings",
    ...(config.tenant !== undefined && { tenant: config.tenant })
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Read PodcastEpisodeSettings records
 *
 * @ashActionType :read
 * @validation true
 */
export async function validate_list_podcast_episode_settings_channel(config: {
  channel: Channel;
  tenant?: string;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "list_podcast_episode_settings",
    ...(config.tenant !== undefined && { tenant: config.tenant })
  },
    config.timeout,
    config
  );
}


/**
 * Read PodcastEpisodeSettings records
 *
 * @ashActionType :read
 */
export async function list_podcast_episode_settings_channel<Fields extends ListPodcastEpisodeSettingsFields>(config: {
  channel: Channel;
  tenant?: string;
  fields: Fields;
  filter?: PodcastEpisodeSettingsFilterInput;
  sort?: SortString<PodcastEpisodeSettingsSortField> | SortString<PodcastEpisodeSettingsSortField>[];
  page?: (
    {
      limit?: number;
      offset?: number;
      count?: boolean;
    } | {
      limit?: number;
      after?: string;
      before?: string;
    }
  );
  result_handler: (result: ListPodcastEpisodeSettingsResult<Fields>) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<ListPodcastEpisodeSettingsResult<Fields>>(
    config.channel,
    {
    action: "list_podcast_episode_settings",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort }),
    ...(config.page && { page: config.page })
  },
    config.timeout,
    config
  );
}


export type UpsertPodcastEpisodeSettingsInput = {
  visible?: boolean;
  ai_enabled?: boolean;
  order?: number;
  youtube_episode_id: UUID;
  podcast_config_id?: UUID | null;
  character_id?: UUID | null;
};

export type UpsertPodcastEpisodeSettingsFields = UnifiedFieldSelection<PodcastEpisodeSettingsResourceSchema>[];

export type InferUpsertPodcastEpisodeSettingsResult<
  Fields extends UpsertPodcastEpisodeSettingsFields | undefined,
> = InferResult<PodcastEpisodeSettingsResourceSchema, Fields>;

export type UpsertPodcastEpisodeSettingsResult<Fields extends UpsertPodcastEpisodeSettingsFields | undefined = undefined> = | { success: true; data: InferUpsertPodcastEpisodeSettingsResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Create a new PodcastEpisodeSettings
 *
 * @ashActionType :create
 */
export async function upsert_podcast_episode_settings<Fields extends UpsertPodcastEpisodeSettingsFields | undefined = undefined>(
  config: {
  tenant?: string;
  input: UpsertPodcastEpisodeSettingsInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<UpsertPodcastEpisodeSettingsResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "upsert_podcast_episode_settings",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<UpsertPodcastEpisodeSettingsResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


/**
 * Validate: Create a new PodcastEpisodeSettings
 *
 * @ashActionType :create
 * @validation true
 */
export async function validate_upsert_podcast_episode_settings(
  config: {
  tenant?: string;
  input: UpsertPodcastEpisodeSettingsInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "upsert_podcast_episode_settings",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Create a new PodcastEpisodeSettings
 *
 * @ashActionType :create
 * @validation true
 */
export async function validate_upsert_podcast_episode_settings_channel(config: {
  channel: Channel;
  tenant?: string;
  input: UpsertPodcastEpisodeSettingsInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "upsert_podcast_episode_settings",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Create a new PodcastEpisodeSettings
 *
 * @ashActionType :create
 */
export async function upsert_podcast_episode_settings_channel<Fields extends UpsertPodcastEpisodeSettingsFields | undefined = undefined>(config: {
  channel: Channel;
  tenant?: string;
  input: UpsertPodcastEpisodeSettingsInput;
  fields?: Fields;
  result_handler: (result: UpsertPodcastEpisodeSettingsResult<Fields>) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<UpsertPodcastEpisodeSettingsResult<Fields>>(
    config.channel,
    {
    action: "upsert_podcast_episode_settings",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  },
    config.timeout,
    config
  );
}


export type CreateYoutubeEpisodeInput = {
  video_id: string;
  video_url: string;
  title: string;
  description?: string | null;
  thumbnail_url?: string | null;
  publish_date?: UtcDateTimeUsec | null;
  duration?: string | null;
  episode_number?: number | null;
  host_ids?: Array<UUID> | null;
  guest_ids?: Array<UUID> | null;
  guest_names?: Array<string> | null;
  conversation_starters?: Array<string> | null;
  used_in_podcast?: boolean | null;
  used_in_public_profile?: boolean | null;
  public_profile_config?: Record<string, any> | null;
  playlist_id?: string | null;
  origin_entity_id: UUID;
  transcript_id?: UUID | null;
};

export type CreateYoutubeEpisodeFields = UnifiedFieldSelection<YoutubeEpisodeResourceSchema>[];

export type InferCreateYoutubeEpisodeResult<
  Fields extends CreateYoutubeEpisodeFields | undefined,
> = InferResult<YoutubeEpisodeResourceSchema, Fields>;

export type CreateYoutubeEpisodeResult<Fields extends CreateYoutubeEpisodeFields | undefined = undefined> = | { success: true; data: InferCreateYoutubeEpisodeResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Create a new YoutubeEpisode
 *
 * @ashActionType :create
 */
export async function create_youtube_episode<Fields extends CreateYoutubeEpisodeFields | undefined = undefined>(
  config: {
  tenant?: string;
  input: CreateYoutubeEpisodeInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<CreateYoutubeEpisodeResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "create_youtube_episode",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<CreateYoutubeEpisodeResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


/**
 * Validate: Create a new YoutubeEpisode
 *
 * @ashActionType :create
 * @validation true
 */
export async function validate_create_youtube_episode(
  config: {
  tenant?: string;
  input: CreateYoutubeEpisodeInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "create_youtube_episode",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Create a new YoutubeEpisode
 *
 * @ashActionType :create
 * @validation true
 */
export async function validate_create_youtube_episode_channel(config: {
  channel: Channel;
  tenant?: string;
  input: CreateYoutubeEpisodeInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "create_youtube_episode",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Create a new YoutubeEpisode
 *
 * @ashActionType :create
 */
export async function create_youtube_episode_channel<Fields extends CreateYoutubeEpisodeFields | undefined = undefined>(config: {
  channel: Channel;
  tenant?: string;
  input: CreateYoutubeEpisodeInput;
  fields?: Fields;
  result_handler: (result: CreateYoutubeEpisodeResult<Fields>) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<CreateYoutubeEpisodeResult<Fields>>(
    config.channel,
    {
    action: "create_youtube_episode",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  },
    config.timeout,
    config
  );
}


export type DeleteYoutubeEpisodeResult = | { success: true; data: {}; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Delete a YoutubeEpisode
 *
 * @ashActionType :destroy
 */
export async function delete_youtube_episode(
  config: {
  tenant?: string;
  identity: UUID;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<DeleteYoutubeEpisodeResult> {
  const payload = {
    action: "delete_youtube_episode",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity
  };

  return executeActionRpcRequest<DeleteYoutubeEpisodeResult>(
    payload,
    config
  );
}


/**
 * Validate: Delete a YoutubeEpisode
 *
 * @ashActionType :destroy
 * @validation true
 */
export async function validate_delete_youtube_episode(
  config: {
  tenant?: string;
  identity: UUID | string;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "delete_youtube_episode",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Delete a YoutubeEpisode
 *
 * @ashActionType :destroy
 * @validation true
 */
export async function validate_delete_youtube_episode_channel(config: {
  channel: Channel;
  tenant?: string;
  identity: UUID | string;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "delete_youtube_episode",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity
  },
    config.timeout,
    config
  );
}


/**
 * Delete a YoutubeEpisode
 *
 * @ashActionType :destroy
 */
export async function delete_youtube_episode_channel(config: {
  channel: Channel;
  tenant?: string;
  identity: UUID;
  result_handler: (result: DeleteYoutubeEpisodeResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<DeleteYoutubeEpisodeResult>(
    config.channel,
    {
    action: "delete_youtube_episode",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity
  },
    config.timeout,
    config
  );
}


export type FetchYoutubeSingleVideoInput = {
  video_id: string;
  origin_entity_id: UUID;
};

export type InferFetchYoutubeSingleVideoResult = Record<string, any>;

export type FetchYoutubeSingleVideoResult = | { success: true; data: InferFetchYoutubeSingleVideoResult; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Execute generic action on YoutubeEpisode
 *
 * @ashActionType :action
 */
export async function fetch_youtube_single_video(
  config: {
  tenant?: string;
  input: FetchYoutubeSingleVideoInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<FetchYoutubeSingleVideoResult> {
  const payload = {
    action: "fetch_youtube_single_video",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeActionRpcRequest<FetchYoutubeSingleVideoResult>(
    payload,
    config
  );
}


/**
 * Validate: Execute generic action on YoutubeEpisode
 *
 * @ashActionType :action
 * @validation true
 */
export async function validate_fetch_youtube_single_video(
  config: {
  tenant?: string;
  input: FetchYoutubeSingleVideoInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "fetch_youtube_single_video",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Execute generic action on YoutubeEpisode
 *
 * @ashActionType :action
 * @validation true
 */
export async function validate_fetch_youtube_single_video_channel(config: {
  channel: Channel;
  tenant?: string;
  input: FetchYoutubeSingleVideoInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "fetch_youtube_single_video",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Execute generic action on YoutubeEpisode
 *
 * @ashActionType :action
 */
export async function fetch_youtube_single_video_channel(config: {
  channel: Channel;
  tenant?: string;
  input: FetchYoutubeSingleVideoInput;
  result_handler: (result: FetchYoutubeSingleVideoResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<FetchYoutubeSingleVideoResult>(
    config.channel,
    {
    action: "fetch_youtube_single_video",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


export type FetchYoutubeTranscriptInput = {
  video_id: string;
};

export type InferFetchYoutubeTranscriptResult = Record<string, any>;

export type FetchYoutubeTranscriptResult = | { success: true; data: InferFetchYoutubeTranscriptResult; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Execute generic action on YoutubeEpisode
 *
 * @ashActionType :action
 */
export async function fetch_youtube_transcript(
  config: {
  tenant?: string;
  input: FetchYoutubeTranscriptInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<FetchYoutubeTranscriptResult> {
  const payload = {
    action: "fetch_youtube_transcript",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeActionRpcRequest<FetchYoutubeTranscriptResult>(
    payload,
    config
  );
}


/**
 * Validate: Execute generic action on YoutubeEpisode
 *
 * @ashActionType :action
 * @validation true
 */
export async function validate_fetch_youtube_transcript(
  config: {
  tenant?: string;
  input: FetchYoutubeTranscriptInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "fetch_youtube_transcript",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Execute generic action on YoutubeEpisode
 *
 * @ashActionType :action
 * @validation true
 */
export async function validate_fetch_youtube_transcript_channel(config: {
  channel: Channel;
  tenant?: string;
  input: FetchYoutubeTranscriptInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "fetch_youtube_transcript",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Execute generic action on YoutubeEpisode
 *
 * @ashActionType :action
 */
export async function fetch_youtube_transcript_channel(config: {
  channel: Channel;
  tenant?: string;
  input: FetchYoutubeTranscriptInput;
  result_handler: (result: FetchYoutubeTranscriptResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<FetchYoutubeTranscriptResult>(
    config.channel,
    {
    action: "fetch_youtube_transcript",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


export type GetYoutubeEpisodeInput = {
  id: UUID;
};

export type GetYoutubeEpisodeFields = UnifiedFieldSelection<YoutubeEpisodeResourceSchema>[];
export type InferGetYoutubeEpisodeResult<
  Fields extends GetYoutubeEpisodeFields,
> = InferResult<YoutubeEpisodeResourceSchema, Fields>;

export type GetYoutubeEpisodeResult<Fields extends GetYoutubeEpisodeFields> = | { success: true; data: InferGetYoutubeEpisodeResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read YoutubeEpisode records
 *
 * @ashActionType :read
 */
export async function get_youtube_episode<Fields extends GetYoutubeEpisodeFields>(
  config: {
  tenant?: string;
  input: GetYoutubeEpisodeInput;
  fields: Fields;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<GetYoutubeEpisodeResult<Fields>> {
  const payload = {
    action: "get_youtube_episode",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<GetYoutubeEpisodeResult<Fields>>(
    payload,
    config
  );
}


/**
 * Validate: Read YoutubeEpisode records
 *
 * @ashActionType :read
 * @validation true
 */
export async function validate_get_youtube_episode(
  config: {
  tenant?: string;
  input: GetYoutubeEpisodeInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "get_youtube_episode",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Read YoutubeEpisode records
 *
 * @ashActionType :read
 * @validation true
 */
export async function validate_get_youtube_episode_channel(config: {
  channel: Channel;
  tenant?: string;
  input: GetYoutubeEpisodeInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "get_youtube_episode",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Read YoutubeEpisode records
 *
 * @ashActionType :read
 */
export async function get_youtube_episode_channel<Fields extends GetYoutubeEpisodeFields>(config: {
  channel: Channel;
  tenant?: string;
  input: GetYoutubeEpisodeInput;
  fields: Fields;
  result_handler: (result: GetYoutubeEpisodeResult<Fields>) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<GetYoutubeEpisodeResult<Fields>>(
    config.channel,
    {
    action: "get_youtube_episode",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  },
    config.timeout,
    config
  );
}


export type ListYoutubeEpisodesFields = UnifiedFieldSelection<YoutubeEpisodeResourceSchema>[];


export type InferListYoutubeEpisodesResult<
  Fields extends ListYoutubeEpisodesFields | undefined,
  Page extends ListYoutubeEpisodesConfig["page"] = undefined
> = ConditionalPaginatedResultMixed<Page, Array<InferResult<YoutubeEpisodeResourceSchema, Fields>>, {
  results: Array<InferResult<YoutubeEpisodeResourceSchema, Fields>>;
  has_more: boolean;
  limit: number;
  offset: number;
  count?: number | null;
  type: "offset";
}, {
  results: Array<InferResult<YoutubeEpisodeResourceSchema, Fields>>;
  has_more: boolean;
  limit: number;
  after: string | null;
  before: string | null;
  previous_page: string;
  next_page: string;
  count?: number | null;
  type: "keyset";
}>;

export type ListYoutubeEpisodesConfig = {
  tenant?: string;
  fields: ListYoutubeEpisodesFields;
  filter?: YoutubeEpisodeFilterInput;
  sort?: SortString<YoutubeEpisodeSortField> | SortString<YoutubeEpisodeSortField>[];
  page?: (
    {
      limit?: number;
      offset?: number;
      count?: boolean;
    } | {
      limit?: number;
      after?: string;
      before?: string;
    }
  );
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
};

export type ListYoutubeEpisodesResult<Fields extends ListYoutubeEpisodesFields, Page extends ListYoutubeEpisodesConfig["page"] = undefined> = | { success: true; data: InferListYoutubeEpisodesResult<Fields, Page>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Read YoutubeEpisode records
 *
 * @ashActionType :read
 */
export async function list_youtube_episodes<Fields extends ListYoutubeEpisodesFields, Config extends ListYoutubeEpisodesConfig = ListYoutubeEpisodesConfig>(
  config: Config & { fields: Fields }
): Promise<ListYoutubeEpisodesResult<Fields, Config["page"]>> {
  const payload = {
    action: "list_youtube_episodes",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort }),
    ...(config.page && { page: config.page })
  };

  return executeActionRpcRequest<ListYoutubeEpisodesResult<Fields, Config["page"]>>(
    payload,
    config
  );
}


/**
 * Validate: Read YoutubeEpisode records
 *
 * @ashActionType :read
 * @validation true
 */
export async function validate_list_youtube_episodes(
  config: {
  tenant?: string;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "list_youtube_episodes",
    ...(config.tenant !== undefined && { tenant: config.tenant })
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Read YoutubeEpisode records
 *
 * @ashActionType :read
 * @validation true
 */
export async function validate_list_youtube_episodes_channel(config: {
  channel: Channel;
  tenant?: string;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "list_youtube_episodes",
    ...(config.tenant !== undefined && { tenant: config.tenant })
  },
    config.timeout,
    config
  );
}


/**
 * Read YoutubeEpisode records
 *
 * @ashActionType :read
 */
export async function list_youtube_episodes_channel<Fields extends ListYoutubeEpisodesFields>(config: {
  channel: Channel;
  tenant?: string;
  fields: Fields;
  filter?: YoutubeEpisodeFilterInput;
  sort?: SortString<YoutubeEpisodeSortField> | SortString<YoutubeEpisodeSortField>[];
  page?: (
    {
      limit?: number;
      offset?: number;
      count?: boolean;
    } | {
      limit?: number;
      after?: string;
      before?: string;
    }
  );
  result_handler: (result: ListYoutubeEpisodesResult<Fields>) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<ListYoutubeEpisodesResult<Fields>>(
    config.channel,
    {
    action: "list_youtube_episodes",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    ...(config.fields !== undefined && { fields: config.fields }),
    ...(config.filter && { filter: config.filter }),
    ...(config.sort && { sort: Array.isArray(config.sort) ? config.sort.join(",") : config.sort }),
    ...(config.page && { page: config.page })
  },
    config.timeout,
    config
  );
}


export type UpdateYoutubeEpisodeInput = {
  video_id?: string;
  video_url?: string;
  title?: string;
  description?: string | null;
  thumbnail_url?: string | null;
  publish_date?: UtcDateTimeUsec | null;
  duration?: string | null;
  episode_number?: number | null;
  host_ids?: Array<UUID> | null;
  guest_ids?: Array<UUID> | null;
  guest_names?: Array<string> | null;
  conversation_starters?: Array<string> | null;
  used_in_podcast?: boolean | null;
  used_in_public_profile?: boolean | null;
  public_profile_config?: Record<string, any> | null;
  playlist_id?: string | null;
  origin_entity_id?: UUID;
  transcript_id?: UUID | null;
};

export type UpdateYoutubeEpisodeFields = UnifiedFieldSelection<YoutubeEpisodeResourceSchema>[];

export type InferUpdateYoutubeEpisodeResult<
  Fields extends UpdateYoutubeEpisodeFields | undefined,
> = InferResult<YoutubeEpisodeResourceSchema, Fields>;

export type UpdateYoutubeEpisodeResult<Fields extends UpdateYoutubeEpisodeFields | undefined = undefined> = | { success: true; data: InferUpdateYoutubeEpisodeResult<Fields>; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Update an existing YoutubeEpisode
 *
 * @ashActionType :update
 */
export async function update_youtube_episode<Fields extends UpdateYoutubeEpisodeFields | undefined = undefined>(
  config: {
  tenant?: string;
  identity: UUID;
  input: UpdateYoutubeEpisodeInput;
  fields?: Fields;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<UpdateYoutubeEpisodeResult<Fields extends undefined ? [] : Fields>> {
  const payload = {
    action: "update_youtube_episode",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  };

  return executeActionRpcRequest<UpdateYoutubeEpisodeResult<Fields extends undefined ? [] : Fields>>(
    payload,
    config
  );
}


/**
 * Validate: Update an existing YoutubeEpisode
 *
 * @ashActionType :update
 * @validation true
 */
export async function validate_update_youtube_episode(
  config: {
  tenant?: string;
  identity: UUID | string;
  input: UpdateYoutubeEpisodeInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "update_youtube_episode",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Update an existing YoutubeEpisode
 *
 * @ashActionType :update
 * @validation true
 */
export async function validate_update_youtube_episode_channel(config: {
  channel: Channel;
  tenant?: string;
  identity: UUID | string;
  input: UpdateYoutubeEpisodeInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "update_youtube_episode",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Update an existing YoutubeEpisode
 *
 * @ashActionType :update
 */
export async function update_youtube_episode_channel<Fields extends UpdateYoutubeEpisodeFields | undefined = undefined>(config: {
  channel: Channel;
  tenant?: string;
  identity: UUID;
  input: UpdateYoutubeEpisodeInput;
  fields?: Fields;
  result_handler: (result: UpdateYoutubeEpisodeResult<Fields>) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<UpdateYoutubeEpisodeResult<Fields>>(
    config.channel,
    {
    action: "update_youtube_episode",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    identity: config.identity,
    input: config.input,
    ...(config.fields !== undefined && { fields: config.fields })
  },
    config.timeout,
    config
  );
}


export type YoutubeFetchPublicDataInput = {
  youtube_url: string;
  origin_entity_id?: UUID | null;
  fetch_all_videos?: boolean | null;
};

export type InferYoutubeFetchPublicDataResult = Record<string, any>;

export type YoutubeFetchPublicDataResult = | { success: true; data: InferYoutubeFetchPublicDataResult; }
| { success: false; errors: AshRpcError[]; }

;

/**
 * Execute generic action on YoutubeEpisode
 *
 * @ashActionType :action
 */
export async function youtube_fetch_public_data(
  config: {
  tenant?: string;
  input: YoutubeFetchPublicDataInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<YoutubeFetchPublicDataResult> {
  const payload = {
    action: "youtube_fetch_public_data",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeActionRpcRequest<YoutubeFetchPublicDataResult>(
    payload,
    config
  );
}


/**
 * Validate: Execute generic action on YoutubeEpisode
 *
 * @ashActionType :action
 * @validation true
 */
export async function validate_youtube_fetch_public_data(
  config: {
  tenant?: string;
  input: YoutubeFetchPublicDataInput;
  headers?: Record<string, string>;
  fetch_options?: RequestInit;
  custom_fetch?: (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;
}
): Promise<ValidationResult> {
  const payload = {
    action: "youtube_fetch_public_data",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  };

  return executeValidationRpcRequest<ValidationResult>(
    payload,
    config
  );
}


/**
 * Validate: Execute generic action on YoutubeEpisode
 *
 * @ashActionType :action
 * @validation true
 */
export async function validate_youtube_fetch_public_data_channel(config: {
  channel: Channel;
  tenant?: string;
  input: YoutubeFetchPublicDataInput;
  result_handler: (result: ValidationResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeValidationChannelPush<ValidationResult>(
    config.channel,
    {
    action: "youtube_fetch_public_data",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}


/**
 * Execute generic action on YoutubeEpisode
 *
 * @ashActionType :action
 */
export async function youtube_fetch_public_data_channel(config: {
  channel: Channel;
  tenant?: string;
  input: YoutubeFetchPublicDataInput;
  result_handler: (result: YoutubeFetchPublicDataResult) => void;
  error_handler?: (error: any) => void;
  timeout_handler?: () => void;
  timeout?: number;
}) {
  executeActionChannelPush<YoutubeFetchPublicDataResult>(
    config.channel,
    {
    action: "youtube_fetch_public_data",
    ...(config.tenant !== undefined && { tenant: config.tenant }),
    input: config.input
  },
    config.timeout,
    config
  );
}

